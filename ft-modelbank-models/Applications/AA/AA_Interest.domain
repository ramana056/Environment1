# UTF-8
/* Interest Processing for Arrangement Architecture */
Domain AA_Interest
    @t24:i (module=AA)
    @java:Package (value=com.temenos.t24.datamodel.aa.aa_interest)
namespace "http://www.temenos.com/t24/AA_Interest"
metamodelVersion 1.30.6
Classes {
/* The file AA.ACCRUAL.FREQUENCY allows the user to define the frequency for interest accruals specific to company or for the whole system.

User can set default accrual frequency for local currency and foreign currency. It is also possible to override the default frequency for a particular interest property. Accruals for that interest property of the all the products (using this property) will accrue according to the special frequency and rest of the interest properties will accrue according to the default frequency.

It is also possible to define different accrual frequency for a given interest property for a given product. The accruals for such an interest property for that particular product will accrue according to the special frequency. Rest of the interest properties for the above product will accrue either according to default frequency or specific frequency if defined. */
    AA_ACCRUAL_FREQUENCY
        @i18n:Description (en="")
        @t24:i (t24Name=AA.ACCRUAL.FREQUENCY, level=I, type=H, allowedFunctions="A C D E I L Q S", localRefApplicationSysNo=13.0, localRefAllowed=true)
    {
        /* This field represents the id of the file AA.ACCRUAL.FREQUENCY. The id can be company code or SYSTEM. */
        ACCRUAL_ID -> AA_Interest:AA_ACCRUAL_FREQUENCY PK required
            @i18n:Label (en="Accrual Method")
            @i18n:Tooltip (en="Indicates the method by which accrual is to happen")
            @t24:i (align=L, inputBehaviour=I, maxLength=11, sysNumber=0.0, core=true, businessType=A, t24Name=ACCRUAL.ID, mandatory=true)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=31.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=32.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=28.0, core=true, t24Name=AUTHORISER, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=29.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=25.0, core=true, t24Name=CURR.NO, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_ACCRUAL_FREQUENCY__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* This field represents the default accrual frequency for all interest properties in foreign currency for all the products.


Validation Rules:
Standard date/frequency format.
Mandatory input */
        DEFAULT_FCY : T24BusinessTypes:FQF
            @i18n:Label (en="Default FCY")
            @i18n:Tooltip (en="Default FCY")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=2.0, core=true, t24Name=DEFAULT.FCY, mandatory=false)
/* This field represents the default accrual frequency for all interest properties in local currency for all the products


Validation Rules:
Standard date/frequency format.
Mandatory input */
        DEFAULT_LCY : T24BusinessTypes:FQF
            @i18n:Label (en="Default LCY")
            @i18n:Tooltip (en="Default LCY")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=1.0, core=true, t24Name=DEFAULT.LCY, mandatory=false)
        /* This field is used to specify if by default system has to project the accruals for all of the properties belonging to all of the products. */
        DEFAULT_OL_ACCRUAL : AA_Interest:AA_ACCRUAL_FREQUENCY__DEFAULT_OL_ACCRUAL
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, t24Name=DEFAULT.OL.ACCRUAL, mandatory=false)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=30.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        FORCED_TO_SOD : AA_Interest:AA_ACCRUAL_FREQUENCY__FORCED_TO_SOD
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=14.0, core=true, t24Name=FORCED.TO.SOD, mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_ACCRUAL_FREQUENCY__INPUTTER *
            @t24:i (t24Name=INPUTTER)
        PRODUCT -> byValue AA_Interest:AA_ACCRUAL_FREQUENCY__PRODUCT *
            @t24:i (t24Name=PRODUCT)
        PROPERTY -> byValue AA_Interest:AA_ACCRUAL_FREQUENCY__PROPERTY *
            @t24:i (t24Name=PROPERTY)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=24.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
        RESERVED01 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=23.0, core=true, t24Name=RESERVED01, mandatory=false)
        RESERVED02 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=22.0, core=true, t24Name=RESERVED02, mandatory=false)
        RESERVED03 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=21.0, core=true, t24Name=RESERVED03, mandatory=false)
        RESERVED04 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=20.0, core=true, t24Name=RESERVED04, mandatory=false)
        RESERVED05 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=19.0, core=true, t24Name=RESERVED05, mandatory=false)
        RESERVED06 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=18.0, core=true, t24Name=RESERVED06, mandatory=false)
        RESERVED07 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=17.0, core=true, t24Name=RESERVED07, mandatory=false)
        RESERVED08 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=16.0, core=true, t24Name=RESERVED08, mandatory=false)
        RESERVED09 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=15.0, core=true, t24Name=RESERVED09, mandatory=false)
    }
    AA_ACCRUAL_FREQUENCY__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=27.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_ACCRUAL_FREQUENCY__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=26.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_ACCRUAL_FREQUENCY__PRODUCT
    {
/* This field represents the specific Arrangement Product for which special accrual frequency is to be set.  Optionally, you can also set the properties under a Product. The frequency can be set for either foreign and (or) local currency.


Validation Rules:
Should be a valid Arrangement Product (ID in AA.PRODUCT)
Optional Input */
        PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @i18n:Label (en=Product)
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=8.0, core=true, businessType=A, t24Name=PRODUCT, mandatory=false)
        PROD_PROP -> byValue AA_Interest:AA_ACCRUAL_FREQUENCY__PRODUCT__PROD_PROP *
            @t24:i (t24Name=PROD.PROP)
    }
    AA_ACCRUAL_FREQUENCY__PRODUCT__PROD_PROP
    {
/* This field represents the special accrual frequency for a particular interest property (Property defined for a Product) in foreign currency.





Validation Rules:
Standard date/frequency format.
Optional input */
        PRD_PROP_FCY : T24BusinessTypes:FQF
            @i18n:Label (en="Prod. Property FCY Feq")
            @i18n:Tooltip (en="Prod. Property FCY Feq")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=12.0, core=true, t24Name=PRD.PROP.FCY, mandatory=false)
/* This field represents the special accrual frequency for a particular interest property (Property defined for a Product) in local currency.


Validation Rules:
Standard date/frequency format.
Optional input */
        PRD_PROP_LCY : T24BusinessTypes:FQF
            @i18n:Label (en="Prod.Property LCY Freq")
            @i18n:Tooltip (en="Prod.Property LCY Freq")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=11.0, core=true, t24Name=PRD.PROP.LCY, mandatory=false)
/* This field represents the specific Property under a given Arrangement Product for which special accrual frequency is needed.


Validation Rules:
Should be a valid ID in AA.PROPERTY
Optional Input */
        PROD_PROP -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Product Property ")
            @i18n:Tooltip (en="Product Property ")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=9.0, core=true, businessType=A, t24Name=PROD.PROP, mandatory=false)
        /* This field is used to specify if the corresponding property that belong to a product is eligible to do online accruals or not */
        PROD_PROP_OL_ACCRUAL : AA_Interest:AA_ACCRUAL_FREQUENCY__PRODUCT__PROD_PROP__PROD_PROP_OL_ACCRUAL
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=10.0, core=true, t24Name=PROD.PROP.OL.ACCRUAL, mandatory=false)
    }
    AA_ACCRUAL_FREQUENCY__PROPERTY
    {
/* This field represents the specific interest property for which special accrual frequency is to be set. The frequency can be set for either foreign and (or) local currency.


Validation Rules:
Should be a valid Property of Interest Class
Optional Input */
        PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en=Property)
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=4.0, core=true, businessType=A, t24Name=PROPERTY, mandatory=false)
/* This field represents the special accrual frequency for a particular interest property (set in the field PROPERTY) in foreign currency.


Validation Rules:
Standard date/frequency format.
Optional input */
        PROPERTY_FCY : T24BusinessTypes:FQF
            @i18n:Label (en="Property FCY Freq")
            @i18n:Tooltip (en="Property FCY Freq")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=7.0, core=true, t24Name=PROPERTY.FCY, mandatory=false)
/* This field represents the special accrual frequency for a particular interest property (set in the field PROPERTY) in Local currency.


Validation Rules:
Standard date/frequency format.
Optional input */
        PROPERTY_LCY : T24BusinessTypes:FQF
            @i18n:Label (en="Property LCY Freq")
            @i18n:Tooltip (en="Property LCY Freq")
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=6.0, core=true, t24Name=PROPERTY.LCY, mandatory=false)
        /* This field is used to specify if the corresponding property is eligible to do online accruals or not */
        PROPERTY_OL_ACCRUAL : AA_Interest:AA_ACCRUAL_FREQUENCY__PROPERTY__PROPERTY_OL_ACCRUAL
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=5.0, core=true, t24Name=PROPERTY.OL.ACCRUAL, mandatory=false)
    }
/* The INTEREST Property Class is used for all interest definition and processing in AA.  A T24 product defined and processed in AA can have multiple interest properties defined (e.g. principal interest, penalty interest, commission, etc.).  The number of interest properties is determined by the users defining the products.
The different condition ids of interest property can be attached to the different types of interest properties at product level */
    AA_ARR_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.ARR.INTEREST, nonStop=true, level=F, type=H, allowedFunctions="A C D E I L Q R S", localRefApplicationSysNo=101.0, localRefAllowed=true)
    {
/* Specifies how the accounting entries will be raised. Whether it needs to work like discounted or fixed type of interest.
Advance: This is nothing but discounted interest, interest amount will be collected inadvance before the interest periods.
Upfront Profit: Interest amount will be collected at the beginning of the contract.
None: Denotes conventional interest bearing contract. */
        ACCOUNTING_MODE : AA_Interest:AA_ARR_INTEREST__ACCOUNTING_MODE
            @i18n:Label (en="Accounting Mode")
            @t24:i (align=L, inputBehaviour=F, maxLength=15, sysNumber=60.0, core=true, t24Name=ACCOUNTING.MODE, mandatory=false)
/* When accruing interest,T24 must determine which amounts to include in each day's balance, the actual days to include in the period (e.g. first day, last day) and how to round any calculations.
T24 allows users to define the calculation method to use, by specifying an accrual rule in EB.ACCRUAL.PARAM.  Users can define the following:
First Day Inclusive (Yes/No)
Last Day Inclusive (Yes/No)
Rounding Rule (Natural, Down, Up, Currency, None)
Intermediate Calculation Rounding (Yes/No)
Credit value date adjustment
Debit value date adjustment */
        ACCRUAL_RULE -> AC_Fees:EB_ACCRUAL_PARAM
            @i18n:Label (en="Accrual Rule")
            @i18n:Tooltip (en="Rule for calculating interest accrual")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=ACCRUAL.RULE, mandatory=false)
        ACTION -> byValue AA_Interest:AA_ARR_INTEREST__ACTION *
            @t24:i (t24Name=ACTION)
/* The activity that caused this arrangement property to be updated.
The activity is taken from the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified */
        ACTIVITY : T24BusinessTypes:A required
            @i18n:Label (en=Activity)
            @i18n:Tooltip (en=Activity)
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=1.0, core=true, t24Name=ACTIVITY, mandatory=true)
/* User can defined Total interest / profit amount for the contract.
Mandatory input if RATE.TYPE is ‘Flat Amount’.
Value in CALC.PROFIT.AMT will be populated in this field which the user can override */
        ACTUAL_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=69.0, core=true, t24Name=ACTUAL.PROFIT.AMT, mandatory=false)
        ADJUST_EXPIRY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=82.0, core=true, t24Name=ADJUST.EXPIRY.DATE, mandatory=false)
        ADJUST_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=79.0, core=true, t24Name=ADJUST.MARGIN, mandatory=false)
        ADJUST_OPERAND : AA_Interest:AA_ARR_INTEREST__ADJUST_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=ADJUST.OPERAND, mandatory=false)
        ADJUST_OPTION : AA_Interest:AA_ARR_INTEREST__ADJUST_OPTION
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.2, core=false, t24Name=ADJUST.OPTION, mandatory=false)
        ADJUST_OVERRIDE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=80.0, core=true, t24Name=ADJUST.OVERRIDE.RATE, mandatory=false)
        ADJUST_REASON : AA_Interest:AA_ARR_INTEREST__ADJUST_REASON
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=81.0, core=true, t24Name=ADJUST.REASON, mandatory=false)
        ADJUST_TYPE : AA_Interest:AA_ARR_INTEREST__ADJUST_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=77.0, core=true, t24Name=ADJUST.TYPE, mandatory=false)
        API_ATTRIBUTE -> byValue AA_Interest:AA_ARR_INTEREST__API_ATTRIBUTE *
            @t24:i (t24Name=API.ATTRIBUTE)
/* The key to the arrangement property condition.
The key is comprised of the following components (delimited by a -)
Arrangement Id
Property name
Effective Date */
        ARRANGEMENT_KEY -> AA_Interest:AA_ARR_INTEREST PK required
            @i18n:Label (en=Arrangement)
            @i18n:Tooltip (en=Arrangement)
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=0.0, core=true, businessType=ANY, t24Name=ARRANGEMENT.KEY, mandatory=true)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=148.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=149.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=145.0, core=true, t24Name=AUTHORISER, mandatory=false)
        /* The user can specify that interest will only be calculated if a balance threshold is surpassed. For credit interest, the user can specify the minimum balance which must be maintained. For debit interest, the user can specify the maximum debit balance for which interest will not be calculated. */
        CALC_THRESHOLD : T24BusinessTypes:AMT
            @i18n:Label (en="Calc Threshold")
            @i18n:Tooltip (en="Calc Threshold")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=CALC.THRESHOLD, typeModifiers=-, mandatory=false)
        CHANGED_FIELDS -> byValue AA_Interest:AA_ARR_INTEREST__CHANGED_FIELDS *
            @t24:i (t24Name=CHANGED.FIELDS)
/* T24 supports the compounding of both debit and credit interest.

Compounding interest applies a factor to the compound interest calculation based upon the  number of compounding periods in the year. This field defines the number of compounding periods to be applied.

Note that the compounding type provides a factor in the calculation only  and does not actually cause the base principal amount to be actually increased by the calculated interest amount.

The following compound frequencies are supported:

DAILY
Interest is compounded 360/5/6 times a year (from days in year in INTEREST.BASIS)

WEEKn
Interest is compounded 52/n times per year based on a number of WEEKS
-- Bi-weekly compounding should be defined as WEEK2 and will result in 26 periods
-- Four-Weekly compounding should be defined as WEEK4 and results in 13 periods

Mnn
Interest is compounded 12/n times a year based on a numberof months
--  Monthly compounding should be defined as M01 and will result in 12 periods
--  Bi-Monthly should be defined as M02 and will result in 6 periods
--  Quarterly should be defined as M03 and will result in 4 periods
--  Semi-Annual should be defined as M06 and will result in 6 periods
--  Annual should be defined as M12 and will result in 1 period
--  Bi-Annual should be defined as M24 and will result in 0.5 periods
--  Any number of months up to 99 may be defined and results in 12/n periods

TWMTH
Interest is compounded twice a month resulting in 24 periods as opposed to WEEK2
which results in 26 periods

Nnnn
- Compounding takes place nnn times per year. */
        COMPOUND_TYPE : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compounding)
            @i18n:Tooltip (en="Type of compounding applied for interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=COMPOUND.TYPE, mandatory=false)
/* This field indicates how the effective rate is calculated, when set to YIELD the interest rate defined is taken as the annual effective rate. System uses the below formula to calculate the interest amount,

Daily Effective Rate(ERd) = (1+Interest Rate)^(1/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}

When the field is set as NULL the effective rate is calculated based on the compounding frequency defined in the field COMPOUND.TYPE using the below formula,

Daily Effective Rate(ERd) = (1+Interest Rate/Compound Freq)^(Compound Freq/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1} */
        COMPOUND_YIELD_METHOD : AA_Interest:AA_ARR_INTEREST__COMPOUND_YIELD_METHOD
            @i18n:Label (en="Compound Yield Method")
            @i18n:Tooltip (en="Compound Yield Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=50.0, core=true, t24Name=COMPOUND.YIELD.METHOD, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=146.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=142.0, core=true, t24Name=CURR.NO, mandatory=false)
        /* If there is a Cutom rate tier then this field is mandatory. It is a AA.CUSTOM.RATE.TYPE record that determines how the custom rate should be calculated */
        CUSTOM_RATE_CALC -> AA_Interest:AA_CUSTOM_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=72.0, core=true, businessType=S, t24Name=CUSTOM.RATE.CALC, mandatory=false)
        CUSTOM_TYPE -> byValue AA_Interest:AA_ARR_INTEREST__CUSTOM_TYPE *
            @t24:i (t24Name=CUSTOM.TYPE)
/* Specifies the date convention to be followed for the reset frequency i.e. if the cycled Periodic Reset date falls on a non-working day, the reset date will be moved based on the option chosen.
Allowed Values:
Arrangement / Null – Based on the date convention specified in the Account Property Class.
Periodic Interest – Based on the convention used by the index i.e. the Periodic Interest record.
o Forward Same Month convention for Monthly periods and Forward convention for all other periods if USE.LAST.WORKING.DAY is set to 'Y'.
o Forward Convention for all periods if USE.LAST.WORKING.DAY is not set. */
        DATE_CONVENTION : AA_Interest:AA_ARR_INTEREST__DATE_CONVENTION
            @i18n:Label (en=Convention)
            @t24:i (align=L, inputBehaviour=I, maxLength=18, sysNumber=61.0, core=true, t24Name=DATE.CONVENTION, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_ARR_INTEREST__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* T24 can calculate interest based upon several different day basis types.  Each T24 Interest Day Basis type is represented as a numerator and denominator.
Numerator (interest days)
o 360  Each month is considered to have 30 days
o 366  The exact number of days will be used
Denominator (days in the year)
o 360  Each year is considered to have 360 days
o 365  Each year is considered to have 365 days
o 366  The exact number of days in the year will be used (365 or 366)

T24 allows the following interest day basis types:
Type        Notes
A (360/360)     A1 and A2 are variations which alter the method of treating a month as 30 days.
B (366/360)
C (366/366)
D (360/366)
E (366/365)
F (360/365)     F1 and F2 are variations which alter the method of treating a month as 30 days.

This is a Mandatory field. */
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Day Basis")
            @i18n:Tooltip (en="Day Basis")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, businessType=A, t24Name=DAY.BASIS, mandatory=false)
/* p>Optional field - Allowed Values are RESETTING and NON-RESETTING.

RESETTING - During any Renewal Activities (for eg : change.product or reset) the property conditions will be reset from the product.

NON-RESETTING - During any Renewal Activities property conditions will be maintained from the Arrangement level.

Leaving the field blank sets NON-RESETTING as the default option.

Not used in Arrangement/Sim level.

Not used in Arrangement/Sim level. */
        DEFAULT_ATTR_OPTION : AA_Interest:AA_ARR_INTEREST__DEFAULT_ATTR_OPTION
            @i18n:Label (en="Default Attribute")
            @i18n:Tooltip (en="Default Attribute")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=119.0, core=true, t24Name=DEFAULT.ATTR.OPTION, mandatory=false)
        /* Not used for arrangement level conditions */
        DEFAULT_NEGOTIABLE : AA_Interest:AA_ARR_INTEREST__DEFAULT_NEGOTIABLE
            @i18n:Label (en="Default Negotiable")
            @i18n:Tooltip (en="Default Negotiable")
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=120.0, core=true, t24Name=DEFAULT.NEGOTIABLE, mandatory=false)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=147.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        FIXED_RATE -> byValue AA_Interest:AA_ARR_INTEREST__FIXED_RATE *
            @t24:i (t24Name=FIXED.RATE)
        FT_TXN_TYPE -> FT_Config:FT_TXN_TYPE_CONDITION
            @i18n:Label (en="Transaction Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=101.1, core=false, businessType=A, t24Name=FT.TXN.TYPE, mandatory=false)
/* Contains the arrangement number, extracted from the first component of the id
System updated field no input allowed */
        ID_COMP_1 : T24BusinessTypes:A
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=131.0, core=true, t24Name=ID.COMP.1, mandatory=false)
/* Contains the name of the property that this arrangement level condition relates to
Name is defined in AA.PRORERTY table
System maintained field no input allowed */
        ID_COMP_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=132.0, core=true, t24Name=ID.COMP.2, mandatory=false)
/* Contains the Effective Date of this property condition
System updated field no input allowed */
        ID_COMP_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=30, sysNumber=133.0, core=true, t24Name=ID.COMP.3, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=134.0, core=true, t24Name=ID.COMP.4, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_5 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=135.0, core=true, t24Name=ID.COMP.5, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_6 : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=136.0, core=true, t24Name=ID.COMP.6, typeModifiers="ARRANGEMENT_START_ANNIVERSARY-D_W_M_Y", mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_ARR_INTEREST__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* Specifies whether interest method is fixed life of the contract or not.
FIXED – Fix the total profit amount regardless of any change to loan parameters. This field is specify whether total interest amount need to fixed or not.
Null – Will be treated as a normal interest bearing property. */
        INTEREST_METHOD : AA_Interest:AA_ARR_INTEREST__INTEREST_METHOD
            @t24:i (align=L, inputBehaviour=F, maxLength=6, sysNumber=66.0, core=true, t24Name=INTEREST.METHOD, mandatory=false)
/* Income / expense will be posted to internal account category specified in Accounting condition instead of PL categories.
This will accept YES or NULL */
        INTERNAL_BOOKING : AA_Interest:AA_ARR_INTEREST__INTERNAL_BOOKING
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, t24Name=INTERNAL.BOOKING, mandatory=false)
        /* Where at least one tier is set to be ‘Linked Rate’, the Arrangement to be linked for calculating the Interest Rate, must be specified here (or automatically defaulted by the system, where a Limit is attached) */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* This field will be used to specify the Interest Property of the Linked Arrangement that should be referred to, for getting the Interest Rate.

Where at least one tier is set to be ‘Linked Rate’, this must be specified. */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=64.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
        MERGE_SELECTION : AA_Interest:AA_ARR_INTEREST__MERGE_SELECTION
            @t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=MERGE.SELECTION, mandatory=false)
/* This field indicates the minimum interest amount to be posted for an arrangement/account.
If the calculated interest amount is less than the amount specified in this field, system will waive the interest amount or adjust the interest amount based on the option set in the field MIN.INT.WAIVE */
        MIN_INT_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Minimum Interest Amount")
            @i18n:Tooltip (en="Minimum Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=MIN.INT.AMOUNT, typeModifiers=-, mandatory=false)
/* This field indicates if the calculated interest amount has to be waived or adjusted.
If the field is set to YES, then the system will waive the interest amount if it is less than the MIN.INT.AMOUNT specified.
If NULL value is set and if the calculated interest amount is less than the MIN.INT.AMOUNT specified, then system will post the minimum interest amount. */
        MIN_INT_WAIVE : AA_Interest:AA_ARR_INTEREST__MIN_INT_WAIVE
            @i18n:Label (en="Minimum Interest Waive")
            @i18n:Tooltip (en="Minimum Interest Waive")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=53.0, core=true, t24Name=MIN.INT.WAIVE, mandatory=false)
/* Negative rate functionality is moved to TIER.NEGATIVE.RATE field, please refer to TIER.NEGATIVE.RATE for help text.
This field is system maintained field, where in if all of the tier negative rate has the same value, then this field will be updated with that value. */
        NEGATIVE_RATE : AA_Interest:AA_ARR_INTEREST__NEGATIVE_RATE
            @i18n:Label (en="Negative Rate")
            @i18n:Tooltip (en="Negative Rate")
            @t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=7.0, core=true, t24Name=NEGATIVE.RATE, mandatory=false)
        NEGOTIATED_FLDS -> byValue AA_Interest:AA_ARR_INTEREST__NEGOTIATED_FLDS *
            @t24:i (t24Name=NEGOTIATED.FLDS)
        NR_ATTRIBUTE -> byValue AA_Interest:AA_ARR_INTEREST__NR_ATTRIBUTE *
            @t24:i (t24Name=NR.ATTRIBUTE)
        OFFSET_DETAILS -> byValue AA_Interest:AA_ARR_INTEREST__OFFSET_DETAILS *
            @t24:i (t24Name=OFFSET.DETAILS)
        ON_ACTIVITY -> byValue AA_Interest:AA_ARR_INTEREST__ON_ACTIVITY *
            @t24:i (t24Name=ON.ACTIVITY)
/* This field is used to define the profit amount realised in the Legacy system. Assuming the total profit amount for the contract is 10,000  and remaining profit amount to be realized during migration is 3,000, one will give the difference (7,000) in this attribute. And 3,000 will be input in the Actual Profit Amount that has to be realized in T24. If someone wants the total profit amount, they can simply add these 2 to get them. Once authorised, the value cannot be changed.
Validation:
• Applicable for Fixed Interest Method
• Input enabled during migration of the contract(through take-over activity) */
        ORIG_PROFIT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=30, sysNumber=70.0, core=true, t24Name=ORIG.PROFIT.AMOUNT, mandatory=false)
        OVERRIDE -> byValue AA_Interest:AA_ARR_INTEREST__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        OWNING_COMPANY -> byValue AA_Interest:AA_ARR_INTEREST__OWNING_COMPANY *
            @t24:i (t24Name=OWNING.COMPANY)
        PACKAGE_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=PACKAGE.PRODUCT, mandatory=false)
        PACKAGE_PROPERTY_CONTROL : AA_Interest:AA_ARR_INTEREST__PACKAGE_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=100.0, core=true, t24Name=PACKAGE.PROPERTY.CONTROL, mandatory=false)
        PRICING_RULES_BENEFIT -> byValue AA_Interest:AA_ARR_INTEREST__PRICING_RULES_BENEFIT *
            @t24:i (t24Name=PRICING.RULES.BENEFIT)
        PROGRAM_LIMIT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=94.0, core=true, t24Name=PROGRAM.LIMIT, mandatory=false)
        PR_ATTRIBUTE -> byValue AA_Interest:AA_ARR_INTEREST__PR_ATTRIBUTE *
            @t24:i (t24Name=PR.ATTRIBUTE)
        RATE_LOCK_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=101.3, core=false, t24Name=RATE.LOCK.ID, mandatory=false)
/* T24 allows for the definition of tiers of interest rates.  Each tier is specified by defining the amount up to which the interest rate applies.  Additionally, each tier can be of a different interest rate type (i.e. fixed, floating, or periodic).  There are three type of tiers; Single, Level, and Banded.
Single Rate
When a single rate tier type is specified a single nominal interest rate will apply for the entire balance amount.
Rate Levels
By specifying level tiers a product can be defined which will calculate interest at different rates depending on the balance amount.  A single rate of interest will be selected and applied based upon the balance amount.  For example for a deposit a single product can have a higher rate of interest for accounts which maintain higher credit balances.  Conversely for a loan product a higher rate of interest can be charged for accounts with higher debit balances.
For example, aassuming a credit balance of 15,000 and level interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  A single rate of 15 per cent will be applied to the entire amount of 15,000.
Banded Rates
Banded tier interest will typically result in a blended interest rate.   This is similar to Level tiers, but allows for the interest rate of each tier to be applied to the portion of the balance that falls within the tier.
For example, assuming a credit balance of 15,000 and banded interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  Two calculations will be performed (i.e. 10 per cent on 10,000 and 15 per cent on 5,000)

This field is Mandatory. */
        RATE_TIER_TYPE : AA_Interest:AA_ARR_INTEREST__RATE_TIER_TYPE
            @i18n:Label (en="Tier Type")
            @i18n:Tooltip (en="Indicates the rate to be applied for each tier")
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=8.0, core=true, t24Name=RATE.TIER.TYPE, mandatory=false)
/* Specifies whether Interest definition is based on a rate or amount and in case of rate what type of rate.
Reducing Rate: Rate defined in Interest rate related fields will be considered as Reducing or Diminishing Rate for Interest calculation.
Flat Rate: Rate defined in interest rate related fields will be considered as a Flat Rate.
Flat Amount: Amount defined in the ACTUAL.PROFIT.AMT will be considered as the total interest amount for the interest property.
This will be allowed only when INTEREST.METHOD is FIEXED */
        RATE_TYPE : AA_Interest:AA_ARR_INTEREST__RATE_TYPE
            @i18n:Label (en="Rate Type")
            @i18n:Tooltip (en="Is the Rate quoted net of tax or gross of tax")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=67.0, core=true, t24Name=RATE.TYPE, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=141.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* For an arrangement under ACCOUNTS Product Line, it is possible link the first tier amount of debit interest to the LIMIT record attached.
It can be achieved by setting the field REFER.LIMIT in INTEREST property condition as ‘YES’. This functionality can be enabled only if the LIMIT is managed by AA. */
        REFER_LIMIT : AA_Interest:AA_ARR_INTEREST__REFER_LIMIT
            @i18n:Label (en="Refer Limit")
            @i18n:Tooltip (en="Refer Limit")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=51.0, core=true, t24Name=REFER.LIMIT, mandatory=false)
        REGIONAL_PRICING_CODE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=84.0, core=true, t24Name=REGIONAL.PRICING.CODE, mandatory=false)
        REGIONAL_PRICING_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=87.0, core=true, t24Name=REGIONAL.PRICING.FIXED.RATE, mandatory=false)
        REGIONAL_PRICING_LEVEL : AA_Interest:AA_ARR_INTEREST__REGIONAL_PRICING_LEVEL
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=83.0, core=true, t24Name=REGIONAL.PRICING.LEVEL, mandatory=false)
        REGIONAL_PRICING_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=86.0, core=true, t24Name=REGIONAL.PRICING.MARGIN, mandatory=false)
        REGIONAL_PRICING_OPERAND : AA_Interest:AA_ARR_INTEREST__REGIONAL_PRICING_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=85.0, core=true, t24Name=REGIONAL.PRICING.OPERAND, mandatory=false)
        REGIONAL_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=88.0, core=true, businessType=A, t24Name=REGIONAL.PRODUCT, mandatory=false)
        REGIONAL_PROPERTY_CONTROL : AA_Interest:AA_ARR_INTEREST__REGIONAL_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=89.0, core=true, t24Name=REGIONAL.PROPERTY.CONTROL, mandatory=false)
        RELATIONSHIP_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=59.0, core=true, t24Name=RELATIONSHIP.FIXED.RATE, mandatory=false)
        /* Rate of discount margin as a result of the relationship of the customer with the bank. Would be evaluated based on the RELATIONSHIP arrangements held by the customer and also the definitions in PREFERENTIAL.PRICING as well as the rules in PRICING.RULES of this arrangement product. Validation: System maintained field. Would be a percentage would be added or subtracted to the original rate. The effective rate would be influenced by the content in this field. */
        RELATIONSHIP_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=57.0, core=true, t24Name=RELATIONSHIP.MARGIN, mandatory=false)
        /* Indicates whether the margin stated gets added or subtracted or multiplied to the originally evaluated rate. Validation: System maintained field. Would be either ADD, SUBTRACT or MULTIPLY */
        RELATIONSHIP_OPERAND : AA_Interest:AA_ARR_INTEREST__RELATIONSHIP_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=56.0, core=true, t24Name=RELATIONSHIP.OPERAND, mandatory=false)
        /* Field to indicate the relationship pricing plan(if any) that has influenced the rate on this arrangement. If a customer has a relationship with the bank which entitles for special margins, then the plan which has contributed to this margin would be updated here. Validation: System maintained field. Would be a valid RELATIONSHIP.PRICING product for this customer */
        RELATIONSHIP_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=55.0, core=true, businessType=A, t24Name=RELATIONSHIP.PRODUCT, mandatory=false)
/* This field holds the variation selected for the arrangement for the interest pricing condition

Validation Rules

System maintained, NOINPUT allowed */
        RELATIONSHIP_VARIATION : AA_Interest:AA_ARR_INTEREST__RELATIONSHIP_VARIATION
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=RELATIONSHIP.VARIATION, mandatory=false)
        RESERVED1 : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=65.0, core=true, t24Name=RESERVED1, mandatory=false)
        RESERVED10 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=96.0, core=true, t24Name=RESERVED10, mandatory=false)
        RESERVED2_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=137.0, core=true, t24Name=RESERVED2.ID, mandatory=false)
        RESERVED4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=62.0, core=true, t24Name=RESERVED4, mandatory=false)
        RESERVED6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=68.0, core=true, t24Name=RESERVED6, mandatory=false)
        RESERVED7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=99.0, core=true, t24Name=RESERVED7, mandatory=false)
        RESERVED8 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=98.0, core=true, t24Name=RESERVED8, mandatory=false)
        RESERVED9 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=97.0, core=true, t24Name=RESERVED9, mandatory=false)
        /* If this field gets checked then rate would not be stored in the EFFECTIVE.RATE field and whenever needed System will calculate it at runtime */
        RUNTIME_RATE_CALC : AA_Interest:AA_ARR_INTEREST__RUNTIME_RATE_CALC
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=73.0, core=true, t24Name=RUNTIME.RATE.CALC, mandatory=false)
        STMT_NOS -> byValue AA_Interest:AA_ARR_INTEREST__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Field accepts the options 'YES' or 'ALTERNATE'.
Indicates whether accruals needs to be Suppressed or Accrual should be done for the Alternate Interest Property.

For Example:
It will be set as YES by the system if the DONOR.ACCRUAL field is set as SUPPRESS for any product / arrangement in INTEREST.COMPENSTION property of a Bundle arrangement.

If it is set as 'ALTERNATE' for a property then the Accrual will be suppressed for the property for which it is set and Accrual will happen for the Alternate property which is set in Activity Restriction.
Blank value in this field denotes that there is no suppression of accrual

This is a Non Mandatory field. */
        SUPPRESS_ACCRUAL : AA_Interest:AA_ARR_INTEREST__SUPPRESS_ACCRUAL
            @i18n:Label (en="Suppress Accrual")
            @i18n:Tooltip (en="Suppress Accrual")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=54.0, core=true, t24Name=SUPPRESS.ACCRUAL, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=SYS.RESERVE1, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=SYS.RESERVE2, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=SYS.RESERVE3, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=SYS.RESERVE6, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=SYS.RESERVE7, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        S_EFFECTIVE_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=N, maxLength=11, core=false, t24Name=S.EFFECTIVE.DATE, mandatory=false)
/* This field denotes the target classic product of the given property class

The already existing T24 products are mapped using AA infrastructure
These are called classic products
These products are mapped to different property clases */
        TARGET_PRODUCT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=138.0, core=true, t24Name=TARGET.PRODUCT, mandatory=false)
        USER_CUSTOM_RATE : AA_Interest:AA_ARR_INTEREST__USER_CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=21.0, core=true, t24Name=USER.CUSTOM.RATE, mandatory=false)
        USER_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=11.0, core=true, t24Name=USER.FIXED.RATE, typeModifiers=-, mandatory=false)
        USER_FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=12.0, core=true, businessType=A, t24Name=USER.FLOATING.INDEX, mandatory=false)
        USER_FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=13.0, core=true, t24Name=USER.FLOATING.NOTICE, mandatory=false)
        USER_INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=19.0, core=true, t24Name=USER.INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
        USER_LINKED_RATE : AA_Interest:AA_ARR_INTEREST__USER_LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=22.0, core=true, t24Name=USER.LINKED.RATE, mandatory=false)
        USER_MARGIN_TYPE -> byValue AA_Interest:AA_ARR_INTEREST__USER_MARGIN_TYPE *
            @t24:i (t24Name=USER.MARGIN.TYPE)
        USER_PERIODIC_INDEX : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=14.0, core=true, t24Name=USER.PERIODIC.INDEX, mandatory=false)
        USER_PERIODIC_METHOD : AA_Interest:AA_ARR_INTEREST__USER_PERIODIC_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=18.0, core=true, t24Name=USER.PERIODIC.METHOD, mandatory=false)
        USER_PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=17.0, core=true, t24Name=USER.PERIODIC.PERIOD, mandatory=false)
        USER_PERIODIC_PERIOD_TYPE : AA_Interest:AA_ARR_INTEREST__USER_PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=15.0, core=true, t24Name=USER.PERIODIC.PERIOD.TYPE, mandatory=false)
        USER_PERIODIC_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=16.0, core=true, t24Name=USER.PERIODIC.RATE, typeModifiers=-, mandatory=false)
        USER_PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=20.0, core=true, t24Name=USER.PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
        USER_RATE_TYPE : AA_Interest:AA_ARR_INTEREST__USER_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=USER.RATE.TYPE, mandatory=false)
        USE_PRICING_GRID : AA_Interest:AA_ARR_INTEREST__USE_PRICING_GRID
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=9.0, core=true, t24Name=USE.PRICING.GRID, mandatory=false)
    }
    AA_ARR_INTEREST__ACTION
    {
/* The activity that caused this arrangement property to be updated.
The activity is taken from the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified */
        ACTION : T24BusinessTypes:A
            @i18n:Label (en=Action)
            @i18n:Tooltip (en=Action)
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=2.0, core=true, t24Name=ACTION, mandatory=false)
    }
    AA_ARR_INTEREST__API_ATTRIBUTE
    {
        API_ATTRIBUTE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=115.0, core=true, t24Name=API.ATTRIBUTE, mandatory=false)
    }
    AA_ARR_INTEREST__CHANGED_FIELDS
    {
        /* No input and multi value field lists all fields that have been negotiated for each dated version of the property. For a subsequently dated property, the attributes listed in this field will appear in "Negotiated.Fields" and only newly negotiated fields will appear on "Changed.Fields" */
        CHANGED_FIELDS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=129.0, core=true, t24Name=CHANGED.FIELDS, mandatory=false)
    }
    AA_ARR_INTEREST__CUSTOM_TYPE
    {
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=75.0, core=true, t24Name=CUSTOM.NAME, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, t24Name=CUSTOM.TYPE, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=76.0, core=true, t24Name=CUSTOM.VALUE, mandatory=false)
    }
    AA_ARR_INTEREST__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=144.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_ARR_INTEREST__FIXED_RATE
    {
        /* Set this field to yes whenever Interest rate should be calculated in a custom way. The method in CUSTOM.RATE.CALC field determines how this custom.Rate should be calculated */
        CUSTOM_RATE : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=36.0, core=true, t24Name=CUSTOM.RATE, mandatory=false)
/* Indicates at which rate is the arrangement currently running on for the date on the Id. This shows the actual rate even if it is running on a Periodic and would include the margin rate calculations as well.

System maintained(NOINPUT) field. */
        EFFECTIVE_RATE : T24BusinessTypes:R
            @i18n:Label (en=Effective)
            @i18n:Tooltip (en="Rate the arrangement is currently running ")
            @t24:i (align=R, inputBehaviour=N, maxLength=13, sysNumber=45.0, core=true, t24Name=EFFECTIVE.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A fixed rate is directly entered by the user specifying the Defined Property and is not defined by an index table.  Fixed rates are entered as positive numbers unless a negative rate is allowed.

This field is part of a multi value set of fields including FIXED.RATE, FLOATING.INDEX, PERIODIC.INDEX, PERIODIC.PERIOD, PERIODIC.BASE, MARGIN.TYPE, MARGIN.OPER, MARGIN.RATE, TIER.MIN.RATE, TIER.MAX.RATE, TIER.AMOUNT,TIER.PERCENT. */
        FIXED_RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en="Indicates the fixed interest rate to be applied")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=26.0, core=true, t24Name=FIXED.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A floating interest rate is tied to a variable base rate (i.e. BASIC.INTEREST).  During interest calculation, T24 will use the currency specific rate applicable for the calculation date (i.e. the rate used for the calculation will change whenever the base rate is changed).

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @i18n:Label (en=Index)
            @i18n:Tooltip (en="Indicates the floating interest rate to be applied")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=27.0, core=true, businessType=A, t24Name=FLOATING.INDEX, mandatory=false)
        /* This field is reserved for future development */
        FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @i18n:Label (en="Floating Notice")
            @i18n:Tooltip (en="Floating Notice")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=28.0, core=true, t24Name=FLOATING.NOTICE, mandatory=false)
/* Specifies the initial date from which the interest rate has to be reset. If specified along with a reset frequency, the initial reset date will be used for deriving the subsequent reset dates. If left blank, the subsequent resets will be based on the base date type in Account Property.

Allowed Options
Relative Dates (Start, Arrangement and Anniversary) or a valid date. */
        INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=34.0, core=true, t24Name=INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
/* It would be possible to specify that the rate for a given tier be ‘Linked’, by setting the field Linked Rate to Yes.

This can be mixed with normal rate calculations as well.

Where the Linked Rate is set to Yes, system would expect that an Arrangement reference in given in the ‘Linked Arrangement’ field along with the Interest property to be linked to. */
        LINKED_RATE : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=37.0, core=true, t24Name=LINKED.RATE, mandatory=false)
        MARGIN_TYPE -> byValue AA_Interest:AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE *
            @t24:i (t24Name=MARGIN.TYPE)
/* A periodic rate is tied to an index (e.g. LIBOR) which is dependent on a period of time (e.g. term) and possibly an amount.  Once the arrangement is active, the rate is fixed until it is reset.  Periodic rates can be reset at a pre-determined frequency.
To define a periodic rate, the user specifies:
PERIODIC.INTEREST index to use
Period (this will default to TERM if a Term Amount Property exists)
Interest selection method if the term is not found on the index. The option currently available is INTERPOLATE.
Rate Reset period

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en=Index)
            @i18n:Tooltip (en="Indicates the Periodic Index key")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=29.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
/* If system does not find a interest rate in the correct bucket of PERIODIC.INTEREST table, it automatically applies a method to arrive at a rate chosen by this method.

Valid method is:

INTERPOLATE - System automatically interpolates the rate. This is the DEFAULT option as well. */
        PERIODIC_METHOD : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__PERIODIC_METHOD
            @i18n:Label (en=Method)
            @i18n:Tooltip (en="Method to be used if interest rate is not defined")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=33.0, core=true, t24Name=PERIODIC.METHOD, mandatory=false)
/* Defines the interest period to be used when linking to PERIODIC.INTEREST.(This will default to TERM if a Term Amount Property exists),. When a period is stated in Years or Weeks, they would be converted to Months and Days respectively and then located on the PI table.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en=Period)
            @i18n:Tooltip (en="Indicates the interest period to be used ")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=32.0, core=true, t24Name=PERIODIC.PERIOD, mandatory=false)
/* This field provides the user to specify which period will be used to fetch interest rate from the Periodic Interest table.
Allowed Values:
Maturity – Interest rate from periodic interest table will be fetched based on the maturity term of the contract.
Renewal – Interest rate from periodic interest table will be fetched based on the renewal term of the contract.
Reset Period - Interest rate from periodic interest table will be fetched based on the reset frequency period (i.e. based on the period to next interest reset date). If a Period is defined in 'Periodic Period' field, the same will be used for fetching the interest rate from Periodic Interest table except for the first and last period.
Periodic Period and Null - Interest rate from periodic interest table will be fetched based on the period defined in 'periodic period' field. */
        PERIODIC_PERIOD_TYPE : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=30.0, core=true, t24Name=PERIODIC.PERIOD.TYPE, mandatory=false)
/* Indicates the rate on which the index runs on the date stated in the key. When a periodic index is used and the field is left blank, then system computes the rate from PERIODIC.INTEREST and defaults it here. When a rate is already present(say, the user changes the system computed rate), then system does not compute the new rate.

Validation rules:

System defaulted and modifiable.
Standard Rate field.
Valid only if Periodic index is stated. */
        PERIODIC_RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en="Indicates the periodic interest rate to be applied")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=31.0, core=true, t24Name=PERIODIC.RATE, typeModifiers=-, mandatory=false)
/* This indicates the reset period of the index stated. System would automatically reset the PERIODIC.RATE at the frequency stated in this field. Applicable only at Arrangement level. Periodic index may still be used without this field meaning, it never resets the computed rate

Validation rule:

Valid PERIOD field. */
        PERIODIC_RESET : T24BusinessTypes:FQU
            @i18n:Label (en="Reset Frequency ")
            @i18n:Tooltip (en="Indicates the reset period of the index stated")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=35.0, core=true, t24Name=PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
/* The tier amount relates to the field RATE.TIER.TYPE and amounts can be entered if either BAND or LEVEL are selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Up To")
            @i18n:Tooltip (en="Indicates the amount for each tier type")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=TIER.AMOUNT, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MAX_RATE : T24BusinessTypes:R
            @i18n:Label (en=Max)
            @i18n:Tooltip (en="Maximum rate applied to calculate interest")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=43.0, core=true, t24Name=TIER.MAX.RATE, typeModifiers=-, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MIN_RATE : T24BusinessTypes:R
            @i18n:Label (en=Min)
            @i18n:Tooltip (en="Minimum rate applied to calculate interest")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=TIER.MIN.RATE, typeModifiers=-, mandatory=false)
/* Originally negative rate was applicable for all of the tiers together, but through this field it is now possible to define negative rate per tier.

Different values can be set in each tier and the options available are YES, BLOCK.MARGIN and NO/Blank.

Blank or NO - Indicates that negative rates cannot be input and if a rate becomes negative as a result of the application of a margin the rate will be set to zero

YES - Indicates that negative rates can be input and if the application margin makes the rate more negative then the more negative rate will be used.

BLOCK.MARGIN - Indicates that negative rates can be used but the following conditions apply

- If the rate is positive and the margin makes the rate negative then the rate will be set to zero

- If the rate is negative and the margin makes the rate more negative then the margin will be ignored and the original negative rate is used. */
        TIER_NEGATIVE_RATE : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=44.0, core=true, t24Name=TIER.NEGATIVE.RATE, mandatory=false)
/* The tier percent relates to the field RATE.TIER.TYPE and percentages can be entered if BAND is selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. Either TIER.PERCENT or TIER.AMOUNT can be entered by not both. If TIER.PERCENT is defined, then the Sum of Tier percentage should be 100% */
        TIER_PERCENT : T24BusinessTypes:N
            @i18n:Label (en="Tier Percentage")
            @i18n:Tooltip (en="Tier Percentage")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=47.0, core=true, t24Name=TIER.PERCENT, mandatory=false)
        USAGE_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=USAGE.PERCENT, mandatory=false)
    }
    AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE
    {
/* The options are ADD or SUB. This indicates whether the MARGIN.RATE should be added or subtracted from the Floating or Periodic Rate.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.RATE and input is only allowed if a rate is entered in MARGIN.RATE. */
        MARGIN_OPER : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER
            @i18n:Label (en=Operand)
            @i18n:Tooltip (en="Whether margin rate should be added or subtracted")
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=40.0, core=true, t24Name=MARGIN.OPER, mandatory=false)
/* A margin rate can be used to adjust the specified rate of interest and to appropriately reflect any rate profit realized.  The result is the nominal rate of interest.  T24 allows for the following margin configuration:
Single or Multiple Margins (Field MARGIN.TYPE)
Rate Adjustment Method (Field MARGIN.OPER)

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.OPER. */
        MARGIN_RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en="Margin rate to adjust specified rate of interest ")
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=41.0, core=true, t24Name=MARGIN.RATE, mandatory=false)
/* Identifies whether there are single or multiple margins

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.OPER and MARGIN.RATE. */
        MARGIN_TYPE : AA_Interest:AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE
            @i18n:Label (en=Type)
            @i18n:Tooltip (en="Whether there are single or multiple margins")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=39.0, core=true, t24Name=MARGIN.TYPE, mandatory=false)
    }
    AA_ARR_INTEREST__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=143.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_ARR_INTEREST__NEGOTIATED_FLDS
    {
/* No input and multi value field that lists the attributes on the arrangement property record that have been fixed and/or negotiated. These are stored when the arrangement level record is committed. When the arrangement level record is next used all items in the NEGOTIATED.FIELDS list will be maintained
Once an attribute has been fixed or negotiated it will remain in negotiated field list on all subsequent dated versions of the property. */
        NEGOTIATED_FLDS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=130.0, core=true, t24Name=NEGOTIATED.FLDS, mandatory=false)
    }
    AA_ARR_INTEREST__NR_ATTRIBUTE
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_ATTRIBUTE : T24BusinessTypes:A
            @i18n:Label (en="Negotiable Attribute")
            @i18n:Tooltip (en="Negotiable Attribute")
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=121.0, core=true, t24Name=NR.ATTRIBUTE, mandatory=false)
        NR_ATTRIBUTE_RULE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=123.0, core=true, t24Name=NR.ATTRIBUTE.RULE, mandatory=false)
        NR_OPTIONS -> byValue AA_Interest:AA_ARR_INTEREST__NR_ATTRIBUTE__NR_OPTIONS *
            @t24:i (t24Name=NR.OPTIONS)
/* Not used for Arrangement Conditions



Input not allowed */
        NR_STD_COMP -> AA_Rules:EB_STANDARD_COMPARISON
            @i18n:Label (en="Negotiable Standard Comp")
            @i18n:Tooltip (en="Negotiable Standard Comp")
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=125.0, core=true, businessType=A, t24Name=NR.STD.COMP, mandatory=false)
        NR_TYPE -> byValue AA_Interest:AA_ARR_INTEREST__NR_ATTRIBUTE__NR_TYPE *
            @t24:i (t24Name=NR.TYPE)
        NR_VALUE_SOURCE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=60, sysNumber=124.0, core=true, t24Name=NR.VALUE.SOURCE, mandatory=false)
    }
    AA_ARR_INTEREST__NR_ATTRIBUTE__NR_OPTIONS
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_OPTIONS : AA_Interest:AA_ARR_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS
            @i18n:Label (en="Negotiable Options")
            @i18n:Tooltip (en="Negotiable Options")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=122.0, core=true, t24Name=NR.OPTIONS, mandatory=false)
    }
    AA_ARR_INTEREST__NR_ATTRIBUTE__NR_TYPE
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_MESSAGE : AA_Interest:AA_ARR_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE
            @i18n:Label (en="Negotiable Message")
            @i18n:Tooltip (en="Negotiable Message")
            @t24:i (align=L, inputBehaviour=N, maxLength=10, sysNumber=128.0, core=true, t24Name=NR.MESSAGE, mandatory=false)
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_TYPE -> AA_Rules:EB_COMPARISON_TYPE
            @i18n:Label (en="Negotiable Type")
            @i18n:Tooltip (en="Negotiable Type")
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=126.0, core=true, businessType=A, t24Name=NR.TYPE, mandatory=false)
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_VALUE : T24BusinessTypes:ANY
            @i18n:Label (en="Negotiable Value")
            @i18n:Tooltip (en="Negotiable Value")
            @t24:i (align=L, inputBehaviour=N, maxLength=60, sysNumber=127.0, core=true, t24Name=NR.VALUE, mandatory=false)
    }
    AA_ARR_INTEREST__OFFSET_DETAILS
    {
        OFFSET_DETAILS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=101.4, core=false, t24Name=OFFSET.DETAILS, mandatory=false)
    }
    AA_ARR_INTEREST__ON_ACTIVITY
    {
/* When the periodic interest rate needs to be recalculated for certain activities, this field in combination with RECALCULATE may be used to setup the activity for which interest needs to be recalculated
Validation Rules
1. Applicable only for Periodic interest rate recalculation
2. Must be a valid activity in AA.ACTIVITY */
        ON_ACTIVITY -> AA_ProductFramework:AA_ACTIVITY
            @i18n:Label (en="On Activity")
            @i18n:Tooltip (en="On Activity")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=48.0, core=true, businessType=A, t24Name=ON.ACTIVITY, mandatory=false)
/* Related to ON.ACTIVITY activity, when flagged to RATE, system will recalculate Periodic rate when the activity setup in ON.ACTIVITY is processed. When the value is NOTHING periodic rate would not be recalculated when the activity setup in ON.ACTIVITY is processed.
Validation Rules
1. Mandatory when ON.ACTIVITY is setup
The fields ON.ACTIVITY and RECALCULATE in interest property class will be enhanced to allow user to define when to recalculate ‘Profit Amount’.
All activities which could result in payment schedule recalculation can be configured to recalculate ‘Profit Amount’ i.e., activities with payment schedule calculate and update action */
        RECALCULATE : AA_Interest:AA_ARR_INTEREST__ON_ACTIVITY__RECALCULATE
            @i18n:Label (en=Recalculate)
            @i18n:Tooltip (en=Recalculate)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=RECALCULATE, mandatory=false)
    }
    AA_ARR_INTEREST__OVERRIDE
    {
        /* Standard Override field containing override messages generated by amendments to this property */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=140.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    AA_ARR_INTEREST__OWNING_COMPANY
    {
/* This field is used to capture the T24 Company(ies) that own the Product Condition record. There is no functionality attached to this field and can be used only for access control through SMS verification.

The input in this field must be a valid record in company table and should belong to current financial group.

This is a multi value field. Input in this field is optional. */
        OWNING_COMPANY -> ST_CompanyCreation:COMPANY
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=114.0, core=true, businessType=AAPRODUCT, t24Name=OWNING.COMPANY, typeModifiers=COMPANY, mvSvExpansionAccess=NOEXPAND, mandatory=false)
    }
    AA_ARR_INTEREST__PRICING_RULES_BENEFIT
    {
        PRICING_RULES_BENEFIT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=PRICING.RULES.BENEFIT, mandatory=false)
        PRICING_RULES_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=93.0, core=true, t24Name=PRICING.RULES.FIXED.RATE, mandatory=false)
        PRICING_RULES_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=92.0, core=true, t24Name=PRICING.RULES.MARGIN, mandatory=false)
        PRICING_RULES_OPERAND : AA_Interest:AA_ARR_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=91.0, core=true, t24Name=PRICING.RULES.OPERAND, mandatory=false)
    }
    AA_ARR_INTEREST__PR_ATTRIBUTE
    {
/* This will hold two values either DUE or CAPITALISE
This Field denote which application method(DUE or CAPITALISE)should be taken while applying periodic rule charges.
Not inputtable at arrangement level */
        PR_APP_METHOD : AA_Interest:AA_ARR_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD
            @i18n:Label (en="Periodic Application Method")
            @i18n:Tooltip (en="Periodic Application Method")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=110.0, core=true, t24Name=PR.APP.METHOD, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* This will have to accept a valid T24 period.
This Field will denote the application period for current aplication method.
Not inputtable at arrangement level */
        PR_APP_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Application Period")
            @i18n:Tooltip (en="Periodic Application Period")
            @t24:i (align=L, inputBehaviour=N, maxLength=4, sysNumber=111.0, core=true, t24Name=PR.APP.PERIOD, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* These are set of Attributes which define the names of the rule, which will be applied on certain attributes of property classes over a period of time


Input must be a valid record from AA.PERIODIC.ATTRIBUTE */
        PR_ATTRIBUTE -> AA_Rules:AA_PERIODIC_ATTRIBUTE
            @i18n:Label (en="Periodic Attribute")
            @i18n:Tooltip (en="Periodic Attribute")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=102.0, core=true, businessType=A, t24Name=PR.ATTRIBUTE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* Not used for arrangement level conditions


Input not allowed. */
        PR_BRK_CHARGE -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Periodic Break Charge")
            @i18n:Tooltip (en="Periodic Break Charge")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=106.0, core=true, businessType=A, t24Name=PR.BRK.CHARGE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* Based on the PR.BRK.RES option selected, this will hold either an OVERRIDE or EB.ERROR id. By default the ids will be picked from the associated EB.COMPARISON.TYPE record. If CAP or FLOOR values are given, then the underlying AA.PERIODIC.ATTRIBUTE.CLASS should have this type defined in it


Input must be a valid OVERRIDE ID or EB.ERROR ID */
        PR_BRK_MSG : T24BusinessTypes:ANY
            @i18n:Label (en="Periodic Break Message")
            @i18n:Tooltip (en="Periodic Break Message")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=105.0, core=true, t24Name=PR.BRK.MSG, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* When a rule is broken what action needs to be taken is defined here. Currently, the various options available are

Error block further processing
Override Give an override
Information  Record details only
CAP the rule value is to be used as a CAP if the rule is broken
FLOOR the rule value is to be used as a FLOOR if the rule is broken


Should be one of the above valid options */
        PR_BRK_RES : AA_Interest:AA_ARR_INTEREST__PR_ATTRIBUTE__PR_BRK_RES
            @i18n:Label (en="Periodic Break Restrcition")
            @i18n:Tooltip (en="Periodic Break Restrcition")
            @t24:i (align=L, inputBehaviour=N, maxLength=13, sysNumber=104.0, core=true, t24Name=PR.BRK.RES, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=PR.RESERVED.1, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=PR.RESERVED.2, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=107.0, core=true, t24Name=PR.RESERVED.3, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* This is the actual rule value, which will be applied on certain attributes of the property classes over a period of time.


Input must be a valid data type defined in the corresponding
AA.PERIODIC.ATTRIBUTE.CLASS record. */
        PR_VALUE : T24BusinessTypes:A
            @i18n:Label (en="Periodic Value")
            @i18n:Tooltip (en="Periodic Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=103.0, core=true, t24Name=PR.VALUE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
    }
    AA_ARR_INTEREST__STMT_NOS
    {
        /* Contains the key to the accounting entries raised by the activity being applied to this arrangement property */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=139.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    AA_ARR_INTEREST__USER_MARGIN_TYPE
    {
        USER_MARGIN_OPER : AA_Interest:AA_ARR_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=24.0, core=true, t24Name=USER.MARGIN.OPER, mandatory=false)
        USER_MARGIN_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=USER.MARGIN.RATE, mandatory=false)
        USER_MARGIN_TYPE : AA_Interest:AA_ARR_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=23.0, core=true, t24Name=USER.MARGIN.TYPE, mandatory=false)
    }
/* This table is used to define the EB.API records that can be used for calculation of Custom rate for interest property class
ID of this can be Alphanumeric. */
    AA_CUSTOM_RATE_TYPE
        @i18n:Description (en="")
        @t24:i (t24Name=AA.CUSTOM.RATE.TYPE, level=I, type=H, allowedFunctions="A C D E H I L P Q R S", localRefApplicationSysNo=8.0, localRefAllowed=true)
    {
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=17.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=18.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=14.0, core=true, t24Name=AUTHORISER, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=15.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=11.0, core=true, t24Name=CURR.NO, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_CUSTOM_RATE_TYPE__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=16.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue AA_Interest:AA_CUSTOM_RATE_TYPE__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        ID -> AA_Interest:AA_CUSTOM_RATE_TYPE PK required
            @i18n:Label (en="Transaction Ref")
            @i18n:Tooltip (en="Transaction Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=ID, mandatory=true)
        INPUTTER -> byValue AA_Interest:AA_CUSTOM_RATE_TYPE__INPUTTER *
            @t24:i (t24Name=INPUTTER)
        OVERRIDE -> byValue AA_Interest:AA_CUSTOM_RATE_TYPE__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=10.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
        RESERVERD_1 : T24BusinessTypes:S
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=7.0, core=true, t24Name=RESERVERD.1, mandatory=false)
        RESERVERD_2 : T24BusinessTypes:S
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=6.0, core=true, t24Name=RESERVERD.2, mandatory=false)
        RESERVERD_3 : T24BusinessTypes:S
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=5.0, core=true, t24Name=RESERVERD.3, mandatory=false)
        RESERVERD_4 : T24BusinessTypes:S
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=4.0, core=true, t24Name=RESERVERD.4, mandatory=false)
        RESERVERD_5 : T24BusinessTypes:S
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=3.0, core=true, t24Name=RESERVERD.5, mandatory=false)
        /* Id of a EB.API record which is suitable for calculating Custom interest rate */
        ROUTINE_NAME -> EB_SystemTables:EB_API required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, businessType=S, t24Name=ROUTINE.NAME, mandatory=true)
    }
    AA_CUSTOM_RATE_TYPE__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=13.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_CUSTOM_RATE_TYPE__DESCRIPTION
    {
        /* Describes method functionality */
        DESCRIPTION : T24BusinessTypes:A required
            @i18n:Label (en=Description)
            @i18n:Tooltip (en=Description)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=1.0, core=true, t24Name=DESCRIPTION, mandatory=true)
    }
    AA_CUSTOM_RATE_TYPE__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=12.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_CUSTOM_RATE_TYPE__OVERRIDE
    {
        OVERRIDE : T24BusinessTypes:A
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=9.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
/* The file AA.INTEREST.ACCRUALS holds the interest accrual information for an arrangement for a given interest property.

This live file stores the interest rate, number of days, principal balance, basis used for a given period and the accrual amount. The fields showing the above information are associated. A new set of multivalues is formed for every change interest rate, basis, balance and period. If there is no change in any of the above said parameters, a new set is shown for every month. */
    AA_INTEREST_ACCRUALS
        @i18n:Description (en="")
        @t24:i (t24Name=AA.INTEREST.ACCRUALS, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        ABB_NEW_METHOD -> byValue AA_Interest:AA_INTEREST_ACCRUALS__ABB_NEW_METHOD *
            @t24:i (t24Name=ABB.NEW.METHOD)
        /* This field is updated with the interest property when system evaluates a restriction rule which results in the interest amount associated for the property is waived and an alternate property interest amount is calculated and made-due */
        ALTERNATE_INTEREST : T24BusinessTypes:A
            @i18n:Label (en="Alternate Interest")
            @i18n:Tooltip (en="Alternate Interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=27.0, core=true, t24Name=ALTERNATE.INTEREST, mandatory=false)
        EFFECTIVE_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__EFFECTIVE_DATE *
            @t24:i (t24Name=EFFECTIVE.DATE)
        /* Denotes the final occurrence of the Fixed Interest Method. On this final date, any rounding differences between the agreed profit amount and accrued amounts so far will be posted. */
        FINAL_SCHEDULE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=53.0, core=true, t24Name=FINAL.SCHEDULE, mandatory=false)
        FIXED_INT_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FIXED_INT_AMT *
            @t24:i (t24Name=FIXED.INT.AMT)
        FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE *
            @t24:i (t24Name=FROM.DATE)
        INFO_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE *
            @t24:i (t24Name=INFO.FROM.DATE)
        INFO_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_PERIOD_START *
            @t24:i (t24Name=INFO.PERIOD.START)
        INT_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INT_EFF_DATE *
            @t24:i (t24Name=INT.EFF.DATE)
        IPROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE *
            @t24:i (t24Name=IPROJ.FROM.DATE)
        IPROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_PERIOD_START *
            @t24:i (t24Name=IPROJ.PERIOD.START)
        /* This field represent the date when the record was last archived. */
        LAST_ARCHIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Last Archive Date")
            @i18n:Tooltip (en="Last Archive Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=12.0, core=true, t24Name=LAST.ARCHIVE.DATE, mandatory=false)
        LINKED_INT_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__LINKED_INT_DATE *
            @t24:i (t24Name=LINKED.INT.DATE)
        LINKED_RATE_CHANGE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__LINKED_RATE_CHANGE *
            @t24:i (t24Name=LINKED.RATE.CHANGE)
        LINK_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__LINK_EFF_DATE *
            @t24:i (t24Name=LINK.EFF.DATE)
        /* This field indicates whether the calculated interest has to be billed at the start of the period or not, This will hold only one value "ADVANCE". If it is set then the TOT.DUE.AMT will be updated at start of the period. */
        PAYMENT_MODE : T24BusinessTypes:A
            @i18n:Label (en="Payment Mode")
            @i18n:Tooltip (en="Mode of payment like account, cash, cheque etc.")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=35.0, core=true, t24Name=PAYMENT.MODE, mandatory=false)
        PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PERIOD_START *
            @t24:i (t24Name=PERIOD.START)
        PROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE *
            @t24:i (t24Name=PROJ.FROM.DATE)
        PROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_PERIOD_START *
            @t24:i (t24Name=PROJ.PERIOD.START)
        /* This field represents the id of the file. The id has two parts - arrangement number and the property name separated by hyphen. */
        PROPERTY_NAME -> AA_Interest:AA_INTEREST_ACCRUALS PK required
            @i18n:Label (en="Property Name")
            @i18n:Tooltip (en="Property Name")
            @t24:i (align=L, inputBehaviour=I, maxLength=50, sysNumber=0.0, core=true, businessType=ANY, t24Name=PROPERTY.NAME, mandatory=true)
    }
/* The file AA.INTEREST.ACCRUALS.WORK holds the interest accrual information for an arrangement for a given interest property at unauth stage. The record is deleted during authorisation.

This live file stores the interest rate, number of days, principal balance, basis used for a given period and the accrual amount. The fields showing the above information are associated. A new set of multivalues is formed for every change interest rate, basis, balance and period. If there is no change in any of the above said parameters, a new set is shown for every month. */
    AA_INTEREST_ACCRUALS_WORK
        @i18n:Description (en="")
        @t24:i (t24Name=AA.INTEREST.ACCRUALS.WORK, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        ABB_NEW_METHOD -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__ABB_NEW_METHOD *
            @t24:i (t24Name=ABB.NEW.METHOD)
        /* This field is updated with the interest property when system evaluates a restriction rule which results in the interest amount associated for the property is waived and an alternate property interest amount is calculated and made-due */
        ALTERNATE_INTEREST : T24BusinessTypes:A
            @i18n:Label (en="Alternate Interest")
            @i18n:Tooltip (en="Alternate Interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=27.0, core=true, t24Name=ALTERNATE.INTEREST, mandatory=false)
        EFFECTIVE_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__EFFECTIVE_DATE *
            @t24:i (t24Name=EFFECTIVE.DATE)
        /* Denotes the final occurrence of the Fixed Interest Method. On this final date, any rounding differences between the agreed profit amount and accrued amounts so far will be posted. */
        FINAL_SCHEDULE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=53.0, core=true, t24Name=FINAL.SCHEDULE, mandatory=false)
        FIXED_INT_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FIXED_INT_AMT *
            @t24:i (t24Name=FIXED.INT.AMT)
        FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE *
            @t24:i (t24Name=FROM.DATE)
        INFO_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE *
            @t24:i (t24Name=INFO.FROM.DATE)
        INFO_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_PERIOD_START *
            @t24:i (t24Name=INFO.PERIOD.START)
        INT_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INT_EFF_DATE *
            @t24:i (t24Name=INT.EFF.DATE)
        IPROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE *
            @t24:i (t24Name=IPROJ.FROM.DATE)
        IPROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_PERIOD_START *
            @t24:i (t24Name=IPROJ.PERIOD.START)
        /* This field represent the date when the record was last archived. */
        LAST_ARCHIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Last Archive Date")
            @i18n:Tooltip (en="Last Archive Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=12.0, core=true, t24Name=LAST.ARCHIVE.DATE, mandatory=false)
        LINKED_INT_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__LINKED_INT_DATE *
            @t24:i (t24Name=LINKED.INT.DATE)
        LINKED_RATE_CHANGE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__LINKED_RATE_CHANGE *
            @t24:i (t24Name=LINKED.RATE.CHANGE)
        LINK_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__LINK_EFF_DATE *
            @t24:i (t24Name=LINK.EFF.DATE)
        /* This field indicates whether the calculated interest has to be billed at the start of the period or not, This will hold only one value "ADVANCE". If it is set then the TOT.DUE.AMT will be updated at start of the period. */
        PAYMENT_MODE : T24BusinessTypes:A
            @i18n:Label (en="Payment Mode")
            @i18n:Tooltip (en="Mode of payment like account, cash, cheque etc.")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=35.0, core=true, t24Name=PAYMENT.MODE, mandatory=false)
        PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PERIOD_START *
            @t24:i (t24Name=PERIOD.START)
        PROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE *
            @t24:i (t24Name=PROJ.FROM.DATE)
        PROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_PERIOD_START *
            @t24:i (t24Name=PROJ.PERIOD.START)
        /* This field represents the id of the file. The id has two parts - arrangement number and the property name separated by hyphen. */
        PROPERTY_NAME -> AA_Interest:AA_INTEREST_ACCRUALS_WORK PK required
            @i18n:Label (en="Property Name")
            @i18n:Tooltip (en="Property Name")
            @t24:i (align=L, inputBehaviour=I, maxLength=50, sysNumber=0.0, core=true, businessType=ANY, t24Name=PROPERTY.NAME, mandatory=true)
    }
    AA_INTEREST_ACCRUALS_WORK__ABB_NEW_METHOD
    {
        ABB_NEW_METHOD : AA_Interest:AA_INTEREST_ACCRUALS_WORK__ABB_NEW_METHOD__ABB_NEW_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=111.0, core=true, t24Name=ABB.NEW.METHOD, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__EFFECTIVE_DATE
    {
        /* Denotes the date from which the Profit Rate is applicable for this Interest method. Arranged in descending order, it will store the related rate on which Interest will be accrued, the corresponding profit amount & the activity reference that resulted in this update. */
        EFFECTIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Effective Date")
            @i18n:Tooltip (en="Effective date of the transaction")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=54.0, core=true, t24Name=EFFECTIVE.DATE, mandatory=false)
        PROFIT_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__EFFECTIVE_DATE__PROFIT_RATE *
            @t24:i (t24Name=PROFIT.RATE)
    }
    AA_INTEREST_ACCRUALS_WORK__EFFECTIVE_DATE__PROFIT_RATE
    {
        /* Denotes the activity reference that has resulted in the update of the above said details. This will be used to remove all related information when the activity is reversed. */
        ACTIVITY_REF : T24BusinessTypes:A
            @i18n:Label (en="Activity ID")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=ACTIVITY.REF, mandatory=false)
        /* Stores the system calculated profit amount if any for the Fixed Interest method. This will happen for Flat Rate/Reducing Rate option when the user defined profit amount does not exist. */
        CALC_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=CALC.PROFIT.AMT, mandatory=false)
        /* Denotes the profit amount applicable for this property for the life of the contract. */
        PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=PROFIT.AMT, mandatory=false)
        /* Denotes the Rate applicable for accrual of Fixed Interest Method. Applicable only for Fixed type of Interest. It is updated along with an effective date from which this rate is applicable and the corresponding activity reference. */
        PROFIT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=55.0, core=true, t24Name=PROFIT.RATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FIXED_INT_AMT
    {
        /* This field is updated with the fixed interest amount defined in Payment Schedule for the current interest period in PERIOD.START and PERIOD.END */
        FIXED_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=34.0, core=true, t24Name=FIXED.INT.AMT, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE
    {
        ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__ACCRUAL_AMT *
            @t24:i (t24Name=ACCRUAL.AMT)
        BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__BALANCE *
            @t24:i (t24Name=BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        BASIS : AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__BASIS
            @i18n:Label (en=Basis)
            @i18n:Tooltip (en=Basis)
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=5.0, core=true, t24Name=BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compound)
            @i18n:Tooltip (en=Compound)
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=10.0, core=true, t24Name=COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__COMPOUND_YIELD
            @i18n:Label (en="Compound Yield")
            @i18n:Tooltip (en="Compound Yield")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=11.0, core=true, t24Name=COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        DAYS : T24BusinessTypes:N
            @i18n:Label (en=Days)
            @i18n:Tooltip (en=Days)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, t24Name=DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        FROM_DATE : T24BusinessTypes:D
            @i18n:Label (en="From Date")
            @i18n:Tooltip (en="From Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=1.0, core=true, t24Name=FROM.DATE, mandatory=false)
        MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__MARGIN *
            @t24:i (t24Name=MARGIN)
        RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__RATE *
            @t24:i (t24Name=RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        TO_DATE : T24BusinessTypes:D
            @i18n:Label (en="To Date")
            @i18n:Tooltip (en="To Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=2.0, core=true, t24Name=TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACCRUAL_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Accrual Amount")
            @i18n:Tooltip (en="Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=8.0, core=true, t24Name=ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACT_ACC_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Actual Accrual Amount")
            @i18n:Tooltip (en="Actual Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=9.0, core=true, t24Name=ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        BALANCE : T24BusinessTypes:AMT
            @i18n:Label (en=Balance)
            @i18n:Tooltip (en=Balance)
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=4.0, core=true, t24Name=BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        MARGIN : AA_Interest:AA_INTEREST_ACCRUALS_WORK__FROM_DATE__MARGIN__MARGIN
            @i18n:Label (en=Margin)
            @i18n:Tooltip (en=Margin)
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=7.0, core=true, t24Name=MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en=Rate)
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=6.0, core=true, t24Name=RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE
    {
        INFO_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_ACCRUAL_AMT *
            @t24:i (t24Name=INFO.ACCRUAL.AMT)
        INFO_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_BALANCE *
            @t24:i (t24Name=INFO.BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        INFO_BASIS : AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=40.0, core=true, t24Name=INFO.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        INFO_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=45.0, core=true, t24Name=INFO.COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        INFO_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=46.0, core=true, t24Name=INFO.COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        INFO_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=38.0, core=true, t24Name=INFO.DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=36.0, core=true, t24Name=INFO.FROM.DATE, mandatory=false)
        INFO_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_MARGIN *
            @t24:i (t24Name=INFO.MARGIN)
        INFO_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_RATE *
            @t24:i (t24Name=INFO.RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=37.0, core=true, t24Name=INFO.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=43.0, core=true, t24Name=INFO.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=44.0, core=true, t24Name=INFO.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        INFO_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=39.0, core=true, t24Name=INFO.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        INFO_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=INFO.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        INFO_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=41.0, core=true, t24Name=INFO.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INFO_PERIOD_START
    {
        INFO_ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=49.0, core=true, t24Name=INFO.ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=48.0, core=true, t24Name=INFO.PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=47.0, core=true, t24Name=INFO.PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.

This is just an information fields, no entries will be raised based on this.

Standard T24 amount field of upto 19 characters */
        INFO_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=INFO.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=INFO.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=INFO.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INT_EFF_DATE
    {
        FLOATING_INDEX -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__INT_EFF_DATE__FLOATING_INDEX *
            @t24:i (t24Name=FLOATING.INDEX)
        /* Associated with FLOATING.INDEX or PERIODIC.INDEX. Stores the dates when there is a change in BASIC.INTEREST or PERIODIC.INTEREST index. */
        INT_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Interest Effective Date")
            @i18n:Tooltip (en="Interest Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=24.0, core=true, t24Name=INT.EFF.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__INT_EFF_DATE__FLOATING_INDEX
    {
        /* BASIC.INTEREST key associated with the INT.EFF.DATE */
        FLOATING_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Floating Index")
            @i18n:Tooltip (en="Floating Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=25.0, core=true, t24Name=FLOATING.INDEX, mandatory=false)
        /* PERIODIC.INTEREST key associated with the INT.EFF.DATE */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @i18n:Tooltip (en="Periodic Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=26.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE
    {
        IPROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT *
            @t24:i (t24Name=IPROJ.ACCRUAL.AMT)
        IPROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_BALANCE *
            @t24:i (t24Name=IPROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=96.0, core=true, t24Name=IPROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        IPROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.0, core=true, t24Name=IPROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        IPROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=102.0, core=true, t24Name=IPROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=94.0, core=true, t24Name=IPROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=92.0, core=true, t24Name=IPROJ.FROM.DATE, mandatory=false)
        IPROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_MARGIN *
            @t24:i (t24Name=IPROJ.MARGIN)
        IPROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_RATE *
            @t24:i (t24Name=IPROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=93.0, core=true, t24Name=IPROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=99.0, core=true, t24Name=IPROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=100.0, core=true, t24Name=IPROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        IPROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=95.0, core=true, t24Name=IPROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=98.0, core=true, t24Name=IPROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=97.0, core=true, t24Name=IPROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__IPROJ_PERIOD_START
    {
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=104.0, core=true, t24Name=IPROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=103.0, core=true, t24Name=IPROJ.PERIOD.START, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        IPROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=105.0, core=true, t24Name=IPROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=107.0, core=true, t24Name=IPROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=106.0, core=true, t24Name=IPROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__LINKED_INT_DATE
    {
        /* The field describes when link rate is changed. It's associated with LINK.INT.RATE field. */
        LINKED_INT_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=108.0, core=true, t24Name=LINKED.INT.DATE, mandatory=false)
        /* The field describes the link rate on a particular date. It's associated with LINK.INT.DATE field. */
        LINKED_INT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=109.0, core=true, t24Name=LINKED.INT.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__LINKED_RATE_CHANGE
    {
        /* This field contains the list of link rate change dates. It'll be cleared once change interest is processed for the child arrangements in COB. */
        LINKED_RATE_CHANGE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=110.0, core=true, t24Name=LINKED.RATE.CHANGE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__LINK_EFF_DATE
    {
        LINKED_ARRANGEMENT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__LINK_EFF_DATE__LINKED_ARRANGEMENT *
            @t24:i (t24Name=LINKED.ARRANGEMENT)
        /* This field indicates the effective date when the arrangement in the field LINKED.ARRANGEMENT is linked to the bundle arrangement */
        LINK_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Link Effective Date")
            @i18n:Tooltip (en="Link Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=28.0, core=true, t24Name=LINK.EFF.DATE, mandatory=false)
/* The maximum % of the recipient balance that can be offset by the Donation.
For ex, in an Offset mortgage, if the Principal is -85,000 and if the Donor balances amount to credit +100,000 then specifying Max Offset as 100% would mean only 85,000 of the credit balances are offset against the Recipient's debit balance.
There is no additional benefit for the surplus 15,000 in the Donor Balances 80% in Max Offset would mean only 68,000 (80% of 85,000) of the credit balance can be offset against the recipient's debit balance In the case of Interest Compensation, this will be set to "" to indicate that there is no "Offset" percentage but only a consolidation of Donor and Recipient balances to calculate interest on */
        MAX_OFF_SET : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=33.0, core=true, t24Name=MAX.OFF.SET, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__LINK_EFF_DATE__LINKED_ARRANGEMENT
    {
        /* This field is updated with the arrangement reference for a bundle arrangement, where the interest is calculated at the master arrangement level pooling the balances of all child/linked arrangements */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @i18n:Label (en="Linked Arrangement")
            @i18n:Tooltip (en="Linked Arrangement")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=29.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* A user can optionally specifiy one or more Balances being donated by the donor.
Sub Valued field to allow multiple balance types to be donated.
Specify the actual Balance type that needs to be taken for compensation.
If this field is left blank, then it means that donor is not donating any balance.
This may prove useful when a participant, by just being part of a package, there is a need to suppress their Interest (say, a Penalty Interest) which would otherwise accrue based on unauthorised overdraft balances */
        LINKED_BALANCE_TYPE -> AC_SoftAccounting:AC_BALANCE_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=31.0, core=true, businessType=A, t24Name=LINKED.BALANCE.TYPE, mandatory=false)
        /* This field indicates the interest property for which the interest amount is calculated at the bundle level rather than at individual arrangements */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Linked Property")
            @i18n:Tooltip (en="Linked Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=30.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
        /* The Credit/Debit specification at the bundle contradicts with the Source Calc type of the Interest property Sub Value field associated with Donor Balance Type Input is allowed only if Donor Balance Type is input Because the Donor Balance type can have Credit or Debit balances on a given day, there would be a flag to specify which balances of the balance type should be used for Offset */
        LINKED_SOURCE_CALC_TYPE -> AA_Framework:AA_SOURCE_CALC_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=32.0, core=true, businessType=A, t24Name=LINKED.SOURCE.CALC.TYPE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        ADJUST_INT_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Adjust Interest Amount")
            @i18n:Tooltip (en="Adjust Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ADJUST.INT.AMT, mandatory=false)
        ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=15.0, core=true, t24Name=ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_END : T24BusinessTypes:D
            @i18n:Label (en="Period End")
            @i18n:Tooltip (en="Period End")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=14.0, core=true, t24Name=PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_START : T24BusinessTypes:D
            @i18n:Label (en="Period Start")
            @i18n:Tooltip (en="Period Start")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=13.0, core=true, t24Name=PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.


Standard T24 amount field of upto 19 characters */
        TOT_ACCR_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Accrual Amount")
            @i18n:Tooltip (en="Total Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=16.0, core=true, t24Name=TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total amount made as due for the interest periods as in PERIOD.START and PERIOD.END. This may be different from the TOT.ACCR.AMT when a bill is issued before the due date and subsequently a change in interest, basis, principal changes the accrued amount, in this case due amount would be based on the bill issued, i.e. the billed amount. */
        TOT_DUE_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Due Amount")
            @i18n:Tooltip (en="Total Due Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=22.0, core=true, t24Name=TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        TOT_RES_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Residual Amount")
            @i18n:Tooltip (en="Total Residual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        TOT_RPY_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Repay Amount")
            @i18n:Tooltip (en="Total Repay Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=19.0, core=true, t24Name=TOT.RPY.AMT, typeModifiers=-, mandatory=false)
/* This field represents the total suspended amount till the accrued date for the current period.
This field will get updated when interest is accrued after arrangement is suspended.
This field will also get updated when SUSPEND activity is run and SUSPEND.OVERDUE is set for the interest property.
The interest accrued from last payment date till suspend activity run date will be updated.

Standard T24 amount field of upto 19 characters */
        TOT_SUSP_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Suspend Amount")
            @i18n:Tooltip (en="Total Suspend Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=17.0, core=true, t24Name=TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE
    {
        PROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT *
            @t24:i (t24Name=PROJ.ACCRUAL.AMT)
        PROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_BALANCE *
            @t24:i (t24Name=PROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=63.0, core=true, t24Name=PROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        PROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=68.0, core=true, t24Name=PROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        PROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=69.0, core=true, t24Name=PROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=61.0, core=true, t24Name=PROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=59.0, core=true, t24Name=PROJ.FROM.DATE, mandatory=false)
        PROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_MARGIN *
            @t24:i (t24Name=PROJ.MARGIN)
        PROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_RATE *
            @t24:i (t24Name=PROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=60.0, core=true, t24Name=PROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=66.0, core=true, t24Name=PROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=67.0, core=true, t24Name=PROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        PROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=62.0, core=true, t24Name=PROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=65.0, core=true, t24Name=PROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=64.0, core=true, t24Name=PROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS_WORK__PROJ_PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        PROJ_ADJUST_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=76.0, core=true, t24Name=PROJ.ADJUST.INT.AMT, mandatory=false)
        /* This field holds the current month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=80.0, core=true, t24Name=PROJ.INT.ACC.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=81.0, core=true, t24Name=PROJ.INT.ACC.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=82.0, core=true, t24Name=PROJ.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field holds the current month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=89.0, core=true, t24Name=PROJ.INT.SUSP.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=90.0, core=true, t24Name=PROJ.INT.SUSP.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=91.0, core=true, t24Name=PROJ.INT.SUSP.PY, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=88.0, core=true, t24Name=PROJ.NEG.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=86.0, core=true, t24Name=PROJ.NEG.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=84.0, core=true, t24Name=PROJ.NEG.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=71.0, core=true, t24Name=PROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=70.0, core=true, t24Name=PROJ.PERIOD.START, mandatory=false)
        PROJ_POS_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=87.0, core=true, t24Name=PROJ.POS.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=85.0, core=true, t24Name=PROJ.POS.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=83.0, core=true, t24Name=PROJ.POS.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=72.0, core=true, t24Name=PROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount that has been made due for that period */
        PROJ_TOT_DUE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=74.0, core=true, t24Name=PROJ.TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=79.0, core=true, t24Name=PROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=PROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        PROJ_TOT_RES_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=77.0, core=true, t24Name=PROJ.TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        PROJ_TOT_RPY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=75.0, core=true, t24Name=PROJ.TOT.RPY.AMT, typeModifiers=-, mandatory=false)
        /* This filed represents the total suspended accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_SUSP_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=73.0, core=true, t24Name=PROJ.TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__ABB_NEW_METHOD
    {
        ABB_NEW_METHOD : AA_Interest:AA_INTEREST_ACCRUALS__ABB_NEW_METHOD__ABB_NEW_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=111.0, core=true, t24Name=ABB.NEW.METHOD, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__EFFECTIVE_DATE
    {
        /* Denotes the date from which the Profit Rate is applicable for this Interest method. Arranged in descending order, it will store the related rate on which Interest will be accrued, the corresponding profit amount & the activity reference that resulted in this update. */
        EFFECTIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Effective Date")
            @i18n:Tooltip (en="Effective date of the transaction")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=54.0, core=true, t24Name=EFFECTIVE.DATE, mandatory=false)
        PROFIT_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__EFFECTIVE_DATE__PROFIT_RATE *
            @t24:i (t24Name=PROFIT.RATE)
    }
    AA_INTEREST_ACCRUALS__EFFECTIVE_DATE__PROFIT_RATE
    {
        /* Denotes the activity reference that has resulted in the update of the above said details. This will be used to remove all related information when the activity is reversed. */
        ACTIVITY_REF : T24BusinessTypes:A
            @i18n:Label (en="Activity ID")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=ACTIVITY.REF, mandatory=false)
        /* Stores the system calculated profit amount if any for the Fixed Interest method. This will happen for Flat Rate/Reducing Rate option when the user defined profit amount does not exist. */
        CALC_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=CALC.PROFIT.AMT, mandatory=false)
        /* Denotes the profit amount applicable for this property for the life of the contract. */
        PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=PROFIT.AMT, mandatory=false)
        /* Denotes the Rate applicable for accrual of Fixed Interest Method. Applicable only for Fixed type of Interest. It is updated along with an effective date from which this rate is applicable and the corresponding activity reference. */
        PROFIT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=55.0, core=true, t24Name=PROFIT.RATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FIXED_INT_AMT
    {
        /* This field is updated with the fixed interest amount defined in Payment Schedule for the current interest period in PERIOD.START and PERIOD.END */
        FIXED_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=34.0, core=true, t24Name=FIXED.INT.AMT, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FROM_DATE
    {
        ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__ACCRUAL_AMT *
            @t24:i (t24Name=ACCRUAL.AMT)
        BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__BALANCE *
            @t24:i (t24Name=BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        BASIS : AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__BASIS
            @i18n:Label (en=Basis)
            @i18n:Tooltip (en=Basis)
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=5.0, core=true, t24Name=BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compound)
            @i18n:Tooltip (en=Compound)
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=10.0, core=true, t24Name=COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__COMPOUND_YIELD
            @i18n:Label (en="Compound Yield")
            @i18n:Tooltip (en="Compound Yield")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=11.0, core=true, t24Name=COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        DAYS : T24BusinessTypes:N
            @i18n:Label (en=Days)
            @i18n:Tooltip (en=Days)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, t24Name=DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        FROM_DATE : T24BusinessTypes:D
            @i18n:Label (en="From Date")
            @i18n:Tooltip (en="From Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=1.0, core=true, t24Name=FROM.DATE, mandatory=false)
        MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__MARGIN *
            @t24:i (t24Name=MARGIN)
        RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__RATE *
            @t24:i (t24Name=RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        TO_DATE : T24BusinessTypes:D
            @i18n:Label (en="To Date")
            @i18n:Tooltip (en="To Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=2.0, core=true, t24Name=TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FROM_DATE__ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACCRUAL_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Accrual Amount")
            @i18n:Tooltip (en="Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=8.0, core=true, t24Name=ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACT_ACC_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Actual Accrual Amount")
            @i18n:Tooltip (en="Actual Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=9.0, core=true, t24Name=ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FROM_DATE__BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        BALANCE : T24BusinessTypes:AMT
            @i18n:Label (en=Balance)
            @i18n:Tooltip (en=Balance)
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=4.0, core=true, t24Name=BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FROM_DATE__MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        MARGIN : AA_Interest:AA_INTEREST_ACCRUALS__FROM_DATE__MARGIN__MARGIN
            @i18n:Label (en=Margin)
            @i18n:Tooltip (en=Margin)
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=7.0, core=true, t24Name=MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__FROM_DATE__RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en=Rate)
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=6.0, core=true, t24Name=RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE
    {
        INFO_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_ACCRUAL_AMT *
            @t24:i (t24Name=INFO.ACCRUAL.AMT)
        INFO_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_BALANCE *
            @t24:i (t24Name=INFO.BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        INFO_BASIS : AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=40.0, core=true, t24Name=INFO.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        INFO_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=45.0, core=true, t24Name=INFO.COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        INFO_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=46.0, core=true, t24Name=INFO.COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        INFO_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=38.0, core=true, t24Name=INFO.DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=36.0, core=true, t24Name=INFO.FROM.DATE, mandatory=false)
        INFO_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_MARGIN *
            @t24:i (t24Name=INFO.MARGIN)
        INFO_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_RATE *
            @t24:i (t24Name=INFO.RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=37.0, core=true, t24Name=INFO.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=43.0, core=true, t24Name=INFO.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=44.0, core=true, t24Name=INFO.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        INFO_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=39.0, core=true, t24Name=INFO.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        INFO_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=INFO.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        INFO_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=41.0, core=true, t24Name=INFO.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INFO_PERIOD_START
    {
        INFO_ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=49.0, core=true, t24Name=INFO.ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=48.0, core=true, t24Name=INFO.PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=47.0, core=true, t24Name=INFO.PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.

This is just an information fields, no entries will be raised based on this.

Standard T24 amount field of upto 19 characters */
        INFO_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=INFO.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=INFO.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=INFO.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INT_EFF_DATE
    {
        FLOATING_INDEX -> byValue AA_Interest:AA_INTEREST_ACCRUALS__INT_EFF_DATE__FLOATING_INDEX *
            @t24:i (t24Name=FLOATING.INDEX)
        /* Associated with FLOATING.INDEX or PERIODIC.INDEX. Stores the dates when there is a change in BASIC.INTEREST or PERIODIC.INTEREST index. */
        INT_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Interest Effective Date")
            @i18n:Tooltip (en="Interest Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=24.0, core=true, t24Name=INT.EFF.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__INT_EFF_DATE__FLOATING_INDEX
    {
        /* BASIC.INTEREST key associated with the INT.EFF.DATE */
        FLOATING_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Floating Index")
            @i18n:Tooltip (en="Floating Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=25.0, core=true, t24Name=FLOATING.INDEX, mandatory=false)
        /* PERIODIC.INTEREST key associated with the INT.EFF.DATE */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @i18n:Tooltip (en="Periodic Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=26.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE
    {
        IPROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT *
            @t24:i (t24Name=IPROJ.ACCRUAL.AMT)
        IPROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_BALANCE *
            @t24:i (t24Name=IPROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=96.0, core=true, t24Name=IPROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        IPROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.0, core=true, t24Name=IPROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        IPROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=102.0, core=true, t24Name=IPROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=94.0, core=true, t24Name=IPROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=92.0, core=true, t24Name=IPROJ.FROM.DATE, mandatory=false)
        IPROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_MARGIN *
            @t24:i (t24Name=IPROJ.MARGIN)
        IPROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_RATE *
            @t24:i (t24Name=IPROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=93.0, core=true, t24Name=IPROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=99.0, core=true, t24Name=IPROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=100.0, core=true, t24Name=IPROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        IPROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=95.0, core=true, t24Name=IPROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=98.0, core=true, t24Name=IPROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=97.0, core=true, t24Name=IPROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__IPROJ_PERIOD_START
    {
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=104.0, core=true, t24Name=IPROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=103.0, core=true, t24Name=IPROJ.PERIOD.START, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        IPROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=105.0, core=true, t24Name=IPROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=107.0, core=true, t24Name=IPROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=106.0, core=true, t24Name=IPROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__LINKED_INT_DATE
    {
        /* The field describes when link rate is changed. It's associated with LINK.INT.RATE field. */
        LINKED_INT_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=108.0, core=true, t24Name=LINKED.INT.DATE, mandatory=false)
        /* The field describes the link rate on a particular date. It's associated with LINK.INT.DATE field. */
        LINKED_INT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=109.0, core=true, t24Name=LINKED.INT.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__LINKED_RATE_CHANGE
    {
        /* This field contains the list of link rate change dates. It'll be cleared once change interest is processed for the child arrangements in COB. */
        LINKED_RATE_CHANGE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=110.0, core=true, t24Name=LINKED.RATE.CHANGE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__LINK_EFF_DATE
    {
        LINKED_ARRANGEMENT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__LINK_EFF_DATE__LINKED_ARRANGEMENT *
            @t24:i (t24Name=LINKED.ARRANGEMENT)
        /* This field indicates the effective date when the arrangement in the field LINKED.ARRANGEMENT is linked to the bundle arrangement */
        LINK_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Link Effective Date")
            @i18n:Tooltip (en="Link Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=28.0, core=true, t24Name=LINK.EFF.DATE, mandatory=false)
/* The maximum % of the recipient balance that can be offset by the Donation.
For ex, in an Offset mortgage, if the Principal is -85,000 and if the Donor balances amount to credit +100,000 then specifying Max Offset as 100% would mean only 85,000 of the credit balances are offset against the Recipient's debit balance.
There is no additional benefit for the surplus 15,000 in the Donor Balances 80% in Max Offset would mean only 68,000 (80% of 85,000) of the credit balance can be offset against the recipient's debit balance In the case of Interest Compensation, this will be set to "" to indicate that there is no "Offset" percentage but only a consolidation of Donor and Recipient balances to calculate interest on */
        MAX_OFF_SET : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=33.0, core=true, t24Name=MAX.OFF.SET, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__LINK_EFF_DATE__LINKED_ARRANGEMENT
    {
        /* This field is updated with the arrangement reference for a bundle arrangement, where the interest is calculated at the master arrangement level pooling the balances of all child/linked arrangements */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @i18n:Label (en="Linked Arrangement")
            @i18n:Tooltip (en="Linked Arrangement")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=29.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* A user can optionally specifiy one or more Balances being donated by the donor.
Sub Valued field to allow multiple balance types to be donated.
Specify the actual Balance type that needs to be taken for compensation.
If this field is left blank, then it means that donor is not donating any balance.
This may prove useful when a participant, by just being part of a package, there is a need to suppress their Interest (say, a Penalty Interest) which would otherwise accrue based on unauthorised overdraft balances */
        LINKED_BALANCE_TYPE -> AC_SoftAccounting:AC_BALANCE_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=31.0, core=true, businessType=A, t24Name=LINKED.BALANCE.TYPE, mandatory=false)
        /* This field indicates the interest property for which the interest amount is calculated at the bundle level rather than at individual arrangements */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Linked Property")
            @i18n:Tooltip (en="Linked Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=30.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
        /* The Credit/Debit specification at the bundle contradicts with the Source Calc type of the Interest property Sub Value field associated with Donor Balance Type Input is allowed only if Donor Balance Type is input Because the Donor Balance type can have Credit or Debit balances on a given day, there would be a flag to specify which balances of the balance type should be used for Offset */
        LINKED_SOURCE_CALC_TYPE -> AA_Framework:AA_SOURCE_CALC_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=32.0, core=true, businessType=A, t24Name=LINKED.SOURCE.CALC.TYPE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        ADJUST_INT_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Adjust Interest Amount")
            @i18n:Tooltip (en="Adjust Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ADJUST.INT.AMT, mandatory=false)
        ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=15.0, core=true, t24Name=ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_END : T24BusinessTypes:D
            @i18n:Label (en="Period End")
            @i18n:Tooltip (en="Period End")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=14.0, core=true, t24Name=PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_START : T24BusinessTypes:D
            @i18n:Label (en="Period Start")
            @i18n:Tooltip (en="Period Start")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=13.0, core=true, t24Name=PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.


Standard T24 amount field of upto 19 characters */
        TOT_ACCR_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Accrual Amount")
            @i18n:Tooltip (en="Total Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=16.0, core=true, t24Name=TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total amount made as due for the interest periods as in PERIOD.START and PERIOD.END. This may be different from the TOT.ACCR.AMT when a bill is issued before the due date and subsequently a change in interest, basis, principal changes the accrued amount, in this case due amount would be based on the bill issued, i.e. the billed amount. */
        TOT_DUE_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Due Amount")
            @i18n:Tooltip (en="Total Due Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=22.0, core=true, t24Name=TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        TOT_RES_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Residual Amount")
            @i18n:Tooltip (en="Total Residual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        TOT_RPY_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Repay Amount")
            @i18n:Tooltip (en="Total Repay Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=19.0, core=true, t24Name=TOT.RPY.AMT, typeModifiers=-, mandatory=false)
/* This field represents the total suspended amount till the accrued date for the current period.
This field will get updated when interest is accrued after arrangement is suspended.
This field will also get updated when SUSPEND activity is run and SUSPEND.OVERDUE is set for the interest property.
The interest accrued from last payment date till suspend activity run date will be updated.

Standard T24 amount field of upto 19 characters */
        TOT_SUSP_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Suspend Amount")
            @i18n:Tooltip (en="Total Suspend Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=17.0, core=true, t24Name=TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE
    {
        PROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT *
            @t24:i (t24Name=PROJ.ACCRUAL.AMT)
        PROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_BALANCE *
            @t24:i (t24Name=PROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=63.0, core=true, t24Name=PROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        PROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=68.0, core=true, t24Name=PROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        PROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=69.0, core=true, t24Name=PROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=61.0, core=true, t24Name=PROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=59.0, core=true, t24Name=PROJ.FROM.DATE, mandatory=false)
        PROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_MARGIN *
            @t24:i (t24Name=PROJ.MARGIN)
        PROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_RATE *
            @t24:i (t24Name=PROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=60.0, core=true, t24Name=PROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=66.0, core=true, t24Name=PROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=67.0, core=true, t24Name=PROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        PROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=62.0, core=true, t24Name=PROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=65.0, core=true, t24Name=PROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=64.0, core=true, t24Name=PROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUALS__PROJ_PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        PROJ_ADJUST_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=76.0, core=true, t24Name=PROJ.ADJUST.INT.AMT, mandatory=false)
        /* This field holds the current month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=80.0, core=true, t24Name=PROJ.INT.ACC.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=81.0, core=true, t24Name=PROJ.INT.ACC.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=82.0, core=true, t24Name=PROJ.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field holds the current month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=89.0, core=true, t24Name=PROJ.INT.SUSP.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=90.0, core=true, t24Name=PROJ.INT.SUSP.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=91.0, core=true, t24Name=PROJ.INT.SUSP.PY, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=88.0, core=true, t24Name=PROJ.NEG.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=86.0, core=true, t24Name=PROJ.NEG.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=84.0, core=true, t24Name=PROJ.NEG.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=71.0, core=true, t24Name=PROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=70.0, core=true, t24Name=PROJ.PERIOD.START, mandatory=false)
        PROJ_POS_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=87.0, core=true, t24Name=PROJ.POS.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=85.0, core=true, t24Name=PROJ.POS.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=83.0, core=true, t24Name=PROJ.POS.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=72.0, core=true, t24Name=PROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount that has been made due for that period */
        PROJ_TOT_DUE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=74.0, core=true, t24Name=PROJ.TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=79.0, core=true, t24Name=PROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=PROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        PROJ_TOT_RES_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=77.0, core=true, t24Name=PROJ.TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        PROJ_TOT_RPY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=75.0, core=true, t24Name=PROJ.TOT.RPY.AMT, typeModifiers=-, mandatory=false)
        /* This filed represents the total suspended accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_SUSP_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=73.0, core=true, t24Name=PROJ.TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
/* The file AA.INTEREST.ACCRUALS.HIST holds the historical information of interest accrual for an arrangement for a given interest property.

This live file stores the interest rate, number of days, principal balance, basis used for a given period and the accrual amount. The fields showing the above information are associated. A new set of multivalues is formed for every change interest rate, basis, balance and period. If there is no change in any of the above said parameters, a new set is shown for every month. */
    AA_INTEREST_ACCRUAL_HIST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.INTEREST.ACCRUAL.HIST, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        ABB_NEW_METHOD -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__ABB_NEW_METHOD *
            @t24:i (t24Name=ABB.NEW.METHOD)
        /* This field is updated with the interest property when system evaluates a restriction rule which results in the interest amount associated for the property is waived and an alternate property interest amount is calculated and made-due */
        ALTERNATE_INTEREST : T24BusinessTypes:A
            @i18n:Label (en="Alternate Interest")
            @i18n:Tooltip (en="Alternate Interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=27.0, core=true, t24Name=ALTERNATE.INTEREST, mandatory=false)
        EFFECTIVE_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__EFFECTIVE_DATE *
            @t24:i (t24Name=EFFECTIVE.DATE)
        /* Denotes the final occurrence of the Fixed Interest Method. On this final date, any rounding differences between the agreed profit amount and accrued amounts so far will be posted. */
        FINAL_SCHEDULE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=53.0, core=true, t24Name=FINAL.SCHEDULE, mandatory=false)
        FIXED_INT_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FIXED_INT_AMT *
            @t24:i (t24Name=FIXED.INT.AMT)
        FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE *
            @t24:i (t24Name=FROM.DATE)
        INFO_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE *
            @t24:i (t24Name=INFO.FROM.DATE)
        INFO_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_PERIOD_START *
            @t24:i (t24Name=INFO.PERIOD.START)
        INT_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INT_EFF_DATE *
            @t24:i (t24Name=INT.EFF.DATE)
        IPROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE *
            @t24:i (t24Name=IPROJ.FROM.DATE)
        IPROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_PERIOD_START *
            @t24:i (t24Name=IPROJ.PERIOD.START)
        /* This field represent the date when the record was last archived. */
        LAST_ARCHIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Last Archive Date")
            @i18n:Tooltip (en="Last Archive Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=12.0, core=true, t24Name=LAST.ARCHIVE.DATE, mandatory=false)
        LINKED_INT_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__LINKED_INT_DATE *
            @t24:i (t24Name=LINKED.INT.DATE)
        LINKED_RATE_CHANGE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__LINKED_RATE_CHANGE *
            @t24:i (t24Name=LINKED.RATE.CHANGE)
        LINK_EFF_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__LINK_EFF_DATE *
            @t24:i (t24Name=LINK.EFF.DATE)
        /* This field indicates whether the calculated interest has to be billed at the start of the period or not, This will hold only one value "ADVANCE". If it is set then the TOT.DUE.AMT will be updated at start of the period. */
        PAYMENT_MODE : T24BusinessTypes:A
            @i18n:Label (en="Payment Mode")
            @i18n:Tooltip (en="Mode of payment like account, cash, cheque etc.")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=35.0, core=true, t24Name=PAYMENT.MODE, mandatory=false)
        PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PERIOD_START *
            @t24:i (t24Name=PERIOD.START)
        PROJ_FROM_DATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE *
            @t24:i (t24Name=PROJ.FROM.DATE)
        PROJ_PERIOD_START -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_PERIOD_START *
            @t24:i (t24Name=PROJ.PERIOD.START)
        /* This field represents the id of the file. The id has two parts - arrangement number and the property name separated by hyphen. */
        PROPERTY_NAME -> AA_Interest:AA_INTEREST_ACCRUAL_HIST PK required
            @i18n:Label (en="Property Name")
            @i18n:Tooltip (en="Property Name")
            @t24:i (align=L, inputBehaviour=I, maxLength=50, sysNumber=0.0, core=true, businessType=ANY, t24Name=PROPERTY.NAME, mandatory=true)
    }
    AA_INTEREST_ACCRUAL_HIST__ABB_NEW_METHOD
    {
        ABB_NEW_METHOD : AA_Interest:AA_INTEREST_ACCRUAL_HIST__ABB_NEW_METHOD__ABB_NEW_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=111.0, core=true, t24Name=ABB.NEW.METHOD, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__EFFECTIVE_DATE
    {
        /* Denotes the date from which the Profit Rate is applicable for this Interest method. Arranged in descending order, it will store the related rate on which Interest will be accrued, the corresponding profit amount & the activity reference that resulted in this update. */
        EFFECTIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Effective Date")
            @i18n:Tooltip (en="Effective date of the transaction")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=54.0, core=true, t24Name=EFFECTIVE.DATE, mandatory=false)
        PROFIT_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__EFFECTIVE_DATE__PROFIT_RATE *
            @t24:i (t24Name=PROFIT.RATE)
    }
    AA_INTEREST_ACCRUAL_HIST__EFFECTIVE_DATE__PROFIT_RATE
    {
        /* Denotes the activity reference that has resulted in the update of the above said details. This will be used to remove all related information when the activity is reversed. */
        ACTIVITY_REF : T24BusinessTypes:A
            @i18n:Label (en="Activity ID")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=ACTIVITY.REF, mandatory=false)
        /* Stores the system calculated profit amount if any for the Fixed Interest method. This will happen for Flat Rate/Reducing Rate option when the user defined profit amount does not exist. */
        CALC_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=CALC.PROFIT.AMT, mandatory=false)
        /* Denotes the profit amount applicable for this property for the life of the contract. */
        PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=PROFIT.AMT, mandatory=false)
        /* Denotes the Rate applicable for accrual of Fixed Interest Method. Applicable only for Fixed type of Interest. It is updated along with an effective date from which this rate is applicable and the corresponding activity reference. */
        PROFIT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=55.0, core=true, t24Name=PROFIT.RATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FIXED_INT_AMT
    {
        /* This field is updated with the fixed interest amount defined in Payment Schedule for the current interest period in PERIOD.START and PERIOD.END */
        FIXED_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=34.0, core=true, t24Name=FIXED.INT.AMT, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE
    {
        ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__ACCRUAL_AMT *
            @t24:i (t24Name=ACCRUAL.AMT)
        BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__BALANCE *
            @t24:i (t24Name=BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        BASIS : AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__BASIS
            @i18n:Label (en=Basis)
            @i18n:Tooltip (en=Basis)
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=5.0, core=true, t24Name=BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compound)
            @i18n:Tooltip (en=Compound)
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=10.0, core=true, t24Name=COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__COMPOUND_YIELD
            @i18n:Label (en="Compound Yield")
            @i18n:Tooltip (en="Compound Yield")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=11.0, core=true, t24Name=COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        DAYS : T24BusinessTypes:N
            @i18n:Label (en=Days)
            @i18n:Tooltip (en=Days)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, t24Name=DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        FROM_DATE : T24BusinessTypes:D
            @i18n:Label (en="From Date")
            @i18n:Tooltip (en="From Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=1.0, core=true, t24Name=FROM.DATE, mandatory=false)
        MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__MARGIN *
            @t24:i (t24Name=MARGIN)
        RATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__RATE *
            @t24:i (t24Name=RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        TO_DATE : T24BusinessTypes:D
            @i18n:Label (en="To Date")
            @i18n:Tooltip (en="To Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=2.0, core=true, t24Name=TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACCRUAL_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Accrual Amount")
            @i18n:Tooltip (en="Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=8.0, core=true, t24Name=ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        ACT_ACC_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Actual Accrual Amount")
            @i18n:Tooltip (en="Actual Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=9.0, core=true, t24Name=ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        BALANCE : T24BusinessTypes:AMT
            @i18n:Label (en=Balance)
            @i18n:Tooltip (en=Balance)
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=4.0, core=true, t24Name=BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        MARGIN : AA_Interest:AA_INTEREST_ACCRUAL_HIST__FROM_DATE__MARGIN__MARGIN
            @i18n:Label (en=Margin)
            @i18n:Tooltip (en=Margin)
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=7.0, core=true, t24Name=MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en=Rate)
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=6.0, core=true, t24Name=RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE
    {
        INFO_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_ACCRUAL_AMT *
            @t24:i (t24Name=INFO.ACCRUAL.AMT)
        INFO_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_BALANCE *
            @t24:i (t24Name=INFO.BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.



1 - 2 type A (alphanumeric) characters */
        INFO_BASIS : AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=40.0, core=true, t24Name=INFO.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        INFO_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=45.0, core=true, t24Name=INFO.COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        INFO_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=46.0, core=true, t24Name=INFO.COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        INFO_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=38.0, core=true, t24Name=INFO.DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=36.0, core=true, t24Name=INFO.FROM.DATE, mandatory=false)
        INFO_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_MARGIN *
            @t24:i (t24Name=INFO.MARGIN)
        INFO_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_RATE *
            @t24:i (t24Name=INFO.RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard date format of 11 characters */
        INFO_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=37.0, core=true, t24Name=INFO.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=43.0, core=true, t24Name=INFO.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. This is just an information fields, no entries will be raised based on this.


Standard T24 amount field of upto 19 characters */
        INFO_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=44.0, core=true, t24Name=INFO.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.
The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL.


Standard T24 amount field of upto 19 characters */
        INFO_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=39.0, core=true, t24Name=INFO.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes.

Validation rules
Standard rate format */
        INFO_MARGIN : AA_Interest:AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=INFO.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate
changes.

Validation rules
Standard rate format */
        INFO_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=41.0, core=true, t24Name=INFO.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INFO_PERIOD_START
    {
        INFO_ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=49.0, core=true, t24Name=INFO.ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=48.0, core=true, t24Name=INFO.PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        INFO_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=47.0, core=true, t24Name=INFO.PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.

This is just an information fields, no entries will be raised based on this.

Standard T24 amount field of upto 19 characters */
        INFO_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=INFO.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=INFO.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        INFO_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=INFO.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INT_EFF_DATE
    {
        FLOATING_INDEX -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__INT_EFF_DATE__FLOATING_INDEX *
            @t24:i (t24Name=FLOATING.INDEX)
        /* Associated with FLOATING.INDEX or PERIODIC.INDEX. Stores the dates when there is a change in BASIC.INTEREST or PERIODIC.INTEREST index. */
        INT_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Interest Effective Date")
            @i18n:Tooltip (en="Interest Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=24.0, core=true, t24Name=INT.EFF.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__INT_EFF_DATE__FLOATING_INDEX
    {
        /* BASIC.INTEREST key associated with the INT.EFF.DATE */
        FLOATING_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Floating Index")
            @i18n:Tooltip (en="Floating Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=25.0, core=true, t24Name=FLOATING.INDEX, mandatory=false)
        /* PERIODIC.INTEREST key associated with the INT.EFF.DATE */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @i18n:Tooltip (en="Periodic Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=26.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE
    {
        IPROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT *
            @t24:i (t24Name=IPROJ.ACCRUAL.AMT)
        IPROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_BALANCE *
            @t24:i (t24Name=IPROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=96.0, core=true, t24Name=IPROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        IPROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.0, core=true, t24Name=IPROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        IPROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=102.0, core=true, t24Name=IPROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=94.0, core=true, t24Name=IPROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=92.0, core=true, t24Name=IPROJ.FROM.DATE, mandatory=false)
        IPROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_MARGIN *
            @t24:i (t24Name=IPROJ.MARGIN)
        IPROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_RATE *
            @t24:i (t24Name=IPROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=93.0, core=true, t24Name=IPROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=99.0, core=true, t24Name=IPROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        IPROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=100.0, core=true, t24Name=IPROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        IPROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=95.0, core=true, t24Name=IPROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=98.0, core=true, t24Name=IPROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        IPROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=97.0, core=true, t24Name=IPROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__IPROJ_PERIOD_START
    {
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=104.0, core=true, t24Name=IPROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        IPROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=103.0, core=true, t24Name=IPROJ.PERIOD.START, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        IPROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=105.0, core=true, t24Name=IPROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=107.0, core=true, t24Name=IPROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total info projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        IPROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=106.0, core=true, t24Name=IPROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__LINKED_INT_DATE
    {
        /* The field describes when link rate is changed. It's associated with LINK.INT.RATE field. */
        LINKED_INT_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=108.0, core=true, t24Name=LINKED.INT.DATE, mandatory=false)
        /* The field describes the link rate on a particular date. It's associated with LINK.INT.DATE field. */
        LINKED_INT_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=109.0, core=true, t24Name=LINKED.INT.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__LINKED_RATE_CHANGE
    {
        /* This field contains the list of link rate change dates. It'll be cleared once change interest is processed for the child arrangements in COB. */
        LINKED_RATE_CHANGE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=110.0, core=true, t24Name=LINKED.RATE.CHANGE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__LINK_EFF_DATE
    {
        LINKED_ARRANGEMENT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__LINK_EFF_DATE__LINKED_ARRANGEMENT *
            @t24:i (t24Name=LINKED.ARRANGEMENT)
        /* This field indicates the effective date when the arrangement in the field LINKED.ARRANGEMENT is linked to the bundle arrangement */
        LINK_EFF_DATE : T24BusinessTypes:D
            @i18n:Label (en="Link Effective Date")
            @i18n:Tooltip (en="Link Effective Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=28.0, core=true, t24Name=LINK.EFF.DATE, mandatory=false)
/* The maximum % of the recipient balance that can be offset by the Donation.
For ex, in an Offset mortgage, if the Principal is -85,000 and if the Donor balances amount to credit +100,000 then specifying Max Offset as 100% would mean only 85,000 of the credit balances are offset against the Recipient's debit balance.
There is no additional benefit for the surplus 15,000 in the Donor Balances 80% in Max Offset would mean only 68,000 (80% of 85,000) of the credit balance can be offset against the recipient's debit balance In the case of Interest Compensation, this will be set to "" to indicate that there is no "Offset" percentage but only a consolidation of Donor and Recipient balances to calculate interest on */
        MAX_OFF_SET : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=33.0, core=true, t24Name=MAX.OFF.SET, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__LINK_EFF_DATE__LINKED_ARRANGEMENT
    {
        /* This field is updated with the arrangement reference for a bundle arrangement, where the interest is calculated at the master arrangement level pooling the balances of all child/linked arrangements */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @i18n:Label (en="Linked Arrangement")
            @i18n:Tooltip (en="Linked Arrangement")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=29.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* A user can optionally specifiy one or more Balances being donated by the donor.
Sub Valued field to allow multiple balance types to be donated.
Specify the actual Balance type that needs to be taken for compensation.
If this field is left blank, then it means that donor is not donating any balance.
This may prove useful when a participant, by just being part of a package, there is a need to suppress their Interest (say, a Penalty Interest) which would otherwise accrue based on unauthorised overdraft balances */
        LINKED_BALANCE_TYPE -> AC_SoftAccounting:AC_BALANCE_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=31.0, core=true, businessType=A, t24Name=LINKED.BALANCE.TYPE, mandatory=false)
        /* This field indicates the interest property for which the interest amount is calculated at the bundle level rather than at individual arrangements */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Linked Property")
            @i18n:Tooltip (en="Linked Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=30.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
        /* The Credit/Debit specification at the bundle contradicts with the Source Calc type of the Interest property Sub Value field associated with Donor Balance Type Input is allowed only if Donor Balance Type is input Because the Donor Balance type can have Credit or Debit balances on a given day, there would be a flag to specify which balances of the balance type should be used for Offset */
        LINKED_SOURCE_CALC_TYPE -> AA_Framework:AA_SOURCE_CALC_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=32.0, core=true, businessType=A, t24Name=LINKED.SOURCE.CALC.TYPE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        ADJUST_INT_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Adjust Interest Amount")
            @i18n:Tooltip (en="Adjust Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ADJUST.INT.AMT, mandatory=false)
        ONLINE_SCHEDULE : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=15.0, core=true, t24Name=ONLINE.SCHEDULE, mandatory=false)
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_END : T24BusinessTypes:D
            @i18n:Label (en="Period End")
            @i18n:Tooltip (en="Period End")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=14.0, core=true, t24Name=PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current  interest periods are stored in multivalue sets. */
        PERIOD_START : T24BusinessTypes:D
            @i18n:Label (en="Period Start")
            @i18n:Tooltip (en="Period Start")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=13.0, core=true, t24Name=PERIOD.START, mandatory=false)
/* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored.


Standard T24 amount field of upto 19 characters */
        TOT_ACCR_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Accrual Amount")
            @i18n:Tooltip (en="Total Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=16.0, core=true, t24Name=TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total amount made as due for the interest periods as in PERIOD.START and PERIOD.END. This may be different from the TOT.ACCR.AMT when a bill is issued before the due date and subsequently a change in interest, basis, principal changes the accrued amount, in this case due amount would be based on the bill issued, i.e. the billed amount. */
        TOT_DUE_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Due Amount")
            @i18n:Tooltip (en="Total Due Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=22.0, core=true, t24Name=TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        TOT_RES_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Residual Amount")
            @i18n:Tooltip (en="Total Residual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        TOT_RPY_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Repay Amount")
            @i18n:Tooltip (en="Total Repay Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=19.0, core=true, t24Name=TOT.RPY.AMT, typeModifiers=-, mandatory=false)
/* This field represents the total suspended amount till the accrued date for the current period.
This field will get updated when interest is accrued after arrangement is suspended.
This field will also get updated when SUSPEND activity is run and SUSPEND.OVERDUE is set for the interest property.
The interest accrued from last payment date till suspend activity run date will be updated.

Standard T24 amount field of upto 19 characters */
        TOT_SUSP_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Suspend Amount")
            @i18n:Tooltip (en="Total Suspend Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=17.0, core=true, t24Name=TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE
    {
        PROJ_ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT *
            @t24:i (t24Name=PROJ.ACCRUAL.AMT)
        PROJ_BALANCE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_BALANCE *
            @t24:i (t24Name=PROJ.BALANCE)
        /* This field represents the interest basis used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_BASIS : AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_BASIS
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=63.0, core=true, t24Name=PROJ.BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded. This field will get updated during online accrual projection. */
        PROJ_COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=68.0, core=true, t24Name=PROJ.COMPOUND.FQU, mandatory=false)
        /* This field represents the compounding method */
        PROJ_COMPOUND_YIELD : AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=69.0, core=true, t24Name=PROJ.COMPOUND.YIELD, mandatory=false)
        /* This field represents the number of days for which accrual has been calculated. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_DAYS : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=61.0, core=true, t24Name=PROJ.DAYS, mandatory=false)
        /* This field represents the start date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_FROM_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=59.0, core=true, t24Name=PROJ.FROM.DATE, mandatory=false)
        PROJ_MARGIN -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_MARGIN *
            @t24:i (t24Name=PROJ.MARGIN)
        PROJ_RATE -> byValue AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_RATE *
            @t24:i (t24Name=PROJ.RATE)
        /* This field represents the end date of the interest period. It also represents the date when there was a change in the principal/rate of the contract. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_TO_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=60.0, core=true, t24Name=PROJ.TO.DATE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_ACCRUAL_AMT
    {
        /* This field represents the rounded accrued  amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACCRUAL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=66.0, core=true, t24Name=PROJ.ACCRUAL.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the unrounded accrued amount for the current multi value set. This field will get updated during online accrual projection. */
        PROJ_ACT_ACC_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=67.0, core=true, t24Name=PROJ.ACT.ACC.AMT, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_BALANCE
    {
        /* This field represents the balance on which accrual was computed. This field will get updated during online accrual projection. */
        PROJ_BALANCE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=62.0, core=true, t24Name=PROJ.BALANCE, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_MARGIN
    {
        /* This field represents the margin value used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_MARGIN : AA_Interest:AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=65.0, core=true, t24Name=PROJ.MARGIN, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_RATE
    {
        /* This field represents the rate used for the accrual calculation. The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. This field will get updated during online accrual projection. */
        PROJ_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=64.0, core=true, t24Name=PROJ.RATE, typeModifiers=-, mandatory=false)
    }
    AA_INTEREST_ACCRUAL_HIST__PROJ_PERIOD_START
    {
        /* This field is updated with the accrual amount that is adjusted for the period specified in PERIOD.START and PERIOD.END */
        PROJ_ADJUST_INT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=76.0, core=true, t24Name=PROJ.ADJUST.INT.AMT, mandatory=false)
        /* This field holds the current month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=80.0, core=true, t24Name=PROJ.INT.ACC.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=81.0, core=true, t24Name=PROJ.INT.ACC.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=82.0, core=true, t24Name=PROJ.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field holds the current month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=89.0, core=true, t24Name=PROJ.INT.SUSP.CM, typeModifiers=-, mandatory=false)
        /* This field holds the previous month suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=90.0, core=true, t24Name=PROJ.INT.SUSP.PM, typeModifiers=-, mandatory=false)
        /* This field holds the previous year suspended accrual amount. An accrual accounting will be raised during cob for this amount. This field will get updated during online accrual projection. */
        PROJ_INT_SUSP_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=91.0, core=true, t24Name=PROJ.INT.SUSP.PY, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=88.0, core=true, t24Name=PROJ.NEG.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=86.0, core=true, t24Name=PROJ.NEG.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_NEG_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=84.0, core=true, t24Name=PROJ.NEG.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field denotes the end date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_END : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=71.0, core=true, t24Name=PROJ.PERIOD.END, mandatory=false)
        /* This field denotes the start date of the interest period. This field will get updated during online accrual projection. */
        PROJ_PERIOD_START : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=70.0, core=true, t24Name=PROJ.PERIOD.START, mandatory=false)
        PROJ_POS_INT_ACC_CM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=87.0, core=true, t24Name=PROJ.POS.INT.ACC.CM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PM : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=85.0, core=true, t24Name=PROJ.POS.INT.ACC.PM, typeModifiers=-, mandatory=false)
        PROJ_POS_INT_ACC_PY : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=83.0, core=true, t24Name=PROJ.POS.INT.ACC.PY, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=72.0, core=true, t24Name=PROJ.TOT.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount that has been made due for that period */
        PROJ_TOT_DUE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=74.0, core=true, t24Name=PROJ.TOT.DUE.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection negative accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_NEG_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=79.0, core=true, t24Name=PROJ.TOT.NEG.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* Specifies the total projection positive accruals for the interest period. This field will be part of the existing period start, Period end multi-value set. */
        PROJ_TOT_POS_ACCR_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=PROJ.TOT.POS.ACCR.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the difference of total accrued amount and total due amounts for corresponding periods. This would get update when property type is set to RESIDUAL.ACCRUAL in AA.PROPERTY. */
        PROJ_TOT_RES_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=77.0, core=true, t24Name=PROJ.TOT.RES.AMT, typeModifiers=-, mandatory=false)
        /* This field represents the total accrued amount repaid for the interset periods as in PERIOD.START and PERIOD.END. */
        PROJ_TOT_RPY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=75.0, core=true, t24Name=PROJ.TOT.RPY.AMT, typeModifiers=-, mandatory=false)
        /* This filed represents the total suspended accrued amount for the period. This field will get updated during online accrual projection. */
        PROJ_TOT_SUSP_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=73.0, core=true, t24Name=PROJ.TOT.SUSP.AMT, typeModifiers=-, mandatory=false)
    }
/* The file AA.INTEREST.ACCRUALS holds the interest accrual information for an arrangement for a given interest property.



This live file stores the interest rate, number of days, principal balance, basis used for a given period and the accrual amount. The fields showing the above information are associated. A new set of multivalues is formed for every change interest rate, basis, balance and period. If there is no change in any of the above said parameters, a new set is shown for every month.

These details would be updated during simulation for enquiry purposes. */
    AA_INTEREST_DETAILS
        @i18n:Description (en="")
        @t24:i (t24Name=AA.INTEREST.DETAILS, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        /* This field represent the date when the record was last archived. */
        LAST_ARCHIVE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Last Archive Date")
            @i18n:Tooltip (en="Last Archive Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=13.0, core=true, t24Name=LAST.ARCHIVE.DATE, mandatory=false)
        PAY_DATE -> byValue AA_Interest:AA_INTEREST_DETAILS__PAY_DATE *
            @t24:i (t24Name=PAY.DATE)
        PERIOD_START -> byValue AA_Interest:AA_INTEREST_DETAILS__PERIOD_START *
            @t24:i (t24Name=PERIOD.START)
        /* This field represents the id of the file. The id has two parts - arrangement number and the property name separated by hyphen. */
        PROPERTY_NAME -> AA_Interest:AA_INTEREST_DETAILS PK required
            @i18n:Label (en="Property Name")
            @i18n:Tooltip (en="Property Name")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=0.0, core=true, businessType=ANY, t24Name=PROPERTY.NAME, mandatory=true)
    }
    AA_INTEREST_DETAILS__PAY_DATE
    {
        ACCRUAL_AMT -> byValue AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__ACCRUAL_AMT *
            @t24:i (t24Name=ACCRUAL.AMT)
        BALANCE -> byValue AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__BALANCE *
            @t24:i (t24Name=BALANCE)
/* This field represent the calculation basis for the accrual amount. The basis mentioned in the interest property condition of the arrangement is considered. For futher information check helptext for INTEREST.BASIS



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. */
        BASIS : AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__BASIS
            @i18n:Label (en=Basis)
            @i18n:Tooltip (en=Basis)
            @t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=6.0, core=true, t24Name=BASIS, mandatory=false)
        /* This field represents the frequency of interest getting compounded */
        COMPOUND_FQU : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compound)
            @i18n:Tooltip (en=Compound)
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=11.0, core=true, t24Name=COMPOUND.FQU, mandatory=false)
        /* This field indicates if yield method is set to YES for the accrual period. System maintained */
        COMPOUND_YIELD : AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__COMPOUND_YIELD
            @i18n:Label (en="Compound Yield")
            @i18n:Tooltip (en="Compound Yield")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=12.0, core=true, t24Name=COMPOUND.YIELD, mandatory=false)
/* This field represents the number of days for which accruals have been made for the current interest period. Accruals are split into current month, previous month and previous year.



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        DAYS : T24BusinessTypes:N
            @i18n:Label (en=Days)
            @i18n:Tooltip (en=Days)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, t24Name=DAYS, mandatory=false)
/* This field represents the start date of the current interest period. This field also represents the date when there was a change in the balance/principal of the contract. Hence whenever there is balance movement a new set of fields are added.



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        FROM_DATE : T24BusinessTypes:D
            @i18n:Label (en="From Date")
            @i18n:Tooltip (en="From Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=2.0, core=true, t24Name=FROM.DATE, mandatory=false)
        MARGIN -> byValue AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__MARGIN *
            @t24:i (t24Name=MARGIN)
        /* Denotes the payment date for the corresponding property. */
        PAY_DATE : T24BusinessTypes:D
            @i18n:Label (en="Pay Date")
            @i18n:Tooltip (en="Pay Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=1.0, core=true, t24Name=PAY.DATE, mandatory=false)
        RATE -> byValue AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__RATE *
            @t24:i (t24Name=RATE)
/* This field represents the end date of the current interest period.Hence whenever there is balance movement a new set of fields are added.



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        TO_DATE : T24BusinessTypes:D
            @i18n:Label (en="To Date")
            @i18n:Tooltip (en="To Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=3.0, core=true, t24Name=TO.DATE, mandatory=false)
    }
    AA_INTEREST_DETAILS__PAY_DATE__ACCRUAL_AMT
    {
/* This field contains the amount of interest accrued for the given period rounded to the number of decimals defined for the currency

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. */
        ACCRUAL_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Accrual Amount")
            @i18n:Tooltip (en="Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=ACCRUAL.AMT, mandatory=false)
/* The ACT.ACCT.AMT contains the amount of interest accrued for the given period rounded to six decimal places.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. */
        ACT_ACC_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Actual Accrual Amount")
            @i18n:Tooltip (en="Actual Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=10.0, core=true, t24Name=ACT.ACC.AMT, mandatory=false)
    }
    AA_INTEREST_DETAILS__PAY_DATE__BALANCE
    {
/* The field BALANCE represents the balance amount or principal effective for the accrual period.

The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. A new subvalue sets (BALANCE - ACT.ACC.AMT) within a mutlivalue set is created if the calculation type is either BAND or LEVEL. */
        BALANCE : T24BusinessTypes:AMT
            @i18n:Label (en=Balance)
            @i18n:Tooltip (en=Balance)
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=5.0, core=true, t24Name=BALANCE, mandatory=false)
    }
    AA_INTEREST_DETAILS__PAY_DATE__MARGIN
    {
/* This field represents the spread or the margin that was used in the calculation of interest in current set of accrual fields.



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        MARGIN : AA_Interest:AA_INTEREST_DETAILS__PAY_DATE__MARGIN__MARGIN
            @i18n:Label (en=Margin)
            @i18n:Tooltip (en=Margin)
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=8.0, core=true, t24Name=MARGIN, mandatory=false)
    }
    AA_INTEREST_DETAILS__PAY_DATE__RATE
    {
/* This field represents the interest rate that was used in the calculation of interest for the current set of interest multivalue fields.



The fields FROM.DATE to ACT.ACC.AMT form an association containing interest accrual data. A new multivalue is created each time a month end is passed or the principal or interest rate changes. */
        RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en=Rate)
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=7.0, core=true, t24Name=RATE, mandatory=false)
    }
    AA_INTEREST_DETAILS__PERIOD_START
    {
        /* This field denotes the end date of a interest period. This field is associated with PERIOD.START. Period start date and end date of previous and current interest periods are stored in multivalue sets. */
        PERIOD_END : T24BusinessTypes:D
            @i18n:Label (en="Period End")
            @i18n:Tooltip (en="Period End")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=15.0, core=true, t24Name=PERIOD.END, mandatory=false)
        /* This field denotes the start date of a interest period. This field is associated with PERIOD.END Period start date and end date of previous and current interest periods are stored in multivalue sets. */
        PERIOD_START : T24BusinessTypes:D
            @i18n:Label (en="Period Start")
            @i18n:Tooltip (en="Period Start")
            @t24:i (align=R, inputBehaviour=I, maxLength=8, sysNumber=14.0, core=true, t24Name=PERIOD.START, mandatory=false)
        /* This field represents the total accrued amount till the accrue date for the current period. This field forms part of the set PERIOD.START.DATE and PERIOD.END.DATE. For every new interest periods, this field will also be multivalued and the accrued amount till date would be stored. */
        TOT_ACCR_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Accrual Amount")
            @i18n:Tooltip (en="Total Accrual Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=16.0, core=true, t24Name=TOT.ACCR.AMT, mandatory=false)
/* This field represents the total suspended amount till the accrued date for the current period.

This field will get updated when interest is accrued after arrangement is suspended.

This field will also get updated when SUSPEND activity is run and SUSPEND.OVERDUE is set for the interest property.

The interest accrued from last payment date till suspend activity run date will be updated. */
        TOT_SUSP_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Suspend Amount")
            @i18n:Tooltip (en="Total Suspend Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=17.0, core=true, t24Name=TOT.SUSP.AMT, mandatory=false)
    }
    AA_INTERNAL_CAP_DETAILS
        @i18n:Description (en="")
        @t24:i (t24Name=AA.INTERNAL.CAP.DETAILS, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        DATE -> byValue AA_Interest:AA_INTERNAL_CAP_DETAILS__DATE *
            @t24:i (t24Name=DATE)
        ID -> AA_Interest:AA_INTERNAL_CAP_DETAILS PK required
            @i18n:Label (en="Transaction Ref")
            @i18n:Tooltip (en="Transaction Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name="@ID", mandatory=true)
        /* Deprecated, not used */
        RECIPIENT_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=1.0, core=true, t24Name=RECIPIENT.ID, mandatory=false)
        RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=10.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=9.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=8.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=7.0, core=true, t24Name=RESERVED.4, mandatory=false)
    }
    AA_INTERNAL_CAP_DETAILS__DATE
    {
        /* Deprecated, not used */
        DATE : T24BusinessTypes:D
            @i18n:Label (en=Date)
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=2.0, core=true, t24Name=DATE, mandatory=false)
        DUE_REF -> byValue AA_Interest:AA_INTERNAL_CAP_DETAILS__DATE__DUE_REF *
            @t24:i (t24Name=DUE.REF)
    }
    AA_INTERNAL_CAP_DETAILS__DATE__DUE_REF
    {
        /* Deprecated, not used */
        AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en=Amount)
            @i18n:Tooltip (en="Indicates the Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=25, sysNumber=6.0, core=true, t24Name=AMOUNT, mandatory=false)
        /* Deprecated, not used */
        DUE_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=40, sysNumber=3.0, core=true, t24Name=DUE.REF, mandatory=false)
        /* Deprecated, not used */
        TRANS_REF : T24BusinessTypes:A
            @i18n:Label (en="Transaction No")
            @i18n:Tooltip (en="Indicates the transaction reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=4.0, core=true, t24Name=TRANS.REF, mandatory=false)
        /* Deprecated, not used */
        TRANS_STATUS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=5.0, core=true, t24Name=TRANS.STATUS, mandatory=false)
    }
/* The INTEREST Property Class is used for all interest definition and processing in AA.  A T24 product defined and processed in AA can have multiple interest properties defined (e.g. principal interest, penalty interest, commission, etc.).  The number of interest properties is determined by the users defining the products.
The different condition ids of interest property can be attached to the different types of interest properties at product level */
    AA_PRD_CAT_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.PRD.CAT.INTEREST, nonStop=true, level=I, type=W, allowedFunctions="L Q S", localRefApplicationSysNo=101.0, localRefAllowed=true)
    {
/* Specifies how the accounting entries will be raised. Whether it needs to work like discounted or fixed type of interest.
Advance: This is nothing but discounted interest, interest amount will be collected inadvance before the interest periods.
Upfront Profit: Interest amount will be collected at the beginning of the contract.
None: Denotes conventional interest bearing contract. */
        ACCOUNTING_MODE : AA_Interest:AA_PRD_CAT_INTEREST__ACCOUNTING_MODE
            @i18n:Label (en="Accounting Mode")
            @t24:i (align=L, inputBehaviour=F, maxLength=15, sysNumber=60.0, core=true, t24Name=ACCOUNTING.MODE, mandatory=false)
/* When accruing interest, T24 must determine which amounts to include in each day’s balance, the actual days to include in the period (e.g. first day, last day) and how to round any calculations.
T24 allows users to define the calculation method to use, by specifying an accrual rule in EB.ACCRUAL.PARAM.  Users can define the following:
• First Day Inclusive (Yes/No)
• Last Day Inclusive (Yes/No)
• Rounding Rule (Natural, Down, Up, Currency, None)
• Intermediate Calculation Rounding (Yes/No)
• Credit value date adjustment
• Debit value date adjustment */
        ACCRUAL_RULE -> AC_Fees:EB_ACCRUAL_PARAM
            @i18n:Label (en="Accrual Rule")
            @i18n:Tooltip (en="Calculation method in EB.ACCRUAL.PARAM")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=ACCRUAL.RULE, mandatory=false)
/* User can defined Total interest / profit amount for the contract.
Mandatory input if RATE.TYPE is ‘Flat Amount’.
Value in CALC.PROFIT.AMT will be populated in this field which the user can override. */
        ACTUAL_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=69.0, core=true, t24Name=ACTUAL.PROFIT.AMT, mandatory=false)
        ADJUST_EXPIRY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=82.0, core=true, t24Name=ADJUST.EXPIRY.DATE, mandatory=false)
        ADJUST_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=79.0, core=true, t24Name=ADJUST.MARGIN, mandatory=false)
        ADJUST_OPERAND : AA_Interest:AA_PRD_CAT_INTEREST__ADJUST_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=ADJUST.OPERAND, mandatory=false)
        ADJUST_OPTION : AA_Interest:AA_PRD_CAT_INTEREST__ADJUST_OPTION
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.2, core=false, t24Name=ADJUST.OPTION, mandatory=false)
        ADJUST_OVERRIDE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=80.0, core=true, t24Name=ADJUST.OVERRIDE.RATE, mandatory=false)
        ADJUST_REASON : AA_Interest:AA_PRD_CAT_INTEREST__ADJUST_REASON
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=81.0, core=true, t24Name=ADJUST.REASON, mandatory=false)
        ADJUST_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__ADJUST_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=77.0, core=true, t24Name=ADJUST.TYPE, mandatory=false)
        API_ATTRIBUTE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__API_ATTRIBUTE *
            @t24:i (t24Name=API.ATTRIBUTE)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=148.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=149.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=145.0, core=true, t24Name=AUTHORISER, mandatory=false)
        /* The user can specify that interest will only be calculated if a balance threshold is surpassed. For credit interest, the user can specify the minimum balance which must be maintained. For debit interest, the user can specify the maximum debit balance for which interest will not be calculated. */
        CALC_THRESHOLD : T24BusinessTypes:AMT
            @i18n:Label (en="Calculation Threshold")
            @i18n:Tooltip (en="Threshold balance for interest calculation")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=CALC.THRESHOLD, typeModifiers=-, mandatory=false)
        CHANGED_FIELDS -> byValue AA_Interest:AA_PRD_CAT_INTEREST__CHANGED_FIELDS *
            @t24:i (t24Name=CHANGED.FIELDS)
/* T24 supports the compounding of both debit and credit interest.

Compounding interest applies a factor to the compound interest calculation based upon the  number of compounding periods in the year. This field defines the number of compounding periods to be applied.

Note that the compounding type provides a factor in the calculation only  and does not actually cause the base principal amount to be actually increased by the calculated interest amount.

The following compound frequencies are supported:

DAILY
– Interest is compounded 360/5/6 times a year (from days in year in INTEREST.BASIS)

WEEKn
– Interest is compounded 52/n times per year based on a number of WEEKS
-- Bi-weekly compounding should be defined as WEEK2 and will result in 26 periods
-- Four-Weekly compounding should be defined as WEEK4 and results in 13 periods

Mnn
– Interest is compounded 12/n times a year based on a numberof months
--  Monthly compounding should be defined as M01 and will result in 12 periods
--  Bi-Monthly should be defined as M02 and will result in 6 periods
--  Quarterly should be defined as M03 and will result in 4 periods
--  Semi-Annual should be defined as M06 and will result in 6 periods
--  Annual should be defined as M12 and will result in 1 period
--  Bi-Annual should be defined as M24 and will result in 0.5 periods
--  Any number of months up to 99 may be defined and results in 12/n periods

TWMTH
– Interest is compounded twice a month resulting in 24 periods as opposed to WEEK2
which results in 26 periods

Nnnn
- Compounding takes place nnn times per year. */
        COMPOUND_TYPE : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compounding)
            @i18n:Tooltip (en="It supports both debit and credit interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=COMPOUND.TYPE, mandatory=false)
/* "This field indicates how the effective rate is calculated, when set to YIELD the interest rate defined is taken as the annual effective rate. System uses the below formula to calculate the interest amount,

Daily Effective Rate(ERd) = (1+Interest Rate)^(1/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}

When the field is set as NULL the effective rate is calculated based on the compounding frequency defined in the field COMPOUND.TYPE using the below formula,

Daily Effective Rate(ERd) = (1+Interest Rate/Compound Freq)^(Compound Freq/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}" */
        COMPOUND_YIELD_METHOD : AA_Interest:AA_PRD_CAT_INTEREST__COMPOUND_YIELD_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=50.0, core=true, t24Name=COMPOUND.YIELD.METHOD, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=146.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=142.0, core=true, t24Name=CURR.NO, mandatory=false)
        /* If there is a Cutom rate tier then this field is mandatory. It is a AA.CUSTOM.RATE.TYPE record that determines how the custom rate should be calculated */
        CUSTOM_RATE_CALC -> AA_Interest:AA_CUSTOM_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=72.0, core=true, businessType=S, t24Name=CUSTOM.RATE.CALC, mandatory=false)
        CUSTOM_TYPE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__CUSTOM_TYPE *
            @t24:i (t24Name=CUSTOM.TYPE)
/* Specifies the date convention to be followed for the reset frequency i.e. if the cycled Periodic Reset date falls on a non-working day, the reset date will be moved based on the option chosen.
Allowed Values:
Arrangement / Null – Based on the date convention specified in the Account Property Class.
Periodic Interest – Based on the convention used by the index i.e. the Periodic Interest record.
o Forward Same Month convention for Monthly periods and Forward convention for all other periods if USE.LAST.WORKING.DAY is set to 'Y'.
o Forward Convention for all periods if USE.LAST.WORKING.DAY is not set. */
        DATE_CONVENTION : AA_Interest:AA_PRD_CAT_INTEREST__DATE_CONVENTION
            @i18n:Label (en=Convention)
            @t24:i (align=L, inputBehaviour=I, maxLength=18, sysNumber=61.0, core=true, t24Name=DATE.CONVENTION, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_PRD_CAT_INTEREST__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* T24 can calculate interest based upon several different day basis types.  Each T24 Interest Day Basis type is represented as a numerator and denominator.
• Numerator (interest days)
o 360 – Each month is considered to have 30 days
o 366 – The exact number of days will be used
• Denominator (days in the year)
o 360 – Each year is considered to have 360 days
o 365 – Each year is considered to have 365 days
o 366 – The exact number of days in the year will be used (365 or 366)

T24 allows the following interest day basis types:
Type        Notes
A (360/360)     A1 and A2 are variations which alter the method of treating a month as 30 days.
B (366/360)
C (366/366)
D (360/366)
E (366/365)
F (360/365)     F1 and F2 are variations which alter the method of treating a month as 30 days.
S (special)      Allows for actual interest amounts to be entered by the user.  The amounts in the     schedule must be within a tolerance of 5% when compared with the product of amount * rate * period.

This is a Mandatory field. */
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Day Basis")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, businessType=A, t24Name=DAY.BASIS, mandatory=false)
/* Optional field - Allowed Values are RESETTING and NON-RESETTING.

RESETTING - During any Renewal Activities (for eg : change.product or reset) the property conditions will be reset from the product.

NON-RESETTING - During any Renewal Activities property conditions will be maintained from the Arrangement level.

Leaving the field blank sets NON-RESETTING as the default option. */
        DEFAULT_ATTR_OPTION : AA_Interest:AA_PRD_CAT_INTEREST__DEFAULT_ATTR_OPTION
            @i18n:Label (en="Default Attribute")
            @i18n:Tooltip (en="Default Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=119.0, core=true, t24Name=DEFAULT.ATTR.OPTION, mandatory=false)
/* Defines whether all attributes (fields) can be negotiable or not.


This field is Mandatory
Valid options are YES and NO */
        DEFAULT_NEGOTIABLE : AA_Interest:AA_PRD_CAT_INTEREST__DEFAULT_NEGOTIABLE required
            @i18n:Label (en="Default Negotiable")
            @i18n:Tooltip (en="Default Negotiable")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=120.0, core=true, t24Name=DEFAULT.NEGOTIABLE, mandatory=true)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=147.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue AA_Interest:AA_PRD_CAT_INTEREST__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        FIXED_RATE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE *
            @t24:i (t24Name=FIXED.RATE)
        FT_TXN_TYPE -> FT_Config:FT_TXN_TYPE_CONDITION
            @i18n:Label (en="Transaction Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=101.1, core=false, businessType=A, t24Name=FT.TXN.TYPE, mandatory=false)
        FULL_DESCRIPTION : T24BusinessTypes:TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=2.0, core=true, t24Name=FULL.DESCRIPTION, mandatory=false)
/* Contains the arrangement number, extracted from the first component of the id
Not applicable at product level
System updated field no input allowed */
        ID_COMP_1 : T24BusinessTypes:A
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=131.0, core=true, t24Name=ID.COMP.1, mandatory=false)
/* The action that was triggered on the arrangement property
The action is taken from ACTIVITY.CLASS record of the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified. */
        ID_COMP_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=132.0, core=true, t24Name=ID.COMP.2, mandatory=false)
/* Contains the effective date of the property condition
Not applicable at product level
System updated field no input allowed */
        ID_COMP_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=30, sysNumber=133.0, core=true, t24Name=ID.COMP.3, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=134.0, core=true, t24Name=ID.COMP.4, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_5 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=135.0, core=true, t24Name=ID.COMP.5, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=136.0, core=true, t24Name=ID.COMP.6, mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_PRD_CAT_INTEREST__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* Specifies whether interest method is fixed life of the contract or not.
FIXED – Fix the total profit amount regardless of any change to loan parameters. This field is specify whether total interest amount need to fixed or not.
Null – Will be treated as a normal interest bearing property */
        INTEREST_METHOD : AA_Interest:AA_PRD_CAT_INTEREST__INTEREST_METHOD
            @t24:i (align=L, inputBehaviour=F, maxLength=6, sysNumber=66.0, core=true, t24Name=INTEREST.METHOD, mandatory=false)
/* Income / expense will be posted to internal account category specified in Accounting condition instead of PL categories.
This will accept YES or NULL */
        INTERNAL_BOOKING : AA_Interest:AA_PRD_CAT_INTEREST__INTERNAL_BOOKING
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, t24Name=INTERNAL.BOOKING, mandatory=false)
        /* Where at least one tier is set to be ‘Linked Rate’, the Arrangement to be linked for calculating the Interest Rate, must be specified here (or automatically defaulted by the system, where a Limit is attached) */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* This field will be used to specify the Interest Property of the Linked Arrangement that should be referred to, for getting the Interest Rate.



Where at least one tier is set to be ‘Linked Rate’, this must be specified. */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=64.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
/* This field indicates the minimum interest amount to be posted for an arrangement/account.
If the calculated interest amount is less than the amount specified in this field, system will waive the interest amount or adjust the interest amount based on the option set in the field MIN.INT.WAIVE */
        MIN_INT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=MIN.INT.AMOUNT, typeModifiers=-, mandatory=false)
/* This field indicates if the calculated interest amount has to be waived or adjusted.
If the field is set to YES, then the system will waive the interest amount if it is less than the MIN.INT.AMOUNT specified.
If NULL value is set and if the calculated interest amount is less than the MIN.INT.AMOUNT specified, then system will post the minimum interest amount. */
        MIN_INT_WAIVE : AA_Interest:AA_PRD_CAT_INTEREST__MIN_INT_WAIVE
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=53.0, core=true, t24Name=MIN.INT.WAIVE, mandatory=false)
/* Negative rate functionality is moved to TIER.NEGATIVE.RATE field, please refer to TIER.NEGATIVE.RATE for help text.
This field is system maintained field, where in if all of the tier negative rate has the same value, then this field will be updated with that value. */
        NEGATIVE_RATE : AA_Interest:AA_PRD_CAT_INTEREST__NEGATIVE_RATE
            @i18n:Label (en="Negative Rate")
            @t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=7.0, core=true, t24Name=NEGATIVE.RATE, mandatory=false)
        NEGOTIATED_FLDS -> byValue AA_Interest:AA_PRD_CAT_INTEREST__NEGOTIATED_FLDS *
            @t24:i (t24Name=NEGOTIATED.FLDS)
        NR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__NR_ATTRIBUTE *
            @t24:i (t24Name=NR.ATTRIBUTE)
        OFFSET_DETAILS -> byValue AA_Interest:AA_PRD_CAT_INTEREST__OFFSET_DETAILS *
            @t24:i (t24Name=OFFSET.DETAILS)
        ON_ACTIVITY -> byValue AA_Interest:AA_PRD_CAT_INTEREST__ON_ACTIVITY *
            @t24:i (t24Name=ON.ACTIVITY)
/* This field is used to define the profit amount realised in the Legacy system. Assuming the total profit amount for the contract is 10,000  and remaining profit amount to be realized during migration is 3,000, one will give the difference (7,000) in this attribute. And 3,000 will be input in the Actual Profit Amount that has to be realized in T24. If someone wants the total profit amount, they can simply add these 2 to get them. Once authorised, the value cannot be changed.
Validation:
• Applicable for Fixed Interest Method
• Input enabled during migration of the contract(through take-over activity) */
        ORIG_PROFIT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=30, sysNumber=70.0, core=true, t24Name=ORIG.PROFIT.AMOUNT, mandatory=false)
        OVERRIDE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        OWNING_COMPANY -> byValue AA_Interest:AA_PRD_CAT_INTEREST__OWNING_COMPANY *
            @t24:i (t24Name=OWNING.COMPANY)
        PACKAGE_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=PACKAGE.PRODUCT, mandatory=false)
        PACKAGE_PROPERTY_CONTROL : AA_Interest:AA_PRD_CAT_INTEREST__PACKAGE_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=100.0, core=true, t24Name=PACKAGE.PROPERTY.CONTROL, mandatory=false)
        PRICING_RULES_BENEFIT -> byValue AA_Interest:AA_PRD_CAT_INTEREST__PRICING_RULES_BENEFIT *
            @t24:i (t24Name=PRICING.RULES.BENEFIT)
/* This field represents the key to the product level conditions of a property class
This field basically has three components seperated by a hyphen
Condition id
Currency - if the property class is currency based. By default takes up local currency
Effective date - if the property class is dated */
        PRODUCT_KEY -> AA_Interest:AA_PRD_CAT_INTEREST PK required
            @i18n:Label (en="Product Key")
            @i18n:Tooltip (en="Product Key")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=0.0, core=true, businessType=ANY, t24Name=PRODUCT.KEY, mandatory=true)
        PROGRAM_LIMIT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=94.0, core=true, t24Name=PROGRAM.LIMIT, mandatory=false)
        PR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__PR_ATTRIBUTE *
            @t24:i (t24Name=PR.ATTRIBUTE)
        RATE_LOCK_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=101.3, core=false, t24Name=RATE.LOCK.ID, mandatory=false)
/* T24 allows for the definition of tiers of interest rates.  Each tier is specified by defining the amount up to which the interest rate applies.  Additionally, each tier can be of a different interest rate type (i.e. fixed, floating, or periodic).  There are three type of tiers; Single, Level, and Banded.
Single Rate
When a single rate tier type is specified a single nominal interest rate will apply for the entire balance amount.
Rate Levels
By specifying level tiers a product can be defined which will calculate interest at different rates depending on the balance amount.  A single rate of interest will be selected and applied based upon the balance amount.  For example for a deposit a single product can have a higher rate of interest for accounts which maintain higher credit balances.  Conversely for a loan product a higher rate of interest can be charged for accounts with higher debit balances.
For example, aassuming a credit balance of 15,000 and level interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  A single rate of 15 per cent will be applied to the entire amount of 15,000.
Banded Rates
Banded tier interest will typically result in a "blended" interest rate.   This is similar to Level tiers, but allows for the interest rate of each tier to be applied to the portion of the balance that falls within the tier.
For example, assuming a credit balance of 15,000 and banded interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  Two calculations will be performed (i.e. 10 per cent on 10,000 and 15 per cent on 5,000)

This field is Mandatory. */
        RATE_TIER_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__RATE_TIER_TYPE
            @i18n:Label (en=Translate)
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=8.0, core=true, t24Name=RATE.TIER.TYPE, mandatory=false)
/* Specifies whether Interest definition is based on a rate or amount and in case of rate what type of rate.
Reducing Rate: Rate defined in Interest rate related fields will be considered as Reducing or Diminishing Rate for Interest calculation.
Flat Rate: Rate defined in interest rate related fields will be considered as a Flat Rate.
Flat Amount: Amount defined in the ACTUAL.PROFIT.AMT will be considered as the total interest amount for the interest property.
This will be allowed only when INTEREST.METHOD is FIEXED */
        RATE_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__RATE_TYPE
            @i18n:Label (en="Rate Type")
            @i18n:Tooltip (en="Is the Rate quoted net of tax or gross of tax")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=67.0, core=true, t24Name=RATE.TYPE, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=141.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* For an arrangement under ACCOUNTS Product Line, it is possible link the first tier amount of debit interest to the LIMIT record attached.
It can be achieved by setting the field REFER.LIMIT in INTEREST property condition as ‘YES’. This functionality can be enabled only if the LIMIT is managed by AA. */
        REFER_LIMIT : AA_Interest:AA_PRD_CAT_INTEREST__REFER_LIMIT
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=51.0, core=true, t24Name=REFER.LIMIT, mandatory=false)
        REGIONAL_PRICING_CODE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=84.0, core=true, t24Name=REGIONAL.PRICING.CODE, mandatory=false)
        REGIONAL_PRICING_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=87.0, core=true, t24Name=REGIONAL.PRICING.FIXED.RATE, mandatory=false)
        REGIONAL_PRICING_LEVEL : AA_Interest:AA_PRD_CAT_INTEREST__REGIONAL_PRICING_LEVEL
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=83.0, core=true, t24Name=REGIONAL.PRICING.LEVEL, mandatory=false)
        REGIONAL_PRICING_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=86.0, core=true, t24Name=REGIONAL.PRICING.MARGIN, mandatory=false)
        REGIONAL_PRICING_OPERAND : AA_Interest:AA_PRD_CAT_INTEREST__REGIONAL_PRICING_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=85.0, core=true, t24Name=REGIONAL.PRICING.OPERAND, mandatory=false)
        REGIONAL_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=88.0, core=true, businessType=A, t24Name=REGIONAL.PRODUCT, mandatory=false)
        REGIONAL_PROPERTY_CONTROL : AA_Interest:AA_PRD_CAT_INTEREST__REGIONAL_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=89.0, core=true, t24Name=REGIONAL.PROPERTY.CONTROL, mandatory=false)
        RELATIONSHIP_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=59.0, core=true, t24Name=RELATIONSHIP.FIXED.RATE, mandatory=false)
        /* Rate of discount margin as a result of the relationship of the customer with the bank. Would be evaluated based on the RELATIONSHIP arrangements held by the customer and also the definitions in PREFERENTIAL.PRICING as well as the rules in PRICING.RULES of this arrangement product. Validation: System maintained field. Would be a percentage would be added or subtracted to the original rate. The effective rate would be influenced by the content in this field. */
        RELATIONSHIP_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=57.0, core=true, t24Name=RELATIONSHIP.MARGIN, mandatory=false)
        /* Indicates whether the margin stated gets added or subtracted or multiplied to the originally evaluated rate. Validation: System maintained field. Would be either ADD, SUBTRACT or MULTIPLY */
        RELATIONSHIP_OPERAND : AA_Interest:AA_PRD_CAT_INTEREST__RELATIONSHIP_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=56.0, core=true, t24Name=RELATIONSHIP.OPERAND, mandatory=false)
        /* Field to indicate the relationship pricing plan(if any) that has influenced the rate on this arrangement. If a customer has a relationship with the bank which entitles for special margins, then the plan which has contributed to this margin would be updated here. Validation: System maintained field. Would be a valid RELATIONSHIP.PRICING product for this customer */
        RELATIONSHIP_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=55.0, core=true, businessType=A, t24Name=RELATIONSHIP.PRODUCT, mandatory=false)
/* This field holds the variation selected for the arrangement for the interest pricing condition

Validation Rules

System maintained, NOINPUT allowed */
        RELATIONSHIP_VARIATION : AA_Interest:AA_PRD_CAT_INTEREST__RELATIONSHIP_VARIATION
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=RELATIONSHIP.VARIATION, mandatory=false)
        RESERVED1 : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=65.0, core=true, t24Name=RESERVED1, mandatory=false)
        RESERVED10 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=96.0, core=true, t24Name=RESERVED10, mandatory=false)
        RESERVED2_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=137.0, core=true, t24Name=RESERVED2.ID, mandatory=false)
        RESERVED4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=62.0, core=true, t24Name=RESERVED4, mandatory=false)
        RESERVED6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=68.0, core=true, t24Name=RESERVED6, mandatory=false)
        RESERVED7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=99.0, core=true, t24Name=RESERVED7, mandatory=false)
        RESERVED8 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=98.0, core=true, t24Name=RESERVED8, mandatory=false)
        RESERVED9 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=97.0, core=true, t24Name=RESERVED9, mandatory=false)
        /* If this field gets checked then rate would not be stored in the EFFECTIVE.RATE field and whenever needed System will calculate it at runtime */
        RUNTIME_RATE_CALC : AA_Interest:AA_PRD_CAT_INTEREST__RUNTIME_RATE_CALC
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=73.0, core=true, t24Name=RUNTIME.RATE.CALC, mandatory=false)
        STMT_NOS -> byValue AA_Interest:AA_PRD_CAT_INTEREST__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Field accepts the options 'YES' or 'ALTERNATE'.
Indicates whether accruals needs to be Suppressed or Accrual should be done for the Alternate Interest Property.

For Example:
It will be set as YES by the system if the DONOR.ACCRUAL field is set as SUPPRESS for any product / arrangement in INTEREST.COMPENSTION property of a Bundle arrangement.

If it is set as 'ALTERNATE' for a property then the Accrual will be suppressed for the property for which it is set and Accrual will happen for the Alternate property which is set in Activity Restriction.
Blank value in this field denotes that there is no suppression of accrual

This is a Non Mandatory field. */
        SUPPRESS_ACCRUAL : AA_Interest:AA_PRD_CAT_INTEREST__SUPPRESS_ACCRUAL
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=54.0, core=true, t24Name=SUPPRESS.ACCRUAL, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=SYS.RESERVE1, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=SYS.RESERVE2, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=SYS.RESERVE3, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=SYS.RESERVE6, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=SYS.RESERVE7, mandatory=false)
/* This field denotes the target classic product of the given property class

The already existing T24 products are mapped using AA infrastructure
These are called classic products
These products are mapped to different property clases */
        TARGET_PRODUCT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=138.0, core=true, t24Name=TARGET.PRODUCT, mandatory=false)
        USER_CUSTOM_RATE : AA_Interest:AA_PRD_CAT_INTEREST__USER_CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=21.0, core=true, t24Name=USER.CUSTOM.RATE, mandatory=false)
        USER_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=11.0, core=true, t24Name=USER.FIXED.RATE, typeModifiers=-, mandatory=false)
        USER_FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=12.0, core=true, businessType=A, t24Name=USER.FLOATING.INDEX, mandatory=false)
        USER_FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=13.0, core=true, t24Name=USER.FLOATING.NOTICE, mandatory=false)
        USER_INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=19.0, core=true, t24Name=USER.INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
        USER_LINKED_RATE : AA_Interest:AA_PRD_CAT_INTEREST__USER_LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=22.0, core=true, t24Name=USER.LINKED.RATE, mandatory=false)
        USER_MARGIN_TYPE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE *
            @t24:i (t24Name=USER.MARGIN.TYPE)
        USER_PERIODIC_INDEX : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=14.0, core=true, t24Name=USER.PERIODIC.INDEX, mandatory=false)
        USER_PERIODIC_METHOD : AA_Interest:AA_PRD_CAT_INTEREST__USER_PERIODIC_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=18.0, core=true, t24Name=USER.PERIODIC.METHOD, mandatory=false)
        USER_PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=17.0, core=true, t24Name=USER.PERIODIC.PERIOD, mandatory=false)
        USER_PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__USER_PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=15.0, core=true, t24Name=USER.PERIODIC.PERIOD.TYPE, mandatory=false)
        USER_PERIODIC_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=16.0, core=true, t24Name=USER.PERIODIC.RATE, typeModifiers=-, mandatory=false)
        USER_PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=20.0, core=true, t24Name=USER.PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
        USER_RATE_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__USER_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=USER.RATE.TYPE, mandatory=false)
        USE_PRICING_GRID : AA_Interest:AA_PRD_CAT_INTEREST__USE_PRICING_GRID
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=9.0, core=true, t24Name=USE.PRICING.GRID, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__API_ATTRIBUTE
    {
        API_ATTRIBUTE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=115.0, core=true, t24Name=API.ATTRIBUTE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__CHANGED_FIELDS
    {
        /* No input and multi value field - lists all fields that have been negotiated for each dated version of the property. For a subsequently dated property, the attributes listed in this field will appear in "Negotiated.Fields" and only newly negotiated fields will appear on "Changed.Fields" */
        CHANGED_FIELDS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=129.0, core=true, t24Name=CHANGED.FIELDS, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__CUSTOM_TYPE
    {
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=75.0, core=true, t24Name=CUSTOM.NAME, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, t24Name=CUSTOM.TYPE, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=76.0, core=true, t24Name=CUSTOM.VALUE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=144.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_PRD_CAT_INTEREST__DESCRIPTION
    {
/* Describes this condition record and is used for enrichment.

This field can be expanded to allow the User to enter the Description of the property condition in various languages. Each expansion of the field will correspond to a new Language as defined in the LANGUAGE table (Ref: GENERAL TABLES), and will indicate to the User the Language in which the Description must be entered.


Up to 35 type A (alphanumeric) characters */
        DESCRIPTION : T24BusinessTypes:ANY required
            @i18n:Label (en=Description)
            @i18n:Tooltip (en=Description)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=1.0, core=true, t24Name=DESCRIPTION, mandatory=true)
    }
    AA_PRD_CAT_INTEREST__FIXED_RATE
    {
        /* Set this field to yes whenever Interest rate should be calculated in a custom way. The method in CUSTOM.RATE.CALC field determines how this custom.Rate should be calculated */
        CUSTOM_RATE : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=36.0, core=true, t24Name=CUSTOM.RATE, mandatory=false)
/* Indicates at which rate is the arrangement currently running on for the date on the Id. This shows the actual rate even if it is running on a Periodic and would include the margin rate calculations as well.

System maintained(NOINPUT) field. */
        EFFECTIVE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=13, sysNumber=45.0, core=true, t24Name=EFFECTIVE.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A fixed rate is directly entered by the user specifying the Defined Property and is not defined by an index table.  Fixed rates are entered as positive numbers unless a negative rate is allowed.

This field is part of a multi value set of fields including FIXED.RATE, FLOATING.INDEX, PERIODIC.INDEX, PERIODIC.PERIOD, PERIODIC.BASE, MARGIN.TYPE, MARGIN.OPER, MARGIN.RATE, TIER.MIN.RATE, TIER.MAX.RATE, TIER.AMOUNT,TIER.PERCENT. */
        FIXED_RATE : T24BusinessTypes:R
            @i18n:Label (en="Fixed Rate")
            @i18n:Tooltip (en="Fixed rate entered by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=26.0, core=true, t24Name=FIXED.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A floating interest rate is tied to a variable base rate (i.e. BASIC.INTEREST).  During interest calculation, T24 will use the currency specific rate applicable for the calculation date (i.e. the rate used for the calculation will change whenever the base rate is changed).

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @i18n:Label (en="Floating Index")
            @i18n:Tooltip (en="Floating key of the BASIC.INTEREST")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=27.0, core=true, businessType=A, t24Name=FLOATING.INDEX, mandatory=false)
        /* This field is reserved for future development */
        FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=28.0, core=true, t24Name=FLOATING.NOTICE, mandatory=false)
/* Specifies the initial date from which the interest rate has to be reset. If specified along with a reset frequency, the initial reset date will be used for deriving the subsequent reset dates. If left blank, the subsequent resets will be based on the base date type in Account Property.

Allowed Options
Relative Dates (Start, Arrangement and Anniversary) or a valid date. */
        INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=34.0, core=true, t24Name=INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
/* It would be possible to specify that the rate for a given tier be ‘Linked’, by setting the field Linked Rate to Yes.



This can be mixed with normal rate calculations as well.



Where the Linked Rate is set to Yes, system would expect that an Arrangement reference in given in the ‘Linked Arrangement’ field along with the Interest property to be linked to. */
        LINKED_RATE : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=37.0, core=true, t24Name=LINKED.RATE, mandatory=false)
        MARGIN_TYPE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE *
            @t24:i (t24Name=MARGIN.TYPE)
/* A periodic rate is tied to an index (e.g. LIBOR) which is dependent on a period of time (e.g. term) and possibly an amount.  Once the arrangement is active, the rate is fixed until it is reset.  Periodic rates can be reset at a pre-determined frequency.
To define a periodic rate, the user specifies:
• PERIODIC.INTEREST index to use
• Period (this will default to TERM if a Term Amount Property exists)
• Interest selection method if the term is not found on the index. The option currently available is INTERPOLATE.
• Rate Reset period

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @i18n:Tooltip (en="Key of the Periodic interest table")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=29.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
/* If system does not find a interest rate in the correct bucket of PERIODIC.INTEREST table, it automatically applies a method to arrive at a rate chosen by this method.

Valid method is:

INTERPOLATE - System automatically interpolates the rate. This is the DEFAULT option as well. */
        PERIODIC_METHOD : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__PERIODIC_METHOD
            @i18n:Label (en="Periodic Method")
            @i18n:Tooltip (en="Valid method is Interpolate")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=33.0, core=true, t24Name=PERIODIC.METHOD, mandatory=false)
/* Defines the interest period to be used when linking to PERIODIC.INTEREST.(This will default to TERM if a Term Amount Property exists),. When a period is stated in Years or Weeks, they would be converted to Months and Days respectively and then located on the PI table.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Period")
            @i18n:Tooltip (en="Defines the interest period to be used")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=32.0, core=true, t24Name=PERIODIC.PERIOD, mandatory=false)
/* This field provides the user to specify which period will be used to fetch interest rate from the Periodic Interest table.
Allowed Values:
Maturity – Interest rate from periodic interest table will be fetched based on the maturity term of the contract.
Renewal – Interest rate from periodic interest table will be fetched based on the renewal term of the contract.
Reset Period - Interest rate from periodic interest table will be fetched based on the reset frequency period (i.e. based on the period to next interest reset date). If a Period is defined in 'Periodic Period' field, the same will be used for fetching the interest rate from Periodic Interest table except for the first and last period.
Periodic Period and Null - Interest rate from periodic interest table will be fetched based on the period defined in 'periodic period' field. */
        PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=30.0, core=true, t24Name=PERIODIC.PERIOD.TYPE, mandatory=false)
/* Indicates the rate on which the index runs on the date stated in the key. When a periodic index is used and the field is left blank, then system computes the rate from PERIODIC.INTEREST and defaults it here. When a rate is already present(say, the user changes the system computed rate), then system does not compute the new rate.

Validation rules:

System defaulted and modifiable.
Standard Rate field.
Valid only if Periodic index is stated. */
        PERIODIC_RATE : T24BusinessTypes:R
            @i18n:Label (en="Periodic Rate")
            @i18n:Tooltip (en="Rate in the index on the date stated in the key")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=31.0, core=true, t24Name=PERIODIC.RATE, typeModifiers=-, mandatory=false)
/* This indicates the reset period of the index stated. System would automatically reset the PERIODIC.RATE at the frequency stated in this field. Applicable only at Arrangement level. Periodic index may still be used without this field meaning, it never resets the computed rate

Validation rule:

Valid PERIOD field. */
        PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=35.0, core=true, t24Name=PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
/* The tier amount relates to the field RATE.TIER.TYPE and amounts can be entered if either BAND or LEVEL are selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Tier Amount")
            @i18n:Tooltip (en="Defines the threshold amount for each tier")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=TIER.AMOUNT, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MAX_RATE : T24BusinessTypes:R
            @i18n:Label (en="Tier Maximum Rate")
            @i18n:Tooltip (en="Maximum rate specified by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=43.0, core=true, t24Name=TIER.MAX.RATE, typeModifiers=-, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Tier Minimum Rate")
            @i18n:Tooltip (en="Minimum tier rate specified by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=TIER.MIN.RATE, typeModifiers=-, mandatory=false)
/* Originally negative rate was applicable for all of the tiers together, but through this field it is now possible to define negative rate per tier.

Different values can be set in each tier and the options available are YES, BLOCK.MARGIN and NO/Blank.

Blank or NO - Indicates that negative rates cannot be input and if a rate becomes negative as a result of the application of a margin the rate will be set to zero

YES - Indicates that negative rates can be input and if the application margin makes the rate more negative then the more negative rate will be used.

BLOCK.MARGIN - Indicates that negative rates can be used but the following conditions apply

- If the rate is positive and the margin makes the rate negative then the rate will be set to zero

- If the rate is negative and the margin makes the rate more negative then the margin will be ignored and the original negative rate is used. */
        TIER_NEGATIVE_RATE : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=44.0, core=true, t24Name=TIER.NEGATIVE.RATE, mandatory=false)
/* The tier percent relates to the field RATE.TIER.TYPE and percentages can be entered if BAND is selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. Either TIER.PERCENT or TIER.AMOUNT can be entered by not both. If TIER.PERCENT is defined, then the Sum of Tier percentage should be 100% */
        TIER_PERCENT : T24BusinessTypes:N
            @i18n:Label (en="Tier Percent")
            @i18n:Tooltip (en="Tier Percent")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=47.0, core=true, t24Name=TIER.PERCENT, mandatory=false)
        USAGE_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=USAGE.PERCENT, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE
    {
/* The options are ADD or SUB. This indicates whether the MARGIN.RATE should be added or subtracted from the Floating or Periodic Rate.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.RATE and input is only allowed if a rate is entered in MARGIN.RATE. */
        MARGIN_OPER : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER
            @i18n:Label (en="Margin Operand")
            @i18n:Tooltip (en="Whether to add or subtract the margin")
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=40.0, core=true, t24Name=MARGIN.OPER, mandatory=false)
/* A margin rate can be used to adjust the specified rate of interest and to appropriately reflect any rate profit realized.  The result is the nominal rate of interest.  T24 allows for the following margin configuration:
• Single or Multiple Margins (Field MARGIN.TYPE)
• Rate Adjustment Method (Field MARGIN.OPER)

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.OPER. */
        MARGIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Margin Rate")
            @i18n:Tooltip (en="Percentage of margin rate used to value portfolio")
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=41.0, core=true, t24Name=MARGIN.RATE, mandatory=false)
/* Identifies whether there are single or multiple margins

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.OPER and MARGIN.RATE. */
        MARGIN_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE
            @i18n:Label (en="Margin Type")
            @i18n:Tooltip (en="Indicates single or multiple margins")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=39.0, core=true, t24Name=MARGIN.TYPE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=143.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__NEGOTIATED_FLDS
    {
/* No input and multi value field that lists the attributes on the arrangement property record that have been fixed and/or negotiated. These are stored when the arrangement level record is committed. When the arrangement level record is next used all items in the NEGOTIATED.FIELDS list will be maintained.
Once an attribute has been fixed or negotiated it will remain in negotiated field list on all subsequent dated versions of the property. */
        NEGOTIATED_FLDS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=130.0, core=true, t24Name=NEGOTIATED.FLDS, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__NR_ATTRIBUTE
    {
/* Defines which individual attribute (field) has the negotiation rules defined in fields "Nr.Negotiable" through to "Nr.Message"


Must be a valid field name within this property
This field is part of a multi value set */
        NR_ATTRIBUTE : T24BusinessTypes:A
            @i18n:Label (en="Negotiable Attribute")
            @i18n:Tooltip (en="Negotiable Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=121.0, core=true, t24Name=NR.ATTRIBUTE, mandatory=false)
        NR_ATTRIBUTE_RULE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=123.0, core=true, t24Name=NR.ATTRIBUTE.RULE, mandatory=false)
        NR_OPTIONS -> byValue AA_Interest:AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_OPTIONS *
            @t24:i (t24Name=NR.OPTIONS)
/* It is possible to define negotiation rules in EB.COMPARISON.TYPE.



Input must be a valid record from EB.STANDARD.COMPARISON */
        NR_STD_COMP -> AA_Rules:EB_STANDARD_COMPARISON
            @i18n:Label (en="Negotiable Standard Comp")
            @i18n:Tooltip (en="Negotiable Standard Comp")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=125.0, core=true, businessType=A, t24Name=NR.STD.COMP, mandatory=false)
        NR_TYPE -> byValue AA_Interest:AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_TYPE *
            @t24:i (t24Name=NR.TYPE)
        NR_VALUE_SOURCE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=124.0, core=true, t24Name=NR.VALUE.SOURCE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_OPTIONS
    {
/* Defines named values to indicate if an option applies to the associated NR.ATTRIBUTE. More than one option can be associated with the NR.ATTRIBUTE, with certain restrictions.


Valid Inputs are NEGOTIABLE, NON-NEGOTIABLE, OVERRIDE, FIX-VALUE, MANDATORY as described below: -

NEGOTIABLE
The value of NEGOTIABLE will indicate that the associated NR.ATTRIBUTE can be negotiated at the arrangement level.

NON-NEGOTIABLE
The value of NON-NEGOTIABLE will indicate that the associated NR.ATTRIBUTE cannot be negotiated at the arrangement level. Both NEGOTIABLE and NON-NEGOTIABLE cannot be defined together

OVERRIDE
Specifies if a default override message should be generated if the default attribute is amended at the arrangement level. If OVERRIDE is not specified, no override will be generated.

FIX-VALUE
Specified if the field value is to be fixed at the arrangement level. If FIX-VALUE is not specified the field value will vary with the changes to the underlying product condition.

MANDATORY
Specifies if the attribute is mandatory for this product condition. Can only be specified where the attribute is not defined as mandatory in the property class. */
        NR_OPTIONS : AA_Interest:AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS
            @i18n:Label (en="Negotiable Options")
            @i18n:Tooltip (en="Negotiable Options")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=122.0, core=true, t24Name=NR.OPTIONS, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_TYPE
    {
/* This field indicates whether to raise error or override message when rule is broken
Allows only two values ERROR or OVERRIDE
ERROR error message is raised
OVERRIDE Override message is generated */
        NR_MESSAGE : AA_Interest:AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE
            @i18n:Label (en="Negotiable Message")
            @i18n:Tooltip (en="Negotiable Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=128.0, core=true, t24Name=NR.MESSAGE, mandatory=false)
/* Defines the negotiation rule that is linked to the field defined in "Nr.Attribute". If an attribute has a negotiation rule linked to it (in associated fields NR.TYPE, NR.VALUE and NR.MESSAGE) the attribute must be defined as negotiable.
It is possible to define more than one negotiation rule for an attribute


Input must be a valid record from EB.COMPARISON.TYPE
This is a multi value set of fields */
        NR_TYPE -> AA_Rules:EB_COMPARISON_TYPE
            @i18n:Label (en="Negotiable Type")
            @i18n:Tooltip (en="Negotiable Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=126.0, core=true, businessType=A, t24Name=NR.TYPE, mandatory=false)
        /* Defines the value that links to the rule in field "Nr.Type" */
        NR_VALUE : T24BusinessTypes:ANY
            @i18n:Label (en="Negotiable Value")
            @i18n:Tooltip (en="Negotiable Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=127.0, core=true, t24Name=NR.VALUE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__OFFSET_DETAILS
    {
        OFFSET_DETAILS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=101.4, core=false, t24Name=OFFSET.DETAILS, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__ON_ACTIVITY
    {
/* When the periodic interest rate needs to be recalculated for certain activities, this field in combination with RECALCULATE may be used to setup the activity for which interest needs to be recalculated
Validation Rules
1. Applicable only for Periodic interest rate recalculation
2. Must be a valid activity in AA.ACTIVITY */
        ON_ACTIVITY -> AA_ProductFramework:AA_ACTIVITY
            @i18n:Label (en="On Activity")
            @i18n:Tooltip (en="Activities for payment schedule recalculation")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=48.0, core=true, businessType=A, t24Name=ON.ACTIVITY, mandatory=false)
/* Related to ON.ACTIVITY activity, when flagged to RATE, system will recalculate Periodic rate when the activity setup in ON.ACTIVITY is processed. When the value is NOTHING periodic rate would not be recalculated when the activity setup in ON.ACTIVITY is processed
Validation Rules
1. Mandatory when ON.ACTIVITY is setup
The fields ON.ACTIVITY and RECALCULATE in interest property class will be enhanced to allow user to define when to recalculate ‘Profit Amount’.
All activities which could result in payment schedule recalculation can be configured to recalculate ‘Profit Amount’ i.e., activities with payment schedule calculate and update action */
        RECALCULATE : AA_Interest:AA_PRD_CAT_INTEREST__ON_ACTIVITY__RECALCULATE
            @i18n:Label (en="Recalculate?")
            @i18n:Tooltip (en="Select if you want to recalculate")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=RECALCULATE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__OVERRIDE
    {
        /* Standard Override field containing override messages generated by amendments to this property */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=140.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__OWNING_COMPANY
    {
/* This field is used to capture the T24 Company(ies) that own the Product Condition record. There is no functionality attached to this field and can be used only for access control through SMS verification.

The input in this field must be a valid record in company table and should belong to current financial group.

This is a multi value field. Input in this field is optional. */
        OWNING_COMPANY -> ST_CompanyCreation:COMPANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=114.0, core=true, businessType=AAPRODUCT, t24Name=OWNING.COMPANY, typeModifiers=COMPANY, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__PRICING_RULES_BENEFIT
    {
        PRICING_RULES_BENEFIT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=PRICING.RULES.BENEFIT, mandatory=false)
        PRICING_RULES_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=93.0, core=true, t24Name=PRICING.RULES.FIXED.RATE, mandatory=false)
        PRICING_RULES_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=92.0, core=true, t24Name=PRICING.RULES.MARGIN, mandatory=false)
        PRICING_RULES_OPERAND : AA_Interest:AA_PRD_CAT_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=91.0, core=true, t24Name=PRICING.RULES.OPERAND, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__PR_ATTRIBUTE
    {
        /* This will hold two values either DUE or CAPITALISE.This Field denote which application method(DUE or CAPITALISE)should be taken while applying periodic rule charges. */
        PR_APP_METHOD : AA_Interest:AA_PRD_CAT_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD
            @i18n:Label (en="Periodic Application Method")
            @i18n:Tooltip (en="Periodic Application Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=110.0, core=true, t24Name=PR.APP.METHOD, mandatory=false)
        /* This will have to accept a valid T24 period.This Field will denote the application period for current aplication method. */
        PR_APP_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Application Period")
            @i18n:Tooltip (en="Periodic Application Period")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=111.0, core=true, t24Name=PR.APP.PERIOD, mandatory=false)
/* These are set of Attributes which define the names of the rule, which will be applied on certain attributes of property classes over a period of time.


Input must be a valid record from AA.PERIODIC.ATTRIBUTE */
        PR_ATTRIBUTE -> AA_Rules:AA_PERIODIC_ATTRIBUTE
            @i18n:Label (en="Periodic Attribute")
            @i18n:Tooltip (en="Periodic Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=102.0, core=true, businessType=A, t24Name=PR.ATTRIBUTE, mandatory=false)
/* Charge can be collected when the periodic rule is broken
This field denotes the charge property. The property and its condition is
defined the product designer and validated during publishing


Valid charge property */
        PR_BRK_CHARGE -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Periodic Break Charge")
            @i18n:Tooltip (en="Periodic Break Charge")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=106.0, core=true, businessType=A, t24Name=PR.BRK.CHARGE, mandatory=false)
/* Based on the PR.BRK.RES option selected, this will hold either an OVERRIDE or EB.ERROR id. By default the ids will be picked from the associated EB.COMPARISON.TYPE record. If CAP or FLOOR values are given, then the underlying AA.PERIODIC.ATTRIBUTE.CLASS should have this type defined in it.


Input must be a valid OVERRIDE ID or EB.ERROR ID. */
        PR_BRK_MSG : T24BusinessTypes:ANY
            @i18n:Label (en="Periodic Break Message")
            @i18n:Tooltip (en="Periodic Break Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=105.0, core=true, t24Name=PR.BRK.MSG, mandatory=false)
/* When a rule is broken what action needs to be taken is defined here. Currently, the various options available are

Error block further processing
Override Give an override
Information  Record details only
CAP the rule value is to be used as a CAP if the rule is broken
FLOOR the rule value is to be used as a FLOOR if the rule is broken


Should be one of the above valid options */
        PR_BRK_RES : AA_Interest:AA_PRD_CAT_INTEREST__PR_ATTRIBUTE__PR_BRK_RES
            @i18n:Label (en="Periodic Break Restrcition")
            @i18n:Tooltip (en="Periodic Break Restrcition")
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=104.0, core=true, t24Name=PR.BRK.RES, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=PR.RESERVED.1, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=PR.RESERVED.2, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=107.0, core=true, t24Name=PR.RESERVED.3, mandatory=false)
/* This is the actual rule value, which will be applied on certain attributes of the property classes over a period of time.


Input must be a valid data type defined in the corresponding
AA.PERIODIC.ATTRIBUTE.CLASS record. */
        PR_VALUE : T24BusinessTypes:A
            @i18n:Label (en="Periodic Value")
            @i18n:Tooltip (en="Periodic Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=103.0, core=true, t24Name=PR.VALUE, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__STMT_NOS
    {
        /* System field - not used for property conditions */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=139.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE
    {
        USER_MARGIN_OPER : AA_Interest:AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=24.0, core=true, t24Name=USER.MARGIN.OPER, mandatory=false)
        USER_MARGIN_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=USER.MARGIN.RATE, mandatory=false)
        USER_MARGIN_TYPE : AA_Interest:AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=23.0, core=true, t24Name=USER.MARGIN.TYPE, mandatory=false)
    }
/* The INTEREST Property Class is used for all interest definition and processing in AA.  A T24 product defined and processed in AA can have multiple interest properties defined (e.g. principal interest, penalty interest, commission, etc.).  The number of interest properties is determined by the users defining the products.
The different condition ids of interest property can be attached to the different types of interest properties at product level */
    AA_PRD_DES_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.PRD.DES.INTEREST, nonStop=true, level=I, type=H, allowedFunctions="A C D E I L Q R S", localRefApplicationSysNo=101.0, localRefAllowed=true)
    {
/* Specifies how the accounting entries will be raised. Whether it needs to work like discounted or fixed type of interest.
Advance: This is nothing but discounted interest, interest amount will be collected inadvance before the interest periods.
Upfront Profit: Interest amount will be collected at the beginning of the contract.
None: Denotes conventional interest bearing contract */
        ACCOUNTING_MODE : AA_Interest:AA_PRD_DES_INTEREST__ACCOUNTING_MODE
            @i18n:Label (en="Accounting Mode")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=60.0, core=true, t24Name=ACCOUNTING.MODE, mandatory=false)
/* When accruing interest,T24 must determine which amounts to include in each day's balance, the actual days to include in the period (e.g. first day, last day) and how to round any calculations.
T24 allows users to define the calculation method to use, by specifying an accrual rule in EB.ACCRUAL.PARAM.  Users can define the following:
First Day Inclusive (Yes/No)
Last Day Inclusive (Yes/No)
Rounding Rule (Natural, Down, Up, Currency, None)
Intermediate Calculation Rounding (Yes/No)
Credit value date adjustment
Debit value date adjustment */
        ACCRUAL_RULE -> AC_Fees:EB_ACCRUAL_PARAM
            @i18n:Label (en="Accrual Rule Definition")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=ACCRUAL.RULE, mandatory=false)
/* User can defined Total interest / profit amount for the contract.
Mandatory input if RATE.TYPE is ‘Flat Amount’.
Value in CALC.PROFIT.AMT will be populated in this field which the user can override. */
        ACTUAL_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=69.0, core=true, t24Name=ACTUAL.PROFIT.AMT, mandatory=false)
        ADJUST_EXPIRY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=82.0, core=true, t24Name=ADJUST.EXPIRY.DATE, mandatory=false)
        ADJUST_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=79.0, core=true, t24Name=ADJUST.MARGIN, mandatory=false)
        ADJUST_OPERAND : AA_Interest:AA_PRD_DES_INTEREST__ADJUST_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=ADJUST.OPERAND, mandatory=false)
        ADJUST_OPTION : AA_Interest:AA_PRD_DES_INTEREST__ADJUST_OPTION
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.2, core=false, t24Name=ADJUST.OPTION, mandatory=false)
        ADJUST_OVERRIDE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=80.0, core=true, t24Name=ADJUST.OVERRIDE.RATE, mandatory=false)
        ADJUST_REASON : AA_Interest:AA_PRD_DES_INTEREST__ADJUST_REASON
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=81.0, core=true, t24Name=ADJUST.REASON, mandatory=false)
        ADJUST_TYPE : AA_Interest:AA_PRD_DES_INTEREST__ADJUST_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=77.0, core=true, t24Name=ADJUST.TYPE, mandatory=false)
        API_ATTRIBUTE -> byValue AA_Interest:AA_PRD_DES_INTEREST__API_ATTRIBUTE *
            @t24:i (t24Name=API.ATTRIBUTE)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=148.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=149.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=145.0, core=true, t24Name=AUTHORISER, mandatory=false)
        /* The user can specify that interest will only be calculated if a balance threshold is surpassed. For credit interest, the user can specify the minimum balance which must be maintained. For debit interest, the user can specify the maximum debit balance for which interest will not be calculated. */
        CALC_THRESHOLD : T24BusinessTypes:AMT
            @i18n:Label (en="Base Amount For Interest Calculation")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=CALC.THRESHOLD, typeModifiers=-, mandatory=false)
        CHANGED_FIELDS -> byValue AA_Interest:AA_PRD_DES_INTEREST__CHANGED_FIELDS *
            @t24:i (t24Name=CHANGED.FIELDS)
/* T24 supports the compounding of both debit and credit interest.

Compounding interest applies a factor to the compound interest calculation based upon the  number of compounding periods in the year. This field defines the number of compounding periods to be applied.

Note that the compounding type provides a factor in the calculation only  and does not actually cause the base principal amount to be actually increased by the calculated interest amount.

The following compound frequencies are supported:

DAILY
Interest is compounded 360/5/6 times a year (from days in year in INTEREST.BASIS)

WEEKn
Interest is compounded 52/n times per year based on a number of WEEKS
-- Bi-weekly compounding should be defined as WEEK2 and will result in 26 periods
-- Four-Weekly compounding should be defined as WEEK4 and results in 13 periods

Mnn
Interest is compounded 12/n times a year based on a numberof months
--  Monthly compounding should be defined as M01 and will result in 12 periods
--  Bi-Monthly should be defined as M02 and will result in 6 periods
--  Quarterly should be defined as M03 and will result in 4 periods
--  Semi-Annual should be defined as M06 and will result in 6 periods
--  Annual should be defined as M12 and will result in 1 period
--  Bi-Annual should be defined as M24 and will result in 0.5 periods
--  Any number of months up to 99 may be defined and results in 12/n periods

TWMTH
Interest is compounded twice a month resulting in 24 periods as opposed to WEEK2
which results in 26 periods

Nnnn
- Compounding takes place nnn times per year. */
        COMPOUND_TYPE : T24BusinessTypes:COMPFQU
            @i18n:Label (en="Compounding Interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=COMPOUND.TYPE, mandatory=false)
/* This field indicates how the effective rate is calculated, when set to YIELD the interest rate defined is taken as the annual effective rate. System uses the below formula to calculate the interest amount,

Daily Effective Rate(ERd) = (1+Interest Rate)^(1/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}

When the field is set as NULL the effective rate is calculated based on the compounding frequency defined in the field COMPOUND.TYPE using the below formula,

Daily Effective Rate(ERd) = (1+Interest Rate/Compound Freq)^(Compound Freq/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1} */
        COMPOUND_YIELD_METHOD : AA_Interest:AA_PRD_DES_INTEREST__COMPOUND_YIELD_METHOD
            @i18n:Label (en="Method Of Interest Yield")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=50.0, core=true, t24Name=COMPOUND.YIELD.METHOD, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=146.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=142.0, core=true, t24Name=CURR.NO, mandatory=false)
        /* If there is a Cutom rate tier then this field is mandatory. It is a AA.CUSTOM.RATE.TYPE record that determines how the custom rate should be calculated */
        CUSTOM_RATE_CALC -> AA_Interest:AA_CUSTOM_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=72.0, core=true, businessType=S, t24Name=CUSTOM.RATE.CALC, mandatory=false)
        CUSTOM_TYPE -> byValue AA_Interest:AA_PRD_DES_INTEREST__CUSTOM_TYPE *
            @t24:i (t24Name=CUSTOM.TYPE)
/* Specifies the date convention to be followed for the reset frequency i.e. if the cycled Periodic Reset date falls on a non-working day, the reset date will be moved based on the option chosen.
Allowed Values:
Arrangement / Null – Based on the date convention specified in the Account Property Class.
Periodic Interest – Based on the convention used by the index i.e. the Periodic Interest record.
o Forward Same Month convention for Monthly periods and Forward convention for all other periods if USE.LAST.WORKING.DAY is set to 'Y'.
o Forward Convention for all periods if USE.LAST.WORKING.DAY is not set. */
        DATE_CONVENTION : AA_Interest:AA_PRD_DES_INTEREST__DATE_CONVENTION
            @i18n:Label (en=Convention)
            @t24:i (align=L, inputBehaviour=I, maxLength=18, sysNumber=61.0, core=true, t24Name=DATE.CONVENTION, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_PRD_DES_INTEREST__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* T24 can calculate interest based upon several different day basis types.  Each T24 Interest Day Basis type is represented as a numerator and denominator.
Numerator (interest days)
o 360  Each month is considered to have 30 days
o 366  The exact number of days will be used
Denominator (days in the year)
o 360  Each year is considered to have 360 days
o 365  Each year is considered to have 365 days
o 366  The exact number of days in the year will be used (365 or 366)

T24 allows the following interest day basis types:
Type        Notes
A (360/360)     A1 and A2 are variations which alter the method of treating a month as 30 days.
B (366/360)
C (366/366)
D (360/366)
E (366/365)
F (360/365)     F1 and F2 are variations which alter the method of treating a month as 30 days.

This is a Mandatory field. */
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Day Basis Of Interest Calculations")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, businessType=A, t24Name=DAY.BASIS, mandatory=false)
/* Optional field - Allowed Values are RESETTING and NON-RESETTING.

RESETTING - During any Renewal Activities (for eg : change.product or reset) the property conditions will be reset from the product.

NON-RESETTING - During any Renewal Activities property conditions will be maintained from the Arrangement level.

Leaving the field blank sets NON-RESETTING as the default option. */
        DEFAULT_ATTR_OPTION : AA_Interest:AA_PRD_DES_INTEREST__DEFAULT_ATTR_OPTION
            @i18n:Label (en="Default Attribute Option")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=119.0, core=true, t24Name=DEFAULT.ATTR.OPTION, mandatory=false)
/* Defines whether all attributes (fields) can be negotiable or not.


This field is Mandatory
Valid options are YES and NO */
        DEFAULT_NEGOTIABLE : AA_Interest:AA_PRD_DES_INTEREST__DEFAULT_NEGOTIABLE required
            @i18n:Label (en="Default Negotiable")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=120.0, core=true, t24Name=DEFAULT.NEGOTIABLE, mandatory=true)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=147.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue AA_Interest:AA_PRD_DES_INTEREST__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        FIXED_RATE -> byValue AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE *
            @t24:i (t24Name=FIXED.RATE)
        FT_TXN_TYPE -> FT_Config:FT_TXN_TYPE_CONDITION
            @i18n:Label (en="Transaction Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=101.1, core=false, businessType=A, t24Name=FT.TXN.TYPE, mandatory=false)
        FULL_DESCRIPTION : T24BusinessTypes:TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=2.0, core=true, t24Name=FULL.DESCRIPTION, mandatory=false)
/* Contains the arrangement number, extracted from the first component of the id
Not applicable at product level
System updated field no input allowed */
        ID_COMP_1 : T24BusinessTypes:A
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=131.0, core=true, t24Name=ID.COMP.1, mandatory=false)
/* The action that was triggered on the arrangement property
The action is taken from ACTIVITY.CLASS record of the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified. */
        ID_COMP_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=132.0, core=true, t24Name=ID.COMP.2, mandatory=false)
/* Contains the effective date of the property condition
Not applicable at product level
System updated field no input allowed */
        ID_COMP_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=30, sysNumber=133.0, core=true, t24Name=ID.COMP.3, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=134.0, core=true, t24Name=ID.COMP.4, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_5 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=135.0, core=true, t24Name=ID.COMP.5, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=136.0, core=true, t24Name=ID.COMP.6, mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_PRD_DES_INTEREST__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* Specifies whether interest method is fixed life of the contract or not.
FIXED – Fix the total profit amount regardless of any change to loan parameters. This field is specify whether total interest amount need to fixed or not.
Null – Will be treated as a normal interest bearing property. */
        INTEREST_METHOD : AA_Interest:AA_PRD_DES_INTEREST__INTEREST_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=66.0, core=true, t24Name=INTEREST.METHOD, mandatory=false)
/* Income / expense will be posted to internal account category specified in Accounting condition instead of PL categories.
This will accept YES or NULL */
        INTERNAL_BOOKING : AA_Interest:AA_PRD_DES_INTEREST__INTERNAL_BOOKING
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, t24Name=INTERNAL.BOOKING, mandatory=false)
        /* Where at least one tier is set to be ‘Linked Rate’, the Arrangement to be linked for calculating the Interest Rate, must be specified here (or automatically defaulted by the system, where a Limit is attached) */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* This field will be used to specify the Interest Property of the Linked Arrangement that should be referred to, for getting the Interest Rate.



Where at least one tier is set to be ‘Linked Rate’, this must be specified. */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=64.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
/* This field indicates the minimum interest amount to be posted for an arrangement/account.
If the calculated interest amount is less than the amount specified in this field, system will waive the interest amount or adjust the interest amount based on the option set in the field MIN.INT.WAIVE
Defines the minimum interest amount that will compared to the system calculated interest amount. If the interest calculated is less than the minimum amount set, then based on the waive setup field, either the min interest amount would be applied or it would be waived.
Validation Rules
Must be a valid T24 Amount input
Optional Field */
        MIN_INT_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Minimum Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=MIN.INT.AMOUNT, typeModifiers=-, mandatory=false)
/* This field indicates if the calculated interest amount has to be waived or adjusted.
If the field is set to YES, then the system will waive the interest amount if it is less than the MIN.INT.AMOUNT specified.
If NULL value is set and if the calculated interest amount is less than the MIN.INT.AMOUNT specified, then system will post the minimum interest amount.
Defines if the calculated interest amount goes below minimum interest amount set in the MIN.INT.AMOUNT field, should the system waive the interest amount or post the minimum amount.
Can be set to either YES or left blank.
Validation Rules
Valid input is YES or left blank.
Default is blank */
        MIN_INT_WAIVE : AA_Interest:AA_PRD_DES_INTEREST__MIN_INT_WAIVE
            @i18n:Label (en="Minimum Interest To Be Waived")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=53.0, core=true, t24Name=MIN.INT.WAIVE, mandatory=false)
/* Negative rate functionality is moved to TIER.NEGATIVE.RATE field, please refer to TIER.NEGATIVE.RATE for help text.
This field is system maintained field, where in if all of the tier negative rate has the same value, then this field will be updated with that value. */
        NEGATIVE_RATE : AA_Interest:AA_PRD_DES_INTEREST__NEGATIVE_RATE
            @i18n:Label (en="Negative Rate")
            @t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=7.0, core=true, t24Name=NEGATIVE.RATE, mandatory=false)
        NEGOTIATED_FLDS -> byValue AA_Interest:AA_PRD_DES_INTEREST__NEGOTIATED_FLDS *
            @t24:i (t24Name=NEGOTIATED.FLDS)
        NR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_DES_INTEREST__NR_ATTRIBUTE *
            @t24:i (t24Name=NR.ATTRIBUTE)
        OFFSET_DETAILS -> byValue AA_Interest:AA_PRD_DES_INTEREST__OFFSET_DETAILS *
            @t24:i (t24Name=OFFSET.DETAILS)
        ON_ACTIVITY -> byValue AA_Interest:AA_PRD_DES_INTEREST__ON_ACTIVITY *
            @t24:i (t24Name=ON.ACTIVITY)
/* This field is used to define the profit amount realised in the Legacy system. Assuming the total profit amount for the contract is 10,000  and remaining profit amount to be realized during migration is 3,000, one will give the difference (7,000) in this attribute. And 3,000 will be input in the Actual Profit Amount that has to be realized in T24. If someone wants the total profit amount, they can simply add these 2 to get them. Once authorised, the value cannot be changed.
Validation:
• Applicable for Fixed Interest Method
• Input enabled during migration of the contract(through take-over activity) */
        ORIG_PROFIT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=30, sysNumber=70.0, core=true, t24Name=ORIG.PROFIT.AMOUNT, mandatory=false)
        OVERRIDE -> byValue AA_Interest:AA_PRD_DES_INTEREST__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        OWNING_COMPANY -> byValue AA_Interest:AA_PRD_DES_INTEREST__OWNING_COMPANY *
            @t24:i (t24Name=OWNING.COMPANY)
        PACKAGE_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=PACKAGE.PRODUCT, mandatory=false)
        PACKAGE_PROPERTY_CONTROL : AA_Interest:AA_PRD_DES_INTEREST__PACKAGE_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=100.0, core=true, t24Name=PACKAGE.PROPERTY.CONTROL, mandatory=false)
        PRICING_RULES_BENEFIT -> byValue AA_Interest:AA_PRD_DES_INTEREST__PRICING_RULES_BENEFIT *
            @t24:i (t24Name=PRICING.RULES.BENEFIT)
/* This field represents the key to the product level conditions of a property class
This field basically has three components seperated by a hyphen
Condition id
Currency - if the property class is currency based. By default takes up local currency
Effective date - if the property class is dated */
        PRODUCT_KEY -> AA_Interest:AA_PRD_DES_INTEREST PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=0.0, core=true, businessType=ANY, t24Name=PRODUCT.KEY, mandatory=true)
        PROGRAM_LIMIT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=94.0, core=true, t24Name=PROGRAM.LIMIT, mandatory=false)
        PR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_DES_INTEREST__PR_ATTRIBUTE *
            @t24:i (t24Name=PR.ATTRIBUTE)
        RATE_LOCK_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=101.3, core=false, t24Name=RATE.LOCK.ID, mandatory=false)
/* T24 allows for the definition of tiers of interest rates.  Each tier is specified by defining the amount up to which the interest rate applies.  Additionally, each tier can be of a different interest rate type (i.e. fixed, floating, or periodic).  There are three type of tiers; Single, Level, and Banded.
Single Rate
When a single rate tier type is specified a single nominal interest rate will apply for the entire balance amount.
Rate Levels
By specifying level tiers a product can be defined which will calculate interest at different rates depending on the balance amount.  A single rate of interest will be selected and applied based upon the balance amount.  For example for a deposit a single product can have a higher rate of interest for accounts which maintain higher credit balances.  Conversely for a loan product a higher rate of interest can be charged for accounts with higher debit balances.
For example, aassuming a credit balance of 15,000 and level interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  A single rate of 15 per cent will be applied to the entire amount of 15,000.
Banded Rates
Banded tier interest will typically result in a blended interest rate.   This is similar to Level tiers, but allows for the interest rate of each tier to be applied to the portion of the balance that falls within the tier.
For example, assuming a credit balance of 15,000 and banded interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  Two calculations will be performed (i.e. 10 per cent on 10,000 and 15 per cent on 5,000)

This field is Mandatory. */
        RATE_TIER_TYPE : AA_Interest:AA_PRD_DES_INTEREST__RATE_TIER_TYPE
            @i18n:Label (en="Tier Type Interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=8.0, core=true, t24Name=RATE.TIER.TYPE, mandatory=false)
/* Specifies whether Interest definition is based on a rate or amount and in case of rate what type of rate.
Reducing Rate: Rate defined in Interest rate related fields will be considered as Reducing or Diminishing Rate for Interest calculation.
Flat Rate: Rate defined in interest rate related fields will be considered as a Flat Rate.
Flat Amount: Amount defined in the ACTUAL.PROFIT.AMT will be considered as the total interest amount for the interest property.
This will be allowed only when INTEREST.METHOD is FIEXED */
        RATE_TYPE : AA_Interest:AA_PRD_DES_INTEREST__RATE_TYPE
            @i18n:Label (en="Rate Type")
            @i18n:Tooltip (en="Is the Rate quoted net of tax or gross of tax")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=67.0, core=true, t24Name=RATE.TYPE, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=141.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* For an arrangement under ACCOUNTS Product Line, it is possible to link the first tier amount of debit interest to the LIMIT record attached.
It can be achieved by setting the field REFER.LIMIT in INTEREST property condition as ‘YES’. This functionality can be enabled only if the LIMIT is managed by AA.
For tiered interest conditions in AA, a value in this field would decide if limit amount to be used as first tier amount, if first tier is left blank. This can be set to either YES or NO or left blank.
When this is set to YES, during interest accrual calculation for an arrangement, system would pick the limit amount from the limit property class attached to the arrangement. If limit is managed outside of AA, first tier cannot be left blank.
Validation Rules
Input of YES or NO or blank is allowed
Optional Field */
        REFER_LIMIT : AA_Interest:AA_PRD_DES_INTEREST__REFER_LIMIT
            @i18n:Label (en="Refer Limit For Interest ")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=51.0, core=true, t24Name=REFER.LIMIT, mandatory=false)
        REGIONAL_PRICING_CODE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=84.0, core=true, t24Name=REGIONAL.PRICING.CODE, mandatory=false)
        REGIONAL_PRICING_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=87.0, core=true, t24Name=REGIONAL.PRICING.FIXED.RATE, mandatory=false)
        REGIONAL_PRICING_LEVEL : AA_Interest:AA_PRD_DES_INTEREST__REGIONAL_PRICING_LEVEL
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=83.0, core=true, t24Name=REGIONAL.PRICING.LEVEL, mandatory=false)
        REGIONAL_PRICING_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=86.0, core=true, t24Name=REGIONAL.PRICING.MARGIN, mandatory=false)
        REGIONAL_PRICING_OPERAND : AA_Interest:AA_PRD_DES_INTEREST__REGIONAL_PRICING_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=85.0, core=true, t24Name=REGIONAL.PRICING.OPERAND, mandatory=false)
        REGIONAL_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=88.0, core=true, businessType=A, t24Name=REGIONAL.PRODUCT, mandatory=false)
        REGIONAL_PROPERTY_CONTROL : AA_Interest:AA_PRD_DES_INTEREST__REGIONAL_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=89.0, core=true, t24Name=REGIONAL.PROPERTY.CONTROL, mandatory=false)
        RELATIONSHIP_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=59.0, core=true, t24Name=RELATIONSHIP.FIXED.RATE, mandatory=false)
/* Rate of discount margin as a result of the relationship of the customer with the bank. Would be evaluated based on the RELATIONSHIP arrangements held by the customer and also the definitions in PREFERENTIAL.PRICING as well as the rules in PRICING.RULES of this arrangement product.
Validation: System maintained field.
Would be a percentage would be added or subtracted to the original rate. The effective rate would be influenced by the content in this field. */
        RELATIONSHIP_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=57.0, core=true, t24Name=RELATIONSHIP.MARGIN, mandatory=false)
/* Indicates whether the margin stated gets added or subtracted or multiplied to the originally evaluated rate.
Validation: System maintained field.
Would be either ADD, SUBTRACT or MULTIPLY */
        RELATIONSHIP_OPERAND : AA_Interest:AA_PRD_DES_INTEREST__RELATIONSHIP_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=56.0, core=true, t24Name=RELATIONSHIP.OPERAND, mandatory=false)
/* Field to indicate the relationship pricing plan(if any) that has influenced the rate on this arrangement. If a customer has a relationship with the bank which entitles for special margins, then the plan which has contributed to this margin would be updated here.
Validation: System maintained field.
Would be a valid RELATIONSHIP.PRICING product for this customer */
        RELATIONSHIP_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=55.0, core=true, businessType=A, t24Name=RELATIONSHIP.PRODUCT, mandatory=false)
/* This field holds the variation selected for the arrangement for the interest pricing condition

Validation Rules

System maintained, NOINPUT allowed */
        RELATIONSHIP_VARIATION : AA_Interest:AA_PRD_DES_INTEREST__RELATIONSHIP_VARIATION
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=RELATIONSHIP.VARIATION, mandatory=false)
        RESERVED1 : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=65.0, core=true, t24Name=RESERVED1, mandatory=false)
        RESERVED10 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=96.0, core=true, t24Name=RESERVED10, mandatory=false)
        RESERVED2_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=137.0, core=true, t24Name=RESERVED2.ID, mandatory=false)
        RESERVED4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=62.0, core=true, t24Name=RESERVED4, mandatory=false)
        RESERVED6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=68.0, core=true, t24Name=RESERVED6, mandatory=false)
        RESERVED7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=99.0, core=true, t24Name=RESERVED7, mandatory=false)
        RESERVED8 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=98.0, core=true, t24Name=RESERVED8, mandatory=false)
        RESERVED9 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=97.0, core=true, t24Name=RESERVED9, mandatory=false)
        /* If this field gets checked then rate would not be stored in the EFFECTIVE.RATE field and whenever needed System will calculate it at runtime */
        RUNTIME_RATE_CALC : AA_Interest:AA_PRD_DES_INTEREST__RUNTIME_RATE_CALC
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=73.0, core=true, t24Name=RUNTIME.RATE.CALC, mandatory=false)
        STMT_NOS -> byValue AA_Interest:AA_PRD_DES_INTEREST__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Field accepts the options 'YES' or 'ALTERNATE'.
Indicates whether accruals needs to be Suppressed or Accrual should be done for the Alternate Interest Property.

For Example:
It will be set as YES by the system if the DONOR.ACCRUAL field is set as SUPPRESS for any product / arrangement in INTEREST.COMPENSTION property of a Bundle arrangement.

If it is set as 'ALTERNATE' for a property then the Accrual will be suppressed for the property for which it is set and Accrual will happen for the Alternate property which is set in Activity Restriction.
Blank value in this field denotes that there is no suppression of accrual

This is a Non Mandatory field. */
        SUPPRESS_ACCRUAL : AA_Interest:AA_PRD_DES_INTEREST__SUPPRESS_ACCRUAL
            @i18n:Label (en="Suppress Accruals")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=54.0, core=true, t24Name=SUPPRESS.ACCRUAL, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=SYS.RESERVE1, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=SYS.RESERVE2, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=SYS.RESERVE3, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=SYS.RESERVE6, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=SYS.RESERVE7, mandatory=false)
/* This field denotes the target classic product of the given property class

The already existing T24 products are mapped using AA infrastructure
These are called classic products
These products are mapped to different property clases */
        TARGET_PRODUCT : T24BusinessTypes:A
            @i18n:Label (en="Target Classic Product")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=138.0, core=true, t24Name=TARGET.PRODUCT, mandatory=false)
        USER_CUSTOM_RATE : AA_Interest:AA_PRD_DES_INTEREST__USER_CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=21.0, core=true, t24Name=USER.CUSTOM.RATE, mandatory=false)
        USER_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=11.0, core=true, t24Name=USER.FIXED.RATE, typeModifiers=-, mandatory=false)
        USER_FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=12.0, core=true, businessType=A, t24Name=USER.FLOATING.INDEX, mandatory=false)
        USER_FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=13.0, core=true, t24Name=USER.FLOATING.NOTICE, mandatory=false)
        USER_INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=19.0, core=true, t24Name=USER.INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
        USER_LINKED_RATE : AA_Interest:AA_PRD_DES_INTEREST__USER_LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=22.0, core=true, t24Name=USER.LINKED.RATE, mandatory=false)
        USER_MARGIN_TYPE -> byValue AA_Interest:AA_PRD_DES_INTEREST__USER_MARGIN_TYPE *
            @t24:i (t24Name=USER.MARGIN.TYPE)
        USER_PERIODIC_INDEX : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=14.0, core=true, t24Name=USER.PERIODIC.INDEX, mandatory=false)
        USER_PERIODIC_METHOD : AA_Interest:AA_PRD_DES_INTEREST__USER_PERIODIC_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=18.0, core=true, t24Name=USER.PERIODIC.METHOD, mandatory=false)
        USER_PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=17.0, core=true, t24Name=USER.PERIODIC.PERIOD, mandatory=false)
        USER_PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_DES_INTEREST__USER_PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=15.0, core=true, t24Name=USER.PERIODIC.PERIOD.TYPE, mandatory=false)
        USER_PERIODIC_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=16.0, core=true, t24Name=USER.PERIODIC.RATE, typeModifiers=-, mandatory=false)
        USER_PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=20.0, core=true, t24Name=USER.PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
        USER_RATE_TYPE : AA_Interest:AA_PRD_DES_INTEREST__USER_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=USER.RATE.TYPE, mandatory=false)
        USE_PRICING_GRID : AA_Interest:AA_PRD_DES_INTEREST__USE_PRICING_GRID
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=9.0, core=true, t24Name=USE.PRICING.GRID, mandatory=false)
    }
    AA_PRD_DES_INTEREST__API_ATTRIBUTE
    {
        API_ATTRIBUTE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=115.0, core=true, t24Name=API.ATTRIBUTE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__CHANGED_FIELDS
    {
        /* No input and multi value field - lists all fields that have been negotiated for each dated version of the property. For a subsequently dated property, the attributes listed in this field will appear in "Negotiated.Fields" and only newly negotiated fields will appear on "Changed.Fields" */
        CHANGED_FIELDS : T24BusinessTypes:A
            @i18n:Label (en="Fields That Have Been Negotiated For The Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=129.0, core=true, t24Name=CHANGED.FIELDS, mandatory=false)
    }
    AA_PRD_DES_INTEREST__CUSTOM_TYPE
    {
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=75.0, core=true, t24Name=CUSTOM.NAME, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, t24Name=CUSTOM.TYPE, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=76.0, core=true, t24Name=CUSTOM.VALUE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=144.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_PRD_DES_INTEREST__DESCRIPTION
    {
/* Describes this condition record and is used for enrichment.

This field can be expanded to allow the User to enter the Description of the property condition in various languages. Each expansion of the field will correspond to a new Language as defined in the LANGUAGE table (Ref: GENERAL TABLES), and will indicate to the User the Language in which the Description must be entered.


Up to 35 type A (alphanumeric) characters */
        DESCRIPTION : T24BusinessTypes:ANY required
            @i18n:Label (en="Describes This Condition Record And Is Used For Enrichment")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=1.0, core=true, t24Name=DESCRIPTION, mandatory=true)
    }
    AA_PRD_DES_INTEREST__FIXED_RATE
    {
        /* Set this field to yes whenever Interest rate should be calculated in a custom way. The method in CUSTOM.RATE.CALC field determines how this custom.Rate should be calculated */
        CUSTOM_RATE : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=36.0, core=true, t24Name=CUSTOM.RATE, mandatory=false)
/* Indicates at which rate is the arrangement currently running on for the date on the Id. This shows the actual rate even if it is running on a Periodic and would include the margin rate calculations as well.

System maintained(NOINPUT) field. */
        EFFECTIVE_RATE : T24BusinessTypes:R
            @i18n:Label (en="Actual Interest Rate")
            @t24:i (align=R, inputBehaviour=N, maxLength=13, sysNumber=45.0, core=true, t24Name=EFFECTIVE.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A fixed rate is directly entered by the user specifying the Defined Property and is not defined by an index table.  Fixed rates are entered as positive numbers unless a negative rate is allowed.

This field is part of a multi value set of fields including FIXED.RATE, FLOATING.INDEX, PERIODIC.INDEX, PERIODIC.PERIOD, PERIODIC.BASE, MARGIN.TYPE, MARGIN.OPER, MARGIN.RATE, TIER.MIN.RATE, TIER.MAX.RATE, TIER.AMOUNT,TIER.PERCENT. */
        FIXED_RATE : T24BusinessTypes:R
            @i18n:Label (en="Fixed Interest Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=26.0, core=true, t24Name=FIXED.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A floating interest rate is tied to a variable base rate (i.e. BASIC.INTEREST).  During interest calculation, T24 will use the currency specific rate applicable for the calculation date (i.e. the rate used for the calculation will change whenever the base rate is changed).

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @i18n:Label (en="Floating Interest Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=27.0, core=true, businessType=A, t24Name=FLOATING.INDEX, mandatory=false)
        /* This field is reserved for future development */
        FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @i18n:Label (en="Floating Notice ")
            @i18n:Tooltip (en="Floating Notice")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=28.0, core=true, t24Name=FLOATING.NOTICE, mandatory=false)
/* Specifies the initial date from which the interest rate has to be reset. If specified along with a reset frequency, the initial reset date will be used for deriving the subsequent reset dates. If left blank, the subsequent resets will be based on the base date type in Account Property.

Allowed Options
Relative Dates (Start, Arrangement and Anniversary) or a valid date. */
        INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=34.0, core=true, t24Name=INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
/* It would be possible to specify that the rate for a given tier be ‘Linked’, by setting the field Linked Rate to Yes.



This can be mixed with normal rate calculations as well.



Where the Linked Rate is set to Yes, system would expect that an Arrangement reference in given in the ‘Linked Arrangement’ field along with the Interest property to be linked to. */
        LINKED_RATE : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=37.0, core=true, t24Name=LINKED.RATE, mandatory=false)
        MARGIN_TYPE -> byValue AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE *
            @t24:i (t24Name=MARGIN.TYPE)
/* A periodic rate is tied to an index (e.g. LIBOR) which is dependent on a period of time (e.g. term) and possibly an amount.  Once the arrangement is active, the rate is fixed until it is reset.  Periodic rates can be reset at a pre-determined frequency.
To define a periodic rate, the user specifies:
PERIODIC.INTEREST index to use
Period (this will default to TERM if a Term Amount Property exists)
Interest selection method if the term is not found on the index. The option currently available is INTERPOLATE.
Rate Reset period

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=29.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
/* If system does not find a interest rate in the correct bucket of PERIODIC.INTEREST table, it automatically applies a method to arrive at a rate chosen by this method.

Valid method is:

INTERPOLATE - System automatically interpolates the rate. This is the DEFAULT option as well. */
        PERIODIC_METHOD : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__PERIODIC_METHOD
            @i18n:Label (en="Periodic Interest Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=33.0, core=true, t24Name=PERIODIC.METHOD, mandatory=false)
/* Defines the interest period to be used when linking to PERIODIC.INTEREST.(This will default to TERM if a Term Amount Property exists),. When a period is stated in Years or Weeks, they would be converted to Months and Days respectively and then located on the PI table.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Interest Period")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=32.0, core=true, t24Name=PERIODIC.PERIOD, mandatory=false)
/* This field provides the user to specify which period will be used to fetch interest rate from the Periodic Interest table.
Allowed Values:
Maturity – Interest rate from periodic interest table will be fetched based on the maturity term of the contract.
Renewal – Interest rate from periodic interest table will be fetched based on the renewal term of the contract.
Reset Period - Interest rate from periodic interest table will be fetched based on the reset frequency period (i.e. based on the period to next interest reset date). If a Period is defined in 'Periodic Period' field, the same will be used for fetching the interest rate from Periodic Interest table except for the first and last period.
Periodic Period and Null - Interest rate from periodic interest table will be fetched based on the period defined in 'periodic period' field. */
        PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=30.0, core=true, t24Name=PERIODIC.PERIOD.TYPE, mandatory=false)
/* Indicates the rate on which the index runs on the date stated in the key. When a periodic index is used and the field is left blank, then system computes the rate from PERIODIC.INTEREST and defaults it here. When a rate is already present(say, the user changes the system computed rate), then system does not compute the new rate.

Validation rules:

System defaulted and modifiable.
Standard Rate field.
Valid only if Periodic index is stated. */
        PERIODIC_RATE : T24BusinessTypes:R
            @i18n:Label (en="Periodic Interest Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=31.0, core=true, t24Name=PERIODIC.RATE, typeModifiers=-, mandatory=false)
/* This indicates the reset period of the index stated. System would automatically reset the PERIODIC.RATE at the frequency stated in this field. Applicable only at Arrangement level. Periodic index may still be used without this field meaning, it never resets the computed rate

Validation rule:

Valid PERIOD field. */
        PERIODIC_RESET : T24BusinessTypes:FQU
            @i18n:Label (en="Interest Reset")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=35.0, core=true, t24Name=PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
/* The tier amount relates to the field RATE.TIER.TYPE and amounts can be entered if either BAND or LEVEL are selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Tier Amount For Interest Calculation")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=TIER.AMOUNT, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MAX_RATE : T24BusinessTypes:R
            @i18n:Label (en="Maximum Interest Rate ")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=43.0, core=true, t24Name=TIER.MAX.RATE, typeModifiers=-, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Minimum Interest Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=TIER.MIN.RATE, typeModifiers=-, mandatory=false)
/* Originally negative rate was applicable for all of the tiers together, but through this field it is now possible to define negative rate per tier.

Different values can be set in each tier and the options available are YES, BLOCK.MARGIN and NO/Blank.

Blank or NO - Indicates that negative rates cannot be input and if a rate becomes negative as a result of the application of a margin the rate will be set to zero

YES - Indicates that negative rates can be input and if the application margin makes the rate more negative then the more negative rate will be used.

BLOCK.MARGIN - Indicates that negative rates can be used but the following conditions apply

- If the rate is positive and the margin makes the rate negative then the rate will be set to zero

- If the rate is negative and the margin makes the rate more negative then the margin will be ignored and the original negative rate is used. */
        TIER_NEGATIVE_RATE : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=44.0, core=true, t24Name=TIER.NEGATIVE.RATE, mandatory=false)
/* The tier percent relates to the field RATE.TIER.TYPE and percentages can be entered if BAND is selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. Either TIER.PERCENT or TIER.AMOUNT can be entered by not both. If TIER.PERCENT is defined, then the Sum of Tier percentage should be 100% */
        TIER_PERCENT : T24BusinessTypes:N
            @i18n:Label (en="Tier Interest Percentage")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=47.0, core=true, t24Name=TIER.PERCENT, mandatory=false)
        USAGE_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=USAGE.PERCENT, mandatory=false)
    }
    AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE
    {
/* The options are ADD or SUB. This indicates whether the MARGIN.RATE should be added or subtracted from the Floating or Periodic Rate.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.RATE and input is only allowed if a rate is entered in MARGIN.RATE. */
        MARGIN_OPER : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER
            @i18n:Label (en="Margin Operand")
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=40.0, core=true, t24Name=MARGIN.OPER, mandatory=false)
/* A margin rate can be used to adjust the specified rate of interest and to appropriately reflect any rate profit realized.  The result is the nominal rate of interest.  T24 allows for the following margin configuration:
Single or Multiple Margins (Field MARGIN.TYPE)
Rate Adjustment Method (Field MARGIN.OPER)

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.OPER. */
        MARGIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Margin Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=41.0, core=true, t24Name=MARGIN.RATE, mandatory=false)
/* Identifies whether there are single or multiple margins

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.OPER and MARGIN.RATE. */
        MARGIN_TYPE : AA_Interest:AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE
            @i18n:Label (en="Margin Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=39.0, core=true, t24Name=MARGIN.TYPE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=143.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_PRD_DES_INTEREST__NEGOTIATED_FLDS
    {
/* No input and multi value field that lists the attributes on the arrangement property record that have been fixed and/or negotiated. These are stored when the arrangement level record is committed. When the arrangement level record is next used all items in the NEGOTIATED.FIELDS list will be maintained.
Once an attribute has been fixed or negotiated it will remain in negotiated field list on all subsequent dated versions of the property. */
        NEGOTIATED_FLDS : T24BusinessTypes:A
            @i18n:Label (en="Negotiated Attributes On Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=130.0, core=true, t24Name=NEGOTIATED.FLDS, mandatory=false)
    }
    AA_PRD_DES_INTEREST__NR_ATTRIBUTE
    {
/* Defines which individual attribute (field) has the negotiation rules defined in fields "Nr.Negotiable" through to "Nr.Message"


Must be a valid field name within this property
This field is part of a multi value set */
        NR_ATTRIBUTE : T24BusinessTypes:A
            @i18n:Label (en="Negotiable Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=121.0, core=true, t24Name=NR.ATTRIBUTE, mandatory=false)
        NR_ATTRIBUTE_RULE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=123.0, core=true, t24Name=NR.ATTRIBUTE.RULE, mandatory=false)
        NR_OPTIONS -> byValue AA_Interest:AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_OPTIONS *
            @t24:i (t24Name=NR.OPTIONS)
/* It is possible to define negotiation rules in EB.COMPARISON.TYPE.



Input must be a valid record from EB.STANDARD.COMPARISON */
        NR_STD_COMP -> AA_Rules:EB_STANDARD_COMPARISON
            @i18n:Label (en="Negotiable Standard Comparison")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=125.0, core=true, businessType=A, t24Name=NR.STD.COMP, mandatory=false)
        NR_TYPE -> byValue AA_Interest:AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_TYPE *
            @t24:i (t24Name=NR.TYPE)
        NR_VALUE_SOURCE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=124.0, core=true, t24Name=NR.VALUE.SOURCE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_OPTIONS
    {
/* Defines named values to indicate if an option applies to the associated NR.ATTRIBUTE. More than one option can be associated with the NR.ATTRIBUTE, with certain restrictions.


Valid Inputs are NEGOTIABLE, NON-NEGOTIABLE, OVERRIDE, FIX-VALUE, MANDATORY as described below: -

NEGOTIABLE
The value of NEGOTIABLE will indicate that the associated NR.ATTRIBUTE can be negotiated at the arrangement level.

NON-NEGOTIABLE
The value of NON-NEGOTIABLE will indicate that the associated NR.ATTRIBUTE cannot be negotiated at the arrangement level. Both NEGOTIABLE and NON-NEGOTIABLE cannot be defined together

OVERRIDE
Specifies if a default override message should be generated if the default attribute is amended at the arrangement level. If OVERRIDE is not specified, no override will be generated.

FIX-VALUE
Specified if the field value is to be fixed at the arrangement level. If FIX-VALUE is not specified the field value will vary with the changes to the underlying product condition.

MANDATORY
Specifies if the attribute is mandatory for this product condition. Can only be specified where the attribute is not defined as mandatory in the property class. */
        NR_OPTIONS : AA_Interest:AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS
            @i18n:Label (en="Negotiable Options")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=122.0, core=true, t24Name=NR.OPTIONS, mandatory=false)
    }
    AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_TYPE
    {
/* This field indicates whether to raise error or override message when rule is broken
Allows only two values ERROR or OVERRIDE
ERROR error message is raised
OVERRIDE Override message is generated */
        NR_MESSAGE : AA_Interest:AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE
            @i18n:Label (en="Negotiable Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=128.0, core=true, t24Name=NR.MESSAGE, mandatory=false)
/* Defines the negotiation rule that is linked to the field defined in "Nr.Attribute". If an attribute has a negotiation rule linked to it (in associated fields NR.TYPE, NR.VALUE and NR.MESSAGE) the attribute must be defined as negotiable.
It is possible to define more than one negotiation rule for an attribute


Input must be a valid record from EB.COMPARISON.TYPE
This is a multi value set of fields */
        NR_TYPE -> AA_Rules:EB_COMPARISON_TYPE
            @i18n:Label (en="Negotiable Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=126.0, core=true, businessType=A, t24Name=NR.TYPE, mandatory=false)
        /* Defines the value that links to the rule in field "Nr.Type" */
        NR_VALUE : T24BusinessTypes:ANY
            @i18n:Label (en="Negotiable Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=127.0, core=true, t24Name=NR.VALUE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__OFFSET_DETAILS
    {
        OFFSET_DETAILS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=101.4, core=false, t24Name=OFFSET.DETAILS, mandatory=false)
    }
    AA_PRD_DES_INTEREST__ON_ACTIVITY
    {
/* When the periodic interest rate needs to be recalculated for certain activities, this field in combination with RECALCULATE may be used to setup the activity for which interest needs to be recalculated
Validation Rules
1. Applicable only for Periodic interest rate recalculation
2. Must be a valid activity in AA.ACTIVITY */
        ON_ACTIVITY -> AA_ProductFramework:AA_ACTIVITY
            @i18n:Label (en="Activity On Which The Condition To Be Triggered")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=48.0, core=true, businessType=A, t24Name=ON.ACTIVITY, mandatory=false)
/* Related to ON.ACTIVITY activity, when flagged to RATE, system will recalculate Periodic rate when the activity setup in ON.ACTIVITY is processed. When the value is NOTHING periodic rate would not be recalculated when the activity setup in ON.ACTIVITY is processed
Validation Rules
1. Mandatory when ON.ACTIVITY is setup

The fields ON.ACTIVITY and RECALCULATE in interest property class will be enhanced to allow user to define when to recalculate ‘Profit Amount’.
All activities which could result in payment schedule recalculation can be configured to recalculate ‘Profit Amount’ i.e., activities with payment schedule calculate and update action */
        RECALCULATE : AA_Interest:AA_PRD_DES_INTEREST__ON_ACTIVITY__RECALCULATE
            @i18n:Label (en=Recalculate)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=RECALCULATE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__OVERRIDE
    {
        /* Standard Override field containing override messages generated by amendments to this property */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=140.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__OWNING_COMPANY
    {
/* This field is used to capture the T24 Company(ies) that own the Product Condition record. There is no functionality attached to this field and can be used only for access control through SMS verification.

The input in this field must be a valid record in company table and should belong to current financial group.

This is a multi value field. Input in this field is optional. */
        OWNING_COMPANY -> ST_CompanyCreation:COMPANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=114.0, core=true, businessType=AAPRODUCT, t24Name=OWNING.COMPANY, typeModifiers=COMPANY, mandatory=false)
    }
    AA_PRD_DES_INTEREST__PRICING_RULES_BENEFIT
    {
        PRICING_RULES_BENEFIT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=PRICING.RULES.BENEFIT, mandatory=false)
        PRICING_RULES_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=93.0, core=true, t24Name=PRICING.RULES.FIXED.RATE, mandatory=false)
        PRICING_RULES_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=92.0, core=true, t24Name=PRICING.RULES.MARGIN, mandatory=false)
        PRICING_RULES_OPERAND : AA_Interest:AA_PRD_DES_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=91.0, core=true, t24Name=PRICING.RULES.OPERAND, mandatory=false)
    }
    AA_PRD_DES_INTEREST__PR_ATTRIBUTE
    {
        /* This will hold two values either DUE or CAPITALISE.This Field denote which application method(DUE or CAPITALISE)should be taken while applying periodic rule charges. */
        PR_APP_METHOD : AA_Interest:AA_PRD_DES_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD
            @i18n:Label (en="Periodic Attribute Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=110.0, core=true, t24Name=PR.APP.METHOD, mandatory=false)
        /* This will have to accept a valid T24 period.This Field will denote the application period for current aplication method. */
        PR_APP_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Attribute Period")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=111.0, core=true, t24Name=PR.APP.PERIOD, mandatory=false)
/* These are set of Attributes which define the names of the rule, which will be applied on certain attributes of property classes over a period of time.


Input must be a valid record from AA.PERIODIC.ATTRIBUTE */
        PR_ATTRIBUTE -> AA_Rules:AA_PERIODIC_ATTRIBUTE
            @i18n:Label (en="Periodic Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=102.0, core=true, businessType=A, t24Name=PR.ATTRIBUTE, mandatory=false)
/* Charge can be collected when the periodic rule is broken
This field denotes the charge property. The property and its condition is
defined the product designer and validated during publishing


Valid charge property */
        PR_BRK_CHARGE -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Attribute Break Charge")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=106.0, core=true, businessType=A, t24Name=PR.BRK.CHARGE, mandatory=false)
/* Based on the PR.BRK.RES option selected, this will hold either an OVERRIDE or EB.ERROR id. By default the ids will be picked from the associated EB.COMPARISON.TYPE record. If CAP or FLOOR values are given, then the underlying AA.PERIODIC.ATTRIBUTE.CLASS should have this type defined in it.


Input must be a valid OVERRIDE ID or EB.ERROR ID. */
        PR_BRK_MSG : T24BusinessTypes:ANY
            @i18n:Label (en="Attribute Break Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=105.0, core=true, t24Name=PR.BRK.MSG, mandatory=false)
/* When a rule is broken what action needs to be taken is defined here. Currently, the various options available are

Error block further processing
Override Give an override
Information  Record details only
CAP the rule value is to be used as a CAP if the rule is broken
FLOOR the rule value is to be used as a FLOOR if the rule is broken

Validation ules
Should be one of the above valid options */
        PR_BRK_RES : AA_Interest:AA_PRD_DES_INTEREST__PR_ATTRIBUTE__PR_BRK_RES
            @i18n:Label (en="Action On Rule Break")
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=104.0, core=true, t24Name=PR.BRK.RES, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=PR.RESERVED.1, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=PR.RESERVED.2, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=107.0, core=true, t24Name=PR.RESERVED.3, mandatory=false)
/* This is the actual rule value, which will be applied on certain attributes of the property classes over a period of time.


Input must be a valid data type defined in the corresponding
AA.PERIODIC.ATTRIBUTE.CLASS record. */
        PR_VALUE : T24BusinessTypes:A
            @i18n:Label (en="Attribute Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=103.0, core=true, t24Name=PR.VALUE, mandatory=false)
    }
    AA_PRD_DES_INTEREST__STMT_NOS
    {
        /* System field - not used for property conditions */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=139.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    AA_PRD_DES_INTEREST__USER_MARGIN_TYPE
    {
        USER_MARGIN_OPER : AA_Interest:AA_PRD_DES_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=24.0, core=true, t24Name=USER.MARGIN.OPER, mandatory=false)
        USER_MARGIN_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=USER.MARGIN.RATE, mandatory=false)
        USER_MARGIN_TYPE : AA_Interest:AA_PRD_DES_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=23.0, core=true, t24Name=USER.MARGIN.TYPE, mandatory=false)
    }
/* The INTEREST Property Class is used for all interest definition and processing in AA.  A T24 product defined and processed in AA can have multiple interest properties defined (e.g. principal interest, penalty interest, commission, etc.).  The number of interest properties is determined by the users defining the products.
The different condition ids of interest property can be attached to the different types of interest properties at product level */
    AA_PRD_PRF_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.PRD.PRF.INTEREST, nonStop=true, level=I, type=W, allowedFunctions="L Q S", localRefApplicationSysNo=101.0, localRefAllowed=true)
    {
/* Specifies how the accounting entries will be raised. Whether it needs to work like discounted or fixed type of interest.
Advance: This is nothing but discounted interest, interest amount will be collected inadvance before the interest periods.
Upfront Profit: Interest amount will be collected at the beginning of the contract.
None: Denotes conventional interest bearing contract. */
        ACCOUNTING_MODE : AA_Interest:AA_PRD_PRF_INTEREST__ACCOUNTING_MODE
            @i18n:Label (en="Accounting Mode")
            @t24:i (align=L, inputBehaviour=F, maxLength=15, sysNumber=60.0, core=true, t24Name=ACCOUNTING.MODE, mandatory=false)
/* When accruing interest, T24 must determine which amounts to include in each day’s balance, the actual days to include in the period (e.g. first day, last day) and how to round any calculations.
T24 allows users to define the calculation method to use, by specifying an accrual rule in EB.ACCRUAL.PARAM.  Users can define the following:
• First Day Inclusive (Yes/No)
• Last Day Inclusive (Yes/No)
• Rounding Rule (Natural, Down, Up, Currency, None)
• Intermediate Calculation Rounding (Yes/No)
• Credit value date adjustment
• Debit value date adjustment */
        ACCRUAL_RULE -> AC_Fees:EB_ACCRUAL_PARAM
            @i18n:Label (en="Accrual Rule")
            @i18n:Tooltip (en="Calculation method in EB.ACCRUAL.PARAM")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=ACCRUAL.RULE, mandatory=false)
/* User can defined Total interest / profit amount for the contract.
Mandatory input if RATE.TYPE is ‘Flat Amount’.
Value in CALC.PROFIT.AMT will be populated in this field which the user can override. */
        ACTUAL_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=69.0, core=true, t24Name=ACTUAL.PROFIT.AMT, mandatory=false)
        ADJUST_EXPIRY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=82.0, core=true, t24Name=ADJUST.EXPIRY.DATE, mandatory=false)
        ADJUST_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=79.0, core=true, t24Name=ADJUST.MARGIN, mandatory=false)
        ADJUST_OPERAND : AA_Interest:AA_PRD_PRF_INTEREST__ADJUST_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=ADJUST.OPERAND, mandatory=false)
        ADJUST_OPTION : AA_Interest:AA_PRD_PRF_INTEREST__ADJUST_OPTION
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.2, core=false, t24Name=ADJUST.OPTION, mandatory=false)
        ADJUST_OVERRIDE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=80.0, core=true, t24Name=ADJUST.OVERRIDE.RATE, mandatory=false)
        ADJUST_REASON : AA_Interest:AA_PRD_PRF_INTEREST__ADJUST_REASON
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=81.0, core=true, t24Name=ADJUST.REASON, mandatory=false)
        ADJUST_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__ADJUST_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=77.0, core=true, t24Name=ADJUST.TYPE, mandatory=false)
        API_ATTRIBUTE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__API_ATTRIBUTE *
            @t24:i (t24Name=API.ATTRIBUTE)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=148.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=149.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=145.0, core=true, t24Name=AUTHORISER, mandatory=false)
        /* The user can specify that interest will only be calculated if a balance threshold is surpassed. For credit interest, the user can specify the minimum balance which must be maintained. For debit interest, the user can specify the maximum debit balance for which interest will not be calculated. */
        CALC_THRESHOLD : T24BusinessTypes:AMT
            @i18n:Label (en="Base Amount For Interest Calculation")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=CALC.THRESHOLD, typeModifiers=-, mandatory=false)
        CHANGED_FIELDS -> byValue AA_Interest:AA_PRD_PRF_INTEREST__CHANGED_FIELDS *
            @t24:i (t24Name=CHANGED.FIELDS)
/* T24 supports the compounding of both debit and credit interest.

Compounding interest applies a factor to the compound interest calculation based upon the  number of compounding periods in the year. This field defines the number of compounding periods to be applied.

Note that the compounding type provides a factor in the calculation only  and does not actually cause the base principal amount to be actually increased by the calculated interest amount.

The following compound frequencies are supported:

DAILY
– Interest is compounded 360/5/6 times a year (from days in year in INTEREST.BASIS)

WEEKn
– Interest is compounded 52/n times per year based on a number of WEEKS
-- Bi-weekly compounding should be defined as WEEK2 and will result in 26 periods
-- Four-Weekly compounding should be defined as WEEK4 and results in 13 periods

Mnn
– Interest is compounded 12/n times a year based on a numberof months
--  Monthly compounding should be defined as M01 and will result in 12 periods
--  Bi-Monthly should be defined as M02 and will result in 6 periods
--  Quarterly should be defined as M03 and will result in 4 periods
--  Semi-Annual should be defined as M06 and will result in 6 periods
--  Annual should be defined as M12 and will result in 1 period
--  Bi-Annual should be defined as M24 and will result in 0.5 periods
--  Any number of months up to 99 may be defined and results in 12/n periods

TWMTH
– Interest is compounded twice a month resulting in 24 periods as opposed to WEEK2
which results in 26 periods

Nnnn
- Compounding takes place nnn times per year. */
        COMPOUND_TYPE : T24BusinessTypes:COMPFQU
            @i18n:Label (en=Compounding)
            @i18n:Tooltip (en="It supports both debit and credit interest")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=COMPOUND.TYPE, mandatory=false)
/* This field indicates how the effective rate is calculated, when set to YIELD the interest rate defined is taken as the annual effective rate. System uses the below formula to calculate the interest amount,

Daily Effective Rate(ERd) = (1+Interest Rate)^(1/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}

When the field is set as NULL the effective rate is calculated based on the compounding frequency defined in the field COMPOUND.TYPE using the below formula,

Daily Effective Rate(ERd) = (1+Interest Rate/Compound Freq)^(Compound Freq/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1} */
        COMPOUND_YIELD_METHOD : AA_Interest:AA_PRD_PRF_INTEREST__COMPOUND_YIELD_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=50.0, core=true, t24Name=COMPOUND.YIELD.METHOD, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=146.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=142.0, core=true, t24Name=CURR.NO, mandatory=false)
        /* If there is a Cutom rate tier then this field is mandatory. It is a AA.CUSTOM.RATE.TYPE record that determines how the custom rate should be calculated */
        CUSTOM_RATE_CALC -> AA_Interest:AA_CUSTOM_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=72.0, core=true, businessType=S, t24Name=CUSTOM.RATE.CALC, mandatory=false)
        CUSTOM_TYPE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__CUSTOM_TYPE *
            @t24:i (t24Name=CUSTOM.TYPE)
/* Specifies the date convention to be followed for the reset frequency i.e. if the cycled Periodic Reset date falls on a non-working day, the reset date will be moved based on the option chosen.
Allowed Values:
Arrangement / Null – Based on the date convention specified in the Account Property Class.
Periodic Interest – Based on the convention used by the index i.e. the Periodic Interest record.
o Forward Same Month convention for Monthly periods and Forward convention for all other periods if USE.LAST.WORKING.DAY is set to 'Y'.
o Forward Convention for all periods if USE.LAST.WORKING.DAY is not set. */
        DATE_CONVENTION : AA_Interest:AA_PRD_PRF_INTEREST__DATE_CONVENTION
            @i18n:Label (en=Convention)
            @t24:i (align=L, inputBehaviour=I, maxLength=18, sysNumber=61.0, core=true, t24Name=DATE.CONVENTION, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_PRD_PRF_INTEREST__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* T24 can calculate interest based upon several different day basis types.  Each T24 Interest Day Basis type is represented as a numerator and denominator.
• Numerator (interest days)
o 360 – Each month is considered to have 30 days
o 366 – The exact number of days will be used
• Denominator (days in the year)
o 360 – Each year is considered to have 360 days
o 365 – Each year is considered to have 365 days
o 366 – The exact number of days in the year will be used (365 or 366)

T24 allows the following interest day basis types:
Type        Notes
A (360/360)     A1 and A2 are variations which alter the method of treating a month as 30 days.
B (366/360)
C (366/366)
D (360/366)
E (366/365)
F (360/365)     F1 and F2 are variations which alter the method of treating a month as 30 days.
S (special)      Allows for actual interest amounts to be entered by the user.  The amounts in the     schedule must be within a tolerance of 5% when compared with the product of amount * rate * period.

This is a Mandatory field. */
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Day Basis")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, businessType=A, t24Name=DAY.BASIS, mandatory=false)
/* Optional field - Allowed Values are RESETTING and NON-RESETTING.

RESETTING - During any Renewal Activities (for eg : change.product or reset) the property conditions will be reset from the product.

NON-RESETTING - During any Renewal Activities property conditions will be maintained from the Arrangement level.

Leaving the field blank sets NON-RESETTING as the default option. */
        DEFAULT_ATTR_OPTION : AA_Interest:AA_PRD_PRF_INTEREST__DEFAULT_ATTR_OPTION
            @i18n:Label (en="Default Attribute Option")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=119.0, core=true, t24Name=DEFAULT.ATTR.OPTION, mandatory=false)
/* Defines whether all attributes (fields) can be negotiable or not.


This field is Mandatory
Valid options are YES and NO */
        DEFAULT_NEGOTIABLE : AA_Interest:AA_PRD_PRF_INTEREST__DEFAULT_NEGOTIABLE required
            @i18n:Label (en="Default Negotiable")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=120.0, core=true, t24Name=DEFAULT.NEGOTIABLE, mandatory=true)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=147.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue AA_Interest:AA_PRD_PRF_INTEREST__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        FIXED_RATE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE *
            @t24:i (t24Name=FIXED.RATE)
        FT_TXN_TYPE -> FT_Config:FT_TXN_TYPE_CONDITION
            @i18n:Label (en="Transaction Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=101.1, core=false, businessType=A, t24Name=FT.TXN.TYPE, mandatory=false)
        FULL_DESCRIPTION : T24BusinessTypes:TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=2.0, core=true, t24Name=FULL.DESCRIPTION, mandatory=false)
/* Contains the arrangement number, extracted from the first component of the id
Not applicable at product level
System updated field no input allowed */
        ID_COMP_1 : T24BusinessTypes:A
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=131.0, core=true, t24Name=ID.COMP.1, mandatory=false)
/* The action that was triggered on the arrangement property
The action is taken from ACTIVITY.CLASS record of the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified. */
        ID_COMP_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=132.0, core=true, t24Name=ID.COMP.2, mandatory=false)
/* Contains the effective date of the property condition
Not applicable at product level
System updated field no input allowed */
        ID_COMP_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=30, sysNumber=133.0, core=true, t24Name=ID.COMP.3, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=134.0, core=true, t24Name=ID.COMP.4, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_5 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=135.0, core=true, t24Name=ID.COMP.5, mandatory=false)
/* Reserved for future use
Not applicable at product level
System field no input allowed */
        ID_COMP_6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=136.0, core=true, t24Name=ID.COMP.6, mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_PRD_PRF_INTEREST__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* Specifies whether interest method is fixed life of the contract or not.
FIXED – Fix the total profit amount regardless of any change to loan parameters. This field is specify whether total interest amount need to fixed or not.
Null – Will be treated as a normal interest bearing property. */
        INTEREST_METHOD : AA_Interest:AA_PRD_PRF_INTEREST__INTEREST_METHOD
            @t24:i (align=L, inputBehaviour=F, maxLength=6, sysNumber=66.0, core=true, t24Name=INTEREST.METHOD, mandatory=false)
/* Income / expense will be posted to internal account category specified in Accounting condition instead of PL categories.
This will accept YES or NULL */
        INTERNAL_BOOKING : AA_Interest:AA_PRD_PRF_INTEREST__INTERNAL_BOOKING
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, t24Name=INTERNAL.BOOKING, mandatory=false)
        /* Where at least one tier is set to be ‘Linked Rate’, the Arrangement to be linked for calculating the Interest Rate, must be specified here (or automatically defaulted by the system, where a Limit is attached) */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* This field will be used to specify the Interest Property of the Linked Arrangement that should be referred to, for getting the Interest Rate.



Where at least one tier is set to be ‘Linked Rate’, this must be specified. */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=64.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
/* This field indicates the minimum interest amount to be posted for an arrangement/account.
If the calculated interest amount is less than the amount specified in this field,
system will waive the interest amount or adjust the interest amount based on the option set in the field MIN.INT.WAIVE */
        MIN_INT_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Minimum Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=MIN.INT.AMOUNT, typeModifiers=-, mandatory=false)
/* This field indicates if the calculated interest amount has to be waived or adjusted.
If the field is set to YES, then the system will waive the interest amount if it is less than the MIN.INT.AMOUNT specified.
If NULL value is set and if the calculated interest amount is less than the MIN.INT.AMOUNT specified, then system will post the minimum interest amount. */
        MIN_INT_WAIVE : AA_Interest:AA_PRD_PRF_INTEREST__MIN_INT_WAIVE
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=53.0, core=true, t24Name=MIN.INT.WAIVE, mandatory=false)
/* Negative rate functionality is moved to TIER.NEGATIVE.RATE field, please refer to TIER.NEGATIVE.RATE for help text.
This field is system maintained field, where in if all of the tier negative rate has the same value, then this field will be updated with that value. */
        NEGATIVE_RATE : AA_Interest:AA_PRD_PRF_INTEREST__NEGATIVE_RATE
            @i18n:Label (en="Negative Rate")
            @t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=7.0, core=true, t24Name=NEGATIVE.RATE, mandatory=false)
        NEGOTIATED_FLDS -> byValue AA_Interest:AA_PRD_PRF_INTEREST__NEGOTIATED_FLDS *
            @t24:i (t24Name=NEGOTIATED.FLDS)
        NR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__NR_ATTRIBUTE *
            @t24:i (t24Name=NR.ATTRIBUTE)
        OFFSET_DETAILS -> byValue AA_Interest:AA_PRD_PRF_INTEREST__OFFSET_DETAILS *
            @t24:i (t24Name=OFFSET.DETAILS)
        ON_ACTIVITY -> byValue AA_Interest:AA_PRD_PRF_INTEREST__ON_ACTIVITY *
            @t24:i (t24Name=ON.ACTIVITY)
/* This field is used to define the profit amount realised in the Legacy system. Assuming the total profit amount for the contract is 10,000  and remaining profit amount to be realized during migration is 3,000, one will give the difference (7,000) in this attribute. And 3,000 will be input in the Actual Profit Amount that has to be realized in T24. If someone wants the total profit amount, they can simply add these 2 to get them. Once authorised, the value cannot be changed.
Validation:
• Applicable for Fixed Interest Method
• Input enabled during migration of the contract(through take-over activity) */
        ORIG_PROFIT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=30, sysNumber=70.0, core=true, t24Name=ORIG.PROFIT.AMOUNT, mandatory=false)
        OVERRIDE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        OWNING_COMPANY -> byValue AA_Interest:AA_PRD_PRF_INTEREST__OWNING_COMPANY *
            @t24:i (t24Name=OWNING.COMPANY)
        PACKAGE_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=PACKAGE.PRODUCT, mandatory=false)
        PACKAGE_PROPERTY_CONTROL : AA_Interest:AA_PRD_PRF_INTEREST__PACKAGE_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=100.0, core=true, t24Name=PACKAGE.PROPERTY.CONTROL, mandatory=false)
        PRICING_RULES_BENEFIT -> byValue AA_Interest:AA_PRD_PRF_INTEREST__PRICING_RULES_BENEFIT *
            @t24:i (t24Name=PRICING.RULES.BENEFIT)
/* This field represents the key to the product level conditions of a property class
This field basically has three components seperated by a hyphen
Condition id
Currency - if the property class is currency based. By default takes up local currency
Effective date - if the property class is dated */
        PRODUCT_KEY -> AA_Interest:AA_PRD_PRF_INTEREST PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=0.0, core=true, businessType=ANY, t24Name=PRODUCT.KEY, mandatory=true)
        PROGRAM_LIMIT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=94.0, core=true, t24Name=PROGRAM.LIMIT, mandatory=false)
        PR_ATTRIBUTE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__PR_ATTRIBUTE *
            @t24:i (t24Name=PR.ATTRIBUTE)
        RATE_LOCK_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=101.3, core=false, t24Name=RATE.LOCK.ID, mandatory=false)
/* T24 allows for the definition of tiers of interest rates.  Each tier is specified by defining the amount up to which the interest rate applies.  Additionally, each tier can be of a different interest rate type (i.e. fixed, floating, or periodic).  There are three type of tiers; Single, Level, and Banded.
Single Rate
When a single rate tier type is specified a single nominal interest rate will apply for the entire balance amount.
Rate Levels
By specifying level tiers a product can be defined which will calculate interest at different rates depending on the balance amount.  A single rate of interest will be selected and applied based upon the balance amount.  For example for a deposit a single product can have a higher rate of interest for accounts which maintain higher credit balances.  Conversely for a loan product a higher rate of interest can be charged for accounts with higher debit balances.
For example, aassuming a credit balance of 15,000 and level interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  A single rate of 15 per cent will be applied to the entire amount of 15,000.
Banded Rates
Banded tier interest will typically result in a "blended" interest rate.   This is similar to Level tiers, but allows for the interest rate of each tier to be applied to the portion of the balance that falls within the tier.
For example, assuming a credit balance of 15,000 and banded interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  Two calculations will be performed (i.e. 10 per cent on 10,000 and 15 per cent on 5,000)

This field is Mandatory. */
        RATE_TIER_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__RATE_TIER_TYPE
            @i18n:Label (en=Translate)
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=8.0, core=true, t24Name=RATE.TIER.TYPE, mandatory=false)
/* Specifies whether Interest definition is based on a rate or amount and in case of rate what type of rate.
Reducing Rate: Rate defined in Interest rate related fields will be considered as Reducing or Diminishing Rate for Interest calculation.
Flat Rate: Rate defined in interest rate related fields will be considered as a Flat Rate.
Flat Amount: Amount defined in the ACTUAL.PROFIT.AMT will be considered as the total interest amount for the interest property.
This will be allowed only when INTEREST.METHOD is FIEXED */
        RATE_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__RATE_TYPE
            @i18n:Label (en="Rate Type")
            @i18n:Tooltip (en="Is the Rate quoted net of tax or gross of tax")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=67.0, core=true, t24Name=RATE.TYPE, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=141.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* For an arrangement under ACCOUNTS Product Line, it is possible link the first tier amount of debit interest to the LIMIT record attached.
It can be achieved by setting the field REFER.LIMIT in INTEREST property condition as ‘YES’. This functionality can be enabled only if the LIMIT is managed by AA. */
        REFER_LIMIT : AA_Interest:AA_PRD_PRF_INTEREST__REFER_LIMIT
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=51.0, core=true, t24Name=REFER.LIMIT, mandatory=false)
        REGIONAL_PRICING_CODE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=84.0, core=true, t24Name=REGIONAL.PRICING.CODE, mandatory=false)
        REGIONAL_PRICING_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=87.0, core=true, t24Name=REGIONAL.PRICING.FIXED.RATE, mandatory=false)
        REGIONAL_PRICING_LEVEL : AA_Interest:AA_PRD_PRF_INTEREST__REGIONAL_PRICING_LEVEL
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=83.0, core=true, t24Name=REGIONAL.PRICING.LEVEL, mandatory=false)
        REGIONAL_PRICING_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=86.0, core=true, t24Name=REGIONAL.PRICING.MARGIN, mandatory=false)
        REGIONAL_PRICING_OPERAND : AA_Interest:AA_PRD_PRF_INTEREST__REGIONAL_PRICING_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=85.0, core=true, t24Name=REGIONAL.PRICING.OPERAND, mandatory=false)
        REGIONAL_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=88.0, core=true, businessType=A, t24Name=REGIONAL.PRODUCT, mandatory=false)
        REGIONAL_PROPERTY_CONTROL : AA_Interest:AA_PRD_PRF_INTEREST__REGIONAL_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=89.0, core=true, t24Name=REGIONAL.PROPERTY.CONTROL, mandatory=false)
        RELATIONSHIP_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=59.0, core=true, t24Name=RELATIONSHIP.FIXED.RATE, mandatory=false)
        /* Rate of discount margin as a result of the relationship of the customer with the bank. Would be evaluated based on the RELATIONSHIP arrangements held by the customer and also the definitions in PREFERENTIAL.PRICING as well as the rules in PRICING.RULES of this arrangement product. Validation: System maintained field. Would be a percentage would be added or subtracted to the original rate. The effective rate would be influenced by the content in this field. */
        RELATIONSHIP_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=57.0, core=true, t24Name=RELATIONSHIP.MARGIN, mandatory=false)
        /* Indicates whether the margin stated gets added or subtracted or multiplied to the originally evaluated rate. Validation: System maintained field. Would be either ADD, SUBTRACT or MULTIPLY */
        RELATIONSHIP_OPERAND : AA_Interest:AA_PRD_PRF_INTEREST__RELATIONSHIP_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=56.0, core=true, t24Name=RELATIONSHIP.OPERAND, mandatory=false)
        /* Field to indicate the relationship pricing plan(if any) that has influenced the rate on this arrangement. If a customer has a relationship with the bank which entitles for special margins, then the plan which has contributed to this margin would be updated here. Validation: System maintained field. Would be a valid RELATIONSHIP.PRICING product for this custome */
        RELATIONSHIP_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=55.0, core=true, businessType=A, t24Name=RELATIONSHIP.PRODUCT, mandatory=false)
/* This field holds the variation selected for the arrangement for the interest pricing condition

Validation Rules

System maintained, NOINPUT allowed */
        RELATIONSHIP_VARIATION : AA_Interest:AA_PRD_PRF_INTEREST__RELATIONSHIP_VARIATION
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=RELATIONSHIP.VARIATION, mandatory=false)
        RESERVED1 : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=65.0, core=true, t24Name=RESERVED1, mandatory=false)
        RESERVED10 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=96.0, core=true, t24Name=RESERVED10, mandatory=false)
        RESERVED2_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=137.0, core=true, t24Name=RESERVED2.ID, mandatory=false)
        RESERVED4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=62.0, core=true, t24Name=RESERVED4, mandatory=false)
        RESERVED6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=68.0, core=true, t24Name=RESERVED6, mandatory=false)
        RESERVED7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=99.0, core=true, t24Name=RESERVED7, mandatory=false)
        RESERVED8 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=98.0, core=true, t24Name=RESERVED8, mandatory=false)
        RESERVED9 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=97.0, core=true, t24Name=RESERVED9, mandatory=false)
        /* If this field gets checked then rate would not be stored in the EFFECTIVE.RATE field and whenever needed System will calculate it at runtime */
        RUNTIME_RATE_CALC : AA_Interest:AA_PRD_PRF_INTEREST__RUNTIME_RATE_CALC
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=73.0, core=true, t24Name=RUNTIME.RATE.CALC, mandatory=false)
        STMT_NOS -> byValue AA_Interest:AA_PRD_PRF_INTEREST__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Field accepts the options 'YES' or 'ALTERNATE'.
Indicates whether accruals needs to be Suppressed or Accrual should be done for the Alternate Interest Property.

For Example:
It will be set as YES by the system if the DONOR.ACCRUAL field is set as SUPPRESS for any product / arrangement in INTEREST.COMPENSTION property of a Bundle arrangement.

If it is set as 'ALTERNATE' for a property then the Accrual will be suppressed for the property for which it is set and Accrual will happen for the Alternate property which is set in Activity Restriction.
Blank value in this field denotes that there is no suppression of accrual

This is a Non Mandatory field. */
        SUPPRESS_ACCRUAL : AA_Interest:AA_PRD_PRF_INTEREST__SUPPRESS_ACCRUAL
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=54.0, core=true, t24Name=SUPPRESS.ACCRUAL, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=SYS.RESERVE1, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=SYS.RESERVE2, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=SYS.RESERVE3, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=SYS.RESERVE6, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=SYS.RESERVE7, mandatory=false)
/* This field denotes the target classic product of the given property class

The already existing T24 products are mapped using AA infrastructure
These are called classic products
These products are mapped to different property clases */
        TARGET_PRODUCT : T24BusinessTypes:A
            @i18n:Label (en="Target Classic Product")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=138.0, core=true, t24Name=TARGET.PRODUCT, mandatory=false)
        USER_CUSTOM_RATE : AA_Interest:AA_PRD_PRF_INTEREST__USER_CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=21.0, core=true, t24Name=USER.CUSTOM.RATE, mandatory=false)
        USER_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=11.0, core=true, t24Name=USER.FIXED.RATE, typeModifiers=-, mandatory=false)
        USER_FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=12.0, core=true, businessType=A, t24Name=USER.FLOATING.INDEX, mandatory=false)
        USER_FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=13.0, core=true, t24Name=USER.FLOATING.NOTICE, mandatory=false)
        USER_INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=19.0, core=true, t24Name=USER.INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
        USER_LINKED_RATE : AA_Interest:AA_PRD_PRF_INTEREST__USER_LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=22.0, core=true, t24Name=USER.LINKED.RATE, mandatory=false)
        USER_MARGIN_TYPE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE *
            @t24:i (t24Name=USER.MARGIN.TYPE)
        USER_PERIODIC_INDEX : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=14.0, core=true, t24Name=USER.PERIODIC.INDEX, mandatory=false)
        USER_PERIODIC_METHOD : AA_Interest:AA_PRD_PRF_INTEREST__USER_PERIODIC_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=18.0, core=true, t24Name=USER.PERIODIC.METHOD, mandatory=false)
        USER_PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=17.0, core=true, t24Name=USER.PERIODIC.PERIOD, mandatory=false)
        USER_PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__USER_PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=15.0, core=true, t24Name=USER.PERIODIC.PERIOD.TYPE, mandatory=false)
        USER_PERIODIC_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=16.0, core=true, t24Name=USER.PERIODIC.RATE, typeModifiers=-, mandatory=false)
        USER_PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=20.0, core=true, t24Name=USER.PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
        USER_RATE_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__USER_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=USER.RATE.TYPE, mandatory=false)
        USE_PRICING_GRID : AA_Interest:AA_PRD_PRF_INTEREST__USE_PRICING_GRID
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=9.0, core=true, t24Name=USE.PRICING.GRID, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__API_ATTRIBUTE
    {
        API_ATTRIBUTE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=115.0, core=true, t24Name=API.ATTRIBUTE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__CHANGED_FIELDS
    {
        /* No input and multi value field - lists all fields that have been negotiated for each dated version of the property. For a subsequently dated property, the attributes listed in this field will appear in "Negotiated.Fields" and only newly negotiated fields will appear on "Changed.Fields" */
        CHANGED_FIELDS : T24BusinessTypes:A
            @i18n:Label (en="Fields That Have Been Negotiated For The Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=129.0, core=true, t24Name=CHANGED.FIELDS, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__CUSTOM_TYPE
    {
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=75.0, core=true, t24Name=CUSTOM.NAME, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, t24Name=CUSTOM.TYPE, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=76.0, core=true, t24Name=CUSTOM.VALUE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=144.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_PRD_PRF_INTEREST__DESCRIPTION
    {
/* Describes this condition record and is used for enrichment.

This field can be expanded to allow the User to enter the Description of the property condition in various languages. Each expansion of the field will correspond to a new Language as defined in the LANGUAGE table (Ref: GENERAL TABLES), and will indicate to the User the Language in which the Description must be entered.


Up to 35 type A (alphanumeric) characters */
        DESCRIPTION : T24BusinessTypes:ANY required
            @i18n:Label (en="Describes This Condition Record And Is Used For Enrichment")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=1.0, core=true, t24Name=DESCRIPTION, mandatory=true)
    }
    AA_PRD_PRF_INTEREST__FIXED_RATE
    {
        /* Set this field to yes whenever Interest rate should be calculated in a custom way. The method in CUSTOM.RATE.CALC field determines how this custom.Rate should be calculated */
        CUSTOM_RATE : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=36.0, core=true, t24Name=CUSTOM.RATE, mandatory=false)
/* Indicates at which rate is the arrangement currently running on for the date on the Id. This shows the actual rate even if it is running on a Periodic and would include the margin rate calculations as well.

System maintained(NOINPUT) field. */
        EFFECTIVE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=13, sysNumber=45.0, core=true, t24Name=EFFECTIVE.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A fixed rate is directly entered by the user specifying the Defined Property and is not defined by an index table.  Fixed rates are entered as positive numbers unless a negative rate is allowed.

This field is part of a multi value set of fields including FIXED.RATE, FLOATING.INDEX, PERIODIC.INDEX, PERIODIC.PERIOD, PERIODIC.BASE, MARGIN.TYPE, MARGIN.OPER, MARGIN.RATE, TIER.MIN.RATE, TIER.MAX.RATE, TIER.AMOUNT,TIER.PERCENT. */
        FIXED_RATE : T24BusinessTypes:R
            @i18n:Label (en="Fixed Rate")
            @i18n:Tooltip (en="Fixed rate entered by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=26.0, core=true, t24Name=FIXED.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A floating interest rate is tied to a variable base rate (i.e. BASIC.INTEREST).  During interest calculation, T24 will use the currency specific rate applicable for the calculation date (i.e. the rate used for the calculation will change whenever the base rate is changed).

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @i18n:Label (en="Floating Index")
            @i18n:Tooltip (en="Floating key of the BASIC.INTEREST")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=27.0, core=true, businessType=A, t24Name=FLOATING.INDEX, mandatory=false)
        /* This field is reserved for future development */
        FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=28.0, core=true, t24Name=FLOATING.NOTICE, mandatory=false)
/* Specifies the initial date from which the interest rate has to be reset. If specified along with a reset frequency, the initial reset date will be used for deriving the subsequent reset dates. If left blank, the subsequent resets will be based on the base date type in Account Property.

Allowed Options
Relative Dates (Start, Arrangement and Anniversary) or a valid date. */
        INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=34.0, core=true, t24Name=INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
/* It would be possible to specify that the rate for a given tier be ‘Linked’, by setting the field Linked Rate to Yes.



This can be mixed with normal rate calculations as well.



Where the Linked Rate is set to Yes, system would expect that an Arrangement reference in given in the ‘Linked Arrangement’ field along with the Interest property to be linked to. */
        LINKED_RATE : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=37.0, core=true, t24Name=LINKED.RATE, mandatory=false)
        MARGIN_TYPE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE *
            @t24:i (t24Name=MARGIN.TYPE)
/* A periodic rate is tied to an index (e.g. LIBOR) which is dependent on a period of time (e.g. term) and possibly an amount.  Once the arrangement is active, the rate is fixed until it is reset.  Periodic rates can be reset at a pre-determined frequency.
To define a periodic rate, the user specifies:
• PERIODIC.INTEREST index to use
• Period (this will default to TERM if a Term Amount Property exists)
• Interest selection method if the term is not found on the index. The option currently available is INTERPOLATE.
• Rate Reset period

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @i18n:Tooltip (en="Key of the Periodic interest table")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=29.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
/* If system does not find a interest rate in the correct bucket of PERIODIC.INTEREST table, it automatically applies a method to arrive at a rate chosen by this method.

Valid method is:

INTERPOLATE - System automatically interpolates the rate. This is the DEFAULT option as well. */
        PERIODIC_METHOD : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__PERIODIC_METHOD
            @i18n:Label (en="Periodic Method")
            @i18n:Tooltip (en="Valid method is Interpolate")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=33.0, core=true, t24Name=PERIODIC.METHOD, mandatory=false)
/* Defines the interest period to be used when linking to PERIODIC.INTEREST.(This will default to TERM if a Term Amount Property exists),. When a period is stated in Years or Weeks, they would be converted to Months and Days respectively and then located on the PI table.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Period")
            @i18n:Tooltip (en="Defines the interest period to be used")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=32.0, core=true, t24Name=PERIODIC.PERIOD, mandatory=false)
/* This field provides the user to specify which period will be used to fetch interest rate from the Periodic Interest table.
Allowed Values:
Maturity – Interest rate from periodic interest table will be fetched based on the maturity term of the contract.
Renewal – Interest rate from periodic interest table will be fetched based on the renewal term of the contract.
Reset Period - Interest rate from periodic interest table will be fetched based on the reset frequency period (i.e. based on the period to next interest reset date). If a Period is defined in 'Periodic Period' field, the same will be used for fetching the interest rate from Periodic Interest table except for the first and last period.
Periodic Period and Null - Interest rate from periodic interest table will be fetched based on the period defined in 'periodic period' field. */
        PERIODIC_PERIOD_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=30.0, core=true, t24Name=PERIODIC.PERIOD.TYPE, mandatory=false)
/* Indicates the rate on which the index runs on the date stated in the key. When a periodic index is used and the field is left blank, then system computes the rate from PERIODIC.INTEREST and defaults it here. When a rate is already present(say, the user changes the system computed rate), then system does not compute the new rate.

Validation rules:

System defaulted and modifiable.
Standard Rate field.
Valid only if Periodic index is stated. */
        PERIODIC_RATE : T24BusinessTypes:R
            @i18n:Label (en="Periodic Rate")
            @i18n:Tooltip (en="Rate in the index on the date stated in the key")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=31.0, core=true, t24Name=PERIODIC.RATE, typeModifiers=-, mandatory=false)
/* This indicates the reset period of the index stated. System would automatically reset the PERIODIC.RATE at the frequency stated in this field. Applicable only at Arrangement level. Periodic index may still be used without this field meaning, it never resets the computed rate

Validation rule:

Valid PERIOD field. */
        PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=35.0, core=true, t24Name=PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
/* The tier amount relates to the field RATE.TIER.TYPE and amounts can be entered if either BAND or LEVEL are selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Tier Amount")
            @i18n:Tooltip (en="Defines the threshold amount for each tier")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=TIER.AMOUNT, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MAX_RATE : T24BusinessTypes:R
            @i18n:Label (en="Tier Maximum Rate")
            @i18n:Tooltip (en="Maximum rate specified by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=43.0, core=true, t24Name=TIER.MAX.RATE, typeModifiers=-, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Tier Minimum Rate")
            @i18n:Tooltip (en="Minimum tier rate specified by the user")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=TIER.MIN.RATE, typeModifiers=-, mandatory=false)
/* Originally negative rate was applicable for all of the tiers together, but through this field it is now possible to define negative rate per tier.

Different values can be set in each tier and the options available are YES, BLOCK.MARGIN and NO/Blank.

Blank or NO - Indicates that negative rates cannot be input and if a rate becomes negative as a result of the application of a margin the rate will be set to zero

YES - Indicates that negative rates can be input and if the application margin makes the rate more negative then the more negative rate will be used.

BLOCK.MARGIN - Indicates that negative rates can be used but the following conditions apply

- If the rate is positive and the margin makes the rate negative then the rate will be set to zero

- If the rate is negative and the margin makes the rate more negative then the margin will be ignored and the original negative rate is used. */
        TIER_NEGATIVE_RATE : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=44.0, core=true, t24Name=TIER.NEGATIVE.RATE, mandatory=false)
/* The tier percent relates to the field RATE.TIER.TYPE and percentages can be entered if BAND is selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. Either TIER.PERCENT or TIER.AMOUNT can be entered by not both. If TIER.PERCENT is defined, then the Sum of Tier percentage should be 100% */
        TIER_PERCENT : T24BusinessTypes:N
            @i18n:Label (en="Tier Percent")
            @i18n:Tooltip (en="Tier Percent")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=47.0, core=true, t24Name=TIER.PERCENT, mandatory=false)
        USAGE_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=USAGE.PERCENT, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE
    {
/* The options are ADD or SUB. This indicates whether the MARGIN.RATE should be added or subtracted from the Floating or Periodic Rate.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.RATE and input is only allowed if a rate is entered in MARGIN.RATE. */
        MARGIN_OPER : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER
            @i18n:Label (en="Margin Operand")
            @i18n:Tooltip (en="Whether to add or subtract the margin")
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=40.0, core=true, t24Name=MARGIN.OPER, mandatory=false)
/* A margin rate can be used to adjust the specified rate of interest and to appropriately reflect any rate profit realized.  The result is the nominal rate of interest.  T24 allows for the following margin configuration:
• Single or Multiple Margins (Field MARGIN.TYPE)
• Rate Adjustment Method (Field MARGIN.OPER)

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.OPER. */
        MARGIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Margin Rate")
            @i18n:Tooltip (en="Percentage of margin rate used to value portfolio")
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=41.0, core=true, t24Name=MARGIN.RATE, mandatory=false)
/* Identifies whether there are single or multiple margins

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.OPER and MARGIN.RATE. */
        MARGIN_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE
            @i18n:Label (en="Margin Type")
            @i18n:Tooltip (en="Indicates single or multiple margins")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=39.0, core=true, t24Name=MARGIN.TYPE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=143.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__NEGOTIATED_FLDS
    {
/* No input and multi value field that lists the attributes on the arrangement property record that have been fixed and/or negotiated. These are stored when the arrangement level record is committed. When the arrangement level record is next used all items in the NEGOTIATED.FIELDS list will be maintained.
Once an attribute has been fixed or negotiated it will remain in negotiated field list on all subsequent dated versions of the property. */
        NEGOTIATED_FLDS : T24BusinessTypes:A
            @i18n:Label (en="Negotiated Attributes On Property")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=130.0, core=true, t24Name=NEGOTIATED.FLDS, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__NR_ATTRIBUTE
    {
/* Defines which individual attribute (field) has the negotiation rules defined in fields "Nr.Negotiable" through to "Nr.Message"


Must be a valid field name within this property
This field is part of a multi value set */
        NR_ATTRIBUTE : T24BusinessTypes:A
            @i18n:Label (en="Negotiable Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=121.0, core=true, t24Name=NR.ATTRIBUTE, mandatory=false)
        NR_ATTRIBUTE_RULE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=100, sysNumber=123.0, core=true, t24Name=NR.ATTRIBUTE.RULE, mandatory=false)
        NR_OPTIONS -> byValue AA_Interest:AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_OPTIONS *
            @t24:i (t24Name=NR.OPTIONS)
/* It is possible to define negotiation rules in EB.COMPARISON.TYPE.



Input must be a valid record from EB.STANDARD.COMPARISON */
        NR_STD_COMP -> AA_Rules:EB_STANDARD_COMPARISON
            @i18n:Label (en="Negotiable Standard Comparison")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=125.0, core=true, businessType=A, t24Name=NR.STD.COMP, mandatory=false)
        NR_TYPE -> byValue AA_Interest:AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_TYPE *
            @t24:i (t24Name=NR.TYPE)
        NR_VALUE_SOURCE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=124.0, core=true, t24Name=NR.VALUE.SOURCE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_OPTIONS
    {
/* Defines named values to indicate if an option applies to the associated NR.ATTRIBUTE. More than one option can be associated with the NR.ATTRIBUTE, with certain restrictions.


Valid Inputs are NEGOTIABLE, NON-NEGOTIABLE, OVERRIDE, FIX-VALUE, MANDATORY as described below: -

NEGOTIABLE
The value of NEGOTIABLE will indicate that the associated NR.ATTRIBUTE can be negotiated at the arrangement level.

NON-NEGOTIABLE
The value of NON-NEGOTIABLE will indicate that the associated NR.ATTRIBUTE cannot be negotiated at the arrangement level. Both NEGOTIABLE and NON-NEGOTIABLE cannot be defined together

OVERRIDE
Specifies if a default override message should be generated if the default attribute is amended at the arrangement level. If OVERRIDE is not specified, no override will be generated.

FIX-VALUE
Specified if the field value is to be fixed at the arrangement level. If FIX-VALUE is not specified the field value will vary with the changes to the underlying product condition.

MANDATORY
Specifies if the attribute is mandatory for this product condition. Can only be specified where the attribute is not defined as mandatory in the property class. */
        NR_OPTIONS : AA_Interest:AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS
            @i18n:Label (en="Negotiable Options")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=122.0, core=true, t24Name=NR.OPTIONS, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_TYPE
    {
/* This field indicates whether to raise error or override message when rule is broken
Allows only two values ERROR or OVERRIDE
ERROR error message is raised
OVERRIDE Override message is generated */
        NR_MESSAGE : AA_Interest:AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE
            @i18n:Label (en="Negotiable Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=128.0, core=true, t24Name=NR.MESSAGE, mandatory=false)
/* Defines the negotiation rule that is linked to the field defined in "Nr.Attribute". If an attribute has a negotiation rule linked to it (in associated fields NR.TYPE, NR.VALUE and NR.MESSAGE) the attribute must be defined as negotiable.
It is possible to define more than one negotiation rule for an attribute


Input must be a valid record from EB.COMPARISON.TYPE
This is a multi value set of fields */
        NR_TYPE -> AA_Rules:EB_COMPARISON_TYPE
            @i18n:Label (en="Negotiable Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=25, sysNumber=126.0, core=true, businessType=A, t24Name=NR.TYPE, mandatory=false)
        /* Defines the value that links to the rule in field "Nr.Type" */
        NR_VALUE : T24BusinessTypes:ANY
            @i18n:Label (en="Negotiable Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=60, sysNumber=127.0, core=true, t24Name=NR.VALUE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__OFFSET_DETAILS
    {
        OFFSET_DETAILS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=101.4, core=false, t24Name=OFFSET.DETAILS, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__ON_ACTIVITY
    {
/* When the periodic interest rate needs to be recalculated for certain activities, this field in combination with RECALCULATE may be used to setup the activity for which interest needs to be recalculated
Validation Rules
1. Applicable only for Periodic interest rate recalculation
2. Must be a valid activity in AA.ACTIVITY */
        ON_ACTIVITY -> AA_ProductFramework:AA_ACTIVITY
            @i18n:Label (en="On Activity")
            @i18n:Tooltip (en="Activities for payment schedule recalculation")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=48.0, core=true, businessType=A, t24Name=ON.ACTIVITY, mandatory=false)
/* Related to ON.ACTIVITY activity, when flagged to RATE, system will recalculate Periodic rate when the activity setup in ON.ACTIVITY is processed. When the value is NOTHING periodic rate would not be recalculated when the activity setup in ON.ACTIVITY is processed
Validation Rules
1. Mandatory when ON.ACTIVITY is setup
The fields ON.ACTIVITY and RECALCULATE in interest property class will be enhanced to allow user to define when to recalculate ‘Profit Amount’.
All activities which could result in payment schedule recalculation can be configured to recalculate ‘Profit Amount’ i.e., activities with payment schedule calculate and update action */
        RECALCULATE : AA_Interest:AA_PRD_PRF_INTEREST__ON_ACTIVITY__RECALCULATE
            @i18n:Label (en="Recalculate?")
            @i18n:Tooltip (en="Select if you want to recalculate")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=RECALCULATE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__OVERRIDE
    {
        /* Standard Override field containing override messages generated by amendments to this property */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=140.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__OWNING_COMPANY
    {
/* This field is used to capture the T24 Company(ies) that own the Product Condition record. There is no functionality attached to this field and can be used only for access control through SMS verification.

The input in this field must be a valid record in company table and should belong to current financial group.

This is a multi value field. Input in this field is optional. */
        OWNING_COMPANY -> ST_CompanyCreation:COMPANY
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=114.0, core=true, businessType=AAPRODUCT, t24Name=OWNING.COMPANY, typeModifiers=COMPANY, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__PRICING_RULES_BENEFIT
    {
        PRICING_RULES_BENEFIT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=PRICING.RULES.BENEFIT, mandatory=false)
        PRICING_RULES_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=93.0, core=true, t24Name=PRICING.RULES.FIXED.RATE, mandatory=false)
        PRICING_RULES_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=92.0, core=true, t24Name=PRICING.RULES.MARGIN, mandatory=false)
        PRICING_RULES_OPERAND : AA_Interest:AA_PRD_PRF_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=91.0, core=true, t24Name=PRICING.RULES.OPERAND, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__PR_ATTRIBUTE
    {
        /* This will hold two values either DUE or CAPITALISE.This Field denote which application method(DUE or CAPITALISE)should be taken while applying periodic rule charges. */
        PR_APP_METHOD : AA_Interest:AA_PRD_PRF_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD
            @i18n:Label (en="Periodic Attribute Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=110.0, core=true, t24Name=PR.APP.METHOD, mandatory=false)
        /* This will have to accept a valid T24 period.This Field will denote the application period for current aplication method. */
        PR_APP_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Attribute Period")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=111.0, core=true, t24Name=PR.APP.PERIOD, mandatory=false)
/* These are set of Attributes which define the names of the rule, which will be applied on certain attributes of property classes over a period of time.


Input must be a valid record from AA.PERIODIC.ATTRIBUTE */
        PR_ATTRIBUTE -> AA_Rules:AA_PERIODIC_ATTRIBUTE
            @i18n:Label (en="Periodic Attribute")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=102.0, core=true, businessType=A, t24Name=PR.ATTRIBUTE, mandatory=false)
/* Charge can be collected when the periodic rule is broken
This field denotes the charge property. The property and its condition is
defined the product designer and validated during publishing


Valid charge property */
        PR_BRK_CHARGE -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en="Attribute Break Charge")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=106.0, core=true, businessType=A, t24Name=PR.BRK.CHARGE, mandatory=false)
/* Based on the PR.BRK.RES option selected, this will hold either an OVERRIDE or EB.ERROR id. By default the ids will be picked from the associated EB.COMPARISON.TYPE record. If CAP or FLOOR values are given, then the underlying AA.PERIODIC.ATTRIBUTE.CLASS should have this type defined in it.


Input must be a valid OVERRIDE ID or EB.ERROR ID. */
        PR_BRK_MSG : T24BusinessTypes:ANY
            @i18n:Label (en="Attribute Break Message")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=105.0, core=true, t24Name=PR.BRK.MSG, mandatory=false)
/* When a rule is broken what action needs to be taken is defined here. Currently, the various options available are

Error block further processing
Override Give an override
Information  Record details only
CAP the rule value is to be used as a CAP if the rule is broken
FLOOR the rule value is to be used as a FLOOR if the rule is broken


Should be one of the above valid options */
        PR_BRK_RES : AA_Interest:AA_PRD_PRF_INTEREST__PR_ATTRIBUTE__PR_BRK_RES
            @i18n:Label (en="Action On Rule Break")
            @t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=104.0, core=true, t24Name=PR.BRK.RES, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=PR.RESERVED.1, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=PR.RESERVED.2, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=107.0, core=true, t24Name=PR.RESERVED.3, mandatory=false)
/* This is the actual rule value, which will be applied on certain attributes of the property classes over a period of time.


Input must be a valid data type defined in the corresponding
AA.PERIODIC.ATTRIBUTE.CLASS record. */
        PR_VALUE : T24BusinessTypes:A
            @i18n:Label (en="Attribute Value")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=103.0, core=true, t24Name=PR.VALUE, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__STMT_NOS
    {
        /* System field - not used for property conditions */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=139.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE
    {
        USER_MARGIN_OPER : AA_Interest:AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=24.0, core=true, t24Name=USER.MARGIN.OPER, mandatory=false)
        USER_MARGIN_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=USER.MARGIN.RATE, mandatory=false)
        USER_MARGIN_TYPE : AA_Interest:AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=23.0, core=true, t24Name=USER.MARGIN.TYPE, mandatory=false)
    }
/* The INTEREST Property Class is used for all interest definition and processing in AA.  A T24 product defined and processed in AA can have multiple interest properties defined (e.g. principal interest, penalty interest, commission, etc.).  The number of interest properties is determined by the users defining the products.
The different condition ids of interest property can be attached to the different types of interest properties at product level */
    AA_SIM_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=AA.SIM.INTEREST, nonStop=true, level=F, type=H, allowedFunctions="A C D E I L Q R S", localRefApplicationSysNo=101.0, localRefAllowed=true)
    {
/* Specifies how the accounting entries will be raised. Whether it needs to work like discounted or fixed type of interest.
Advance: This is nothing but discounted interest, interest amount will be collected inadvance before the interest periods.
Upfront Profit: Interest amount will be collected at the beginning of the contract.
None: Denotes conventional interest bearing contract. */
        ACCOUNTING_MODE : AA_Interest:AA_SIM_INTEREST__ACCOUNTING_MODE
            @i18n:Label (en="Accounting Mode")
            @t24:i (align=L, inputBehaviour=F, maxLength=15, sysNumber=60.0, core=true, t24Name=ACCOUNTING.MODE, mandatory=false)
/* When accruing interest,T24 must determine which amounts to include in each day's balance, the actual days to include in the period (e.g. first day, last day) and how to round any calculations.
T24 allows users to define the calculation method to use, by specifying an accrual rule in EB.ACCRUAL.PARAM.  Users can define the following:
First Day Inclusive (Yes/No)
Last Day Inclusive (Yes/No)
Rounding Rule (Natural, Down, Up, Currency, None)
Intermediate Calculation Rounding (Yes/No)
Credit value date adjustment
Debit value date adjustment */
        ACCRUAL_RULE -> AC_Fees:EB_ACCRUAL_PARAM
            @i18n:Label (en="Accrual Rule")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=ACCRUAL.RULE, mandatory=false)
        ACTION -> byValue AA_Interest:AA_SIM_INTEREST__ACTION *
            @t24:i (t24Name=ACTION)
/* The activity that caused this arrangement property to be updated.
The activity is taken from the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified */
        ACTIVITY : T24BusinessTypes:A required
            @i18n:Label (en=Activity)
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=1.0, core=true, t24Name=ACTIVITY, mandatory=true)
/* User can defined Total interest / profit amount for the contract.
Mandatory input if RATE.TYPE is ‘Flat Amount’.
Value in CALC.PROFIT.AMT will be populated in this field which the user can override. */
        ACTUAL_PROFIT_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=69.0, core=true, t24Name=ACTUAL.PROFIT.AMT, mandatory=false)
        ADJUST_EXPIRY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=82.0, core=true, t24Name=ADJUST.EXPIRY.DATE, mandatory=false)
        ADJUST_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=79.0, core=true, t24Name=ADJUST.MARGIN, mandatory=false)
        ADJUST_OPERAND : AA_Interest:AA_SIM_INTEREST__ADJUST_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=19, sysNumber=78.0, core=true, t24Name=ADJUST.OPERAND, mandatory=false)
        ADJUST_OPTION : AA_Interest:AA_SIM_INTEREST__ADJUST_OPTION
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=101.2, core=false, t24Name=ADJUST.OPTION, mandatory=false)
        ADJUST_OVERRIDE_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=80.0, core=true, t24Name=ADJUST.OVERRIDE.RATE, mandatory=false)
        ADJUST_REASON : AA_Interest:AA_SIM_INTEREST__ADJUST_REASON
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=81.0, core=true, t24Name=ADJUST.REASON, mandatory=false)
        ADJUST_TYPE : AA_Interest:AA_SIM_INTEREST__ADJUST_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=77.0, core=true, t24Name=ADJUST.TYPE, mandatory=false)
        API_ATTRIBUTE -> byValue AA_Interest:AA_SIM_INTEREST__API_ATTRIBUTE *
            @t24:i (t24Name=API.ATTRIBUTE)
/* The key to the arrangement property condition. The key is comprised of the following components (delimited by a -)
Arrangement Id
Property name>
Effective Date */
        ARRANGEMENT_KEY -> AA_Interest:AA_SIM_INTEREST PK required
            @i18n:Label (en="Arrangement Key")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=0.0, core=true, businessType=ANY, t24Name=ARRANGEMENT.KEY, mandatory=true)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=148.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=149.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=145.0, core=true, t24Name=AUTHORISER, mandatory=false)
        /* The user can specify that interest will only be calculated if a balance threshold is surpassed. For credit interest, the user can specify the minimum balance which must be maintained. For debit interest, the user can specify the maximum debit balance for which interest will not be calculated. */
        CALC_THRESHOLD : T24BusinessTypes:AMT
            @i18n:Label (en="Threshold Amount")
            @i18n:Tooltip (en="Minimum Balance Amount For Which Interest Will Not Be Calculated")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=CALC.THRESHOLD, typeModifiers=-, mandatory=false)
        CHANGED_FIELDS -> byValue AA_Interest:AA_SIM_INTEREST__CHANGED_FIELDS *
            @t24:i (t24Name=CHANGED.FIELDS)
/* T24 supports the compounding of both debit and credit interest.

Compounding interest applies a factor to the compound interest calculation based upon the  number of compounding periods in the year. This field defines the number of compounding periods to be applied.

Note that the compounding type provides a factor in the calculation only  and does not actually cause the base principal amount to be actually increased by the calculated interest amount.

The following compound frequencies are supported:

DAILY
Interest is compounded 360/5/6 times a year (from days in year in INTEREST.BASIS)

WEEKn
Interest is compounded 52/n times per year based on a number of WEEKS
-- Bi-weekly compounding should be defined as WEEK2 and will result in 26 periods
-- Four-Weekly compounding should be defined as WEEK4 and results in 13 periods

Mnn
Interest is compounded 12/n times a year based on a numberof months
--  Monthly compounding should be defined as M01 and will result in 12 periods
--  Bi-Monthly should be defined as M02 and will result in 6 periods
--  Quarterly should be defined as M03 and will result in 4 periods
--  Semi-Annual should be defined as M06 and will result in 6 periods
--  Annual should be defined as M12 and will result in 1 period
--  Bi-Annual should be defined as M24 and will result in 0.5 periods
--  Any number of months up to 99 may be defined and results in 12/n periods

TWMTH
Interest is compounded twice a month resulting in 24 periods as opposed to WEEK2
which results in 26 periods

Nnnn
- Compounding takes place nnn times per year. */
        COMPOUND_TYPE : T24BusinessTypes:COMPFQU
            @i18n:Label (en="Compound Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=COMPOUND.TYPE, mandatory=false)
/* This field indicates how the effective rate is calculated, when set to YIELD the interest rate defined is taken as the annual effective rate. System uses the below formula to calculate the interest amount,

Daily Effective Rate(ERd) = (1+Interest Rate)^(1/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1}

When the field is set as NULL the effective rate is calculated based on the compounding frequency defined in the field COMPOUND.TYPE using the below formula,

Daily Effective Rate(ERd) = (1+Interest Rate/Compound Freq)^(Compound Freq/Days in Year) - 1
Interest Amount = (Source Balance + Interest accrued any for the current Period)*{ (1+ERd)^(No of Days in Int Period) – 1} */
        COMPOUND_YIELD_METHOD : AA_Interest:AA_SIM_INTEREST__COMPOUND_YIELD_METHOD
            @i18n:Label (en="Compoun Yield Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=50.0, core=true, t24Name=COMPOUND.YIELD.METHOD, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=146.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=142.0, core=true, t24Name=CURR.NO, mandatory=false)
        /* If there is a Cutom rate tier then this field is mandatory. It is a AA.CUSTOM.RATE.TYPE record that determines how the custom rate should be calculated */
        CUSTOM_RATE_CALC -> AA_Interest:AA_CUSTOM_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=72.0, core=true, businessType=S, t24Name=CUSTOM.RATE.CALC, mandatory=false)
        CUSTOM_TYPE -> byValue AA_Interest:AA_SIM_INTEREST__CUSTOM_TYPE *
            @t24:i (t24Name=CUSTOM.TYPE)
/* Specifies the date convention to be followed for the reset frequency i.e. if the cycled Periodic Reset date falls on a non-working day, the reset date will be moved based on the option chosen.
Allowed Values:
Arrangement / Null – Based on the date convention specified in the Account Property Class.
Periodic Interest – Based on the convention used by the index i.e. the Periodic Interest record.
o Forward Same Month convention for Monthly periods and Forward convention for all other periods if USE.LAST.WORKING.DAY is set to 'Y'.
o Forward Convention for all periods if USE.LAST.WORKING.DAY is not set. */
        DATE_CONVENTION : AA_Interest:AA_SIM_INTEREST__DATE_CONVENTION
            @i18n:Label (en=Convention)
            @t24:i (align=L, inputBehaviour=I, maxLength=18, sysNumber=61.0, core=true, t24Name=DATE.CONVENTION, mandatory=false)
        DATE_TIME -> byValue AA_Interest:AA_SIM_INTEREST__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* T24 can calculate interest based upon several different day basis types.  Each T24 Interest Day Basis type is represented as a numerator and denominator.
Numerator (interest days)
o 360  Each month is considered to have 30 days
o 366  The exact number of days will be used
Denominator (days in the year)
o 360  Each year is considered to have 360 days
o 365  Each year is considered to have 365 days
o 366  The exact number of days in the year will be used (365 or 366)

T24 allows the following interest day basis types:
Type        Notes
A (360/360)     A1 and A2 are variations which alter the method of treating a month as 30 days.
B (366/360)
C (366/366)
D (360/366)
E (366/365)
F (360/365)     F1 and F2 are variations which alter the method of treating a month as 30 days.

This is a Mandatory field. */
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Interest Day Basis")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=4.0, core=true, businessType=A, t24Name=DAY.BASIS, mandatory=false)
/* p>Optional field - Allowed Values are RESETTING and NON-RESETTING.

RESETTING - During any Renewal Activities (for eg : change.product or reset) the property conditions will be reset from the product.

NON-RESETTING - During any Renewal Activities property conditions will be maintained from the Arrangement level.

Not used in Arrangement/Sim level.

Leaving the field blank sets NON-RESETTING as the default option. */
        DEFAULT_ATTR_OPTION : AA_Interest:AA_SIM_INTEREST__DEFAULT_ATTR_OPTION
            @i18n:Label (en="Default Attribute Options")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=119.0, core=true, t24Name=DEFAULT.ATTR.OPTION, mandatory=false)
        /* Not used for arrangement level conditions */
        DEFAULT_NEGOTIABLE : AA_Interest:AA_SIM_INTEREST__DEFAULT_NEGOTIABLE
            @i18n:Label (en="Default Negotiable")
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=120.0, core=true, t24Name=DEFAULT.NEGOTIABLE, mandatory=false)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Department Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=147.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        FIXED_RATE -> byValue AA_Interest:AA_SIM_INTEREST__FIXED_RATE *
            @t24:i (t24Name=FIXED.RATE)
        FT_TXN_TYPE -> FT_Config:FT_TXN_TYPE_CONDITION
            @i18n:Label (en="Transaction Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=101.1, core=false, businessType=A, t24Name=FT.TXN.TYPE, mandatory=false)
/* Contains the arrangement number, extracted from the first component of the id
System updated field no input allowed */
        ID_COMP_1 : T24BusinessTypes:A
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=131.0, core=true, t24Name=ID.COMP.1, mandatory=false)
/* Contains the name of the property that this arrangement level condition relates to
Name is defined in AA.PRORERTY table
System maintained field no input allowed */
        ID_COMP_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=132.0, core=true, t24Name=ID.COMP.2, mandatory=false)
/* Contains the Effective Date of this property condition
System updated field no input allowed */
        ID_COMP_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=30, sysNumber=133.0, core=true, t24Name=ID.COMP.3, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=134.0, core=true, t24Name=ID.COMP.4, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_5 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=135.0, core=true, t24Name=ID.COMP.5, mandatory=false)
/* Reserved for future use
System field no input allowed */
        ID_COMP_6 : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=136.0, core=true, t24Name=ID.COMP.6, typeModifiers="ARRANGEMENT_START_ANNIVERSARY-D_W_M_Y", mandatory=false)
        INPUTTER -> byValue AA_Interest:AA_SIM_INTEREST__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* Specifies whether interest method is fixed life of the contract or not.
FIXED – Fix the total profit amount regardless of any change to loan parameters. This field is specify whether total interest amount need to fixed or not.
Null – Will be treated as a normal interest bearing property. */
        INTEREST_METHOD : AA_Interest:AA_SIM_INTEREST__INTEREST_METHOD
            @t24:i (align=L, inputBehaviour=F, maxLength=6, sysNumber=66.0, core=true, t24Name=INTEREST.METHOD, mandatory=false)
/* Income / expense will be posted to internal account category specified in Accounting condition instead of PL categories.
This will accept YES or NULL */
        INTERNAL_BOOKING : AA_Interest:AA_SIM_INTEREST__INTERNAL_BOOKING
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, t24Name=INTERNAL.BOOKING, mandatory=false)
        /* Where at least one tier is set to be ‘Linked Rate’, the Arrangement to be linked for calculating the Interest Rate, must be specified here (or automatically defaulted by the system, where a Limit is attached) */
        LINKED_ARRANGEMENT : T24BusinessTypes:ARR
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=LINKED.ARRANGEMENT, mandatory=false)
/* This field will be used to specify the Interest Property of the Linked Arrangement that should be referred to, for getting the Interest Rate.



Where at least one tier is set to be ‘Linked Rate’, this must be specified. */
        LINKED_PROPERTY -> AA_ProductFramework:AA_PROPERTY
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=64.0, core=true, businessType=A, t24Name=LINKED.PROPERTY, mandatory=false)
/* This field indicates the minimum interest amount to be posted for an arrangement/account.
If the calculated interest amount is less than the amount specified in this field, system will waive the interest amount or adjust the interest amount based on the option set in the field MIN.INT.WAIVE */
        MIN_INT_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Minimum Interest Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=MIN.INT.AMOUNT, typeModifiers=-, mandatory=false)
/* This field indicates if the calculated interest amount has to be waived or adjusted.
If the field is set to YES, then the system will waive the interest amount if it is less than the MIN.INT.AMOUNT specified.
If NULL value is set and if the calculated interest amount is less than the MIN.INT.AMOUNT specified, then system will post the minimum interest amount. */
        MIN_INT_WAIVE : AA_Interest:AA_SIM_INTEREST__MIN_INT_WAIVE
            @i18n:Label (en="Minimum Interest Amount To Waive")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=53.0, core=true, t24Name=MIN.INT.WAIVE, mandatory=false)
/* Negative rate functionality is moved to TIER.NEGATIVE.RATE field, please refer to TIER.NEGATIVE.RATE for help text.
This field is system maintained field, where in if all of the tier negative rate has the same value, then this field will be updated with that value. */
        NEGATIVE_RATE : AA_Interest:AA_SIM_INTEREST__NEGATIVE_RATE
            @i18n:Label (en="Negative Rate")
            @t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=7.0, core=true, t24Name=NEGATIVE.RATE, mandatory=false)
        NEGOTIATED_FLDS -> byValue AA_Interest:AA_SIM_INTEREST__NEGOTIATED_FLDS *
            @t24:i (t24Name=NEGOTIATED.FLDS)
        NR_ATTRIBUTE -> byValue AA_Interest:AA_SIM_INTEREST__NR_ATTRIBUTE *
            @t24:i (t24Name=NR.ATTRIBUTE)
        OFFSET_DETAILS -> byValue AA_Interest:AA_SIM_INTEREST__OFFSET_DETAILS *
            @t24:i (t24Name=OFFSET.DETAILS)
        ON_ACTIVITY -> byValue AA_Interest:AA_SIM_INTEREST__ON_ACTIVITY *
            @t24:i (t24Name=ON.ACTIVITY)
/* This field is used to define the profit amount realised in the Legacy system. Assuming the total profit amount for the contract is 10,000  and remaining profit amount to be realized during migration is 3,000, one will give the difference (7,000) in this attribute. And 3,000 will be input in the Actual Profit Amount that has to be realized in T24. If someone wants the total profit amount, they can simply add these 2 to get them. Once authorised, the value cannot be changed.
Validation:
• Applicable for Fixed Interest Method
• Input enabled during migration of the contract(through take-over activity) */
        ORIG_PROFIT_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=30, sysNumber=70.0, core=true, t24Name=ORIG.PROFIT.AMOUNT, mandatory=false)
        OVERRIDE -> byValue AA_Interest:AA_SIM_INTEREST__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        OWNING_COMPANY -> byValue AA_Interest:AA_SIM_INTEREST__OWNING_COMPANY *
            @t24:i (t24Name=OWNING.COMPANY)
        PACKAGE_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=PACKAGE.PRODUCT, mandatory=false)
        PACKAGE_PROPERTY_CONTROL : AA_Interest:AA_SIM_INTEREST__PACKAGE_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=100.0, core=true, t24Name=PACKAGE.PROPERTY.CONTROL, mandatory=false)
        PRICING_RULES_BENEFIT -> byValue AA_Interest:AA_SIM_INTEREST__PRICING_RULES_BENEFIT *
            @t24:i (t24Name=PRICING.RULES.BENEFIT)
        PROGRAM_LIMIT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=94.0, core=true, t24Name=PROGRAM.LIMIT, mandatory=false)
        PR_ATTRIBUTE -> byValue AA_Interest:AA_SIM_INTEREST__PR_ATTRIBUTE *
            @t24:i (t24Name=PR.ATTRIBUTE)
        RATE_LOCK_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=101.3, core=false, t24Name=RATE.LOCK.ID, mandatory=false)
/* T24 allows for the definition of tiers of interest rates.  Each tier is specified by defining the amount up to which the interest rate applies.  Additionally, each tier can be of a different interest rate type (i.e. fixed, floating, or periodic).  There are three type of tiers; Single, Level, and Banded.
Single Rate
When a single rate tier type is specified a single nominal interest rate will apply for the entire balance amount.
Rate Levels
By specifying level tiers a product can be defined which will calculate interest at different rates depending on the balance amount.  A single rate of interest will be selected and applied based upon the balance amount.  For example for a deposit a single product can have a higher rate of interest for accounts which maintain higher credit balances.  Conversely for a loan product a higher rate of interest can be charged for accounts with higher debit balances.
For example, aassuming a credit balance of 15,000 and level interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  A single rate of 15 per cent will be applied to the entire amount of 15,000.
Banded Rates
Banded tier interest will typically result in a blended interest rate.   This is similar to Level tiers, but allows for the interest rate of each tier to be applied to the portion of the balance that falls within the tier.
For example, assuming a credit balance of 15,000 and banded interest which specifies an applicable rate of 10 per cent up to 10,000 and 15 per cent above 10,000.  Two calculations will be performed (i.e. 10 per cent on 10,000 and 15 per cent on 5,000)

This field is Mandatory. */
        RATE_TIER_TYPE : AA_Interest:AA_SIM_INTEREST__RATE_TIER_TYPE
            @i18n:Label (en="Tier Type For Rate")
            @t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=8.0, core=true, t24Name=RATE.TIER.TYPE, mandatory=false)
/* Specifies whether Interest definition is based on a rate or amount and in case of rate what type of rate.
Reducing Rate: Rate defined in Interest rate related fields will be considered as Reducing or Diminishing Rate for Interest calculation.
Flat Rate: Rate defined in interest rate related fields will be considered as a Flat Rate.
Flat Amount: Amount defined in the ACTUAL.PROFIT.AMT will be considered as the total interest amount for the interest property.
This will be allowed only when INTEREST.METHOD is FIEXED */
        RATE_TYPE : AA_Interest:AA_SIM_INTEREST__RATE_TYPE
            @i18n:Label (en="Rate Type")
            @i18n:Tooltip (en="Is the Rate quoted net of tax or gross of tax")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=67.0, core=true, t24Name=RATE.TYPE, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=141.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* For an arrangement under ACCOUNTS Product Line, it is possible link the first tier amount of debit interest to the LIMIT record attached.
It can be achieved by setting the field REFER.LIMIT in INTEREST property condition as ‘YES’. This functionality can be enabled only if the LIMIT is managed by AA. */
        REFER_LIMIT : AA_Interest:AA_SIM_INTEREST__REFER_LIMIT
            @i18n:Label (en="Refer Limit")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=51.0, core=true, t24Name=REFER.LIMIT, mandatory=false)
        REGIONAL_PRICING_CODE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=84.0, core=true, t24Name=REGIONAL.PRICING.CODE, mandatory=false)
        REGIONAL_PRICING_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=87.0, core=true, t24Name=REGIONAL.PRICING.FIXED.RATE, mandatory=false)
        REGIONAL_PRICING_LEVEL : AA_Interest:AA_SIM_INTEREST__REGIONAL_PRICING_LEVEL
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=83.0, core=true, t24Name=REGIONAL.PRICING.LEVEL, mandatory=false)
        REGIONAL_PRICING_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=86.0, core=true, t24Name=REGIONAL.PRICING.MARGIN, mandatory=false)
        REGIONAL_PRICING_OPERAND : AA_Interest:AA_SIM_INTEREST__REGIONAL_PRICING_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=85.0, core=true, t24Name=REGIONAL.PRICING.OPERAND, mandatory=false)
        REGIONAL_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=88.0, core=true, businessType=A, t24Name=REGIONAL.PRODUCT, mandatory=false)
        REGIONAL_PROPERTY_CONTROL : AA_Interest:AA_SIM_INTEREST__REGIONAL_PROPERTY_CONTROL
            @t24:i (align=L, inputBehaviour=I, maxLength=20, sysNumber=89.0, core=true, t24Name=REGIONAL.PROPERTY.CONTROL, mandatory=false)
        RELATIONSHIP_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=59.0, core=true, t24Name=RELATIONSHIP.FIXED.RATE, mandatory=false)
        /* Rate of discount margin as a result of the relationship of the customer with the bank. Would be evaluated based on the RELATIONSHIP arrangements held by the customer and also the definitions in PREFERENTIAL.PRICING as well as the rules in PRICING.RULES of this arrangement product. Validation: System maintained field. Would be a percentage would be added or subtracted to the original rate. The effective rate would be influenced by the content in this field. */
        RELATIONSHIP_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=57.0, core=true, t24Name=RELATIONSHIP.MARGIN, mandatory=false)
        /* Indicates whether the margin stated gets added or subtracted or multiplied to the originally evaluated rate. Validation: System maintained field. Would be either ADD, SUBTRACT or MULTIPLY */
        RELATIONSHIP_OPERAND : AA_Interest:AA_SIM_INTEREST__RELATIONSHIP_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=56.0, core=true, t24Name=RELATIONSHIP.OPERAND, mandatory=false)
        /* Field to indicate the relationship pricing plan(if any) that has influenced the rate on this arrangement. If a customer has a relationship with the bank which entitles for special margins, then the plan which has contributed to this margin would be updated here. Validation: System maintained field. Would be a valid RELATIONSHIP.PRICING product for this customer */
        RELATIONSHIP_PRODUCT -> AA_ProductManagement:AA_PRODUCT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=55.0, core=true, businessType=A, t24Name=RELATIONSHIP.PRODUCT, mandatory=false)
/* This field holds the variation selected for the arrangement for the interest pricing condition

Validation Rules

System maintained, NOINPUT allowed */
        RELATIONSHIP_VARIATION : AA_Interest:AA_SIM_INTEREST__RELATIONSHIP_VARIATION
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=58.0, core=true, t24Name=RELATIONSHIP.VARIATION, mandatory=false)
        RESERVED1 : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=12, sysNumber=65.0, core=true, t24Name=RESERVED1, mandatory=false)
        RESERVED10 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=96.0, core=true, t24Name=RESERVED10, mandatory=false)
        RESERVED2_ID : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=137.0, core=true, t24Name=RESERVED2.ID, mandatory=false)
        RESERVED4 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=62.0, core=true, t24Name=RESERVED4, mandatory=false)
        RESERVED6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=68.0, core=true, t24Name=RESERVED6, mandatory=false)
        RESERVED7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=99.0, core=true, t24Name=RESERVED7, mandatory=false)
        RESERVED8 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=98.0, core=true, t24Name=RESERVED8, mandatory=false)
        RESERVED9 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=97.0, core=true, t24Name=RESERVED9, mandatory=false)
        /* If this field gets checked then rate would not be stored in the EFFECTIVE.RATE field and whenever needed System will calculate it at runtime */
        RUNTIME_RATE_CALC : AA_Interest:AA_SIM_INTEREST__RUNTIME_RATE_CALC
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=73.0, core=true, t24Name=RUNTIME.RATE.CALC, mandatory=false)
        STMT_NOS -> byValue AA_Interest:AA_SIM_INTEREST__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Field accepts the options 'YES' or 'ALTERNATE'.
Indicates whether accruals needs to be Suppressed or Accrual should be done for the Alternate Interest Property.

For Example:
It will be set as YES by the system if the DONOR.ACCRUAL field is set as SUPPRESS for any product / arrangement in INTEREST.COMPENSTION property of a Bundle arrangement.

If it is set as 'ALTERNATE' for a property then the Accrual will be suppressed for the property for which it is set and Accrual will happen for the Alternate property which is set in Activity Restriction.
Blank value in this field denotes that there is no suppression of accrual

This is a Non Mandatory field. */
        SUPPRESS_ACCRUAL : AA_Interest:AA_SIM_INTEREST__SUPPRESS_ACCRUAL
            @i18n:Label (en="Suppress Accrual")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=54.0, core=true, t24Name=SUPPRESS.ACCRUAL, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=SYS.RESERVE1, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=SYS.RESERVE2, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=SYS.RESERVE3, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE6 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=SYS.RESERVE6, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        SYS_RESERVE7 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=SYS.RESERVE7, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* This field denotes the target classic product of the given property class

The already existing T24 products are mapped using AA infrastructure
These are called classic products
These products are mapped to different property clases */
        TARGET_PRODUCT : T24BusinessTypes:A
            @i18n:Label (en="Target Product")
            @t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=138.0, core=true, t24Name=TARGET.PRODUCT, mandatory=false)
        USER_CUSTOM_RATE : AA_Interest:AA_SIM_INTEREST__USER_CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=21.0, core=true, t24Name=USER.CUSTOM.RATE, mandatory=false)
        USER_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=11.0, core=true, t24Name=USER.FIXED.RATE, typeModifiers=-, mandatory=false)
        USER_FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=12.0, core=true, businessType=A, t24Name=USER.FLOATING.INDEX, mandatory=false)
        USER_FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=13.0, core=true, t24Name=USER.FLOATING.NOTICE, mandatory=false)
        USER_INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=19.0, core=true, t24Name=USER.INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
        USER_LINKED_RATE : AA_Interest:AA_SIM_INTEREST__USER_LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=22.0, core=true, t24Name=USER.LINKED.RATE, mandatory=false)
        USER_MARGIN_TYPE -> byValue AA_Interest:AA_SIM_INTEREST__USER_MARGIN_TYPE *
            @t24:i (t24Name=USER.MARGIN.TYPE)
        USER_PERIODIC_INDEX : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=14.0, core=true, t24Name=USER.PERIODIC.INDEX, mandatory=false)
        USER_PERIODIC_METHOD : AA_Interest:AA_SIM_INTEREST__USER_PERIODIC_METHOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=18.0, core=true, t24Name=USER.PERIODIC.METHOD, mandatory=false)
        USER_PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=17.0, core=true, t24Name=USER.PERIODIC.PERIOD, mandatory=false)
        USER_PERIODIC_PERIOD_TYPE : AA_Interest:AA_SIM_INTEREST__USER_PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=15.0, core=true, t24Name=USER.PERIODIC.PERIOD.TYPE, mandatory=false)
        USER_PERIODIC_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=16.0, core=true, t24Name=USER.PERIODIC.RATE, typeModifiers=-, mandatory=false)
        USER_PERIODIC_RESET : T24BusinessTypes:FQU
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=20.0, core=true, t24Name=USER.PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
        USER_RATE_TYPE : AA_Interest:AA_SIM_INTEREST__USER_RATE_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=USER.RATE.TYPE, mandatory=false)
        USE_PRICING_GRID : AA_Interest:AA_SIM_INTEREST__USE_PRICING_GRID
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=9.0, core=true, t24Name=USE.PRICING.GRID, mandatory=false)
    }
    AA_SIM_INTEREST__ACTION
    {
/* The activity that caused this arrangement property to be updated.
The activity is taken from the underlying AA.ARRANGEMENT.ACTIVITY request that caused the record to be modified */
        ACTION : T24BusinessTypes:A
            @i18n:Label (en=Action)
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=2.0, core=true, t24Name=ACTION, mandatory=false)
    }
    AA_SIM_INTEREST__API_ATTRIBUTE
    {
        API_ATTRIBUTE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=115.0, core=true, t24Name=API.ATTRIBUTE, mandatory=false)
    }
    AA_SIM_INTEREST__CHANGED_FIELDS
    {
        /* No input and multi value field lists all fields that have been negotiated for each dated version of the property. For a subsequently dated property, the attributes listed in this field will appear in "Negotiated.Fields" and only newly negotiated fields will appear on "Changed.Fields" */
        CHANGED_FIELDS : T24BusinessTypes:A
            @i18n:Label (en="Negotiated Fields")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=129.0, core=true, t24Name=CHANGED.FIELDS, mandatory=false)
    }
    AA_SIM_INTEREST__CUSTOM_TYPE
    {
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=75.0, core=true, t24Name=CUSTOM.NAME, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, t24Name=CUSTOM.TYPE, mandatory=false)
        /* This is just a meta data field that will be used by the routine in filed CUSTOM.RATE.CALC to calculate custom Rate */
        CUSTOM_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=76.0, core=true, t24Name=CUSTOM.VALUE, mandatory=false)
    }
    AA_SIM_INTEREST__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=144.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    AA_SIM_INTEREST__FIXED_RATE
    {
        /* Set this field to yes whenever Interest rate should be calculated in a custom way. The method in CUSTOM.RATE.CALC field determines how this custom.Rate should be calculated */
        CUSTOM_RATE : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__CUSTOM_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=36.0, core=true, t24Name=CUSTOM.RATE, mandatory=false)
/* Indicates at which rate is the arrangement currently running on for the date on the Id. This shows the actual rate even if it is running on a Periodic and would include the margin rate calculations as well.

System maintained(NOINPUT) field. */
        EFFECTIVE_RATE : T24BusinessTypes:R
            @i18n:Label (en="Effective Rate")
            @t24:i (align=R, inputBehaviour=N, maxLength=13, sysNumber=45.0, core=true, t24Name=EFFECTIVE.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A fixed rate is directly entered by the user specifying the Defined Property and is not defined by an index table.  Fixed rates are entered as positive numbers unless a negative rate is allowed.

This field is part of a multi value set of fields including FIXED.RATE, FLOATING.INDEX, PERIODIC.INDEX, PERIODIC.PERIOD, PERIODIC.BASE, MARGIN.TYPE, MARGIN.OPER, MARGIN.RATE, TIER.MIN.RATE, TIER.MAX.RATE, TIER.AMOUNT,TIER.PERCENT. */
        FIXED_RATE : T24BusinessTypes:R
            @i18n:Label (en="Fixed Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=26.0, core=true, t24Name=FIXED.RATE, typeModifiers=-, mandatory=false)
/* Three basic types of interest are supported by T24; Fixed, Floating, and Periodic.  Each of these interest types can include one or more margins and can be specified in a tiered structure.

A floating interest rate is tied to a variable base rate (i.e. BASIC.INTEREST).  During interest calculation, T24 will use the currency specific rate applicable for the calculation date (i.e. the rate used for the calculation will change whenever the base rate is changed).

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        FLOATING_INDEX -> ST_RateParameters:BASIC_RATE_TEXT
            @i18n:Label (en="Floating Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=27.0, core=true, businessType=A, t24Name=FLOATING.INDEX, mandatory=false)
        /* This field is reserved for future development */
        FLOATING_NOTICE : T24BusinessTypes:PERIOD
            @i18n:Label (en="Floating Notice")
            @i18n:Tooltip (en="Field For Future Use")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=28.0, core=true, t24Name=FLOATING.NOTICE, mandatory=false)
/* Specifies the initial date from which the interest rate has to be reset. If specified along with a reset frequency, the initial reset date will be used for deriving the subsequent reset dates. If left blank, the subsequent resets will be based on the base date type in Account Property.

Allowed Options
Relative Dates (Start, Arrangement and Anniversary) or a valid date. */
        INITIAL_RESET_DATE : T24BusinessTypes:DP
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=34.0, core=true, t24Name=INITIAL.RESET.DATE, typeModifiers="START_ARRANGEMENT_ANNIVERSARYPERIODNEGATIVED_W_M_Y", mandatory=false)
/* It would be possible to specify that the rate for a given tier be ‘Linked’, by setting the field Linked Rate to Yes.



This can be mixed with normal rate calculations as well.



Where the Linked Rate is set to Yes, system would expect that an Arrangement reference in given in the ‘Linked Arrangement’ field along with the Interest property to be linked to. */
        LINKED_RATE : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__LINKED_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=37.0, core=true, t24Name=LINKED.RATE, mandatory=false)
        MARGIN_TYPE -> byValue AA_Interest:AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE *
            @t24:i (t24Name=MARGIN.TYPE)
/* A periodic rate is tied to an index (e.g. LIBOR) which is dependent on a period of time (e.g. term) and possibly an amount.  Once the arrangement is active, the rate is fixed until it is reset.  Periodic rates can be reset at a pre-determined frequency.
To define a periodic rate, the user specifies:
PERIODIC.INTEREST index to use
Period (this will default to TERM if a Term Amount Property exists)
Interest selection method if the term is not found on the index. The option currently available is INTERPOLATE.
Rate Reset period

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_INDEX : T24BusinessTypes:A
            @i18n:Label (en="Periodic Index")
            @t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=29.0, core=true, t24Name=PERIODIC.INDEX, mandatory=false)
/* If system does not find a interest rate in the correct bucket of PERIODIC.INTEREST table, it automatically applies a method to arrive at a rate chosen by this method.

Valid method is:

INTERPOLATE - System automatically interpolates the rate. This is the DEFAULT option as well. */
        PERIODIC_METHOD : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__PERIODIC_METHOD
            @i18n:Label (en="Periodic Method")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=33.0, core=true, t24Name=PERIODIC.METHOD, mandatory=false)
/* Defines the interest period to be used when linking to PERIODIC.INTEREST.(This will default to TERM if a Term Amount Property exists),. When a period is stated in Years or Weeks, they would be converted to Months and Days respectively and then located on the PI table.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        PERIODIC_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en="Periodic Period")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=32.0, core=true, t24Name=PERIODIC.PERIOD, mandatory=false)
/* This field provides the user to specify which period will be used to fetch interest rate from the Periodic Interest table.
Allowed Values:
Maturity – Interest rate from periodic interest table will be fetched based on the maturity term of the contract.
Renewal – Interest rate from periodic interest table will be fetched based on the renewal term of the contract.
Reset Period - Interest rate from periodic interest table will be fetched based on the reset frequency period (i.e. based on the period to next interest reset date). If a Period is defined in 'Periodic Period' field, the same will be used for fetching the interest rate from Periodic Interest table except for the first and last period.
Periodic Period and Null - Interest rate from periodic interest table will be fetched based on the period defined in 'periodic period' field. */
        PERIODIC_PERIOD_TYPE : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=30.0, core=true, t24Name=PERIODIC.PERIOD.TYPE, mandatory=false)
/* Indicates the rate on which the index runs on the date stated in the key. When a periodic index is used and the field is left blank, then system computes the rate from PERIODIC.INTEREST and defaults it here. When a rate is already present(say, the user changes the system computed rate), then system does not compute the new rate.

Validation rules:

System defaulted and modifiable.
Standard Rate field.
Valid only if Periodic index is stated. */
        PERIODIC_RATE : T24BusinessTypes:R
            @i18n:Label (en="Periodic Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=31.0, core=true, t24Name=PERIODIC.RATE, typeModifiers=-, mandatory=false)
/* This indicates the reset period of the index stated. System would automatically reset the PERIODIC.RATE at the frequency stated in this field. Applicable only at Arrangement level. Periodic index may still be used without this field meaning, it never resets the computed rate

Validation rule:

Valid PERIOD field. */
        PERIODIC_RESET : T24BusinessTypes:FQU
            @i18n:Label (en="Periodic Reset")
            @t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=35.0, core=true, t24Name=PERIODIC.RESET, typeModifiers=EXTENDED, mandatory=false)
/* The tier amount relates to the field RATE.TIER.TYPE and amounts can be entered if either BAND or LEVEL are selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_AMOUNT : T24BusinessTypes:AMT
            @i18n:Label (en="Tier Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=TIER.AMOUNT, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MAX_RATE : T24BusinessTypes:R
            @i18n:Label (en="Maximum Rate For Tier")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=43.0, core=true, t24Name=TIER.MAX.RATE, typeModifiers=-, mandatory=false)
/* For each tier defined, a user can specify a minimum and/or maximum rate for the tier.  This is applicable for variable and periodic rates and allows the tier rate to be controlled.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. */
        TIER_MIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Minimum Rate For Tier")
            @t24:i (align=R, inputBehaviour=I, maxLength=13, sysNumber=42.0, core=true, t24Name=TIER.MIN.RATE, typeModifiers=-, mandatory=false)
/* Originally negative rate was applicable for all of the tiers together, but through this field it is now possible to define negative rate per tier.

Different values can be set in each tier and the options available are YES, BLOCK.MARGIN and NO/Blank.

Blank or NO - Indicates that negative rates cannot be input and if a rate becomes negative as a result of the application of a margin the rate will be set to zero

YES - Indicates that negative rates can be input and if the application margin makes the rate more negative then the more negative rate will be used.

BLOCK.MARGIN - Indicates that negative rates can be used but the following conditions apply

- If the rate is positive and the margin makes the rate negative then the rate will be set to zero

- If the rate is negative and the margin makes the rate more negative then the margin will be ignored and the original negative rate is used. */
        TIER_NEGATIVE_RATE : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE
            @t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=44.0, core=true, t24Name=TIER.NEGATIVE.RATE, mandatory=false)
/* The tier percent relates to the field RATE.TIER.TYPE and percentages can be entered if BAND is selected.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. Either TIER.PERCENT or TIER.AMOUNT can be entered by not both. If TIER.PERCENT is defined, then the Sum of Tier percentage should be 100% */
        TIER_PERCENT : T24BusinessTypes:N
            @i18n:Label (en="Tier Percentage")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=47.0, core=true, t24Name=TIER.PERCENT, mandatory=false)
        USAGE_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=USAGE.PERCENT, mandatory=false)
    }
    AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE
    {
/* The options are ADD or SUB. This indicates whether the MARGIN.RATE should be added or subtracted from the Floating or Periodic Rate.

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.RATE and input is only allowed if a rate is entered in MARGIN.RATE. */
        MARGIN_OPER : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER
            @i18n:Label (en="Margin Operand")
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=40.0, core=true, t24Name=MARGIN.OPER, mandatory=false)
/* A margin rate can be used to adjust the specified rate of interest and to appropriately reflect any rate profit realized.  The result is the nominal rate of interest.  T24 allows for the following margin configuration:
Single or Multiple Margins (Field MARGIN.TYPE)
Rate Adjustment Method (Field MARGIN.OPER)

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.TYPE and MARGIN.OPER. */
        MARGIN_RATE : T24BusinessTypes:R
            @i18n:Label (en="Margin Rate")
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=41.0, core=true, t24Name=MARGIN.RATE, mandatory=false)
/* Identifies whether there are single or multiple margins

This field is part of a multi value set of fields listed in the helptext for FIXED.RATE. It is also part of a sub value set with MARGIN.OPER and MARGIN.RATE. */
        MARGIN_TYPE : AA_Interest:AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE
            @i18n:Label (en="Margin Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=39.0, core=true, t24Name=MARGIN.TYPE, mandatory=false)
    }
    AA_SIM_INTEREST__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=143.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    AA_SIM_INTEREST__NEGOTIATED_FLDS
    {
/* No input and multi value field that lists the attributes on the arrangement property record that have been fixed and/or negotiated. These are stored when the arrangement level record is committed. When the arrangement level record is next used all items in the NEGOTIATED.FIELDS list will be maintained
Once an attribute has been fixed or negotiated it will remain in negotiated field list on all subsequent dated versions of the property. */
        NEGOTIATED_FLDS : T24BusinessTypes:A
            @i18n:Label (en="Negotiated Fields")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=130.0, core=true, t24Name=NEGOTIATED.FLDS, mandatory=false)
    }
    AA_SIM_INTEREST__NR_ATTRIBUTE
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_ATTRIBUTE : T24BusinessTypes:A
            @i18n:Label (en=Attribute)
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=121.0, core=true, t24Name=NR.ATTRIBUTE, mandatory=false)
        NR_ATTRIBUTE_RULE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=123.0, core=true, t24Name=NR.ATTRIBUTE.RULE, mandatory=false)
        NR_OPTIONS -> byValue AA_Interest:AA_SIM_INTEREST__NR_ATTRIBUTE__NR_OPTIONS *
            @t24:i (t24Name=NR.OPTIONS)
/* Not used for Arrangement Conditions



Input not allowed */
        NR_STD_COMP -> AA_Rules:EB_STANDARD_COMPARISON
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=125.0, core=true, businessType=A, t24Name=NR.STD.COMP, mandatory=false)
        NR_TYPE -> byValue AA_Interest:AA_SIM_INTEREST__NR_ATTRIBUTE__NR_TYPE *
            @t24:i (t24Name=NR.TYPE)
        NR_VALUE_SOURCE : T24BusinessTypes:ANY
            @t24:i (align=L, inputBehaviour=N, maxLength=60, sysNumber=124.0, core=true, t24Name=NR.VALUE.SOURCE, mandatory=false)
    }
    AA_SIM_INTEREST__NR_ATTRIBUTE__NR_OPTIONS
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_OPTIONS : AA_Interest:AA_SIM_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS
            @i18n:Label (en=Options)
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=122.0, core=true, t24Name=NR.OPTIONS, mandatory=false)
    }
    AA_SIM_INTEREST__NR_ATTRIBUTE__NR_TYPE
    {
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_MESSAGE : AA_Interest:AA_SIM_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE
            @i18n:Label (en=Message)
            @t24:i (align=L, inputBehaviour=N, maxLength=10, sysNumber=128.0, core=true, t24Name=NR.MESSAGE, mandatory=false)
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_TYPE -> AA_Rules:EB_COMPARISON_TYPE
            @i18n:Label (en=Type)
            @t24:i (align=L, inputBehaviour=N, maxLength=25, sysNumber=126.0, core=true, businessType=A, t24Name=NR.TYPE, mandatory=false)
/* System field - not used for arrangement conditions


System Field NoINPUT allowed - */
        NR_VALUE : T24BusinessTypes:ANY
            @i18n:Label (en=Value)
            @t24:i (align=L, inputBehaviour=N, maxLength=60, sysNumber=127.0, core=true, t24Name=NR.VALUE, mandatory=false)
    }
    AA_SIM_INTEREST__OFFSET_DETAILS
    {
        OFFSET_DETAILS : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=101.4, core=false, t24Name=OFFSET.DETAILS, mandatory=false)
    }
    AA_SIM_INTEREST__ON_ACTIVITY
    {
/* When the periodic interest rate needs to be recalculated for certain activities, this field in combination with RECALCULATE may be used to setup the activity for which interest needs to be recalculated
Validation Rules
1. Applicable only for Periodic interest rate recalculation
2. Must be a valid activity in AA.ACTIVITY */
        ON_ACTIVITY -> AA_ProductFramework:AA_ACTIVITY
            @i18n:Label (en="On Activity")
            @t24:i (align=L, inputBehaviour=I, maxLength=80, sysNumber=48.0, core=true, businessType=A, t24Name=ON.ACTIVITY, mandatory=false)
/* Related to ON.ACTIVITY activity, when flagged to RATE, system will recalculate Periodic rate when the activity setup in ON.ACTIVITY is processed. When the value is NOTHING periodic rate would not be recalculated when the activity setup in ON.ACTIVITY is processed
Validation Rules
1. Mandatory when ON.ACTIVITY is setup
The fields ON.ACTIVITY and RECALCULATE in interest property class will be enhanced to allow user to define when to recalculate ‘Profit Amount’.
All activities which could result in payment schedule recalculation can be configured to recalculate ‘Profit Amount’ i.e., activities with payment schedule calculate and update action */
        RECALCULATE : AA_Interest:AA_SIM_INTEREST__ON_ACTIVITY__RECALCULATE
            @i18n:Label (en=Recalculate)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=RECALCULATE, mandatory=false)
    }
    AA_SIM_INTEREST__OVERRIDE
    {
        /* Standard Override field containing override messages generated by amendments to this property */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=140.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    AA_SIM_INTEREST__OWNING_COMPANY
    {
/* This field is used to capture the T24 Company(ies) that own the Product Condition record. There is no functionality attached to this field and can be used only for access control through SMS verification.

The input in this field must be a valid record in company table and should belong to current financial group.

This is a multi value field. Input in this field is optional. */
        OWNING_COMPANY -> ST_CompanyCreation:COMPANY
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=114.0, core=true, businessType=AAPRODUCT, t24Name=OWNING.COMPANY, typeModifiers=COMPANY, mvSvExpansionAccess=NOEXPAND, mandatory=false)
    }
    AA_SIM_INTEREST__PRICING_RULES_BENEFIT
    {
        PRICING_RULES_BENEFIT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=PRICING.RULES.BENEFIT, mandatory=false)
        PRICING_RULES_FIXED_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=93.0, core=true, t24Name=PRICING.RULES.FIXED.RATE, mandatory=false)
        PRICING_RULES_MARGIN : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=92.0, core=true, t24Name=PRICING.RULES.MARGIN, mandatory=false)
        PRICING_RULES_OPERAND : AA_Interest:AA_SIM_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=91.0, core=true, t24Name=PRICING.RULES.OPERAND, mandatory=false)
    }
    AA_SIM_INTEREST__PR_ATTRIBUTE
    {
/* This will hold two values either DUE or CAPITALISE
This Field denote which application method(DUE or CAPITALISE)should be taken while applying periodic rule charges.
Not inputtable at arrangement level */
        PR_APP_METHOD : AA_Interest:AA_SIM_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD
            @i18n:Label (en="Application Method")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=110.0, core=true, t24Name=PR.APP.METHOD, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* This will have to accept a valid T24 period.
This Field will denote the application period for current aplication method.
Not inputtable at arrangement level */
        PR_APP_PERIOD : T24BusinessTypes:PERIOD
            @i18n:Label (en=Period)
            @t24:i (align=L, inputBehaviour=N, maxLength=4, sysNumber=111.0, core=true, t24Name=PR.APP.PERIOD, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* These are set of Attributes which define the names of the rule, which will be applied on certain attributes of property classes over a period of time


Input must be a valid record from AA.PERIODIC.ATTRIBUTE */
        PR_ATTRIBUTE -> AA_Rules:AA_PERIODIC_ATTRIBUTE
            @i18n:Label (en=Attribute)
            @t24:i (align=L, inputBehaviour=N, maxLength=30, sysNumber=102.0, core=true, businessType=A, t24Name=PR.ATTRIBUTE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* Not used for arrangement level conditions


Input not allowed. */
        PR_BRK_CHARGE -> AA_ProductFramework:AA_PROPERTY
            @i18n:Label (en=Charge)
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=106.0, core=true, businessType=A, t24Name=PR.BRK.CHARGE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* Based on the PR.BRK.RES option selected, this will hold either an OVERRIDE or EB.ERROR id. By default the ids will be picked from the associated EB.COMPARISON.TYPE record. If CAP or FLOOR values are given, then the underlying AA.PERIODIC.ATTRIBUTE.CLASS should have this type defined in it


Input must be a valid OVERRIDE ID or EB.ERROR ID */
        PR_BRK_MSG : T24BusinessTypes:ANY
            @i18n:Label (en=Message)
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=105.0, core=true, t24Name=PR.BRK.MSG, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* When a rule is broken what action needs to be taken is defined here. Currently, the various options available are

Error block further processing
Override Give an override
Information  Record details only
CAP the rule value is to be used as a CAP if the rule is broken
FLOOR the rule value is to be used as a FLOOR if the rule is broken


Should be one of the above valid options */
        PR_BRK_RES : AA_Interest:AA_SIM_INTEREST__PR_ATTRIBUTE__PR_BRK_RES
            @i18n:Label (en=Action)
            @t24:i (align=L, inputBehaviour=N, maxLength=13, sysNumber=104.0, core=true, t24Name=PR.BRK.RES, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_1 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=PR.RESERVED.1, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_2 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=PR.RESERVED.2, mvSvExpansionAccess=NOEXPAND, mandatory=false)
        /* System field - reserved for future use */
        PR_RESERVED_3 : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=107.0, core=true, t24Name=PR.RESERVED.3, mvSvExpansionAccess=NOEXPAND, mandatory=false)
/* This is the actual rule value, which will be applied on certain attributes of the property classes over a period of time.


Input must be a valid data type defined in the corresponding
AA.PERIODIC.ATTRIBUTE.CLASS record. */
        PR_VALUE : T24BusinessTypes:A
            @i18n:Label (en=Value)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=103.0, core=true, t24Name=PR.VALUE, mvSvExpansionAccess=NOEXPAND, mandatory=false)
    }
    AA_SIM_INTEREST__STMT_NOS
    {
        /* Contains the key to the accounting entries raised by the activity being applied to this arrangement property */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Statement Number")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=139.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    AA_SIM_INTEREST__USER_MARGIN_TYPE
    {
        USER_MARGIN_OPER : AA_Interest:AA_SIM_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER
            @t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=24.0, core=true, t24Name=USER.MARGIN.OPER, mandatory=false)
        USER_MARGIN_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=USER.MARGIN.RATE, mandatory=false)
        USER_MARGIN_TYPE : AA_Interest:AA_SIM_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=23.0, core=true, t24Name=USER.MARGIN.TYPE, mandatory=false)
    }
    NOFILE_AA_INTEREST
        @i18n:Description (en="")
        @t24:i (t24Name=NOFILE.AA.INTEREST, level=I, type=T, allowedFunctions="L P S", localRefAllowed=false)
    {
        ACCRUAL_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Accrual Amt")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=8.0, core=true, t24Name=ACCRUAL.AMT, mandatory=false)
        ACC_DATE_FROM : T24BusinessTypes:D
            @i18n:Label (en="Accrue From")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=3.0, core=true, t24Name=ACC.DATE.FROM, mandatory=false)
        ACC_DATE_TO : T24BusinessTypes:D
            @i18n:Label (en="Accrue To")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=4.0, core=true, t24Name=ACC.DATE.TO, mandatory=false)
        ACTUAL_ACCRUAL_AMT : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=9.0, core=true, t24Name=ACTUAL.ACCRUAL.AMT, mandatory=false)
        ARRANGEMENT_ID : T24BusinessTypes:N
            @i18n:Label (en="Arrangement Id")
            @t24:i (align=R, inputBehaviour=N, maxLength=20, core=false, t24Name=ARRANGEMENT.ID, genOperation=E.AA.INTEREST.PROJECTOR, mandatory=true)
        ARRANGEMENT_NO : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=20, sysNumber=20.0, core=true, t24Name=ARRANGEMENT.NO, mandatory=false)
        COMPOUND_FQU : T24BusinessTypes:N
            @i18n:Label (en=Compounding)
            @t24:i (align=R, inputBehaviour=N, maxLength=5, sysNumber=13.0, core=true, t24Name=COMPOUND.FQU, mandatory=false)
        DATE_FROM : T24BusinessTypes:D
            @i18n:Label (en="Date From")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, core=false, t24Name=DATE.FROM, mandatory=false)
        DATE_TO : T24BusinessTypes:D
            @i18n:Label (en="Date To")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, core=false, t24Name=DATE.TO, mandatory=false)
        DAYS : T24BusinessTypes:N
            @i18n:Label (en=Days)
            @t24:i (align=R, inputBehaviour=N, maxLength=4, sysNumber=5.0, core=true, t24Name=DAYS, mandatory=false)
        DAY_BASIS -> ST_RateParameters:INTEREST_BASIS
            @i18n:Label (en="Day Basis")
            @t24:i (align=R, inputBehaviour=N, maxLength=2, sysNumber=10.0, core=true, t24Name=DAY.BASIS, mandatory=false)
        MERGE_DETAIL : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=3, core=false, t24Name=MERGE.DETAIL, mandatory=false)
        NOMINAL_RATE : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=52, sysNumber=14.0, core=true, t24Name=NOMINAL.RATE, mandatory=false)
        PAYMENT_DATE : T24BusinessTypes:D
            @i18n:Label (en="Payment Date")
            @i18n:Tooltip (en="It Indicates the payment date of the bill")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=2.0, core=true, t24Name=PAYMENT.DATE, mandatory=false)
        PAYMENT_TYPE : T24BusinessTypes:N
            @i18n:Label (en="Payment Type")
            @i18n:Tooltip (en="Type of Payment (Annuity or Linear)")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=1.0, core=true, t24Name=PAYMENT.TYPE, mandatory=false)
        PRINCIPAL : T24BusinessTypes:AMT
            @i18n:Label (en=Principal)
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=6.0, core=true, t24Name=PRINCIPAL, mandatory=false)
        PROPERTY : T24BusinessTypes:N
            @i18n:Label (en=Property)
            @t24:i (align=R, inputBehaviour=N, maxLength=15, core=false, t24Name=PROPERTY, mandatory=false)
        RATE : T24BusinessTypes:R
            @i18n:Label (en=Rate)
            @i18n:Tooltip (en="Rate at which dividend/coupon is being paid")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=7.0, core=true, t24Name=RATE, mandatory=false)
        SIM_REF : T24BusinessTypes:N
            @i18n:Label (en=Simulation)
            @t24:i (align=R, inputBehaviour=N, maxLength=20, core=false, t24Name=SIM.REF, mandatory=false)
    }
}
Enumerations {
    AA_ACCRUAL_FREQUENCY__DEFAULT_OL_ACCRUAL : mml:string
        NULL=NULL
        YES=YES
    AA_ACCRUAL_FREQUENCY__FORCED_TO_SOD : mml:string
        NULL=NULL
        YES=YES
    AA_ACCRUAL_FREQUENCY__PRODUCT__PROD_PROP__PROD_PROP_OL_ACCRUAL : mml:string
        NULL=NULL
        YES=YES
    AA_ACCRUAL_FREQUENCY__PROPERTY__PROPERTY_OL_ACCRUAL : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__ACCOUNTING_MODE : mml:string
        ADVANCE=ADVANCE
        UPFRONT_PROFIT=UPFRONT.PROFIT
    AA_ARR_INTEREST__ADJUST_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_ARR_INTEREST__ADJUST_OPTION : mml:string
        ADJUST=ADJUST
        REVERSE=REVERSE
    AA_ARR_INTEREST__ADJUST_REASON : mml:string
        IRA_DEATH_DIST=IRA.DEATH.DIST
        IRA_DIST_STATUS=IRA.DIST.STATUS
        IRA_USER_WAIVE=IRA.USER.WAIVE
        RETAIN_CUSTOMERS_BUSINESS=RETAIN.CUSTOMERS.BUSINESS
        VALUED_CUSTOMER=VALUED.CUSTOMER
    AA_ARR_INTEREST__ADJUST_TYPE : mml:string
        ADJUST=ADJUST
        OVERRIDE=OVERRIDE
    AA_ARR_INTEREST__COMPOUND_YIELD_METHOD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_ARR_INTEREST__DATE_CONVENTION : mml:string
        ARRANGEMENT=ARRANGEMENT
        NULL=NULL
        PERIODIC_INTEREST="PERIODIC INTEREST"
    AA_ARR_INTEREST__DEFAULT_ATTR_OPTION : mml:string
        NON__RESETTING=NON-RESETTING
        RESETTING=RESETTING
    AA_ARR_INTEREST__DEFAULT_NEGOTIABLE : mml:string
        NO=NO
        YES=YES
    AA_ARR_INTEREST__FIXED_RATE__CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__FIXED_RATE__LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_ARR_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_ARR_INTEREST__FIXED_RATE__PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_ARR_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_ARR_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_ARR_INTEREST__INTEREST_METHOD : mml:string
        FIXED=FIXED
    AA_ARR_INTEREST__INTERNAL_BOOKING : mml:string
        YES=YES
    AA_ARR_INTEREST__MERGE_SELECTION : mml:string
        NO=NO
        YES=YES
    AA_ARR_INTEREST__MIN_INT_WAIVE : mml:string
        YES=YES
    AA_ARR_INTEREST__NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_ARR_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS : mml:string
        COUNT=COUNT
        FIX__VALUE=FIX-VALUE
        MANDATORY=MANDATORY
        NEGOTIABLE=NEGOTIABLE
        NON__NEGOTIABLE=NON-NEGOTIABLE
        NON__RESETTING=NON-RESETTING
        OVERRIDE=OVERRIDE
        RESETTING=RESETTING
    AA_ARR_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        OVERRIDE=OVERRIDE
    AA_ARR_INTEREST__ON_ACTIVITY__RECALCULATE : mml:string
        NOTHING=NOTHING
        PROFIT_AMOUNT=PROFIT.AMOUNT
        PROFIT_AMOUNT_AND_RATE=PROFIT.AMOUNT.AND.RATE
        RATE=RATE
    AA_ARR_INTEREST__PACKAGE_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_ARR_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_ARR_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD : mml:string
        CAPITALISE=CAPITALISE
        DEFER=DEFER
        DUE=DUE
    AA_ARR_INTEREST__PR_ATTRIBUTE__PR_BRK_RES : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        INFORMATION=INFORMATION
        OVERRIDE=OVERRIDE
    AA_ARR_INTEREST__RATE_TIER_TYPE : mml:string
        BAND=BAND
        LEVEL=LEVEL
        SINGLE=SINGLE
    AA_ARR_INTEREST__RATE_TYPE : mml:string
        FLAT_AMOUNT=FLAT.AMOUNT
        FLAT_RATE=FLAT.RATE
        REDUCING_RATE=REDUCING.RATE
    AA_ARR_INTEREST__REFER_LIMIT : mml:string
        NO=NO
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__REGIONAL_PRICING_LEVEL : mml:string
        AREA=AREA
        BRANCH=BRANCH
        DIVISION=DIVISION
        REGION=REGION
        STATE=STATE
    AA_ARR_INTEREST__REGIONAL_PRICING_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_ARR_INTEREST__REGIONAL_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_ARR_INTEREST__RELATIONSHIP_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_ARR_INTEREST__RELATIONSHIP_VARIATION : mml:string
        A=A
        AA=AA
        AAA=AAA
        B=B
        BRANCH=BRANCH
        BRONZE=BRONZE
        C=C
        GOLD=GOLD
        INTERNET=INTERNET
        INT_ON_MA=INT.ON.MA
        MOBILE=MOBILE
        NO_INT_ON_MA=NO.INT.ON.MA
        OD_ON_MA=OD.ON.MA
        PLATINUM=PLATINUM
        PREFERENTIAL=PREFERENTIAL
        REGULAR=REGULAR
        SENIORCITIZENS=SENIORCITIZENS
        SILVER=SILVER
        STAFF=STAFF
    AA_ARR_INTEREST__RUNTIME_RATE_CALC : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__SUPPRESS_ACCRUAL : mml:string
        ALTERNATE=ALTERNATE
        INFO_ONLY=INFO.ONLY
        YES=YES
    AA_ARR_INTEREST__USER_CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__USER_LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_ARR_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_ARR_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_ARR_INTEREST__USER_PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_ARR_INTEREST__USER_PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_ARR_INTEREST__USER_RATE_TYPE : mml:string
        CUSTOM=CUSTOM
        FIXED=FIXED
        FLOATING=FLOATING
        LINKED=LINKED
        PERIODIC=PERIODIC
    AA_ARR_INTEREST__USE_PRICING_GRID : mml:string
        NULL=NULL
        YES=YES
    AA_INTEREST_ACCRUALS_WORK__ABB_NEW_METHOD__ABB_NEW_METHOD : mml:string
        NULL=NULL
        YES=YES
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS_WORK__FROM_DATE__MARGIN__MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS_WORK__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS_WORK__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS_WORK__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS__ABB_NEW_METHOD__ABB_NEW_METHOD : mml:string
        NULL=NULL
        YES=YES
    AA_INTEREST_ACCRUALS__FROM_DATE__BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS__FROM_DATE__COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS__FROM_DATE__MARGIN__MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUALS__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUAL_HIST__ABB_NEW_METHOD__ABB_NEW_METHOD : mml:string
        NULL=NULL
        YES=YES
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUAL_HIST__FROM_DATE__MARGIN__MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUAL_HIST__INFO_FROM_DATE__INFO_MARGIN__INFO_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUAL_HIST__IPROJ_FROM_DATE__IPROJ_MARGIN__IPROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_BASIS : mml:string
        A=A
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_ACCRUAL_HIST__PROJ_FROM_DATE__PROJ_MARGIN__PROJ_MARGIN : mml:string
        R=R
    AA_INTEREST_DETAILS__PAY_DATE__BASIS : mml:string
        A=A
    AA_INTEREST_DETAILS__PAY_DATE__COMPOUND_YIELD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_INTEREST_DETAILS__PAY_DATE__MARGIN__MARGIN : mml:string
        R=R
    AA_PRD_CAT_INTEREST__ACCOUNTING_MODE : mml:string
        ADVANCE=ADVANCE
        UPFRONT_PROFIT=UPFRONT.PROFIT
    AA_PRD_CAT_INTEREST__ADJUST_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_CAT_INTEREST__ADJUST_OPTION : mml:string
        ADJUST=ADJUST
        REVERSE=REVERSE
    AA_PRD_CAT_INTEREST__ADJUST_REASON : mml:string
        IRA_DEATH_DIST=IRA.DEATH.DIST
        IRA_DIST_STATUS=IRA.DIST.STATUS
        IRA_USER_WAIVE=IRA.USER.WAIVE
        RETAIN_CUSTOMERS_BUSINESS=RETAIN.CUSTOMERS.BUSINESS
        VALUED_CUSTOMER=VALUED.CUSTOMER
    AA_PRD_CAT_INTEREST__ADJUST_TYPE : mml:string
        ADJUST=ADJUST
        OVERRIDE=OVERRIDE
    AA_PRD_CAT_INTEREST__COMPOUND_YIELD_METHOD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_PRD_CAT_INTEREST__DATE_CONVENTION : mml:string
        ARRANGEMENT=ARRANGEMENT
        NULL=NULL
        PERIODIC_INTEREST="PERIODIC INTEREST"
    AA_PRD_CAT_INTEREST__DEFAULT_ATTR_OPTION : mml:string
        NON__RESETTING=NON-RESETTING
        RESETTING=RESETTING
    AA_PRD_CAT_INTEREST__DEFAULT_NEGOTIABLE : mml:string
        NO=NO
        YES=YES
    AA_PRD_CAT_INTEREST__FIXED_RATE__CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__FIXED_RATE__LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_CAT_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_CAT_INTEREST__FIXED_RATE__PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_CAT_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_CAT_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_CAT_INTEREST__INTEREST_METHOD : mml:string
        FIXED=FIXED
    AA_PRD_CAT_INTEREST__INTERNAL_BOOKING : mml:string
        YES=YES
    AA_PRD_CAT_INTEREST__MIN_INT_WAIVE : mml:string
        YES=YES
    AA_PRD_CAT_INTEREST__NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS : mml:string
        COUNT=COUNT
        FIX__VALUE=FIX-VALUE
        MANDATORY=MANDATORY
        NEGOTIABLE=NEGOTIABLE
        NON__NEGOTIABLE=NON-NEGOTIABLE
        NON__RESETTING=NON-RESETTING
        OVERRIDE=OVERRIDE
        RESETTING=RESETTING
    AA_PRD_CAT_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        OVERRIDE=OVERRIDE
    AA_PRD_CAT_INTEREST__ON_ACTIVITY__RECALCULATE : mml:string
        NOTHING=NOTHING
        PROFIT_AMOUNT=PROFIT.AMOUNT
        PROFIT_AMOUNT_AND_RATE=PROFIT.AMOUNT.AND.RATE
        RATE=RATE
    AA_PRD_CAT_INTEREST__PACKAGE_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_CAT_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_CAT_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD : mml:string
        CAPITALISE=CAPITALISE
        DEFER=DEFER
        DUE=DUE
    AA_PRD_CAT_INTEREST__PR_ATTRIBUTE__PR_BRK_RES : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        INFORMATION=INFORMATION
        OVERRIDE=OVERRIDE
    AA_PRD_CAT_INTEREST__RATE_TIER_TYPE : mml:string
        BAND=BAND
        LEVEL=LEVEL
        SINGLE=SINGLE
    AA_PRD_CAT_INTEREST__RATE_TYPE : mml:string
        FLAT_AMOUNT=FLAT.AMOUNT
        FLAT_RATE=FLAT.RATE
        REDUCING_RATE=REDUCING.RATE
    AA_PRD_CAT_INTEREST__REFER_LIMIT : mml:string
        NO=NO
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__REGIONAL_PRICING_LEVEL : mml:string
        AREA=AREA
        BRANCH=BRANCH
        DIVISION=DIVISION
        REGION=REGION
        STATE=STATE
    AA_PRD_CAT_INTEREST__REGIONAL_PRICING_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_CAT_INTEREST__REGIONAL_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_CAT_INTEREST__RELATIONSHIP_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_CAT_INTEREST__RELATIONSHIP_VARIATION : mml:string
        A=A
        AA=AA
        AAA=AAA
        B=B
        BRANCH=BRANCH
        BRONZE=BRONZE
        C=C
        GOLD=GOLD
        INTERNET=INTERNET
        INT_ON_MA=INT.ON.MA
        MOBILE=MOBILE
        NO_INT_ON_MA=NO.INT.ON.MA
        OD_ON_MA=OD.ON.MA
        PLATINUM=PLATINUM
        PREFERENTIAL=PREFERENTIAL
        REGULAR=REGULAR
        SENIORCITIZENS=SENIORCITIZENS
        SILVER=SILVER
        STAFF=STAFF
    AA_PRD_CAT_INTEREST__RUNTIME_RATE_CALC : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__SUPPRESS_ACCRUAL : mml:string
        ALTERNATE=ALTERNATE
        INFO_ONLY=INFO.ONLY
        YES=YES
    AA_PRD_CAT_INTEREST__USER_CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__USER_LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_CAT_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_CAT_INTEREST__USER_PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_CAT_INTEREST__USER_PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_CAT_INTEREST__USER_RATE_TYPE : mml:string
        CUSTOM=CUSTOM
        FIXED=FIXED
        FLOATING=FLOATING
        LINKED=LINKED
        PERIODIC=PERIODIC
    AA_PRD_CAT_INTEREST__USE_PRICING_GRID : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__ACCOUNTING_MODE : mml:string
        ADVANCE=ADVANCE
        UPFRONT_PROFIT=UPFRONT.PROFIT
    AA_PRD_DES_INTEREST__ADJUST_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_DES_INTEREST__ADJUST_OPTION : mml:string
        ADJUST=ADJUST
        REVERSE=REVERSE
    AA_PRD_DES_INTEREST__ADJUST_REASON : mml:string
        IRA_DEATH_DIST=IRA.DEATH.DIST
        IRA_DIST_STATUS=IRA.DIST.STATUS
        IRA_USER_WAIVE=IRA.USER.WAIVE
        RETAIN_CUSTOMERS_BUSINESS=RETAIN.CUSTOMERS.BUSINESS
        VALUED_CUSTOMER=VALUED.CUSTOMER
    AA_PRD_DES_INTEREST__ADJUST_TYPE : mml:string
        ADJUST=ADJUST
        OVERRIDE=OVERRIDE
    AA_PRD_DES_INTEREST__COMPOUND_YIELD_METHOD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_PRD_DES_INTEREST__DATE_CONVENTION : mml:string
        ARRANGEMENT=ARRANGEMENT
        NULL=NULL
        PERIODIC_INTEREST="PERIODIC INTEREST"
    AA_PRD_DES_INTEREST__DEFAULT_ATTR_OPTION : mml:string
        NON__RESETTING=NON-RESETTING
        RESETTING=RESETTING
    AA_PRD_DES_INTEREST__DEFAULT_NEGOTIABLE : mml:string
        NO=NO
        YES=YES
    AA_PRD_DES_INTEREST__FIXED_RATE__CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__FIXED_RATE__LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_DES_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_DES_INTEREST__FIXED_RATE__PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_DES_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_DES_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_DES_INTEREST__INTEREST_METHOD : mml:string
        FIXED=FIXED
    AA_PRD_DES_INTEREST__INTERNAL_BOOKING : mml:string
        YES=YES
    AA_PRD_DES_INTEREST__MIN_INT_WAIVE : mml:string
        YES=YES
    AA_PRD_DES_INTEREST__NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS : mml:string
        COUNT=COUNT
        FIX__VALUE=FIX-VALUE
        MANDATORY=MANDATORY
        NEGOTIABLE=NEGOTIABLE
        NON__NEGOTIABLE=NON-NEGOTIABLE
        NON__RESETTING=NON-RESETTING
        OVERRIDE=OVERRIDE
        RESETTING=RESETTING
    AA_PRD_DES_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        OVERRIDE=OVERRIDE
    AA_PRD_DES_INTEREST__ON_ACTIVITY__RECALCULATE : mml:string
        NOTHING=NOTHING
        PROFIT_AMOUNT=PROFIT.AMOUNT
        PROFIT_AMOUNT_AND_RATE=PROFIT.AMOUNT.AND.RATE
        RATE=RATE
    AA_PRD_DES_INTEREST__PACKAGE_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_DES_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_DES_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD : mml:string
        CAPITALISE=CAPITALISE
        DEFER=DEFER
        DUE=DUE
    AA_PRD_DES_INTEREST__PR_ATTRIBUTE__PR_BRK_RES : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        INFORMATION=INFORMATION
        OVERRIDE=OVERRIDE
    AA_PRD_DES_INTEREST__RATE_TIER_TYPE : mml:string
        BAND=BAND
        LEVEL=LEVEL
        SINGLE=SINGLE
    AA_PRD_DES_INTEREST__RATE_TYPE : mml:string
        FLAT_AMOUNT=FLAT.AMOUNT
        FLAT_RATE=FLAT.RATE
        REDUCING_RATE=REDUCING.RATE
    AA_PRD_DES_INTEREST__REFER_LIMIT : mml:string
        NO=NO
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__REGIONAL_PRICING_LEVEL : mml:string
        AREA=AREA
        BRANCH=BRANCH
        DIVISION=DIVISION
        REGION=REGION
        STATE=STATE
    AA_PRD_DES_INTEREST__REGIONAL_PRICING_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_DES_INTEREST__REGIONAL_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_DES_INTEREST__RELATIONSHIP_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_DES_INTEREST__RELATIONSHIP_VARIATION : mml:string
        A=A
        AA=AA
        AAA=AAA
        B=B
        BRANCH=BRANCH
        BRONZE=BRONZE
        C=C
        GOLD=GOLD
        INTERNET=INTERNET
        INT_ON_MA=INT.ON.MA
        MOBILE=MOBILE
        NO_INT_ON_MA=NO.INT.ON.MA
        OD_ON_MA=OD.ON.MA
        PLATINUM=PLATINUM
        PREFERENTIAL=PREFERENTIAL
        REGULAR=REGULAR
        SENIORCITIZENS=SENIORCITIZENS
        SILVER=SILVER
        STAFF=STAFF
    AA_PRD_DES_INTEREST__RUNTIME_RATE_CALC : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__SUPPRESS_ACCRUAL : mml:string
        ALTERNATE=ALTERNATE
        INFO_ONLY=INFO.ONLY
        YES=YES
    AA_PRD_DES_INTEREST__USER_CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__USER_LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_DES_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_DES_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_DES_INTEREST__USER_PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_DES_INTEREST__USER_PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_DES_INTEREST__USER_RATE_TYPE : mml:string
        CUSTOM=CUSTOM
        FIXED=FIXED
        FLOATING=FLOATING
        LINKED=LINKED
        PERIODIC=PERIODIC
    AA_PRD_DES_INTEREST__USE_PRICING_GRID : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__ACCOUNTING_MODE : mml:string
        ADVANCE=ADVANCE
        UPFRONT_PROFIT=UPFRONT.PROFIT
    AA_PRD_PRF_INTEREST__ADJUST_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_PRF_INTEREST__ADJUST_OPTION : mml:string
        ADJUST=ADJUST
        REVERSE=REVERSE
    AA_PRD_PRF_INTEREST__ADJUST_REASON : mml:string
        IRA_DEATH_DIST=IRA.DEATH.DIST
        IRA_DIST_STATUS=IRA.DIST.STATUS
        IRA_USER_WAIVE=IRA.USER.WAIVE
        RETAIN_CUSTOMERS_BUSINESS=RETAIN.CUSTOMERS.BUSINESS
        VALUED_CUSTOMER=VALUED.CUSTOMER
    AA_PRD_PRF_INTEREST__ADJUST_TYPE : mml:string
        ADJUST=ADJUST
        OVERRIDE=OVERRIDE
    AA_PRD_PRF_INTEREST__COMPOUND_YIELD_METHOD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_PRD_PRF_INTEREST__DATE_CONVENTION : mml:string
        ARRANGEMENT=ARRANGEMENT
        NULL=NULL
        PERIODIC_INTEREST="PERIODIC INTEREST"
    AA_PRD_PRF_INTEREST__DEFAULT_ATTR_OPTION : mml:string
        NON__RESETTING=NON-RESETTING
        RESETTING=RESETTING
    AA_PRD_PRF_INTEREST__DEFAULT_NEGOTIABLE : mml:string
        NO=NO
        YES=YES
    AA_PRD_PRF_INTEREST__FIXED_RATE__CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__FIXED_RATE__LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_PRF_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_PRF_INTEREST__FIXED_RATE__PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_PRF_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_PRF_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_PRF_INTEREST__INTEREST_METHOD : mml:string
        FIXED=FIXED
    AA_PRD_PRF_INTEREST__INTERNAL_BOOKING : mml:string
        YES=YES
    AA_PRD_PRF_INTEREST__MIN_INT_WAIVE : mml:string
        YES=YES
    AA_PRD_PRF_INTEREST__NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS : mml:string
        COUNT=COUNT
        FIX__VALUE=FIX-VALUE
        MANDATORY=MANDATORY
        NEGOTIABLE=NEGOTIABLE
        NON__NEGOTIABLE=NON-NEGOTIABLE
        NON__RESETTING=NON-RESETTING
        OVERRIDE=OVERRIDE
        RESETTING=RESETTING
    AA_PRD_PRF_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        OVERRIDE=OVERRIDE
    AA_PRD_PRF_INTEREST__ON_ACTIVITY__RECALCULATE : mml:string
        NOTHING=NOTHING
        PROFIT_AMOUNT=PROFIT.AMOUNT
        PROFIT_AMOUNT_AND_RATE=PROFIT.AMOUNT.AND.RATE
        RATE=RATE
    AA_PRD_PRF_INTEREST__PACKAGE_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_PRF_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_PRF_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD : mml:string
        CAPITALISE=CAPITALISE
        DEFER=DEFER
        DUE=DUE
    AA_PRD_PRF_INTEREST__PR_ATTRIBUTE__PR_BRK_RES : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        INFORMATION=INFORMATION
        OVERRIDE=OVERRIDE
    AA_PRD_PRF_INTEREST__RATE_TIER_TYPE : mml:string
        BAND=BAND
        LEVEL=LEVEL
        SINGLE=SINGLE
    AA_PRD_PRF_INTEREST__RATE_TYPE : mml:string
        FLAT_AMOUNT=FLAT.AMOUNT
        FLAT_RATE=FLAT.RATE
        REDUCING_RATE=REDUCING.RATE
    AA_PRD_PRF_INTEREST__REFER_LIMIT : mml:string
        NO=NO
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__REGIONAL_PRICING_LEVEL : mml:string
        AREA=AREA
        BRANCH=BRANCH
        DIVISION=DIVISION
        REGION=REGION
        STATE=STATE
    AA_PRD_PRF_INTEREST__REGIONAL_PRICING_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_PRF_INTEREST__REGIONAL_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_PRD_PRF_INTEREST__RELATIONSHIP_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_PRD_PRF_INTEREST__RELATIONSHIP_VARIATION : mml:string
        A=A
        AA=AA
        AAA=AAA
        B=B
        BRANCH=BRANCH
        BRONZE=BRONZE
        C=C
        GOLD=GOLD
        INTERNET=INTERNET
        INT_ON_MA=INT.ON.MA
        MOBILE=MOBILE
        NO_INT_ON_MA=NO.INT.ON.MA
        OD_ON_MA=OD.ON.MA
        PLATINUM=PLATINUM
        PREFERENTIAL=PREFERENTIAL
        REGULAR=REGULAR
        SENIORCITIZENS=SENIORCITIZENS
        SILVER=SILVER
        STAFF=STAFF
    AA_PRD_PRF_INTEREST__RUNTIME_RATE_CALC : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__SUPPRESS_ACCRUAL : mml:string
        ALTERNATE=ALTERNATE
        INFO_ONLY=INFO.ONLY
        YES=YES
    AA_PRD_PRF_INTEREST__USER_CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__USER_LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_PRD_PRF_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_PRD_PRF_INTEREST__USER_PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_PRD_PRF_INTEREST__USER_PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_PRD_PRF_INTEREST__USER_RATE_TYPE : mml:string
        CUSTOM=CUSTOM
        FIXED=FIXED
        FLOATING=FLOATING
        LINKED=LINKED
        PERIODIC=PERIODIC
    AA_PRD_PRF_INTEREST__USE_PRICING_GRID : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__ACCOUNTING_MODE : mml:string
        ADVANCE=ADVANCE
        UPFRONT_PROFIT=UPFRONT.PROFIT
    AA_SIM_INTEREST__ADJUST_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_SIM_INTEREST__ADJUST_OPTION : mml:string
        ADJUST=ADJUST
        REVERSE=REVERSE
    AA_SIM_INTEREST__ADJUST_REASON : mml:string
        IRA_DEATH_DIST=IRA.DEATH.DIST
        IRA_DIST_STATUS=IRA.DIST.STATUS
        IRA_USER_WAIVE=IRA.USER.WAIVE
        RETAIN_CUSTOMERS_BUSINESS=RETAIN.CUSTOMERS.BUSINESS
        VALUED_CUSTOMER=VALUED.CUSTOMER
    AA_SIM_INTEREST__ADJUST_TYPE : mml:string
        ADJUST=ADJUST
        OVERRIDE=OVERRIDE
    AA_SIM_INTEREST__COMPOUND_YIELD_METHOD : mml:string
        NULL=NULL
        YIELD=YIELD
    AA_SIM_INTEREST__DATE_CONVENTION : mml:string
        ARRANGEMENT=ARRANGEMENT
        NULL=NULL
        PERIODIC_INTEREST="PERIODIC INTEREST"
    AA_SIM_INTEREST__DEFAULT_ATTR_OPTION : mml:string
        NON__RESETTING=NON-RESETTING
        RESETTING=RESETTING
    AA_SIM_INTEREST__DEFAULT_NEGOTIABLE : mml:string
        NO=NO
        YES=YES
    AA_SIM_INTEREST__FIXED_RATE__CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__FIXED_RATE__LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_SIM_INTEREST__FIXED_RATE__MARGIN_TYPE__MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_SIM_INTEREST__FIXED_RATE__PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_SIM_INTEREST__FIXED_RATE__PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_SIM_INTEREST__FIXED_RATE__TIER_NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_SIM_INTEREST__INTEREST_METHOD : mml:string
        FIXED=FIXED
    AA_SIM_INTEREST__INTERNAL_BOOKING : mml:string
        YES=YES
    AA_SIM_INTEREST__MIN_INT_WAIVE : mml:string
        YES=YES
    AA_SIM_INTEREST__NEGATIVE_RATE : mml:string
        BLOCK_MARGIN=BLOCK.MARGIN
        FLOOR_MARGIN=FLOOR.MARGIN
        NO=NO
        YES=YES
    AA_SIM_INTEREST__NR_ATTRIBUTE__NR_OPTIONS__NR_OPTIONS : mml:string
        COUNT=COUNT
        FIX__VALUE=FIX-VALUE
        MANDATORY=MANDATORY
        NEGOTIABLE=NEGOTIABLE
        NON__NEGOTIABLE=NON-NEGOTIABLE
        NON__RESETTING=NON-RESETTING
        OVERRIDE=OVERRIDE
        RESETTING=RESETTING
    AA_SIM_INTEREST__NR_ATTRIBUTE__NR_TYPE__NR_MESSAGE : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        OVERRIDE=OVERRIDE
    AA_SIM_INTEREST__ON_ACTIVITY__RECALCULATE : mml:string
        NOTHING=NOTHING
        PROFIT_AMOUNT=PROFIT.AMOUNT
        PROFIT_AMOUNT_AND_RATE=PROFIT.AMOUNT.AND.RATE
        RATE=RATE
    AA_SIM_INTEREST__PACKAGE_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_SIM_INTEREST__PRICING_RULES_BENEFIT__PRICING_RULES_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_SIM_INTEREST__PR_ATTRIBUTE__PR_APP_METHOD : mml:string
        CAPITALISE=CAPITALISE
        DEFER=DEFER
        DUE=DUE
    AA_SIM_INTEREST__PR_ATTRIBUTE__PR_BRK_RES : mml:string
        CAP=CAP
        ERROR=ERROR
        FLOOR=FLOOR
        INFORMATION=INFORMATION
        OVERRIDE=OVERRIDE
    AA_SIM_INTEREST__RATE_TIER_TYPE : mml:string
        BAND=BAND
        LEVEL=LEVEL
        SINGLE=SINGLE
    AA_SIM_INTEREST__RATE_TYPE : mml:string
        FLAT_AMOUNT=FLAT.AMOUNT
        FLAT_RATE=FLAT.RATE
        REDUCING_RATE=REDUCING.RATE
    AA_SIM_INTEREST__REFER_LIMIT : mml:string
        NO=NO
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__REGIONAL_PRICING_LEVEL : mml:string
        AREA=AREA
        BRANCH=BRANCH
        DIVISION=DIVISION
        REGION=REGION
        STATE=STATE
    AA_SIM_INTEREST__REGIONAL_PRICING_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_SIM_INTEREST__REGIONAL_PROPERTY_CONTROL : mml:string
        MERGE=MERGE
        REPLACE=REPLACE
        SUPPRESS=SUPPRESS
    AA_SIM_INTEREST__RELATIONSHIP_OPERAND : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUBTRACT=SUBTRACT
    AA_SIM_INTEREST__RELATIONSHIP_VARIATION : mml:string
        A=A
        AA=AA
        AAA=AAA
        B=B
        BRANCH=BRANCH
        BRONZE=BRONZE
        C=C
        GOLD=GOLD
        INTERNET=INTERNET
        INT_ON_MA=INT.ON.MA
        MOBILE=MOBILE
        NO_INT_ON_MA=NO.INT.ON.MA
        OD_ON_MA=OD.ON.MA
        PLATINUM=PLATINUM
        PREFERENTIAL=PREFERENTIAL
        REGULAR=REGULAR
        SENIORCITIZENS=SENIORCITIZENS
        SILVER=SILVER
        STAFF=STAFF
    AA_SIM_INTEREST__RUNTIME_RATE_CALC : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__SUPPRESS_ACCRUAL : mml:string
        ALTERNATE=ALTERNATE
        INFO_ONLY=INFO.ONLY
        YES=YES
    AA_SIM_INTEREST__USER_CUSTOM_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__USER_LINKED_RATE : mml:string
        NULL=NULL
        YES=YES
    AA_SIM_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_OPER : mml:string
        ADD=ADD
        MULTIPLY=MULTIPLY
        SUB=SUB
    AA_SIM_INTEREST__USER_MARGIN_TYPE__USER_MARGIN_TYPE : mml:string
        0_125Round=0.125Round
        0_25Round=0.25Round
        0_5Round=0.5Round
        LOAN_SPREAD=LOAN.SPREAD
        SINGLE=SINGLE
    AA_SIM_INTEREST__USER_PERIODIC_METHOD : mml:string
        CLOSEST=CLOSEST
        INTERPOLATE=INTERPOLATE
        NEXT=NEXT
        PREVIOUS=PREVIOUS
    AA_SIM_INTEREST__USER_PERIODIC_PERIOD_TYPE : mml:string
        MATURITY=MATURITY
        NULL=NULL
        PERIODIC_PERIOD=PERIODIC.PERIOD
        RENEWAL=RENEWAL
        RESET_PERIOD=RESET.PERIOD
    AA_SIM_INTEREST__USER_RATE_TYPE : mml:string
        CUSTOM=CUSTOM
        FIXED=FIXED
        FLOATING=FLOATING
        LINKED=LINKED
        PERIODIC=PERIODIC
    AA_SIM_INTEREST__USE_PRICING_GRID : mml:string
        NULL=NULL
        YES=YES
}