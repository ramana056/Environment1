# UTF-8
/* Purchase Processing */
Domain IS_Purchase
    @t24:i (module=IS)
    @java:Package (value=com.temenos.t24.datamodel.is.is_purchase)
namespace "http://www.temenos.com/t24/IS_Purchase"
metamodelVersion 1.30.6
Classes {
/* IS.COST.TYPE is the table to define the various costs incurred during purchase of an asset.
All the possible additional cost incurred during the process of purchase is defined in this table. */
    IS_ARRANGEMENT_DETS
        @i18n:Description (en="")
        @t24:i (t24Name=IS.ARRANGEMENT.DETS, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        COMMODITY_SALE_REF -> byValue IS_Purchase:IS_ARRANGEMENT_DETS__COMMODITY_SALE_REF *
            @t24:i (t24Name=COMMODITY.SALE.REF)
        DISBURSE_REF -> byValue IS_Purchase:IS_ARRANGEMENT_DETS__DISBURSE_REF *
            @t24:i (t24Name=DISBURSE.REF)
        /* The Settlement reference through which the down payment is settled ass repayment to the Finance contract. The settlement of down payment is done through FT screen as AA Repayment. */
        DP_SETTLE_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=7.0, core=true, t24Name=DP.SETTLE.REF, mandatory=false)
        /* ID of the file IS.ARRANGEMENT.DETS is the AA.ARRANGEMENT key. When a AA finance contract is created, this file gets updated during authorization. */
        IS_ARRANGEMENT_DETS_ID -> IS_Purchase:IS_ARRANGEMENT_DETS PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.ARRANGEMENT.DETS.ID, mandatory=true)
        /* The IS product reference for which AA finance is created. The field is updated with a record key of the application IS.PARAMETER */
        IS_PRODUCT -> IS_Config:IS_PARAMETER
            @t24:i (align=R, inputBehaviour=I, maxLength=5, sysNumber=2.0, core=true, t24Name=IS.PRODUCT, mandatory=false)
        /* The contract reference for which a finance is created. The field is updated with a record key of the application IS.CONTRACT */
        PURCHASE_REF -> IS_Purchase:IS_CONTRACT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, businessType=A, t24Name=PURCHASE.REF, mandatory=false)
        /* The contract reference for which the WAKALA(AA) contract is related to IS.CONTRACT. The field is updated with a record key of the application IS.CONTRACT */
        WAK_PURCHASE_REF -> IS_Purchase:IS_CONTRACT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=3.0, core=true, businessType=A, t24Name=WAK.PURCHASE.REF, mandatory=false)
    }
    IS_ARRANGEMENT_DETS__COMMODITY_SALE_REF
    {
/* The list of Commodity Sale reference as captured in the application IS.COMMODITY.SALE. This Sale declaration for differential pricing is captured in this record.

The finance is created for the list of commodity sale records are captured in this field */
        COMMODITY_SALE_REF -> IS_Purchase:IS_COMMODITY_SALE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=4.0, core=true, businessType=A, t24Name=COMMODITY.SALE.REF, mandatory=false)
    }
    IS_ARRANGEMENT_DETS__DISBURSE_REF
    {
        /* Reserved for Future use. */
        DISBURSE_REF -> IS_Payment:IS_DISBURSEMENT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=5.0, core=true, businessType=A, t24Name=DISBURSE.REF, mandatory=false)
        /* Reserved for Future use. */
        DISB_ACTIVITY_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=6.0, core=true, t24Name=DISB.ACTIVITY.REF, mandatory=false)
    }
/* IS.ASSET.REVIEW is the application to capture the survey and appraiser details of the asset being purchased.
The information like date visited, the result of the survey, appraiser fees are captured in this table. */
    IS_ASSET_REVIEW
        @i18n:Description (en="")
        @t24:i (t24Name=IS.ASSET.REVIEW, level=F, type=H, allowedFunctions="A C D E H I L P Q R S", localRefApplicationSysNo=37.0, localRefAllowed=true)
    {
        /* The amount that the appraiser has valuated on the asset. */
        APPRAISER_VALUE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=20, sysNumber=14.0, core=true, t24Name=APPRAISER.VALUE, mandatory=false)
/* The asset for which the review is performed. The asset should one of the assets required in the contract through the application IS.CONTRACT

Validation Rules:
1. The asset must be purchased in the Contract. */
        ASSET_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=4.0, core=true, t24Name=ASSET.REF, mandatory=false)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=47.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=48.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=44.0, core=true, t24Name=AUTHORISER, mandatory=false)
/* Bank's contribution for the Appraiser fees.

Validation Rules:
1. Bank contribution and Customer contribution should sum to the value in the field VALUATION.FEES */
        BANK_SHARE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=19, sysNumber=19.0, core=true, t24Name=BANK.SHARE, mandatory=false)
/* Commodity of the purchase contract that is being reviewed. The commodity should be purchased in the contract through the application IS.CONTRACT.

Validation Rules:
1. The commodity must be purchased in the Contract. */
        COMMODITY -> IS_Config:IS_COMMODITY
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, t24Name=COMMODITY, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=45.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
/* Currency of the Purchase contract.

Validation Rules:
1. Defaulted from the IS.CONTRACT record.
2. Valid record from CURRENCY table */
        CURRENCY -> ST_CurrencyConfig:CURRENCY
            @i18n:Label (en=Ccy)
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=5.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=41.0, core=true, t24Name=CURR.NO, mandatory=false)
/* The Customer who requests the asset and the contract is booked for the same.

Validation Rules:
1. Field Mandatory and No Change.
2. Must be a valid record in the table CUSTOMER. */
        CUSTOMER -> ST_Customer:CUSTOMER required
            @i18n:Label (en=Customer)
            @i18n:Tooltip (en="Customer Liability")
            @t24:i (align=R, inputBehaviour=F, maxLength=10, sysNumber=1.0, core=true, businessType=CUS, t24Name=CUSTOMER, mandatory=true)
/* The Customer account recorded in the Asset Request Contract.

The Customer contribution is collected from the account given in this field. The account number is defaulted from the Asset Request contract

Validation Rules:
1. Must be a valid record in the table ACCOUNT.
2. Account must belong to the Contract Customer.
3. NOSTRO or VOSTRO account not allowed. */
        CUSTOMER_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=8.0, core=true, businessType=ANT, t24Name=CUSTOMER.ACCT, mandatory=false)
/* Customer's contribution for the Appraiser fees.

Validation Rules:
1. Bank contribution and Customer contribution should sum to the value in the field VALUATION.FEES */
        CUST_SHARE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=F, maxLength=19, sysNumber=20.0, core=true, t24Name=CUST.SHARE, mandatory=false)
        DATE_TIME -> byValue IS_Purchase:IS_ASSET_REVIEW__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=46.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue IS_Purchase:IS_ASSET_REVIEW__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        INPUTTER -> byValue IS_Purchase:IS_ASSET_REVIEW__INPUTTER *
            @t24:i (t24Name=INPUTTER)
        /* The ID of the application IS.ASSET.REVIEW is an auto-generated ID. The same contract is used to capture Review details carried out for the asset or commodity. */
        IS_ASSET_REQUEST_ID -> IS_Purchase:IS_ASSET_REVIEW PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.ASSET.REQUEST.ID, mandatory=true)
/* The Next visit date of the Review planned.

Validation Rules:
1. Standard T24 Date field.
2. Date should be greater than TODAY. */
        NEXT_REVIEW_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=21.0, core=true, t24Name=NEXT.REVIEW.DATE, mandatory=false)
/* Any comments provided by the appraiser during the survey is recorded in this field.

Validation Rules:
1. Standard T24 Alphanumeric field. */
        NOTES : T24BusinessTypes:A
            @i18n:Label (en=Notes)
            @t24:i (align=L, inputBehaviour=I, maxLength=50, sysNumber=17.0, core=true, t24Name=NOTES, mandatory=false)
        OVERRIDE -> byValue IS_Purchase:IS_ASSET_REVIEW__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
/* The Progress percentage of the asset (say Building Construction) being reviewed. It will be a user-defined value fed by the user based on the appraisal details.

Validation Rules:
1. Accepts a Valid Percentage. */
        PROGRESS_PERCENT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=15.0, core=true, t24Name=PROGRESS.PERCENT, mandatory=false)
/* The Progress status of the Appraisal whether the asset manufacture/ building construction is in Schedule, Late, etc.

Validation Rules:
1. The valid values are On Schedule, Late, Ahead of Schedule. */
        PROJECT_STATUS : IS_Purchase:IS_ASSET_REVIEW__PROJECT_STATUS
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=16.0, core=true, t24Name=PROJECT.STATUS, mandatory=false)
/* The Contract through which the Customer has initiated the Asset Request.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record from the table IS.CONTRACT. */
        PURCHASE_REF -> IS_Purchase:IS_CONTRACT required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, businessType=A, t24Name=PURCHASE.REF, mandatory=true)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=40.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
        RESERVED_1 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=36.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_10 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=27.0, core=true, t24Name=RESERVED.10, mandatory=false)
        RESERVED_11 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=26.0, core=true, t24Name=RESERVED.11, mandatory=false)
        RESERVED_12 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=25.0, core=true, t24Name=RESERVED.12, mandatory=false)
        RESERVED_13 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=24.0, core=true, t24Name=RESERVED.13, mandatory=false)
        RESERVED_14 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=23.0, core=true, t24Name=RESERVED.14, mandatory=false)
        RESERVED_15 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=22.0, core=true, t24Name=RESERVED.15, mandatory=false)
        RESERVED_2 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=35.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=34.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=33.0, core=true, t24Name=RESERVED.4, mandatory=false)
        RESERVED_5 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=32.0, core=true, t24Name=RESERVED.5, mandatory=false)
        RESERVED_6 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=31.0, core=true, t24Name=RESERVED.6, mandatory=false)
        RESERVED_7 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=30.0, core=true, t24Name=RESERVED.7, mandatory=false)
        RESERVED_8 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=29.0, core=true, t24Name=RESERVED.8, mandatory=false)
        RESERVED_9 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=28.0, core=true, t24Name=RESERVED.9, mandatory=false)
/* The Appraiser or Surveyor who reviews the asset.

Validation Rules:
1. Valid record in the table IS.REVIEWER.
2. Field Mandatory. */
        REVIEWER -> IS_Config:IS_REVIEWER required
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=9.0, core=true, businessType=CUS, t24Name=REVIEWER, mandatory=true)
/* Account of the Reviewer in the logged-in Company and Contract Currency. This account number is defaulted from the parameterization of IS.REVIEWER record.

Validation Rules:
1. Must be a valid record in the table ACCOUNT.
2. Account must belong to the Reviewer Customer.
3. NOSTRO or VOSTRO account not allowed. */
        REVIEWER_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=12.0, core=true, businessType=ANT, t24Name=REVIEWER.ACCT, mandatory=false)
/* Name of the Appraiser. Defaulted as Name of the Customer (@ID being Customer Reference). It can be over-written to specify User-Defined names.

Validation Rules:
1. Defaulted as NAME.1 from the Customer table.
2. Standard T24 Alphanumeric field. */
        REVIEWER_NAME : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=10.0, core=true, t24Name=REVIEWER.NAME, mandatory=false)
/* The Type of review the reviewer will perform.

Validation Rules:
1. Field No-input.
2. The value to be defaulted from "TYPE" field of the IS.REVIEWER table. */
        REVIEWER_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=11.0, core=true, t24Name=REVIEWER.TYPE, mandatory=false)
/* The Visit Date of the appraiser. This is the date on which the Appraiser visits and records review status of the Asset.

Validation Rules:
1. Standard T24 Date field. */
        REVIEW_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=13.0, core=true, t24Name=REVIEW.DATE, mandatory=false)
        STMT_NOS -> byValue IS_Purchase:IS_ASSET_REVIEW__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Valuation Fees to be paid to the Appraiser.

Validation Rules:
1. Field Mandatory.
2. Must be a non-negative value.
3. Bank contribution and Customer contribution should sum to the value in the field VALUATION.FEES */
        VALUATION_FEES : T24BusinessTypes:AMT required
            @t24:i (align=R, inputBehaviour=F, maxLength=19, sysNumber=18.0, core=true, t24Name=VALUATION.FEES, mandatory=true)
/* The date that the valuation is effective.

Validation Rules:
1. Standard T24 Date field. */
        VALUE_DATE : T24BusinessTypes:D
            @i18n:Label (en="Value Date")
            @i18n:Tooltip (en="Indicates the Value date of the Contract")
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=6.0, core=true, t24Name=VALUE.DATE, mandatory=false)
    }
    IS_ASSET_REVIEW__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=43.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    IS_ASSET_REVIEW__DESCRIPTION
    {
/* Captures the Description of the Review.

Validation Rules:
1. Standard T24 Alphanumeric field. */
        DESCRIPTION : T24BusinessTypes:A
            @i18n:Label (en=Description)
            @i18n:Tooltip (en=Description)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=7.0, core=true, t24Name=DESCRIPTION, mandatory=false)
    }
    IS_ASSET_REVIEW__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=42.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    IS_ASSET_REVIEW__OVERRIDE
    {
/* Contains all the override messages which the user has confirmed during input.
During Commit, the system may generate a series of screen override messages waiting for User Approval. If the record is subsequently displayed or printed, all override messages are shown in this field.

Validation Rules:
System generated. Multi-value field. */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=39.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    IS_ASSET_REVIEW__STMT_NOS
    {
/* List of Statement Entries generated as the result of Accounting.

The Accounting entries are raised for the Bank Contribution Amount. */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=38.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    IS_COMMODITY_LIMIT_POSITION
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.LIMIT.POSITION, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        BUY_BROKER -> IS_Config:IS_BROKER
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, t24Name=BUY.BROKER, mandatory=false)
        COMMODITY -> IS_Config:IS_COMMODITY
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, t24Name=COMMODITY, mandatory=false)
        CURRENCY -> ST_CurrencyConfig:CURRENCY
            @i18n:Label (en=Ccy)
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=false)
        DAILY_ALLWD_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=5.0, core=true, t24Name=DAILY.ALLWD.AMT, typeModifiers="3", mandatory=false)
        DAILY_ALLWD_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=4.0, core=true, t24Name=DAILY.ALLWD.QTY, mandatory=false)
        DAILY_AVL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=DAILY.AVL.AMT, typeModifiers="3", mandatory=false)
        DAILY_AVL_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=8.0, core=true, t24Name=DAILY.AVL.QTY, mandatory=false)
        DAILY_BUY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=11.0, core=true, t24Name=DAILY.BUY.AMT, typeModifiers="3", mandatory=false)
        DAILY_BUY_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=10.0, core=true, t24Name=DAILY.BUY.QTY, mandatory=false)
        DAILY_RET_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=13.0, core=true, t24Name=DAILY.RET.AMT, typeModifiers="3", mandatory=false)
        DAILY_RET_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=12.0, core=true, t24Name=DAILY.RET.QTY, mandatory=false)
        DAILY_REV_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=15.0, core=true, t24Name=DAILY.REV.AMT, typeModifiers="3", mandatory=false)
        DAILY_REV_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=14.0, core=true, t24Name=DAILY.REV.QTY, mandatory=false)
        DAILY_USED_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=7.0, core=true, t24Name=DAILY.USED.AMT, typeModifiers="3", mandatory=false)
        DAILY_USED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=6.0, core=true, t24Name=DAILY.USED.QTY, mandatory=false)
        IS_COMMODITY_LIMIT_POSITION_ID -> IS_Purchase:IS_COMMODITY_LIMIT_POSITION PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.LIMIT.POSITION.ID, mandatory=true)
    }
    IS_COMMODITY_LIMIT_POSITION_HIST
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.LIMIT.POSITION.HIST, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        BUY_BROKER -> IS_Config:IS_BROKER
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, t24Name=BUY.BROKER, mandatory=false)
        COMMODITY -> IS_Config:IS_COMMODITY
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, t24Name=COMMODITY, mandatory=false)
        CURRENCY -> ST_CurrencyConfig:CURRENCY
            @i18n:Label (en=Ccy)
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=3.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=false)
        DAILY_ALLWD_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=5.0, core=true, t24Name=DAILY.ALLWD.AMT, typeModifiers="3", mandatory=false)
        DAILY_ALLWD_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=4.0, core=true, t24Name=DAILY.ALLWD.QTY, mandatory=false)
        DAILY_AVL_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=DAILY.AVL.AMT, typeModifiers="3", mandatory=false)
        DAILY_AVL_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=8.0, core=true, t24Name=DAILY.AVL.QTY, mandatory=false)
        DAILY_BUY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=11.0, core=true, t24Name=DAILY.BUY.AMT, typeModifiers="3", mandatory=false)
        DAILY_BUY_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=10.0, core=true, t24Name=DAILY.BUY.QTY, mandatory=false)
        DAILY_RET_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=13.0, core=true, t24Name=DAILY.RET.AMT, typeModifiers="3", mandatory=false)
        DAILY_RET_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=12.0, core=true, t24Name=DAILY.RET.QTY, mandatory=false)
        DAILY_REV_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=15.0, core=true, t24Name=DAILY.REV.AMT, typeModifiers="3", mandatory=false)
        DAILY_REV_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=14.0, core=true, t24Name=DAILY.REV.QTY, mandatory=false)
        DAILY_USED_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=7.0, core=true, t24Name=DAILY.USED.AMT, typeModifiers="3", mandatory=false)
        DAILY_USED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=6.0, core=true, t24Name=DAILY.USED.QTY, mandatory=false)
        IS_COMMODITY_LIMIT_POSITION_HIST_ID -> IS_Purchase:IS_COMMODITY_LIMIT_POSITION_HIST PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.LIMIT.POSITION.HIST.ID, mandatory=true)
    }
    IS_COMMODITY_LIMIT_TXNS
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.LIMIT.TXNS, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        IS_COMMODITY_LIMIT_TXNS_ID -> IS_Purchase:IS_COMMODITY_LIMIT_TXNS PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.LIMIT.TXNS.ID, mandatory=true)
        IS_CON_REF -> byValue IS_Purchase:IS_COMMODITY_LIMIT_TXNS__IS_CON_REF *
            @t24:i (t24Name=IS.CON.REF)
    }
    IS_COMMODITY_LIMIT_TXNS_HIST
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.LIMIT.TXNS.HIST, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        IS_COMMODITY_LIMIT_TXNS_HIST_ID -> IS_Purchase:IS_COMMODITY_LIMIT_TXNS_HIST PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.LIMIT.TXNS.HIST.ID, mandatory=true)
        IS_CON_REF -> byValue IS_Purchase:IS_COMMODITY_LIMIT_TXNS_HIST__IS_CON_REF *
            @t24:i (t24Name=IS.CON.REF)
    }
    IS_COMMODITY_LIMIT_TXNS_HIST__IS_CON_REF
    {
        BUY_AMT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=4.0, core=true, t24Name=BUY.AMT, mandatory=false)
        BUY_BROKER : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, t24Name=BUY.BROKER, mandatory=false)
        BUY_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=3.0, core=true, t24Name=BUY.QTY, mandatory=false)
        IS_CON_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, t24Name=IS.CON.REF, mandatory=false)
        PUR_REV_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=10.0, core=true, t24Name=PUR.REV.DATE, mandatory=false)
        PUR_VAL_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=PUR.VAL.DATE, mandatory=false)
        REC_STATUS : T24BusinessTypes:A
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=11.0, core=true, t24Name=REC.STATUS, mandatory=false)
        RET_AMT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=7.0, core=true, t24Name=RET.AMT, mandatory=false)
        RET_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=6.0, core=true, t24Name=RET.QTY, mandatory=false)
        RET_VAL_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=RET.VAL.DATE, mandatory=false)
        SELL_RET_BROKER : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=9.0, core=true, t24Name=SELL.RET.BROKER, mandatory=false)
    }
    IS_COMMODITY_LIMIT_TXNS__IS_CON_REF
    {
        BUY_AMT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=4.0, core=true, t24Name=BUY.AMT, mandatory=false)
        BUY_BROKER : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, t24Name=BUY.BROKER, mandatory=false)
        BUY_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=3.0, core=true, t24Name=BUY.QTY, mandatory=false)
        IS_CON_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, t24Name=IS.CON.REF, mandatory=false)
        PUR_REV_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=10.0, core=true, t24Name=PUR.REV.DATE, mandatory=false)
        PUR_VAL_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=PUR.VAL.DATE, mandatory=false)
        REC_STATUS : T24BusinessTypes:A
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=11.0, core=true, t24Name=REC.STATUS, mandatory=false)
        RET_AMT : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=7.0, core=true, t24Name=RET.AMT, mandatory=false)
        RET_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=6.0, core=true, t24Name=RET.QTY, mandatory=false)
        RET_VAL_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=RET.VAL.DATE, mandatory=false)
        SELL_RET_BROKER : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=9.0, core=true, t24Name=SELL.RET.BROKER, mandatory=false)
    }
    /* The table IS.COMMODITY.POSITION is a record of complete delivery of the Asset or Commodity as captured in the application IS.CONTRACT.DELIVERY. */
    IS_COMMODITY_POSITION
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.POSITION, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
    {
        /* The Asset for which the delivery of goods is being tracked. */
        ASSET_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=3.0, core=true, t24Name=ASSET.REF, mandatory=false)
        /* The quantity available for sale. This quantity increases with the delivery and decreases with the reversal or sale. */
        AVAILABLE_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=7.0, core=true, t24Name=AVAILABLE.QTY, mandatory=false)
        /* The Commodity for which the delivery of goods is being tracked. */
        COMMODITY -> IS_Config:IS_COMMODITY
            @t24:i (align=R, inputBehaviour=I, maxLength=35, sysNumber=2.0, core=true, t24Name=COMMODITY, mandatory=false)
        DELIVERED_DATE -> byValue IS_Purchase:IS_COMMODITY_POSITION__DELIVERED_DATE *
            @t24:i (t24Name=DELIVERED.DATE)
/* ID of the table IS.COMMODITY.POSITION.
Id format:
1. <PURCHASE.ID> - <COMMODITY> - for Quantified Commodities
2. <PURCHASE.ID> - <COMMODITY> - <ASSET.ID> - for Named Quantities */
        IS_COMMODITY_POSITION_ID -> IS_Purchase:IS_COMMODITY_POSITION PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.POSITION.ID, mandatory=true)
        /* The quantity of Asset or Commodity purchased in the Contract. */
        PURCHASE_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=4.0, core=true, t24Name=PURCHASE.QTY, mandatory=false)
        /* The Contract Reference of the Asset through which the Asset is requested and purchased. */
        PURCHASE_REF -> IS_Purchase:IS_CONTRACT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=1.0, core=true, businessType=A, t24Name=PURCHASE.REF, mandatory=false)
        /* The quantity of Asset that are reversed out of the delivered quantity. When the goods delivered are reversed, the quantity available for sale also goes down with the same count. */
        REVERSED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=8.0, core=true, t24Name=REVERSED.QTY, mandatory=false)
        SALE_DATE -> byValue IS_Purchase:IS_COMMODITY_POSITION__SALE_DATE *
            @t24:i (t24Name=SALE.DATE)
        /* The total quantity of Asset or Commodity delivered out of the total purchase quantity. */
        TOT_DELIVERED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=5.0, core=true, t24Name=TOT.DELIVERED.QTY, mandatory=false)
        /* The total quantity of Asset or Commodity sold out of the total delivered quantity. */
        TOT_SOLD_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=TOT.SOLD.QTY, mandatory=false)
    }
    IS_COMMODITY_POSITION__DELIVERED_DATE
    {
        /* The date on which certain quantity of Asset or Commodity has been delivered. */
        DELIVERED_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=9.0, core=true, t24Name=DELIVERED.DATE, mandatory=false)
        DELIVERED_QTY -> byValue IS_Purchase:IS_COMMODITY_POSITION__DELIVERED_DATE__DELIVERED_QTY *
            @t24:i (t24Name=DELIVERED.QTY)
        RESERVED_10 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=13.0, core=true, t24Name=RESERVED.10, mandatory=false)
        RESERVED_11 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=12.0, core=true, t24Name=RESERVED.11, mandatory=false)
        RESERVED_12 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=11.0, core=true, t24Name=RESERVED.12, mandatory=false)
        RESERVED_8 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=15.0, core=true, t24Name=RESERVED.8, mandatory=false)
        RESERVED_9 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=14.0, core=true, t24Name=RESERVED.9, mandatory=false)
    }
    IS_COMMODITY_POSITION__DELIVERED_DATE__DELIVERED_QTY
    {
        /* The quantity of Asset or Commodity delivered as on the Delivery Date. */
        DELIVERED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=DELIVERED.QTY, mandatory=false)
    }
    IS_COMMODITY_POSITION__SALE_DATE
    {
        DECLARATION_REF -> byValue IS_Purchase:IS_COMMODITY_POSITION__SALE_DATE__DECLARATION_REF *
            @t24:i (t24Name=DECLARATION.REF)
        /* The date on which certain quantity of Asset or Commodity has been sold. */
        SALE_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=16.0, core=true, t24Name=SALE.DATE, mandatory=false)
    }
    IS_COMMODITY_POSITION__SALE_DATE__DECLARATION_REF
    {
        /* The declaration reference as captured in the application IS.COMMODITY.SALE. This record will contain the actual selling price declared for the delivered Commodity or Asset. */
        DECLARATION_REF -> IS_Purchase:IS_COMMODITY_SALE
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=17.0, core=true, businessType=A, t24Name=DECLARATION.REF, mandatory=false)
        RESERVED_1 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=27.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_2 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=26.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=25.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=24.0, core=true, t24Name=RESERVED.4, mandatory=false)
        RESERVED_5 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=23.0, core=true, t24Name=RESERVED.5, mandatory=false)
        RESERVED_6 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=22.0, core=true, t24Name=RESERVED.6, mandatory=false)
        RESERVED_7 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=21.0, core=true, t24Name=RESERVED.7, mandatory=false)
        /* The Sale Reference through which certain quantity of Asset has been sold on the SOLD.DATE. */
        SALE_REFERENCE -> AA_Framework:AA_ARRANGEMENT
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=18.0, core=true, businessType=A, t24Name=SALE.REFERENCE, mandatory=false)
        /* Unit Price of the Asset or Commodity sold. */
        SOLD_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=SOLD.PRICE, mandatory=false)
        /* The quantity of Asset or Commodity sold as on the Sold Date. */
        SOLD_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=19.0, core=true, t24Name=SOLD.QTY, mandatory=false)
    }
/* IS.COMMODITY.SALE is the application to capture the Sale details of the Asset or Commodity that are sold by Delivery Tracking.

The goods that are delivered could be sold to any number of customers at any price. The price agreed for Sale is declared in this application.

The Profit or Expense arising out of the differential pricing is accounted through this application. */
    IS_COMMODITY_SALE
        @i18n:Header1 (en="Authorise Commodity/Asset Sale")
        @i18n:Description (en="")
        @t24:i (t24Name=IS.COMMODITY.SALE, level=F, type=H, allowedFunctions="A C D E H I L P Q R S", localRefApplicationSysNo=46.0, localRefAllowed=true)
    {
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=56.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=57.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=53.0, core=true, t24Name=AUTHORISER, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=54.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
/* The Currency in which the Asset Request contract is booked.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record in the table CURRENCY. */
        CURRENCY -> ST_CurrencyConfig:CURRENCY required
            @i18n:Label (en=Currency)
            @i18n:Tooltip (en=Currency)
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=2.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=true)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=50.0, core=true, t24Name=CURR.NO, mandatory=false)
/* The Customer for whom the Sale is placed with/without differential pricing.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record in the table CUSTOMER. */
        CUSTOMER -> ST_Customer:CUSTOMER required
            @i18n:Label (en=Customer)
            @i18n:Tooltip (en=Customer)
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=1.0, core=true, businessType=CUS, t24Name=CUSTOMER, mandatory=true)
        DATE_TIME -> byValue IS_Purchase:IS_COMMODITY_SALE__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=55.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        INPUTTER -> byValue IS_Purchase:IS_COMMODITY_SALE__INPUTTER *
            @t24:i (t24Name=INPUTTER)
        /* The ID of the application IS.COMMODITY.SALE is an auto-generated ID with the prefix 'CS'. */
        IS_COMMODITY_SALE_ID -> IS_Purchase:IS_COMMODITY_SALE PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.COMMODITY.SALE.ID, mandatory=true)
        OVERRIDE -> byValue IS_Purchase:IS_COMMODITY_SALE__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
/* The Islamic Product in which the asset request is processed.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record from the table IS.PARAMETER. */
        PRODUCT -> IS_Config:IS_PARAMETER required
            @i18n:Label (en=Product)
            @i18n:Tooltip (en=Product)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=8.0, core=true, t24Name=PRODUCT, mandatory=true)
        PURCHASE_REF -> byValue IS_Purchase:IS_COMMODITY_SALE__PURCHASE_REF *
            @t24:i (t24Name=PURCHASE.REF)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=49.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* The Commodity for which the goods are being delivered.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record from the table IS.COMMODITY. */
        REQ_COMMODITY -> IS_Config:IS_COMMODITY required
            @i18n:Label (en="Commodity in Request")
            @i18n:Tooltip (en="Commodity in Request")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=5.0, core=true, t24Name=REQ.COMMODITY, mandatory=true)
/* The method through which the commodity can be selected for Commodity Trading from the available commodity.

This field will opened during Bulk Trading Features are enhanced. */
        REQ_PRIORITY : IS_Purchase:IS_COMMODITY_SALE__REQ_PRIORITY
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=7.0, core=true, t24Name=REQ.PRIORITY, mandatory=false)
/* The quantity of the Commodity being sold.

Validation Rules:
1. Field Mandatory. */
        REQ_QUANTITY : T24BusinessTypes:R required
            @i18n:Label (en="Quantity in Request")
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=6.0, core=true, t24Name=REQ.QUANTITY, mandatory=true)
/* The type of request that the sale is placed for.

This field will opened during Bulk Trading Features are enhanced. */
        REQ_TYPE : IS_Purchase:IS_COMMODITY_SALE__REQ_TYPE
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=4.0, core=true, t24Name=REQ.TYPE, mandatory=false)
        RESERVED_1 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=45.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_10 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=36.0, core=true, t24Name=RESERVED.10, mandatory=false)
        RESERVED_11 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=35.0, core=true, t24Name=RESERVED.11, mandatory=false)
        RESERVED_12 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=34.0, core=true, t24Name=RESERVED.12, mandatory=false)
        RESERVED_13 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=33.0, core=true, t24Name=RESERVED.13, mandatory=false)
        RESERVED_14 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=32.0, core=true, t24Name=RESERVED.14, mandatory=false)
        RESERVED_15 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=31.0, core=true, t24Name=RESERVED.15, mandatory=false)
        RESERVED_2 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=44.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=43.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=42.0, core=true, t24Name=RESERVED.4, mandatory=false)
        RESERVED_5 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=41.0, core=true, t24Name=RESERVED.5, mandatory=false)
        RESERVED_6 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=40.0, core=true, t24Name=RESERVED.6, mandatory=false)
        RESERVED_7 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=39.0, core=true, t24Name=RESERVED.7, mandatory=false)
        RESERVED_8 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=38.0, core=true, t24Name=RESERVED.8, mandatory=false)
        RESERVED_9 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=37.0, core=true, t24Name=RESERVED.9, mandatory=false)
/* The AA arrangement reference is captured in this field when the contract is financed.

Validation Rules:
1. Field No-input. */
        SALE_REFERENCE -> AA_Framework:AA_ARRANGEMENT
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=30.0, core=true, businessType=A, t24Name=SALE.REFERENCE, mandatory=false)
        STMT_NOS -> byValue IS_Purchase:IS_COMMODITY_SALE__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Total Price of the Asset/commodity with UNIT.PRICE defined in the Asset Request contract.

Validation Rules:
1. Field No-input.
2. System defaults the amount that is sum of all the New Sale Prices, i.e., PURCHASE.PRICE */
        TOT_ORIG_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=27.0, core=true, t24Name=TOT.ORIG.PRICE, typeModifiers="2", mandatory=false)
/* The Total Sale Price which will be eligible for Finance. This is derived from the new Sale Prices declared.

Validation Rules:
1. Field No-input.
2. System defaults the amount that is sum of all the New Sale Prices, i.e., ASSET.SALE.PRICE */
        TOT_SALE_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=28.0, core=true, t24Name=TOT.SALE.PRICE, typeModifiers="2", mandatory=false)
/* The Total Number of quantities declared for Sale.

Validation Rules:
1. Field No-input. */
        TOT_SALE_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=26.0, core=true, t24Name=TOT.SALE.QTY, mandatory=false)
/* The Profit or Loss incurred as a result of declaration of the new Selling Price.

If the new Selling Price is same as the original price, then this field will be zero. No PL entry is raised.

Validation Rules:
1. Field No-input.
2. System defaults the difference of TOT.SALE.PRICE and TOT.ORIG.PRICE. */
        TXN_PL_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=29.0, core=true, t24Name=TXN.PL.AMOUNT, typeModifiers="2", mandatory=false)
/* Value Date on which the Sale Contract is agreed.

Validation Rules:
1. Standard T24 Date field.
2. Cannot be future dated. */
        VALUE_DATE : T24BusinessTypes:D required
            @i18n:Label (en="Value Date")
            @i18n:Tooltip (en="Value Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=3.0, core=true, t24Name=VALUE.DATE, mandatory=true)
    }
    IS_COMMODITY_SALE__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=52.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    IS_COMMODITY_SALE__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=51.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    IS_COMMODITY_SALE__OVERRIDE
    {
/* Contains all the override messages which the user has confirmed during input.
During Commit, the system may generate a series of screen override messages waiting for User Approval. If the record is subsequently displayed or printed, all override messages are shown in this field.

Validation Rules:
1. System generated. Multi-value field */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=48.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    IS_COMMODITY_SALE__PURCHASE_REF
    {
/* The Asset for which the goods are being delivered.

Validation Rules:
1. Mandatory Input for Named Assets.
2. Must be a valid record in the Asset table defined in the Commodity record. */
        ASSET_REF : T24BusinessTypes:A
            @i18n:Label (en="Asset Reference")
            @i18n:Tooltip (en="Asset Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=10.0, core=true, t24Name=ASSET.REF, mandatory=false)
/* Total Price of all the new Sale Price for an asset.

Validation Rules:
1. Field No-input.
2. System updates the amount that is sum of all the new sale price for the asset in the multi-value, i.e., sum of the field SALE.PRICE */
        ASSET_SALE_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=19.0, core=true, t24Name=ASSET.SALE.PRICE, typeModifiers="2", mandatory=false)
/* Total Price of all the Sale Price derived from the Asset Request record for the given quantity of an asset.

Validation Rules:
1. Field No-input.
2. System updates the amount that is sum of all the old sale price for the asset in the multi-value, i.e., sum of the field ORIG.SALE.PRICE */
        PURCHASE_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=20.0, core=true, t24Name=PURCHASE.PRICE, typeModifiers="2", mandatory=false)
/* The quantity of the Asset or Commodity requested in the Contract.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record. */
        PURCHASE_QTY : T24BusinessTypes:R
            @i18n:Label (en="Purchase Quantity")
            @i18n:Tooltip (en="Purchase Quantity")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=12.0, core=true, t24Name=PURCHASE.QTY, mandatory=false)
/* The Contract through which the Customer has initiated the Asset Request.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record from the table IS.CONTRACT. */
        PURCHASE_REF -> IS_Purchase:IS_CONTRACT required
            @i18n:Label (en="Purchase Reference")
            @i18n:Tooltip (en="Purchase Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=9.0, core=true, businessType=A, t24Name=PURCHASE.REF, mandatory=true)
        RESERVED_16 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=25.0, core=true, t24Name=RESERVED.16, mandatory=false)
        RESERVED_17 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=24.0, core=true, t24Name=RESERVED.17, mandatory=false)
        RESERVED_18 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=23.0, core=true, t24Name=RESERVED.18, mandatory=false)
        RESERVED_19 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=22.0, core=true, t24Name=RESERVED.19, mandatory=false)
        RESERVED_20 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=21.0, core=true, t24Name=RESERVED.20, mandatory=false)
        SALE_UNIT_PRICE -> byValue IS_Purchase:IS_COMMODITY_SALE__PURCHASE_REF__SALE_UNIT_PRICE *
            @t24:i (t24Name=SALE.UNIT.PRICE)
/* The total quantity of Commodity delivered out of the total purchase quantity.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.COMMODITY.POSITION record. */
        TOT_DELIVERED_QTY : T24BusinessTypes:R
            @i18n:Label (en="Total Delivered Quantity")
            @i18n:Tooltip (en="Total Delivered Quantity")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=13.0, core=true, t24Name=TOT.DELIVERED.QTY, mandatory=false)
/* The Total Quantity that are sold out of delivered goods.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.COMMODITY.POSITION record. */
        TOT_SOLD_QTY : T24BusinessTypes:R
            @i18n:Label (en="Total Sold Quantity")
            @i18n:Tooltip (en="Total Sold Quantity")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=14.0, core=true, t24Name=TOT.SOLD.QTY, mandatory=false)
/* Unit Price of the Asset or Commodity as captured in the IS.CONTRACT

Validation Rules:
1. Field No-input. */
        UNIT_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Unit Price")
            @i18n:Tooltip (en="Unit Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=11.0, core=true, t24Name=UNIT.PRICE, typeModifiers="2", mandatory=false)
    }
    IS_COMMODITY_SALE__PURCHASE_REF__SALE_UNIT_PRICE
    {
/* The Sale Price derived for the given quantity with the UNIT.PRICE quoted in the IS.CONTRACT.

Validation Rules:
1. Field No-input.
2. System defaults the amount as the product of UNIT.PRICE and SALE.QTY formatted to the currency. */
        ORIG_SALE_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Original Sale Price")
            @i18n:Tooltip (en="Original Sale Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=18.0, core=true, t24Name=ORIG.SALE.PRICE, typeModifiers="2", mandatory=false)
/* The Total New Price derived for an asset on account of requested quantity and differential pricing declared.

Validation Rules:
1. Field No-input.
2. System defaults Total Sale Price as the product of SALE.UNIT.PRICE and SALE.QTY. */
        SALE_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Total Sale Price")
            @i18n:Tooltip (en="Total Sale Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=17.0, core=true, t24Name=SALE.PRICE, typeModifiers="2", mandatory=false)
/* The quantity of the Asset or Commodity that the Customer is willing to purchase out of the Available quantity.

Validation Rules:
1. Field Mandatory.
2. The value must not exceed the Total Available quantity as of VALUE.DATE. */
        SALE_QTY : T24BusinessTypes:R required
            @i18n:Label (en="Quantity for Sale")
            @i18n:Tooltip (en="Quantity for Sale")
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=16.0, core=true, t24Name=SALE.QTY, mandatory=true)
/* The newly proposed Unit Price for the Asset or Commodity

Validation Rules:
1. Field Mandatory. */
        SALE_UNIT_PRICE : T24BusinessTypes:AMT required
            @i18n:Label (en="Sale Price")
            @i18n:Tooltip (en="Sale Price")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=15.0, core=true, t24Name=SALE.UNIT.PRICE, typeModifiers="2", mandatory=true)
    }
    IS_COMMODITY_SALE__STMT_NOS
    {
/* List of Statement Entries generated as the result of Accounting.

Validation Rules:
1. Field No-input. */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=47.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
/* IS.CONTRACT is the application to create Islamic Contracts. The entire workflow of the product with different statuses is captured in this application.

The contract evolves into different statuses like REQUEST, APPROVAL, PURCHASE based on the workflow defined in the product parameter file IS.PARAMETER. */
    IS_CONTRACT
        @i18n:Header1 (en=Action)
        @i18n:Description (en="")
        @t24:i (t24Name=IS.CONTRACT, level=F, type=H, allowedFunctions="A D E I L P Q R S", localRefApplicationSysNo=126.0, localRefAllowed=true)
    {
        ACCOUNTING_EVENT -> byValue IS_Purchase:IS_CONTRACT__ACCOUNTING_EVENT *
            @t24:i (t24Name=ACCOUNTING.EVENT)
        ACTION_CODE -> byValue IS_Purchase:IS_CONTRACT__ACTION_CODE *
            @t24:i (t24Name=ACTION.CODE)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=136.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=137.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=133.0, core=true, t24Name=AUTHORISER, mandatory=false)
        COMMODITY -> byValue IS_Purchase:IS_CONTRACT__COMMODITY *
            @t24:i (t24Name=COMMODITY)
        COST_TYPE -> byValue IS_Purchase:IS_CONTRACT__COST_TYPE *
            @t24:i (t24Name=COST.TYPE)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=134.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
/* The currency in which the contract is booked.

Validation Rules:
1. Field Mandatory
2. Must be a valid record in the table CURRENCY. */
        CURRENCY -> ST_CurrencyConfig:CURRENCY required
            @i18n:Label (en=Currency)
            @i18n:Tooltip (en=Currency)
            @t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=7.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=true)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=130.0, core=true, t24Name=CURR.NO, mandatory=false)
/* The Customer who requests the asset and the contract is booked for.

Validation Rules:
1. Field Mandatory.
2. Must be a valid record in the table CUSTOMER. */
        CUSTOMER -> ST_Customer:CUSTOMER required
            @i18n:Label (en=Customer)
            @i18n:Tooltip (en=Customer)
            @t24:i (align=R, inputBehaviour=F, maxLength=10, sysNumber=2.0, core=true, businessType=CUS, t24Name=CUSTOMER, mandatory=true)
/* The Customer account recorded in the Purchase contract.

Validation Rules:
1. Must be a valid record in the table ACCOUNT.
2. Account must belong to the Contract Customer.
3. NOSTRO or VOSTRO account not allowed. */
        CUSTOMER_ACCT -> AC_AccountOpening:ACCOUNT
            @i18n:Label (en="Customer Account")
            @i18n:Tooltip (en="Customer Account")
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=40.0, core=true, businessType=ANT, t24Name=CUSTOMER.ACCT, mandatory=false)
        DATE_TIME -> byValue IS_Purchase:IS_CONTRACT__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* The Deal date of the contract specifies the actual date the Customer requests for the asset and the details being captured in the screen. The current date of the system(TODAY) is defaulted in this field.

Validation Rules:
1. Field No-input.
2. Standard T24 Date Field.
3. Current date (TODAY) is defaulted. */
        DEAL_DATE : T24BusinessTypes:D
            @i18n:Label (en="Deal Date")
            @i18n:Tooltip (en="Deal Date")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=8.0, core=true, t24Name=DEAL.DATE, mandatory=false)
/* The Delivery Date of the asset.

Validation Rules:
1. Standard T24 Date Field.
2. Functionality yet to be developed for delivery of Assets. */
        DELIVERY_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=93.0, core=true, t24Name=DELIVERY.DATE, mandatory=false)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=135.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        DESCRIPTION -> byValue IS_Purchase:IS_CONTRACT__DESCRIPTION *
            @t24:i (t24Name=DESCRIPTION)
        DP_COMMODITY -> byValue IS_Purchase:IS_CONTRACT__DP_COMMODITY *
            @t24:i (t24Name=DP.COMMODITY)
/* The Customer who will act as Guarantor for the contract.

Validation Rules:
1. Must be a valid record from the table CUSTOMER. */
        GUARANTOR -> ST_Customer:CUSTOMER
            @t24:i (align=R, inputBehaviour=N, maxLength=10, sysNumber=61.0, core=true, businessType=CUS, t24Name=GUARANTOR, mandatory=false)
        INPUTTER -> byValue IS_Purchase:IS_CONTRACT__INPUTTER *
            @t24:i (t24Name=INPUTTER)
        /* The ID of the application IS.CONTRACT is an auto-generated ID. The same contract is used to capture Request, Purchase  and other related operations. */
        IS_CONTRACT_ID -> IS_Purchase:IS_CONTRACT PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.CONTRACT.ID, mandatory=true)
        LIMIT_VALUE_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=107.0, core=true, t24Name=LIMIT.VALUE.DATE, mandatory=false)
        /* The MMIA Agreement reference of the Purchase contract. */
        MMIA_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=73.0, core=true, t24Name=MMIA.REF, mandatory=false)
/* Defines the expected Next Status of the Contract which is defined in the IS.PARAMETER workflow.

Validation Rules:
1. The values to the field are defined in the EB.LOOKUP table with prefix "IS.WF.STATUS*".
2. Field No-input.
3. Defaulted by the system based on the value in the field STATUS. */
        NEXT_STATUS : T24BusinessTypes:A
            @i18n:Label (en="Next Status")
            @i18n:Tooltip (en="Next Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=96.0, core=true, t24Name=NEXT.STATUS, mandatory=false)
/* Captures additional information about the contract

Validation Rules:
1. Standard T24 Alphanumeric field. */
        NOTES : T24BusinessTypes:A
            @i18n:Label (en=Notes)
            @i18n:Tooltip (en=Notes)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=77.0, core=true, t24Name=NOTES, mandatory=false)
/* The number of units that the Finance Amount is comprised of. This is a user given value to represents the units of the Finance amount.

Validation Rules:
1. Any numeric value.
2. When value is given in this field, only one asset or commodity can be transacted. */
        NO_OF_UNITS : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=38.0, core=true, t24Name=NO.OF.UNITS, typeModifiers="7", mandatory=false)
        OVERRIDE -> byValue IS_Purchase:IS_CONTRACT__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
        PREV_STATUS -> byValue IS_Purchase:IS_CONTRACT__PREV_STATUS *
            @t24:i (t24Name=PREV.STATUS)
/* This field is to specify the type of product the asset will be processed

Validation Rules:
1. Field Mandatory
2. Must be a valid record from the table IS.PARAMETER. */
        PRODUCT -> IS_Config:IS_PARAMETER required
            @i18n:Label (en="IS Product")
            @i18n:Tooltip (en="Product Code")
            @t24:i (align=R, inputBehaviour=F, maxLength=3, sysNumber=3.0, core=true, t24Name=PRODUCT, mandatory=true)
        REBATE_ALLWD : IS_Purchase:IS_CONTRACT__REBATE_ALLWD
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=10.0, core=true, t24Name=REBATE.ALLWD, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=129.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
/* Specifies the Amount received during Repossession of an asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        REPOS_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=89.0, core=true, t24Name=REPOS.AMOUNT, typeModifiers="7", mandatory=false)
/* The reference captured during repurchase of the repossessed asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        REPOS_PUR_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=90.0, core=true, t24Name=REPOS.PUR.REF, mandatory=false)
/* Specifies if the contract is repurchased after repossession.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        REPURCHASE : IS_Purchase:IS_CONTRACT__REPURCHASE
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=91.0, core=true, t24Name=REPURCHASE, mandatory=false)
/* The Finance amount requested by the customer.

Validation Rules:
1. Must be a Numeric Field with Decimals. */
        REQUEST_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=106.0, core=true, t24Name=REQUEST.AMOUNT, typeModifiers="7D", mandatory=false)
/* Specifies if the contract is resold after repossession

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE : IS_Purchase:IS_CONTRACT__RESALE
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=92.0, core=true, t24Name=RESALE, mandatory=false)
        RESALE_CUSTOMER -> byValue IS_Purchase:IS_CONTRACT__RESALE_CUSTOMER *
            @t24:i (t24Name=RESALE.CUSTOMER)
        RESERVED_1 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=125.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_10 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=116.0, core=true, t24Name=RESERVED.10, mandatory=false)
        RESERVED_11 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=115.0, core=true, t24Name=RESERVED.11, mandatory=false)
        RESERVED_12 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=114.0, core=true, t24Name=RESERVED.12, mandatory=false)
        RESERVED_13 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=113.0, core=true, t24Name=RESERVED.13, mandatory=false)
        RESERVED_14 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=112.0, core=true, t24Name=RESERVED.14, mandatory=false)
        RESERVED_15 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=111.0, core=true, t24Name=RESERVED.15, mandatory=false)
        RESERVED_16 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=110.0, core=true, t24Name=RESERVED.16, mandatory=false)
        RESERVED_17 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=109.0, core=true, t24Name=RESERVED.17, mandatory=false)
        RESERVED_18 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=108.0, core=true, t24Name=RESERVED.18, mandatory=false)
        RESERVED_2 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=124.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=123.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=122.0, core=true, t24Name=RESERVED.4, mandatory=false)
        RESERVED_5 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=121.0, core=true, t24Name=RESERVED.5, mandatory=false)
        RESERVED_6 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=120.0, core=true, t24Name=RESERVED.6, mandatory=false)
        RESERVED_7 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=119.0, core=true, t24Name=RESERVED.7, mandatory=false)
        RESERVED_8 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=118.0, core=true, t24Name=RESERVED.8, mandatory=false)
        RESERVED_9 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=117.0, core=true, t24Name=RESERVED.9, mandatory=false)
/* The AA arrangement reference is captured in this field when the contract is financed.

Validation Rules:
1. Field No-input. */
        SALE_REFERENCE -> AA_Framework:AA_ARRANGEMENT
            @i18n:Label (en="Sale Reference")
            @i18n:Tooltip (en="Sale Reference")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=95.0, core=true, businessType=A, t24Name=SALE.REFERENCE, mandatory=false)
/* The AA simulation reference which can be linked to the Purchase contract. User can simulate the finance and the repayment schedules using the AA simulation and the same can be linked here during purchase.

Validation Rules:
1. Must be a valid record from the table "AA.SIMULATION.CAPTURE".
2. The Total Purchase amount should not exceed the Simulation Amount. */
        SIMULATION_REF -> AA_Framework:AA_ARRANGEMENT_SIM
            @i18n:Label (en="Simulation Reference")
            @i18n:Tooltip (en="Simulation Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=94.0, core=true, businessType=A, t24Name=SIMULATION.REF, mandatory=false)
/* The status of the Contract like REQUEST, APPROVAL, PURCHASE. The status transition happens according to the workflow defined in the IS.PARAMETER.

Validation Rules:
1. The values to the field are defined in the EB.LOOKUP table with prefix "IS.WF.STATUS*".
2. Currently, APPROVAL and PURCHASE is designed to raise accounting entries. The Accounting entries for Cost is raised during the PURCHASE status */
        STATUS : IS_Purchase:IS_CONTRACT__STATUS
            @i18n:Label (en=Status)
            @i18n:Tooltip (en=Status)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=4.0, core=true, t24Name=STATUS, mandatory=false)
/* Date on which the Status is effective. Defaulted to the date given in the field VALUE.DATE.

Validation Rules:
1. Standard T24 Date.
2. Defaulted to the value in the field VALUE.DATE if null */
        STATUS_VALUE_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=STATUS.VALUE.DATE, mandatory=false)
        STMT_NOS -> byValue IS_Purchase:IS_CONTRACT__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The Total Down Payment amount of the Contract

Validation Rules:
1. Field No-input.
2. System updates the field value as sum of all the down payment specified. */
        TOTAL_DP_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Total Down Pay Amount")
            @i18n:Tooltip (en="Total Down Pay Amount")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=59.0, core=true, t24Name=TOTAL.DP.AMT, typeModifiers="7", mandatory=false)
/* The Total Down Payment amount contributed in terms of DP.CONTRIB.PAYTO as 'Bank' and DP.CONTRIB.TYPE as 'Cash'.

Validation Rules:
1. Field No-input. */
        TOTAL_DP_CASH : T24BusinessTypes:AMT
            @i18n:Label (en="Total Down Payment by Cash")
            @i18n:Tooltip (en="Total Down Payment by Cash")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=60.0, core=true, t24Name=TOTAL.DP.CASH, typeModifiers="7", mandatory=false)
/* The Total Price of the contract (Sum of the purchase amounts of all Commodities/Assets)

Validation Rules:
1. Field No-input.
2. System updates the field value as sum of all the purchase price. */
        TOT_PURCHASE_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Total Purchase Price")
            @i18n:Tooltip (en="Total Purchase Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=37.0, core=true, t24Name=TOT.PURCHASE.PRICE, typeModifiers="7", mandatory=false)
/* The unit value of the Asset or Commodity defined.

System defaults the value by dividing the Finance Amount with NO.OF.UNITS. Finance Amount is calculated as Sum of Total Purchase Price and Cost(Type Finance) exclusive of Down Payment.

Validation Rules:
1. Field No-input. */
        UNIT_VALUE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=39.0, core=true, t24Name=UNIT.VALUE, typeModifiers="7", mandatory=false)
/* Value date of the Contract.

Validation Rules:
1. Defaulted to TODAY.
2. Cannot be future dated.
3. Standard T24 Date field. */
        VALUE_DATE : T24BusinessTypes:D required
            @i18n:Label (en="Value Date")
            @i18n:Tooltip (en="Value Date")
            @t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=9.0, core=true, t24Name=VALUE.DATE, mandatory=true)
/* The account that is used to act as Wash account for Vendor Payment. This Account is credited during Purchase and the Vendor Payments are disbursed from this account.

The account formed is of the format
<ContractCurrency><MultiSupplierCategory><ProductCode><SubDivisionCode>

Validation Rules:
1. Field No-input.
2. Defaulted with the account formed from the Multi Supplier Category of the Parameter record */
        VENDOR_WASH_ACCT : T24BusinessTypes:A
            @i18n:Label (en="Vendor Wash Account")
            @i18n:Tooltip (en="Vendor Wash Account")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=41.0, core=true, t24Name=VENDOR.WASH.ACCT, mandatory=false)
/* The AA Arrangement Contract reference which acts as a Wakala contract.
This is keyed in case of linked products like WAKALA-MUSAWAMA.

Validation Rules:
1. Valid record in the table AA.ARRANGEMENT. */
        WAKALA_REF -> AA_Framework:AA_ARRANGEMENT
            @i18n:Label (en="Wakala Reference")
            @i18n:Tooltip (en="Wakala Reference")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=42.0, core=true, businessType=A, t24Name=WAKALA.REF, mandatory=false)
    }
    /* IS.CONTRACT.DELIVERY is the table to capture the delivery of an Asset or a Commodity which is purchased. The tracking of the Asset or Commodity is captured as and when a delivery happens. The delivery is captured until all the purchased items are delivered. */
    IS_CONTRACT_DELIVERY
        @i18n:Header1 (en="Authorise - Contract Delivery")
        @i18n:Description (en="")
        @t24:i (t24Name=IS.CONTRACT.DELIVERY, level=F, type=H, allowedFunctions="A C D E H I L P Q R S", localRefApplicationSysNo=30.0, localRefAllowed=true)
    {
/* The Asset for which the goods are being delivered.

Validation Rules:
1. Field No-input.
2. Defaulted from ID of the current record.
3. Must be a valid record in the Asset table defined in the Commodity record.
4. Fields COMMODITY and ASSET.REF are mutually exclusive. */
        ASSET_REF : T24BusinessTypes:A
            @i18n:Label (en="Asset Reference")
            @i18n:Tooltip (en="Asset Reference")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=5.0, core=true, t24Name=ASSET.REF, mandatory=false)
        AUDITOR_CODE : T24BusinessTypes:A
            @i18n:Label (en="Auditor Code")
            @i18n:Tooltip (en="Auditor Code")
            @t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=40.0, core=true, t24Name=AUDITOR.CODE, mandatory=false)
        AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Audit Date Time")
            @i18n:Tooltip (en="Date and time when record was audited")
            @t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=41.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
        AUTHORISER : T24BusinessTypes:A
            @i18n:Label (en=Authoriser)
            @i18n:Tooltip (en="Indicates the User who Authorises the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=37.0, core=true, t24Name=AUTHORISER, mandatory=false)
/* The Commodity for which the goods are being delivered.

Validation Rules:
1. Field No-input.
2. Defaulted from ID of the current record.
3. Must be a valid entry in the IS.COMMODITY table.
4. Allowed only for Quantified Assets.
5. Fields COMMODITY and ASSET.REF are mutually exclusive. */
        COMMODITY -> IS_Config:IS_COMMODITY
            @i18n:Label (en=Commodity)
            @i18n:Tooltip (en=Commodity)
            @t24:i (align=R, inputBehaviour=F, maxLength=3, sysNumber=4.0, core=true, t24Name=COMMODITY, mandatory=false)
        CO_CODE -> ST_CompanyCreation:COMPANY
            @i18n:Label (en=Company)
            @t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=38.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####", mandatory=false)
/* The Currency in which the Asset Request contract is booked.

Validation Rules:
1. Field No-input.
2. Defaulted from the IS.CONTRACT record.
2. Must be a valid record in the table CURRENCY. */
        CURRENCY -> ST_CurrencyConfig:CURRENCY
            @i18n:Label (en=Currency)
            @i18n:Tooltip (en=Currency)
            @t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=6.0, core=true, businessType=CCY, t24Name=CURRENCY, mandatory=false)
        CURR_NO : T24BusinessTypes:N
            @i18n:Label (en="Curr No")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=34.0, core=true, t24Name=CURR.NO, mandatory=false)
/* The Contract Customer who delivers the Asset.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record.
3. Must be a valid record in the table CUSTOMER. */
        CUSTOMER -> ST_Customer:CUSTOMER
            @i18n:Label (en=Customer)
            @i18n:Tooltip (en=Customer)
            @t24:i (align=R, inputBehaviour=N, maxLength=10, sysNumber=1.0, core=true, businessType=CUS, t24Name=CUSTOMER, mandatory=false)
        DATE_TIME -> byValue IS_Purchase:IS_CONTRACT_DELIVERY__DATE_TIME *
            @t24:i (t24Name=DATE.TIME)
/* The date on which the goods are delivered. The value in this field gets cleared off after authorisation of the contract.

Validation Rules:
1. Field Mandatory.
2. Defaulted to TODAY.
3. Cannot be future dated.
4. Standard T24 Date field. */
        DELIVERY_DATE : T24BusinessTypes:D required
            @i18n:Label (en="Reversal Date")
            @i18n:Tooltip (en="Reversal Date")
            @t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=12.0, core=true, t24Name=DELIVERY.DATE, mandatory=true)
/* The quantity of Asset or Commodity being delivered on the given Delivery Date. The value in this field gets cleared off after authorisation of the contract.

Validation Rules:
1. Either Deliver Qty or Reverse Qty is mandatory.
2. Both Deliver Qty and Reverse Qty are not allowed. */
        DELIVER_QTY : T24BusinessTypes:R
            @i18n:Label (en="Deliver Quantity")
            @i18n:Tooltip (en="Deliver Quantity")
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=13.0, core=true, t24Name=DELIVER.QTY, mandatory=false)
        DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
            @i18n:Label (en="Dept Code")
            @t24:i (align=R, inputBehaviour=N, maxLength=6, sysNumber=39.0, core=true, t24Name=DEPT.CODE, mandatory=false)
        INPUTTER -> byValue IS_Purchase:IS_CONTRACT_DELIVERY__INPUTTER *
            @t24:i (t24Name=INPUTTER)
/* ID of the table IS.CONTRACT.DELIVERY.
Id format:
1. <PURCHASE.ID> - <COMMODITY> - for Quantified Commodities
2. <PURCHASE.ID> - <COMMODITY> - <ASSET.ID> - for Named Quantities */
        IS_CONTRACT_DELIVERY_ID -> IS_Purchase:IS_CONTRACT_DELIVERY PK required
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=0.0, core=true, businessType=A, t24Name=IS.CONTRACT.DELIVERY.ID, mandatory=true)
        OVERRIDE -> byValue IS_Purchase:IS_CONTRACT_DELIVERY__OVERRIDE *
            @t24:i (t24Name=OVERRIDE)
/* The pending quantity of the Asset or Commodity yet to be delivered out of the total purchase quantity.

Validation Rules:
1. Field No-input.
2. Defaulted from the IS.COMMODITY.POSITION record */
        PENDING_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=11.0, core=true, t24Name=PENDING.QTY, mandatory=false)
/* The Islamic Product on which the customer requests the Asset.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record.
3. Must be a valid record from the table IS.PARAMETER. */
        PRODUCT -> IS_Config:IS_PARAMETER
            @i18n:Label (en=Product)
            @i18n:Tooltip (en=Product)
            @t24:i (align=R, inputBehaviour=F, maxLength=3, sysNumber=3.0, core=true, t24Name=PRODUCT, mandatory=false)
/* The Total Purchase Price of the Asset or Commodity being delivered.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record. */
        PURCHASE_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Purchase Price")
            @i18n:Tooltip (en="Purchase Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=9.0, core=true, t24Name=PURCHASE.PRICE, typeModifiers="6", mandatory=false)
/* The quantity of Asset or Commodity purchased in the Contract.

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record. */
        PURCHASE_QTY : T24BusinessTypes:R
            @i18n:Label (en="Purchase Quantity")
            @i18n:Tooltip (en="Purchase Quantity")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=8.0, core=true, t24Name=PURCHASE.QTY, mandatory=false)
/* The Contract through which the Customer initiated the Asset Request.

Validation Rules:
1. Field No-input.
2. Defaulted from ID of the current record.
2. Must be a valid record from the table IS.CONTRACT. */
        PURCHASE_REF -> IS_Purchase:IS_CONTRACT
            @i18n:Label (en="Purchase Reference")
            @i18n:Tooltip (en="Purchase Reference")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=2.0, core=true, businessType=A, t24Name=PURCHASE.REF, mandatory=false)
        RECORD_STATUS : T24BusinessTypes:STAT
            @i18n:Label (en="Record Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=6, sysNumber=33.0, core=true, t24Name=RECORD.STATUS, mandatory=false)
        RESERVED_1 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=29.0, core=true, t24Name=RESERVED.1, mandatory=false)
        RESERVED_10 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=20.0, core=true, t24Name=RESERVED.10, mandatory=false)
        RESERVED_11 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=19.0, core=true, t24Name=RESERVED.11, mandatory=false)
        RESERVED_12 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=18.0, core=true, t24Name=RESERVED.12, mandatory=false)
        RESERVED_13 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=17.0, core=true, t24Name=RESERVED.13, mandatory=false)
        RESERVED_14 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=16.0, core=true, t24Name=RESERVED.14, mandatory=false)
        RESERVED_15 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=15.0, core=true, t24Name=RESERVED.15, mandatory=false)
        RESERVED_2 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=28.0, core=true, t24Name=RESERVED.2, mandatory=false)
        RESERVED_3 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=27.0, core=true, t24Name=RESERVED.3, mandatory=false)
        RESERVED_4 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=26.0, core=true, t24Name=RESERVED.4, mandatory=false)
        RESERVED_5 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=25.0, core=true, t24Name=RESERVED.5, mandatory=false)
        RESERVED_6 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=24.0, core=true, t24Name=RESERVED.6, mandatory=false)
        RESERVED_7 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=23.0, core=true, t24Name=RESERVED.7, mandatory=false)
        RESERVED_8 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=22.0, core=true, t24Name=RESERVED.8, mandatory=false)
        RESERVED_9 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=21.0, core=true, t24Name=RESERVED.9, mandatory=false)
/* The quantity of Asset or Commodity being reversed on the given Delivery Date. The value in this field gets cleared off after authorisation of the contract.

Validation Rules:
1. Either Deliver Qty or Reverse Qty is mandatory.
2. Both Deliver Qty and Reverse Qty are not allowed. */
        REVERSE_QTY : T24BusinessTypes:R
            @i18n:Label (en="Reversal Quantity")
            @i18n:Tooltip (en="Reversal Quantity")
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=14.0, core=true, t24Name=REVERSE.QTY, mandatory=false)
        STMT_NOS -> byValue IS_Purchase:IS_CONTRACT_DELIVERY__STMT_NOS *
            @t24:i (t24Name=STMT.NOS)
/* The total quantity of Asset or Commodity delivered out of the total purchase quantity.

Validation Rules:
1. Field No-input. */
        TOT_DELIVERED_QTY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=10.0, core=true, t24Name=TOT.DELIVERED.QTY, mandatory=false)
/* Unit Price of the Asset or Commodity as captured in the IS.CONTRACT

Validation Rules:
1. Field No-input.
2. Defaulted from the corresponding IS.CONTRACT record. */
        UNIT_PRICE : T24BusinessTypes:AMT
            @i18n:Label (en="Unit Price")
            @i18n:Tooltip (en="Unit Price")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=7.0, core=true, t24Name=UNIT.PRICE, typeModifiers="6", mandatory=false)
    }
    IS_CONTRACT_DELIVERY__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=36.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    IS_CONTRACT_DELIVERY__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=35.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    IS_CONTRACT_DELIVERY__OVERRIDE
    {
/* Contains all the override messages which the user has confirmed during input.
During Commit, the system may generate a series of screen override messages waiting for User Approval. If the record is subsequently displayed or printed, all override messages are shown in this field.

Validation Rules:
1. System generated. Multi-value field. */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=32.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    IS_CONTRACT_DELIVERY__STMT_NOS
    {
/* List of Statement Entries generated as the result of Accounting.

Validation Rules:
1. Field No-input. */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=31.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
    IS_CONTRACT__ACCOUNTING_EVENT
    {
/* Accounting Events that are expected to take place when the Contract traverses to the mentioned Status.

Validation Rules:
1. Field No-Input.
2. Defaulted from the Accounting Events defined in the IS.STATUS */
        ACCOUNTING_EVENT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=6.0, core=true, t24Name=ACCOUNTING.EVENT, mandatory=false)
    }
    IS_CONTRACT__ACTION_CODE
    {
/* The action codes act as checklist to be performed as part of the Purchase Contract. The actions are defined in the table IS.CONTRACT.TASK.

Validation Rules:
1. Valid record from the table IS.CONTRACT */
        ACTION_CODE -> IS_Config:IS_CONTRACT_TASK
            @i18n:Label (en=Action)
            @i18n:Tooltip (en=Task)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=74.0, core=true, businessType=A, t24Name=ACTION.CODE, mandatory=false)
/* The status of the action code linked in ACT.CODE field is marked completed by setting this field YES or NULL.

Validation Rules:
1. Valid values are "YES" and NULL */
        ACTION_COMPLETED : IS_Purchase:IS_CONTRACT__ACTION_CODE__ACTION_COMPLETED
            @i18n:Label (en="Action Completed")
            @i18n:Tooltip (en="Task Completion")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=75.0, core=true, t24Name=ACTION.COMPLETED, mandatory=false)
/* The status of the action code linked in ACT.CODE field is marked Success by setting this field YES or NULL.

Validation Rules:
1. Valid values are "YES" and NULL */
        ACTION_SUCCESS : IS_Purchase:IS_CONTRACT__ACTION_CODE__ACTION_SUCCESS
            @i18n:Label (en="Action Success")
            @i18n:Tooltip (en="Action Success")
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=76.0, core=true, t24Name=ACTION.SUCCESS, mandatory=false)
    }
    IS_CONTRACT__COMMODITY
    {
        ASSET_REF -> byValue IS_Purchase:IS_CONTRACT__COMMODITY__ASSET_REF *
            @t24:i (t24Name=ASSET.REF)
/* The commodity being purchased in the contract.

Validation Rules:
1. Field Mandatory
2. Must be a valid entry in the IS.COMMODITY table
3. No Duplicates Allowed. */
        COMMODITY -> IS_Config:IS_COMMODITY
            @i18n:Label (en=Commodity)
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=11.0, core=true, t24Name=COMMODITY, mandatory=false)
    }
    IS_CONTRACT__COMMODITY__ASSET_REF
    {
/* The asset being purchased is captured in this field.

Validation Rules:
1. Mandatory Input for Named Assets.
2. Must be a valid record in the Asset table defined in the Commodity record */
        ASSET_REF : T24BusinessTypes:A
            @i18n:Label (en="Asset Reference")
            @i18n:Tooltip (en="Asset Reference")
            @t24:i (align=L, inputBehaviour=F, maxLength=35, sysNumber=12.0, core=true, t24Name=ASSET.REF, mandatory=false)
/* The Broker from whom the commodities are bought.

Validation Rules:
1. Must be a valid record from the table IS.BROKER.
2. Must be a valid Buy Broker in the COMMODITY. */
        BUY_BROKER -> IS_Config:IS_BROKER
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=15.0, core=true, businessType=CUS, t24Name=BUY.BROKER, mandatory=false)
/* Broker Account of the Buy Broker to which the Broker Payment will be paid.

This is defaulted from the IS.BROKER table w.r.t to the Company and Currency. If no account configured for the company and specified currency, no value is defaulted.

Validation Rules:
1. Must be a valid record in the table ACCOUNT.
2. Either Broker Account or Beneficiary should be specified.
3. Field Mandatory if BUY.BROKER is specified. */
        BUY_BROKER_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=16.0, core=true, businessType=ANT, t24Name=BUY.BROKER.ACCT, mandatory=false)
/* Broker Wash Account through which Broker Payments are paid out to Buy Broker.
The account formed is of the format
<ContractCurrency><BrokerWashCategory><ProductCode><SubDivisionCode>

The value to this field is defaulted with the Account formed from the Broker Wash Category defined in the IS.BROKER application. If there is no Wash Category defined in IS.BROKER then the category defined in BROKER.BUY.CAT of the IS.PARAMETER record.

Validation Rules:
1. Field No-Input.
2. System defaults with Buy Broker Wash Account. */
        BUY_BR_WASH_ACCT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=17.0, core=true, t24Name=BUY.BR.WASH.ACCT, mandatory=false)
        CUSTOMER_POSSESSION : IS_Purchase:IS_CONTRACT__COMMODITY__ASSET_REF__CUSTOMER_POSSESSION
            @t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=30.0, core=true, t24Name=CUSTOMER.POSSESSION, mandatory=false)
/* This field specifies the total price of the commodity or asset.

Validation Rules:
1. Field No-input. */
        PURCHASE_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=24.0, core=true, t24Name=PURCHASE.PRICE, typeModifiers="7", mandatory=false)
/* This field specifies the quantity of the commodity or asset being requested

Validation Rules:
1. This is a Mandatory field */
        QUANTITY : T24BusinessTypes:R
            @i18n:Label (en="Purchase Quantity")
            @i18n:Tooltip (en="Purchase Quantity")
            @t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=23.0, core=true, t24Name=QUANTITY, mandatory=false)
        REBATE_ACTION : IS_Purchase:IS_CONTRACT__COMMODITY__ASSET_REF__REBATE_ACTION
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=31.0, core=true, t24Name=REBATE.ACTION, mandatory=false)
        REBATE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=34.0, core=true, t24Name=REBATE.AMT, mandatory=false)
        REBATE_BANK_SHARE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=36.0, core=true, t24Name=REBATE.BANK.SHARE, mandatory=false)
        REBATE_CUST_SHARE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=35.0, core=true, t24Name=REBATE.CUST.SHARE, mandatory=false)
        REBATE_TYPE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=32.0, core=true, t24Name=REBATE.TYPE, mandatory=false)
        REBATE_VALUE : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=33.0, core=true, t24Name=REBATE.VALUE, mandatory=false)
        RETURN_QUANTITY : T24BusinessTypes:R
            @t24:i (align=R, inputBehaviour=N, maxLength=16, sysNumber=25.0, core=true, t24Name=RETURN.QUANTITY, mandatory=false)
        RETURN_SOLD_LOSS : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=29.0, core=true, t24Name=RETURN.SOLD.LOSS, typeModifiers="7", mandatory=false)
        RETURN_SOLD_PROFIT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=28.0, core=true, t24Name=RETURN.SOLD.PROFIT, typeModifiers="7", mandatory=false)
        RETURN_TOTAL_SOLD_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=27.0, core=true, t24Name=RETURN.TOTAL.SOLD.PRICE, typeModifiers="7", mandatory=false)
        RETURN_UNIT_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=26.0, core=true, t24Name=RETURN.UNIT.PRICE, typeModifiers="7", mandatory=false)
/* The Broker to whom the commodities are sold

Validation Rules:
1. Must be a valid record from the table IS.BROKER.
2. Must be a valid Sell Broker in the COMMODITY */
        SELL_BROKER -> IS_Config:IS_BROKER
            @t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=18.0, core=true, businessType=CUS, t24Name=SELL.BROKER, mandatory=false)
        /* Broker Account of the Sell Broker to which the Broker Payment will be paid. */
        SELL_BROKER_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=19.0, core=true, businessType=ANT, t24Name=SELL.BROKER.ACCT, mandatory=false)
        /* Broker Wash Account through which Broker Payments are paid out to Sell Broker */
        SELL_BR_WASH_ACCT : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=20.0, core=true, t24Name=SELL.BR.WASH.ACCT, mandatory=false)
/* The units in which the commodity or asset can be expressed. For example, Tin could be expressed in Tons, Real Estate building could be expressed in acres or square feet.

Validation Rules:
1. The values to the field are defined in the EB.LOOKUP table with prefix "IS.UNIT*" */
        UNITS : IS_Purchase:IS_CONTRACT__COMMODITY__ASSET_REF__UNITS
            @i18n:Label (en=Units)
            @i18n:Tooltip (en=Units)
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=21.0, core=true, t24Name=UNITS, mandatory=false)
/* The unit price of the Commodity or Asset being requested

Validation Rules:
1. Field Mandatory.
2. Defaulted from the Asset Table in case of Named Assets. */
        UNIT_PRICE : T24BusinessTypes:PRICE
            @i18n:Label (en="Unit Price")
            @i18n:Tooltip (en="Unit Price of the Asset")
            @t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=22.0, core=true, t24Name=UNIT.PRICE, typeModifiers="7D", mandatory=false)
/* The Vendor of the Contract who supplies the asset. The Vendor could be an individual or a company.

Validation Rules:
1. Field Mandatory
2. Must be a valid record in the table IS.VENDOR. */
        VENDOR -> IS_Config:IS_VENDOR
            @i18n:Label (en=Vendor)
            @i18n:Tooltip (en=Vendor)
            @t24:i (align=L, inputBehaviour=F, maxLength=35, sysNumber=13.0, core=true, businessType=A, t24Name=VENDOR, mandatory=false)
/* The name of the Vendor who supplies the asset. The value for the field is defaulted from the Vendor table. User-defined Names can be overwritten.

Validation Rules:
1. Standard T24 Alphanumeric field. */
        VENDOR_NAME : T24BusinessTypes:A
            @i18n:Label (en="Vendor Name")
            @i18n:Tooltip (en="Name of the Vendor")
            @t24:i (align=L, inputBehaviour=F, maxLength=35, sysNumber=14.0, core=true, t24Name=VENDOR.NAME, mandatory=false)
    }
    IS_CONTRACT__COST_TYPE
    {
/* The cost amount for the chosen Cost type in the Purchase Contract Currency

Validation Rules:
1. Field Mandatory if Cost Type is given
2. Defaulted from the FT Commission type or FT Charge type record of the Cost */
        COST_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Cost Amount")
            @i18n:Tooltip (en="Cost Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=64.0, core=true, t24Name=COST.AMT, typeModifiers="7", mandatory=false)
/* The Credit Account for the Cost Payment

Validation Rules:
1.  This field is defaulted with supplier account defined in the Parameter table
2. This field is user inputtable if the Cost Pay Type is Purchase */
        COST_CR_ACCT : T24BusinessTypes:A
            @i18n:Label (en="Cost Credit Account")
            @i18n:Tooltip (en="Cost Credit Account")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=66.0, core=true, t24Name=COST.CR.ACCT, mandatory=false)
/* The description of the costs incurred during purchase of the Contract.

Validation Rules:
1. Standard T24 Alphanumeric field */
        COST_DESC : T24BusinessTypes:A
            @i18n:Label (en="Description abt the Cost")
            @i18n:Tooltip (en="Description about the Cost")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=67.0, core=true, t24Name=COST.DESC, mandatory=false)
/* The Debit Account for the Cost Payment

Validation Rules:
1. Mandatory if Cost Type is given
2. Defaulted from the FT Commission type or FT Charge type record of the Cost type specified */
        COST_DR_ACCT : T24BusinessTypes:A
            @i18n:Label (en="Cost Debit Account")
            @i18n:Tooltip (en="Cost Debit Account")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=65.0, core=true, t24Name=COST.DR.ACCT, mandatory=false)
/* The Payment Type by which the Cost has to be paid.

Validation Rules:
1. Valid values are Purchase, Vendor and Finance
2. Field Mandatory if Cost Type is given */
        COST_PAY_TYPE : IS_Purchase:IS_CONTRACT__COST_TYPE__COST_PAY_TYPE
            @i18n:Label (en="Cost Payment Type")
            @i18n:Tooltip (en="Cost Payment Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=63.0, core=true, t24Name=COST.PAY.TYPE, mandatory=false)
/* Specifies the costs incurred during purchase of the Contract.

Validation Rules:
1. Must be a valid record in the table IS.COST.TYPE. */
        COST_TYPE -> IS_Config:IS_COST_TYPE
            @i18n:Label (en="Cost Type")
            @i18n:Tooltip (en="Cost Type")
            @t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=62.0, core=true, t24Name=COST.TYPE, mandatory=false)
        RESERVED_26 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=72.0, core=true, t24Name=RESERVED.26, mandatory=false)
        RESERVED_27 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=71.0, core=true, t24Name=RESERVED.27, mandatory=false)
        RESERVED_28 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=70.0, core=true, t24Name=RESERVED.28, mandatory=false)
        RESERVED_29 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=69.0, core=true, t24Name=RESERVED.29, mandatory=false)
        RESERVED_30 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=68.0, core=true, t24Name=RESERVED.30, mandatory=false)
    }
    IS_CONTRACT__DATE_TIME
    {
        DATE_TIME : T24BusinessTypes:RELTIME
            @i18n:Label (en="Date time")
            @i18n:Tooltip (en="Date and time when record was input or authorised")
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=132.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##", mandatory=false)
    }
    IS_CONTRACT__DESCRIPTION
    {
/* The Description of the Contract or the Asset being requested.

Validation Rules:
1. Standard T24 Alphanumeric field.
2. Mandatory Input. */
        DESCRIPTION : T24BusinessTypes:A required
            @i18n:Label (en="Asset Description")
            @i18n:Tooltip (en="Asset Description")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, multiLanguage=true, sysNumber=1.0, core=true, t24Name=DESCRIPTION, mandatory=true)
    }
    IS_CONTRACT__DP_COMMODITY
    {
        DP_ACCOUNT -> byValue IS_Purchase:IS_CONTRACT__DP_COMMODITY__DP_ACCOUNT *
            @t24:i (t24Name=DP.ACCOUNT)
/* The Asset against which the Down Payment is paid.

Validation Rules:
1. Must be a valid record in the Asset table of IS.COMMODITY.
2. The commodity must be one of the defined assets in the Purchase contract.
3. Input allowed only if DP.BY.ASSET flag is set to 'YES'. */
        DP_ASSET_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=44.0, core=true, t24Name=DP.ASSET.REF, mandatory=false)
/* The Total Bank Contribution for the Asset or Commodity. This field is updated with the difference of Customer contribution from the Purchase Price of the Asset or Commodity.

Validation Rules:
1. Field No-input.
2. System updates the difference of Customer contribution from the Purchase Price of the Asset or Commodity in this field. */
        DP_BANK_CONTRIB : T24BusinessTypes:AMT
            @i18n:Label (en="Bank Contribution")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=49.0, core=true, t24Name=DP.BANK.CONTRIB, typeModifiers="7", mandatory=false)
/* The Total Customer Contribution for all the assets and commodity in terms of Cash to the Bank. This field is updated with the sum of the Cash Contribution to the Bank by the customer, i.e., Sum of the values in the field DP.CASH.CONTRIB.

Validation Rules:
1. Field No-input.
2. System updates with the amount of customer contribution in terms of Cash to the Bank for all the assets and commodity. */
        DP_CASH_CONTRIB : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=53.0, core=true, t24Name=DP.CASH.CONTRIB, typeModifiers="7", mandatory=false)
/* The Commodity against which the Down Payment is paid.

Validation Rules:
1. Must be a valid record in the table IS.COMMODITY.
2. The commodity must be one of the defined commodities in the Purchase contract.
3. Input not allowed if DP.BY.ASSET flag is set to 'YES'. */
        DP_COMMODITY -> IS_Config:IS_COMMODITY
            @i18n:Label (en="Down Payment Commodity")
            @i18n:Tooltip (en="Down Payment Commodity")
            @t24:i (align=R, inputBehaviour=N, maxLength=15, sysNumber=43.0, core=true, t24Name=DP.COMMODITY, mandatory=false)
        DP_CONTRIB_TYPE -> byValue IS_Purchase:IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE *
            @t24:i (t24Name=DP.CONTRIB.TYPE)
/* The Total contribution of the Customer for the Asset or Commodity.

Validation Rules:
1. Field No-input.
2. System updates the customer contribution as DP.VALUE of the down payment. */
        DP_CUST_CONTRIB : T24BusinessTypes:AMT
            @i18n:Label (en="Customer Contribution")
            @i18n:Tooltip (en="Customer Contribution")
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=48.0, core=true, t24Name=DP.CUST.CONTRIB, typeModifiers="7", mandatory=false)
/* The Down Payment Percentage for the individual asset or a commodity.

Validation Rules:
1. Input Mandatory if DP.TYPE is Percentage.
2. System defaults amount in case DP.TYPE is Amount and a valid percentage is given in the field DP.PERCENT
3. Valid percentage (less than 100). */
        DP_PERCENT : T24BusinessTypes:R
            @i18n:Label (en="Down Pay Percentage")
            @i18n:Tooltip (en="Down Pay Percentage")
            @t24:i (align=R, inputBehaviour=I, maxLength=15, sysNumber=47.0, core=true, t24Name=DP.PERCENT, mandatory=false)
/* Specifies if the Down Payment Amount can be defined in Amount or Percentage.

Validation Rules:
1. Valid values are Percentage and Amount
2. Input Mandatory if DP.VALUE or DP.PERCENT is given */
        DP_TYPE : IS_Purchase:IS_CONTRACT__DP_COMMODITY__DP_TYPE
            @i18n:Label (en="Down Payment Type")
            @i18n:Tooltip (en="Down Payment Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=45.0, core=true, t24Name=DP.TYPE, mandatory=false)
/* The Down Payment Amount for the individual asset or a commodity.

Validation Rules:
1. Input Mandatory if DP.TYPE is Amount.
2. System defaults percentage in case DP.TYPE is Percentage and a valid amount is given in the field DP.AMOUNT
3. Valid percentage (less than 100). */
        DP_VALUE : T24BusinessTypes:AMT
            @i18n:Label (en="Down Payment Amount")
            @i18n:Tooltip (en="Down Payment Amount")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=46.0, core=true, t24Name=DP.VALUE, typeModifiers="7", mandatory=false)
        RESERVED_31 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=58.0, core=true, t24Name=RESERVED.31, mandatory=false)
        RESERVED_32 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=57.0, core=true, t24Name=RESERVED.32, mandatory=false)
        RESERVED_33 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=56.0, core=true, t24Name=RESERVED.33, mandatory=false)
    }
    IS_CONTRACT__DP_COMMODITY__DP_ACCOUNT
    {
        DP_ACCOUNT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=I, maxLength=36, sysNumber=54.0, core=true, businessType=ANT, t24Name=DP.ACCOUNT, mandatory=false)
        DP_REC_AMOUNT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=55.0, core=true, t24Name=DP.REC.AMOUNT, typeModifiers="7D", mandatory=false)
    }
    IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE
    {
/* This field is used to specify the Contribution Amounts for Corresponding Contribution types

Validation Rules:
1. Sum of the Contribution Amounts must match the DP.CUST.CONTRIB value */
        DP_CONTRIB_AMT : T24BusinessTypes:AMT
            @i18n:Label (en="Contribution Amt")
            @i18n:Tooltip (en="Contribution Amt")
            @t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=52.0, core=true, t24Name=DP.CONTRIB.AMT, typeModifiers="7", mandatory=false)
/* Specifies whether the Customer Contribution to be paid to Bank or Supplier.

Validation Rules:
1. Valid values are Bank and Supplier.
2. Input Mandatory if DP.TYPE is given. */
        DP_CONTRIB_PAYTO : IS_Purchase:IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE__DP_CONTRIB_PAYTO
            @i18n:Label (en="Contribution Counterparty")
            @i18n:Tooltip (en="Down Payment Contribution Counterparty")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=51.0, core=true, t24Name=DP.CONTRIB.PAYTO, mandatory=false)
/* Specifies the Type of Customer Contribution Payment.

Validation Rules:
1. Valid values are Cash and Asset.
2. Field Mandatory if DP.TYPE is given. */
        DP_CONTRIB_TYPE : IS_Purchase:IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE__DP_CONTRIB_TYPE
            @i18n:Label (en="Contribution Type")
            @i18n:Tooltip (en="Down Payment Contribution Type")
            @t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=50.0, core=true, t24Name=DP.CONTRIB.TYPE, mandatory=false)
    }
    IS_CONTRACT__INPUTTER
    {
        INPUTTER : T24BusinessTypes:A
            @i18n:Label (en=Inputter)
            @i18n:Tooltip (en="The user who last updated the record")
            @t24:i (align=L, inputBehaviour=N, maxLength=100, sysNumber=131.0, core=true, t24Name=INPUTTER, mandatory=false)
    }
    IS_CONTRACT__OVERRIDE
    {
/* Contains all the override messages which the user has confirmed during input.

During Commit, the system may generate a series of screen override messages waiting for User Approval. If the record is subsequently displayed or printed, all override messages are shown in this field.

Validation Rules:
System generated. Multi-value field. */
        OVERRIDE : T24BusinessTypes:N
            @i18n:Label (en=Override)
            @i18n:Tooltip (en="Overrides generated by this transaction")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=128.0, core=true, t24Name=OVERRIDE, mandatory=false)
    }
    IS_CONTRACT__PREV_STATUS
    {
        PREV_ACCOUNTING -> byValue IS_Purchase:IS_CONTRACT__PREV_STATUS__PREV_ACCOUNTING *
            @t24:i (t24Name=PREV.ACCOUNTING)
/* The status in which the contract was present before transition.

Validation Rules:
1. Field No-input.
2. Copied from the field STATUS during status transition. */
        PREV_STATUS : T24BusinessTypes:A
            @i18n:Label (en="Previous Status")
            @i18n:Tooltip (en="Previous Status")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=97.0, core=true, t24Name=PREV.STATUS, mandatory=false)
/* The value date of the previous status in which the contract was present before transition.

Validation Rules:
1. Field No-input.
2. Copied from the field STATUS.VALUE.DATE during status transition. */
        PREV_STATUS_DATE : T24BusinessTypes:D
            @i18n:Label (en="Previous Status Date")
            @i18n:Tooltip (en="Previous Status Date")
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=98.0, core=true, t24Name=PREV.STATUS.DATE, mandatory=false)
/* This field holds the details of the last action happened on the contract.

Validation Rules:
1. Field No-input. */
        PREV_STATUS_NOTES : T24BusinessTypes:A
            @i18n:Label (en="Previous Status Notes")
            @i18n:Tooltip (en="Previous Status Notes")
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=99.0, core=true, t24Name=PREV.STATUS.NOTES, mandatory=false)
        RESERVED_21 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=105.0, core=true, t24Name=RESERVED.21, mandatory=false)
        RESERVED_22 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=104.0, core=true, t24Name=RESERVED.22, mandatory=false)
        RESERVED_23 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=103.0, core=true, t24Name=RESERVED.23, mandatory=false)
        RESERVED_24 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=102.0, core=true, t24Name=RESERVED.24, mandatory=false)
        RESERVED_25 : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=101.0, core=true, t24Name=RESERVED.25, mandatory=false)
    }
    IS_CONTRACT__PREV_STATUS__PREV_ACCOUNTING
    {
/* Accounting Events that took place when the Contract traversed to the Status defined in the Previous Status. This field holds the record of Accounting Events as present in STATUS field.
Validation Rules:
1. Field No-Input.
2. Defaulted from the Accounting Events as in ACCOUNTING.EVENT field of STATUS multi-value set */
        PREV_ACCOUNTING : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=100.0, core=true, t24Name=PREV.ACCOUNTING, mandatory=false)
    }
    IS_CONTRACT__RESALE_CUSTOMER
    {
/* The Amount which is recoverable for the Repossessed Asset.
This is the difference between market value inputted during the Finance re-posses and the resale price inputted during Purchase.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RECOVERABLE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=85.0, core=true, t24Name=RECOVERABLE.AMT, typeModifiers="7", mandatory=false)
/* The Amount which is recovered for the Repossessed Asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RECOVERED_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=86.0, core=true, t24Name=RECOVERED.AMT, typeModifiers="7", mandatory=false)
/* Specifies the account in which the recovered amount is maintained.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future.
2. Must be a valid record from the table ACCOUNT */
        RECOVERY_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=N, maxLength=36, sysNumber=87.0, core=true, businessType=ANT, t24Name=RECOVERY.ACCT, mandatory=false)
/* Specifies the amount yet to be recovered for the repossessed asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RECOVERY_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=84.0, core=true, t24Name=RECOVERY.AMT, typeModifiers="7", mandatory=false)
        /* Specifies the total Resale value of the repossessed asset. */
        RESALE_AMT : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=83.0, core=true, t24Name=RESALE.AMT, typeModifiers="7", mandatory=false)
/* The Customer to whom the repossessed asset is sold.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE_CUSTOMER -> ST_Customer:CUSTOMER
            @t24:i (align=R, inputBehaviour=N, maxLength=10, sysNumber=78.0, core=true, businessType=CUS, t24Name=RESALE.CUSTOMER, mandatory=false)
/* The account of the Resale Customer.

Validation Rules:
1. Must be a valid record in the table ACCOUNT.
2. No-input field currently. Will be opened for user-input in future. */
        RESALE_CUST_ACCT -> AC_AccountOpening:ACCOUNT
            @t24:i (align=L, inputBehaviour=N, maxLength=36, sysNumber=79.0, core=true, businessType=ANT, t24Name=RESALE.CUST.ACCT, mandatory=false)
/* The date on which the Resale of the repossessed asset is performed.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE_DATE : T24BusinessTypes:D
            @t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=82.0, core=true, t24Name=RESALE.DATE, mandatory=false)
/* The reference captured during the resale of the repossessed asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE_FT_REF : T24BusinessTypes:A
            @t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=88.0, core=true, t24Name=RESALE.FT.REF, mandatory=false)
/* The price at which the repossessed asset is sold again.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE_PRICE : T24BusinessTypes:AMT
            @t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=80.0, core=true, t24Name=RESALE.PRICE, typeModifiers="7", mandatory=false)
/* The quantity of the asset resold during Resale of the repossessed asset.

Validation Rules:
1. No-input field currently. Will be opened for user-input in future. */
        RESALE_QTY : T24BusinessTypes:N
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=81.0, core=true, t24Name=RESALE.QTY, mandatory=false)
    }
    IS_CONTRACT__STMT_NOS
    {
        /* List of Statement Entries generated as the result of Accounting. */
        STMT_NOS : T24BusinessTypes:N
            @i18n:Label (en="Stmt Nos")
            @i18n:Tooltip (en="Stmt Nos")
            @t24:i (align=R, inputBehaviour=N, maxLength=35, sysNumber=127.0, core=true, t24Name=STMT.NOS, mandatory=false)
    }
}
Enumerations {
    IS_ASSET_REVIEW__PROJECT_STATUS : mml:string
        Ahead_of_Schedule="Ahead of Schedule"
        Late=Late
        On_Schedule="On Schedule"
    IS_COMMODITY_SALE__REQ_PRIORITY : mml:string
        Direct=Direct
        FIFO=FIFO
        LIFO=LIFO
    IS_COMMODITY_SALE__REQ_TYPE : mml:string
        BulkTrading=BulkTrading
        SingleTrading=SingleTrading
    IS_CONTRACT__ACTION_CODE__ACTION_COMPLETED : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__ACTION_CODE__ACTION_SUCCESS : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__COMMODITY__ASSET_REF__CUSTOMER_POSSESSION : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__COMMODITY__ASSET_REF__REBATE_ACTION : mml:string
        Finance=Finance
        NoRebate=NoRebate
        Payment=Payment
    IS_CONTRACT__COMMODITY__ASSET_REF__UNITS : mml:string
        KG=KG
        LITRE=LITRE
        NUM=NUM
        TON=TON
    IS_CONTRACT__COST_TYPE__COST_PAY_TYPE : mml:string
        CONTRACT=CONTRACT
        FINANCE=FINANCE
        VENDOR=VENDOR
    IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE__DP_CONTRIB_PAYTO : mml:string
        Bank=Bank
        Supplier=Supplier
    IS_CONTRACT__DP_COMMODITY__DP_CONTRIB_TYPE__DP_CONTRIB_TYPE : mml:string
        Asset=Asset
        Cash=Cash
    IS_CONTRACT__DP_COMMODITY__DP_TYPE : mml:string
        Amount=Amount
        Percentage=Percentage
    IS_CONTRACT__REBATE_ALLWD : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__REPURCHASE : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__RESALE : mml:string
        NULL=NULL
        YES=YES
    IS_CONTRACT__STATUS : mml:string
        ADVANCE_PURCHASE=ADVANCE.PURCHASE
        APPROVAL=APPROVAL
        APPROVAL_UC=APPROVAL.UC
        DELIVERY=DELIVERY
        PURCHASE=PURCHASE
        REQUEST=REQUEST
        RESALE=RESALE
        RETURN_COMMODITY=RETURN.COMMODITY
        SELL_COMMODITY=SELL.COMMODITY
        UPDATE=UPDATE
}